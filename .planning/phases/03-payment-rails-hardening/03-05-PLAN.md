---
phase: 03-payment-rails-hardening
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/[locale]/economics/components/FAQ.tsx
  - apps/web/src/app/[locale]/economics/components/CTASection.tsx
  - apps/web/src/app/[locale]/economics/components/HowItWorks.tsx
  - apps/web/src/app/[locale]/economics/components/FlywheelDiagram.tsx
  - apps/web/src/__tests__/services/economics-fee-split-copy.test.ts
autonomous: true
requirements: [PAY-08]

must_haves:
  truths:
    - "No surface states a 70/30 revenue split, because no code implements one"
    - "No surface states a 1% trading fee, because nothing charges one"
    - "The /economics page still explains the model honestly — free participation, ₪50 creation to the platform — without inventing a percentage"
    - "The page's investment claims are left untouched and clearly attributed to the blocked COIN-04 plan, so a reader of the diff can see the boundary was deliberate"
  artifacts:
    - path: "apps/web/src/app/[locale]/economics/components/FAQ.tsx"
      provides: "FAQ answers with no unbacked split percentage"
    - path: "apps/web/src/app/[locale]/economics/components/FlywheelDiagram.tsx"
      provides: "revenue-stream table with no 70/30 and no 1% trading fee"
    - path: "apps/web/src/__tests__/services/economics-fee-split-copy.test.ts"
      provides: "source guard over the four economics components"
  key_links: []
---

<objective>
Remove the `70% / 30%` revenue-split claim and the `1% על כל עסקה` trading-fee claim from the `/economics` page, on all four components that repeat them.

Purpose: ROADMAP Phase 3 success criterion #2 names this explicitly — every user-facing surface must state the real model, with *"no per-vote civic share, no '70%'"*. The figure is not merely stale; **nothing in the codebase implements it**. `bagsService.createDefaultFeeShareConfig` (`services/bags/index.ts:326`) exists but no route or ledger applies a 70/30 civic split, and the payments webhook deposits amounts whole (and after plan 03-02, not at all for creation fees). A percentage Taruu cannot compute from its own ledger is not a claim Taruu can back.

Output: four corrected components and a source guard, with the page's *investment* claims deliberately left for the blocked COIN-04 plan.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-payment-rails-hardening/03-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Every line below verified by grep on 2026-08-03. `/economics` renders HeroSection, LiveDashboard,
     FlywheelDiagram, HowItWorks, FAQ, CTASection in that order (economics/page.tsx:35-40). -->

**IN SCOPE — the unbacked split (PAY-08):**
```
FAQ.tsx:17            '70% זורם ישירות לקרן הקהילתית. 30% מממן את הפלטפורמה. …'
FAQ.tsx:42            '30% מעמלות המסחר ומדמי יצירת הצבעות. …'
CTASection.tsx:16     { value: '70%', label: 'לקרן הקהילתית' }        // one of three trustStats
HowItWorks.tsx:222    <span className={styles.feeNum}>70%</span>
HowItWorks.tsx:225    מכל עמלות המסחר זורמים לקרן הקהילתית של הרשות
HowItWorks.tsx:227    <span className={styles.feeSub}>30% מממנים את התחזוקה והפיתוח של הפלטפורמה</span>
FlywheelDiagram.tsx:85  { id: 'fees', title: 'עמלות מחולקות', description: '70% לקרן הרשות, 30% לפלטפורמה', … }
FlywheelDiagram.tsx:91  { stream: 'עמלות מסחר',    source: '1% על כל עסקה', allocation: '70% לקרן · 30% לפלטפורמה' }
FlywheelDiagram.tsx:92  { stream: 'רכישות חיצוניות', source: 'תמיכה → BAGS ב-bags.fm', allocation: '100% לקופת הקרן' }
```

**IN SCOPE and already TRUE — keep exactly as written:**
```
FlywheelDiagram.tsx:90  { stream: 'יצירת הצבעה', source: '₪50 להצבעה חדשה', allocation: 'תפעול הפלטפורמה' }
CTASection.tsx:15       { value: 'חינם', label: 'עלות הצבעה' }
```
These two are the model. `allocation: 'תפעול הפלטפורמה'` for the ₪50 is exactly PAY-06's
"100% platform; the civic pool is not credited on creation" — do not soften it.

**OUT OF SCOPE — investment claims, owned by plan 03-13 (COIN-04), which is BLOCKED on COIN-01.
Do not touch these in this plan:**
```
FAQ.tsx:12              'אתה לא רק תורם, אלא מחזיק נכס שמייצג את התמיכה שלך, בדיוק כמו במניה.
                         אם יותר אנשים משקיעים, ה-BAG שווה יותר.'
HeroSection.tsx:103     'לאנשים מבחוץ להשקיע בתנועה הכלכלית של ההצבעה, בדיוק כמו במניה, ולתמוך …'
economics/page.tsx:15-21  metadata + openGraph investment framing
FAQ.tsx:20-53           the crypto / bags.fm / certificate / safety answers
LiveDashboard.tsx       reads real numbers from the API — no hardcoded claim
```
This boundary is deliberate and recorded in `03-CONTEXT.md`: **removing an unbacked percentage needs no
lawyer, but rewording what the token *is* does.** A plan that quietly rewrote the securities language would
be pre-empting a gated decision.

**What replacement copy may say.** Only what the ledger can produce today:
- participation is free;
- creating a vote costs ₪50 and funds the platform;
- the civic pool is filled by outside purchases of the vote's BAG, not by residents' money
  (this exact statement is already approved and shipped at
  `app/[locale]/faq/data/faqData.ts:65-67` and `app/[locale]/terms/page.tsx:99` — reuse its phrasing);
- every movement of the fund is published.

It may **not** say: a percentage, a trading-fee rate, a pool size, an expected return, or a guarantee.

Source-assertion precedent: `apps/web/src/__tests__/services/dashboard-free-mvp.test.ts:20-35`.
This repo has no component-test setup; the guard must be a `.ts` file asserting against source.
</interfaces>
</context>

<tasks>

<task type="auto" id="03-05-T1" tdd="true">
  <name>Task 1: FAQ and CTA — drop the split percentage from the answers and the trust stats</name>
  <read_first>
    - apps/web/src/app/[locale]/economics/components/FAQ.tsx (whole file — the `faqs` array at :8-54; note which answers are in scope and which are COIN-04's)
    - apps/web/src/app/[locale]/economics/components/CTASection.tsx lines 1-45 (the three `trustStats`)
    - apps/web/src/app/[locale]/faq/data/faqData.ts lines 56-68 (the approved, already-shipped wording to reuse)
    - apps/web/src/app/[locale]/terms/page.tsx lines 95-102 (the same statement in the terms)
    - .planning/phases/03-payment-rails-hardening/03-CONTEXT.md — assumption A6 (nothing implements a 70/30 split) and A8
  </read_first>
  <files>apps/web/src/app/[locale]/economics/components/FAQ.tsx, apps/web/src/app/[locale]/economics/components/CTASection.tsx</files>
  <behavior>
    - `FAQ.tsx` source contains no `70%` and no `30%`
    - The `מה קורה לכסף?` answer still answers the question — where the money goes — without a percentage
    - The `איך הפלטפורמה מרוויחה כסף?` answer still answers it, naming the ₪50 creation fee
    - The `faqs` array still has 9 entries and every `question` string is unchanged
    - `CTASection.tsx` still renders three trust stats, and none of them is a percentage of revenue
  </behavior>
  <action>
**`FAQ.tsx`.** Rewrite the *answers* only — leave all nine `question` strings byte-identical so the page's
information architecture is unchanged.

- `:15-18` (`מה קורה לכסף?`): replace the `70% / 30%` sentence. Say what is true: participation costs
  residents nothing, creating a vote costs ₪50 and funds the platform, and the civic fund is filled by
  outside purchases of the vote's BAG rather than by residents' money — reusing the phrasing already shipped
  in `faqData.ts:65-67`. Keep the transparency sentence about every movement being visible.
- `:40-43` (`איך הפלטפורמה מרוויחה כסף?`): replace `30% מעמלות המסחר ומדמי יצירת הצבעות` with the ₪50
  creation fee alone. Keep `אנחנו לא תלויים במשקיעים חיצוניים` — that is a statement about Taruu's funding
  and it is true.
- `:45-48` (`מה קורה כשההצבעה מסתיימת?`) contains `הכספים מועברים לקרן הקהילתית` with no percentage. Leave it.
- **Do not touch** `:10-13` (`למה לא פשוט לתרום ישירות?`) — it carries `בדיוק כמו במניה` and
  `ה-BAG שווה יותר`, which are plan 03-13's under the COIN-01 gate. Add a one-line `//` comment directly
  above that entry recording why it was left, referencing COIN-04, so the boundary is visible in the diff.

**`CTASection.tsx`.** Replace the `{ value: '70%', label: 'לקרן הקהילתית' }` trust stat at `:16` with a
figure the product can stand behind. `{ value: '₪50', label: 'ליצירת הצבעה' }` is the natural pair for the
existing `{ value: 'חינם', label: 'עלות הצבעה' }`. Keep three stats, keep the array shape, and leave
`{ value: 'תעודה', label: 'לכל משתתף' }` alone.

Design-token discipline applies (`CLAUDE.md`): these are text-content edits only. Do not touch a class name,
a CSS module, an animation prop, or a component's structure.
  </action>
  <verify>
    <!-- Self-contained: the guard test is Task 3 of this plan. -->
    <automated>pnpm --filter @sync/web typecheck &amp;&amp; ! grep -q "70%" "apps/web/src/app/[locale]/economics/components/FAQ.tsx" &amp;&amp; ! grep -q "70%" "apps/web/src/app/[locale]/economics/components/CTASection.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @sync/web typecheck` exits 0
    - `grep -c "70%" "apps/web/src/app/[locale]/economics/components/FAQ.tsx"` returns `0`
    - `grep -c "30%" "apps/web/src/app/[locale]/economics/components/FAQ.tsx"` returns `0`
    - `grep -c "question:" "apps/web/src/app/[locale]/economics/components/FAQ.tsx"` returns `9` and each question string is unchanged (`git diff` shows no `question:` line modified)
    - `grep -c "בדיוק כמו במניה" "apps/web/src/app/[locale]/economics/components/FAQ.tsx"` returns `1` — the COIN-04 line is deliberately preserved
    - `grep -c "COIN-04" "apps/web/src/app/[locale]/economics/components/FAQ.tsx"` is at least `1` — the boundary comment exists
    - `grep -c "70%" "apps/web/src/app/[locale]/economics/components/CTASection.tsx"` returns `0`
    - `grep -c "value:" "apps/web/src/app/[locale]/economics/components/CTASection.tsx"` returns `3`
    - `git diff "apps/web/src/app/[locale]/economics/components/"*.module.css` is empty
    - `pnpm --filter @sync/web test` (full suite) exits 0
  </acceptance_criteria>
  <done>The FAQ and the closing CTA state the model without a percentage Taruu cannot compute, and the gated investment answer is visibly untouched on purpose.</done>
</task>

<task type="auto" id="03-05-T2" tdd="true">
  <name>Task 2: HowItWorks and the flywheel — drop the fee-split callout and the 1% trading fee</name>
  <read_first>
    - apps/web/src/app/[locale]/economics/components/HowItWorks.tsx lines 100-135 (residentSteps / supporterSteps) and 215-235 (the fee-split ink block)
    - apps/web/src/app/[locale]/economics/components/HowItWorks.module.css (the `.feeNote`, `.feeNum`, `.feeBody`, `.feeTitle`, `.feeSub` rules — read before deciding whether to remove or repurpose the block)
    - apps/web/src/app/[locale]/economics/components/FlywheelDiagram.tsx lines 80-100 (`flywheelSteps`, `revenueStreams`, `sustainabilityPoints`)
    - apps/web/src/services/bags/index.ts lines 320-360 (`createDefaultFeeShareConfig` — read it to confirm for yourself that no route applies it, per assumption A6)
  </read_first>
  <files>apps/web/src/app/[locale]/economics/components/HowItWorks.tsx, apps/web/src/app/[locale]/economics/components/FlywheelDiagram.tsx</files>
  <behavior>
    - Neither file's source contains `70%`, `30%`, or `1% על כל עסקה`
    - The flywheel still has six steps and the revenue table still has three rows
    - `{ stream: 'יצירת הצבעה', source: '₪50 להצבעה חדשה', allocation: 'תפעול הפלטפורמה' }` is unchanged
    - `HowItWorks` still renders both tracks (residents, supporters) and its closing block, if kept, states something true
  </behavior>
  <action>
**`HowItWorks.tsx`.** The ink-block callout at `:220-229` exists to say where fee money goes. Two acceptable
outcomes — pick one and record which in the SUMMARY:

- *Preferred:* keep the block's visual weight and replace its content with a true statement. `feeNum`
  becomes `₪50` and the body says that a created vote's fee funds the platform while the civic fund is
  filled by outside BAG purchases rather than residents' money. This preserves the layout and every
  CSS-module class, so no `.module.css` change is needed.
- *Acceptable:* delete the block entirely, and delete the now-unused `.feeNote` / `.feeNum` / `.feeBody` /
  `.feeTitle` / `.feeSub` rules from `HowItWorks.module.css`. Only do this if the copy cannot be made honest
  without inventing something.

Do not touch `residentSteps` / `supporterSteps` — step 5's `מעקב אחר הקרן · רואים את הכסף נאסף בזמן אמת`
(`:118`) is a transparency claim, not a split claim, and `LiveDashboard` reads real figures.

**`FlywheelDiagram.tsx`.**
- `:85` — `{ id: 'fees', title: 'עמלות מחולקות', description: '70% לקרן הרשות, 30% לפלטפורמה' }`: keep the
  step so the flywheel stays six-long, and restate the description without a split. What is true is that
  trading activity on a vote's BAG flows to that vote's civic fund; how it is divided is COIN-02's ledger to
  establish and it does not exist yet.
- `:91` — the `עמלות מסחר` row: `1% על כל עסקה` is a rate nothing charges, and `70% לקרן · 30% לפלטפורמה` is
  the same unbacked split. Restate the row's `source` and `allocation` without either number, or drop the
  row and leave two revenue streams. Prefer restating: a three-row table matching the three real streams
  reads better than a two-row one.
- `:92` — `100% לקופת הקרן` for outside BAG purchases. This one is arguably true (nothing skims it today)
  but it is still a hard allocation guarantee with no ledger behind it. Restate it as a direction rather
  than a percentage.
- `:90` — **unchanged.** The ₪50 row is correct.
- `sustainabilityPoints` (`:95-100`) makes no numeric claim. Leave it.

Text-content edits only. No class names, no CSS modules (unless you took the *acceptable* branch above), no
animation props, no component structure.
  </action>
  <verify>
    <automated>pnpm --filter @sync/web typecheck &amp;&amp; ! grep -qE "70%|30%" "apps/web/src/app/[locale]/economics/components/HowItWorks.tsx" &amp;&amp; ! grep -qE "70%|30%|1% על כל עסקה" "apps/web/src/app/[locale]/economics/components/FlywheelDiagram.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @sync/web typecheck` exits 0
    - `grep -cE "70%|30%" "apps/web/src/app/[locale]/economics/components/HowItWorks.tsx"` returns `0`
    - `grep -cE "70%|30%" "apps/web/src/app/[locale]/economics/components/FlywheelDiagram.tsx"` returns `0`
    - `grep -c "1% על כל עסקה" "apps/web/src/app/[locale]/economics/components/FlywheelDiagram.tsx"` returns `0`
    - `grep -c "id: '" "apps/web/src/app/[locale]/economics/components/FlywheelDiagram.tsx"` returns `6` — six flywheel steps survive
    - `grep -c "₪50 להצבעה חדשה" "apps/web/src/app/[locale]/economics/components/FlywheelDiagram.tsx"` returns `1` — the true row is untouched
    - `grep -c "residentSteps\|supporterSteps" "apps/web/src/app/[locale]/economics/components/HowItWorks.tsx"` is unchanged
    - If the fee block was kept, `git diff "apps/web/src/app/[locale]/economics/components/HowItWorks.module.css"` is empty
    - `pnpm --filter @sync/web test` (full suite) exits 0
  </acceptance_criteria>
  <done>Neither the how-it-works page nor the flywheel diagram states a split or a trading-fee rate that no code produces.</done>
</task>

<task type="auto" id="03-05-T3">
  <name>Task 3: Guard the economics page against the split claim returning</name>
  <read_first>
    - apps/web/src/__tests__/services/dashboard-free-mvp.test.ts (the `readFileSync` + `code()` pattern)
    - apps/web/src/__tests__/services/money-model-copy.test.ts (plan 03-04's sibling guard — read it if it exists to avoid duplicate assertions; if it does not exist yet, that is fine, the two plans are parallel and their scopes do not overlap)
    - apps/web/vitest.config.ts (must be a `.ts` file; `.tsx` is never collected)
    - The four components as edited in Tasks 1 and 2
  </read_first>
  <files>apps/web/src/__tests__/services/economics-fee-split-copy.test.ts</files>
  <action>
Create `apps/web/src/__tests__/services/economics-fee-split-copy.test.ts`. Header docstring must state the
scope and, just as importantly, what it deliberately does not assert:

> Guards the four `/economics` components against the unbacked `70% / 30%` revenue split and the `1%`
> trading-fee rate. Nothing in the codebase implements either — `createDefaultFeeShareConfig`
> (`services/bags/index.ts:326`) is never applied by a route or a ledger.
>
> **Deliberately NOT asserted:** the page's investment language (`בדיוק כמו במניה`,
> `ה-BAG שווה יותר`) in `FAQ.tsx` and `HeroSection.tsx`. Rewording what the token *is* is COIN-04, gated on
> COIN-01's written legal sign-off. This guard must stay green both before and after that gate clears.

Read each of `FAQ.tsx`, `CTASection.tsx`, `HowItWorks.tsx`, `FlywheelDiagram.tsx` with `readFileSync` from
`join(process.cwd(), 'src/app/[locale]/economics/components', …)`, strip comments with `code()`, and assert:

`describe('no unbacked revenue split')` — for each of the four files, the comment-free source contains
neither `70%` nor `30%`. Drive this with a `it.each` over the four filenames so a fifth component added later
is a one-line addition.

`describe('no unbacked trading-fee rate')` — `FlywheelDiagram.tsx` contains no `1% על כל עסקה`.

`describe('the true figures survive')` —
- `FlywheelDiagram.tsx` still contains `₪50 להצבעה חדשה` and `תפעול הפלטפורמה`;
- `CTASection.tsx` still contains `חינם`;
- `FAQ.tsx` still has nine `question:` entries.

`describe('COIN-04 boundary is intact')` — `FAQ.tsx` still contains `בדיוק כמו במניה`. Assert it **positively**,
with a comment explaining that this is not an endorsement of the claim: it is proof that this plan did not
silently pre-empt a gated decision, and plan 03-13 will flip this assertion when COIN-01 clears.

Use `vitest run` semantics only — no `--watch`, no `.only`.
  </action>
  <verify>
    <automated>pnpm --filter @sync/web test -- src/__tests__/services/economics-fee-split-copy.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @sync/web test -- src/__tests__/services/economics-fee-split-copy.test.ts` exits 0 with at least 8 passing tests
    - `grep -c "it.each" apps/web/src/__tests__/services/economics-fee-split-copy.test.ts` is at least `1`
    - `grep -c "COIN-04" apps/web/src/__tests__/services/economics-fee-split-copy.test.ts` is at least `2` (the header note and the boundary describe)
    - `grep -c "Ticker\|MoneyTransparency\|TreasuryDashboard\|PricingContent" apps/web/src/__tests__/services/economics-fee-split-copy.test.ts` returns `0` — this guard stays inside its own plan's files
    - `grep -cE "\.only\(|--watch" apps/web/src/__tests__/services/economics-fee-split-copy.test.ts` returns `0`
    - `pnpm --filter @sync/web test` (full suite) exits 0
  </acceptance_criteria>
  <done>A regression that reintroduces `70%`, `30%` or a `1%` trading fee on `/economics` fails the suite, and the untouched COIN-04 boundary is asserted rather than assumed.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @sync/web typecheck` — exits 0
2. `pnpm --filter @sync/web test` — full suite exits 0
3. `grep -rnE "70%|30%" "apps/web/src/app/[locale]/economics/components/"*.tsx` — no matches
4. `grep -rn "1% על כל עסקה" apps/web/src` — no matches
5. `git diff --stat "apps/web/src/app/[locale]/economics/components/HeroSection.tsx" "apps/web/src/app/[locale]/economics/page.tsx"` — empty (COIN-04's surfaces untouched)
6. `git diff --stat apps/web/src/components/press/Ticker/ apps/web/src/services/email/` — empty (plan 03-04's surfaces untouched)
</verification>

<success_criteria>
- No `/economics` component states a `70%` / `30%` split or a `1%` trading fee
- The two figures that are true — free participation and ₪50-to-platform creation — survive verbatim
- The page's investment language is untouched, deliberately, and a test asserts it so the boundary is explicit
- A source guard covers the four components and reaches into no other plan's files
</success_criteria>

<output>
After completion, create `.planning/phases/03-payment-rails-hardening/03-05-SUMMARY.md`
</output>
