---
phase: 03-payment-rails-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/press/Ticker/Ticker.tsx
  - apps/web/src/components/sections/MoneyTransparency/MoneyTransparency.tsx
  - apps/web/src/components/sections/MoneyTransparency/MoneyTransparency.module.css
  - apps/web/src/components/sections/MoneyTransparency/index.ts
  - apps/web/src/components/sections/index.ts
  - apps/web/src/services/email/index.ts
  - apps/mobile/app/(auth)/index.tsx
  - apps/web/src/__tests__/services/money-model-copy.test.ts
autonomous: true
requirements: [PAY-08]

must_haves:
  truths:
    - "No surface tells a resident that money is collected per vote — the homepage ticker no longer says ₪2 of every vote goes to the fund"
    - "The vote-created email states what a creator actually paid and what actually happens next, with no ₪3 per-ballot accrual"
    - "The mobile welcome screen no longer advertises ₪3 to vote"
    - "The component whose entire premise was a ₪3 fee split is gone from the tree, not left exported and rotting"
  artifacts:
    - path: "apps/web/src/components/press/Ticker/Ticker.tsx"
      provides: "homepage/explore ticker with no per-vote money claim"
    - path: "apps/web/src/services/email/index.ts"
      provides: "vote-created email with no ₪3 per-ballot accrual claim"
    - path: "apps/web/src/__tests__/services/money-model-copy.test.ts"
      provides: "source-level guard over the four surfaces this plan owns"
  key_links:
    - from: "apps/web/src/components/sections/index.ts"
      to: "MoneyTransparency"
      via: "export REMOVED along with the component"
      pattern: "MoneyTransparency"
---

<objective>
Delete the per-vote money claim from every surface that still makes one: the homepage ticker, the retired `MoneyTransparency` section, the vote-created email, and the mobile welcome screen.

Purpose: participation has been free since `cfa5d25` and Phase 02.1 made the free ballot persist — but the homepage still tells every visitor `₪2 מכל הצבעה נצברים לקרן הקהילתית`, the vote-created email still tells a creator `כל קול (₪3) נצבר בקופת ההצבעה`, and the mobile welcome screen still shows `₪3 / להצבעה` as a trust stat. Nothing collects money per vote. PAY-08 requires every user-facing surface to state the real model.

Output: four corrected surfaces, one deleted component, and a source-level guard so none of it comes back.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-payment-rails-hardening/03-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Exact locations, verified 2026-08-03 by grep. -->

`apps/web/src/components/press/Ticker/Ticker.tsx:10-16` — `DEFAULT_ITEMS`, rendered on the homepage
(`app/[locale]/page.tsx:34`) and `/explore` (`:65`):
```typescript
const DEFAULT_ITEMS = [
  'נפתחים בכל הארץ, בבת אחת · 04.08.26',
  '1,247 קולות מאומתים נחתמו השבוע',                    // hardcoded figure — OUT OF SCOPE, see below
  'כל קול חתום בבלוקצ׳יין · בלתי ניתן לזיוף',            // chain claim — OUT OF SCOPE, see below
  '₪2 מכל הצבעה נצברים לקרן הקהילתית',                  // <- THIS ONE. Nothing collects ₪2 per vote.
  'מודדים · מאמתים · מנגישים',
];
```

`apps/web/src/components/sections/MoneyTransparency/MoneyTransparency.tsx` — the whole component is a
₪3 → ₪2/₪1 split: the docstring (`:24-32`), the eyebrow `₪3, ולמה זה לא קנס` (`:47`), the standfirst
`דמי ההשתתפות (₪3)` (`:63`), the `aria-label` *"מתוך שלושה שקלים: שני שקלים לקרן קהילתית, שקל אחד לתפעול ופיתוח"*
(`:74`), the proportional bar and the two `₪2` / `₪1` cards. Exported from `components/sections/index.ts:7`
and — verified — rendered on **zero** pages.

`apps/web/src/services/email/index.ts:367-370` — inside the vote-created email:
```
מה הלאה? כל קול (₪3) נצבר בקופת ההצבעה. בסיומה התוצאות מוגשות
למועצה, והקופה זורעת מטבע קהילה (Issue Coin) שמנציח את ההישג.
```

`apps/mobile/app/(auth)/index.tsx:70-80` — the welcome screen's three trust stats:
`100% / שקיפות` · `₪3 / להצבעה` · `GPS / מאומת`.

**Already correct — do not touch, and use as the wording precedent:**
- `apps/web/src/app/[locale]/faq/data/faqData.ts:57-68` — *"כלום. ההשתתפות בהצבעות חינם…"* and
  *"הקרן מתמלאת מהשקעות חיצוניות ב-BAG של כל הצבעה, לא מכסף של תושבים."*
- `apps/web/src/app/[locale]/terms/page.tsx:99` — the same statement in the terms.
- `packages/shared/src/constants/index.ts:11-12` — `VOTE_PARTICIPATION_COST = 0`, `CREATE_VOTE_COST = 50`.

**Deliberately out of scope** (recorded so the executor does not widen the blast radius):
- The ticker's `1,247 קולות…` hardcoded figure and its `חתום בבלוקצ׳יין` claim. Neither is a money-model
  claim. The chain-copy sweep of marketing surfaces was explicitly recorded as a known gap by
  `02.1-VALIDATION.md`. **Note both in the SUMMARY; change neither here.**
- `apps/web/src/components/sections/FundTransparency/FundTransparency.tsx` — unmounted, membership-era
  `monthlyAccumulation` framing. Record, do not sweep.
- The `70%` / `30%` fee-split claims — plan 03-05 (`/economics`) and plan 03-09 (treasury, pricing).
  **This plan's guard test must not assert on them**, or it will fail while 03-05 is still in flight.

Source-assertion precedent for the guard: `apps/web/src/__tests__/services/dashboard-free-mvp.test.ts:1-35`
(the `readFileSync` + `code()` comment-stripper pattern). This repo has no component-test setup —
`vitest.config.ts` sets `environment: 'node'` and its include glob never collects `.tsx`.
</interfaces>
</context>

<tasks>

<task type="auto" id="03-04-T1" tdd="true">
  <name>Task 1: Fix the homepage ticker and delete the retired MoneyTransparency section</name>
  <read_first>
    - apps/web/src/components/press/Ticker/Ticker.tsx (whole file, 40 lines)
    - apps/web/src/app/[locale]/page.tsx lines 1-45 and apps/web/src/app/[locale]/explore/page.tsx lines 1-70 (the two render sites — confirm neither passes a custom `items` prop)
    - apps/web/src/components/sections/MoneyTransparency/ (all files in the directory)
    - apps/web/src/components/sections/index.ts (the barrel)
    - apps/web/src/app/[locale]/faq/data/faqData.ts lines 56-68 (the approved wording precedent)
  </read_first>
  <files>apps/web/src/components/press/Ticker/Ticker.tsx, apps/web/src/components/sections/MoneyTransparency/MoneyTransparency.tsx, apps/web/src/components/sections/MoneyTransparency/MoneyTransparency.module.css, apps/web/src/components/sections/MoneyTransparency/index.ts, apps/web/src/components/sections/index.ts</files>
  <behavior>
    - `Ticker.tsx` contains no `₪2`, no `₪3`, and no claim that money accrues per vote
    - `Ticker`'s `items` prop and marquee behaviour are unchanged; only `DEFAULT_ITEMS` content changes
    - `apps/web/src/components/sections/MoneyTransparency/` does not exist
    - `components/sections/index.ts` no longer exports `MoneyTransparency`, and every other export is unchanged
  </behavior>
  <action>
**Part A — `Ticker.tsx`.** Replace the `'₪2 מכל הצבעה נצברים לקרן הקהילתית'` entry with a statement that is
true under the current model. Follow `faqData.ts`'s approved phrasing rather than inventing a new claim —
something in the shape of *"ההשתתפות בהצבעות חינם · ₪50 ליצירת הצבעה"*. Do not add a percentage, a pool
size, or an expected return. Change nothing else in the array and nothing in the component body.

**Part B — delete the `MoneyTransparency` section.** `git rm -r apps/web/src/components/sections/MoneyTransparency/`.
Its entire premise is a ₪3 participation fee split ₪2/₪1 — the docstring, the eyebrow, the standfirst, the
`aria-label`, the bar and both cards. There is nothing salvageable and it renders on no page. Rewriting a
dead component to describe a token-funded pool would also stray into COIN territory, which is gated.
Then remove the `export { MoneyTransparency } from './MoneyTransparency';` line from
`apps/web/src/components/sections/index.ts` and leave the other ten exports exactly as they are.

Before deleting, run `grep -rn "MoneyTransparency" apps/web/src` and confirm the only hits are the component's
own directory and the barrel line. If any page imports it, **stop and rewrite instead of deleting** — record
that in the SUMMARY.
  </action>
  <verify>
    <!-- Self-contained: the guard test is Task 3 of this plan. -->
    <automated>pnpm --filter @sync/web typecheck &amp;&amp; ! grep -q "₪2" apps/web/src/components/press/Ticker/Ticker.tsx &amp;&amp; test ! -d apps/web/src/components/sections/MoneyTransparency</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @sync/web typecheck` exits 0
    - `grep -c "₪2" apps/web/src/components/press/Ticker/Ticker.tsx` returns `0`
    - `grep -c "לקרן הקהילתית" apps/web/src/components/press/Ticker/Ticker.tsx` returns `0`
    - `grep -c "DEFAULT_ITEMS" apps/web/src/components/press/Ticker/Ticker.tsx` is `2` and the array still has 5 entries
    - `test ! -d apps/web/src/components/sections/MoneyTransparency` exits 0
    - `grep -rc "MoneyTransparency" apps/web/src` totals `0`
    - `grep -c "export {" apps/web/src/components/sections/index.ts` returns `10` (was 11)
    - `pnpm --filter @sync/web test` (full suite) exits 0
  </acceptance_criteria>
  <done>The homepage ticker states a true price and the ₪3-split section is out of the tree.</done>
</task>

<task type="auto" id="03-04-T2" tdd="true">
  <name>Task 2: Fix the vote-created email and the mobile welcome screen</name>
  <read_first>
    - apps/web/src/services/email/index.ts lines 330-395 (the vote-created template, including the `מה הלאה?` paragraph at :367)
    - apps/web/src/services/email/index.ts lines 140-160 and 460-480 (the receipt-email signature and its `isVote` branch — read to confirm nothing else quotes a participation price)
    - apps/mobile/app/(auth)/index.tsx lines 60-90 (the three trust stats)
    - apps/web/src/app/[locale]/faq/data/faqData.ts lines 56-68 (wording precedent)
  </read_first>
  <files>apps/web/src/services/email/index.ts, apps/mobile/app/(auth)/index.tsx</files>
  <behavior>
    - The vote-created email body contains no `₪3` and makes no claim that each ballot accrues money
    - The email still tells the creator what happens next: the vote is open, results go to the council when it closes
    - The mobile welcome screen's middle trust stat states that voting is free
    - No other email template gains or loses a price claim
  </behavior>
  <action>
**Part A — `apps/web/src/services/email/index.ts`.** Rewrite the `מה הלאה?` paragraph at `:367-370`. What is
true today: the vote is now open to the municipality, and when it closes the results are submitted to the
council. What is **not** true: that each ballot accrues ₪3 into a per-vote pot.

The trailing `והקופה זורעת מטבע קהילה (Issue Coin) שמנציח את ההישג` describes the Bags/Issue-Coin mechanism,
which is gated on COIN-01. Since this is a *money* claim in the same sentence, drop it here rather than
restating it — the plan that may reinstate token language is 03-13, behind the legal gate. Keep the
paragraph short, Hebrew, and free of any percentage or pool figure.

Then grep this file for any other price claim (`₪`, `נצבר`, `קופה`, `דמי`) and confirm the only remaining
money references are the receipt email's `params.amount`, which is a real charged amount passed in by the
caller. Change nothing structural: same function, same HTML scaffold, same inline styles.

**Part B — `apps/mobile/app/(auth)/index.tsx`.** Replace the `₪3` / `להצבעה` trust stat at `:76-78`. The
honest version of that stat is that voting is free — e.g. value `חינם`, label `להצבעה`. Keep the three-stat
layout, the dividers, the `Animated.View` entrance and the NativeWind classes exactly as they are; this is a
two-string change. Do not touch the `100% / שקיפות` or `GPS / מאומת` stats.
  </action>
  <verify>
    <automated>pnpm --filter @sync/web typecheck &amp;&amp; pnpm --filter @sync/mobile typecheck &amp;&amp; ! grep -q "₪3" apps/web/src/services/email/index.ts &amp;&amp; ! grep -q "₪3" "apps/mobile/app/(auth)/index.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @sync/web typecheck` and `pnpm --filter @sync/mobile typecheck` both exit 0
    - `grep -c "₪3" apps/web/src/services/email/index.ts` returns `0`
    - `grep -c "נצבר בקופת ההצבעה" apps/web/src/services/email/index.ts` returns `0`
    - `grep -c "Issue Coin" apps/web/src/services/email/index.ts` returns `0`
    - `grep -c "₪3" "apps/mobile/app/(auth)/index.tsx"` returns `0`
    - `grep -c "חינם" "apps/mobile/app/(auth)/index.tsx"` is at least `1`
    - `git diff --stat "apps/mobile/app/(auth)/index.tsx"` shows fewer than 6 changed lines
    - `pnpm --filter @sync/web test` (full suite) exits 0
    - The four behaviours above hold once Task 3 lands
  </acceptance_criteria>
  <done>A creator's email and a first-time mobile visitor both see the real price of voting: nothing.</done>
</task>

<task type="auto" id="03-04-T3">
  <name>Task 3: Guard the four surfaces against a per-vote money claim returning</name>
  <read_first>
    - apps/web/src/__tests__/services/dashboard-free-mvp.test.ts (whole file — the `readFileSync` + `code()` pattern and how it scopes its assertions)
    - apps/web/src/__tests__/services/participation-cost-legacy.test.ts (the sibling ₪3 guard from plan 02.1-03 — read it to avoid duplicating assertions and to match naming)
    - apps/web/vitest.config.ts (`environment: 'node'`, include glob excludes `.tsx` — this must be a `.ts` file)
    - The four surfaces as edited in Tasks 1 and 2
  </read_first>
  <files>apps/web/src/__tests__/services/money-model-copy.test.ts</files>
  <action>
Create `apps/web/src/__tests__/services/money-model-copy.test.ts`. Open with a header docstring that states
the scope precisely, in the style of `dashboard-free-mvp.test.ts`:

> Guards the four surfaces plan 03-04 owns: the press Ticker, the vote-created email, the mobile welcome
> screen, and the absence of the retired MoneyTransparency section. **Deliberately NOT asserted here:** the
> `70%` / `30%` fee-split claims (plans 03-05 and 03-09), the ticker's hardcoded `1,247` figure, and the
> chain-seal copy on marketing surfaces (recorded as a known gap by `02.1-VALIDATION.md`). This repo has no
> component-test setup, so these assert against SOURCE.

Reuse the `code()` comment-stripper. Read each file with `readFileSync` relative to `process.cwd()`; the
mobile file lives outside `apps/web`, so resolve it as `join(process.cwd(), '../../apps/mobile/app/(auth)/index.tsx')`
— confirm the path resolves before asserting, and `expect(existsSync(path)).toBe(true)` first so a repo move
fails loudly rather than silently passing.

Assertions:

`describe('press Ticker')`
- source contains no `₪2` and no `₪3`
- source contains no `נצברים לקרן` and no `מכל הצבעה`
- `DEFAULT_ITEMS` still has five entries (a regex count of quoted strings inside the array is fine)

`describe('vote-created email')`
- `services/email/index.ts` source contains no `₪3`, no `נצבר בקופת ההצבעה`, no `Issue Coin`
- it still contains the vote title interpolation and the CTA link, so the template was edited rather than gutted

`describe('MoneyTransparency is retired')`
- `existsSync('src/components/sections/MoneyTransparency')` is `false`
- `components/sections/index.ts` source contains no `MoneyTransparency`

`describe('mobile welcome screen')`
- source contains no `₪3`
- source contains `חינם`

Use `vitest run` semantics only — no `--watch`, no `.only`.
  </action>
  <verify>
    <automated>pnpm --filter @sync/web test -- src/__tests__/services/money-model-copy.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @sync/web test -- src/__tests__/services/money-model-copy.test.ts` exits 0 with at least 8 passing tests
    - `grep -c "70%" apps/web/src/__tests__/services/money-model-copy.test.ts` returns `0` — this guard does not reach into plan 03-05's or 03-09's surfaces
    - `grep -c "existsSync" apps/web/src/__tests__/services/money-model-copy.test.ts` is at least `2`
    - `grep -cE "\.only\(|--watch" apps/web/src/__tests__/services/money-model-copy.test.ts` returns `0`
    - `pnpm --filter @sync/web test` (full suite) exits 0
  </acceptance_criteria>
  <done>A regression that reintroduces a per-vote money claim on any of the four surfaces fails the suite.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @sync/web typecheck` and `pnpm --filter @sync/mobile typecheck` — both exit 0
2. `pnpm --filter @sync/web test` — full suite exits 0
3. `grep -rn "₪2 מכל הצבעה" apps/web/src` — no matches
4. `grep -rn "MoneyTransparency" apps/web/src` — no matches
5. `grep -rn "₪3" apps/web/src/services apps/mobile/app` — no matches outside comments that describe the retirement
6. `git diff --stat apps/web/src/app/\[locale\]/economics/` — empty (plan 03-05's surface untouched)
</verification>

<success_criteria>
- The homepage and `/explore` ticker no longer claim money accrues per vote
- The vote-created email describes what actually happens next, with no ₪3 and no gated token language
- The mobile welcome screen says voting is free
- `MoneyTransparency` is deleted, its barrel export removed, and nothing referenced it
- A source-level guard covers exactly these four surfaces and reaches into no other plan's files
</success_criteria>

<output>
After completion, create `.planning/phases/03-payment-rails-hardening/03-04-SUMMARY.md`
</output>
