---
phase: 03-payment-rails-hardening
plan: 13
type: execute
wave: 3
depends_on: ["03-10"]
blocked_by: COIN-01
files_modified:
  - apps/web/docs/COIN-CLAIM-INVENTORY.md
  - apps/web/src/app/[locale]/economics/components/FAQ.tsx
  - apps/web/src/app/[locale]/economics/components/HeroSection.tsx
  - apps/web/src/app/[locale]/economics/page.tsx
  - apps/web/src/app/[locale]/coin/page.tsx
  - apps/web/src/__tests__/services/token-claim-register.test.ts
  - apps/web/src/__tests__/services/economics-fee-split-copy.test.ts
autonomous: false
requirements: [COIN-04]

must_haves:
  truths:
    - "Every public claim about the token is one a lawyer ruled on in writing, and the ruling is on file next to the claim"
    - "No surface implies a guaranteed profit, and none implies a guaranteed civic outcome"
    - "A claim that was ruled prohibited is not merely softened — it is gone"
    - "A new unapproved claim added later fails the suite, because the register is the source of truth and a test enforces it"
  artifacts:
    - path: "apps/web/docs/COIN-CLAIM-INVENTORY.md"
      provides: "the register, now carrying the lawyer's verdict and approved wording per row"
      contains: "Verdict"
    - path: "apps/web/src/__tests__/services/token-claim-register.test.ts"
      provides: "guard binding shipped copy to the approved register"
  key_links:
    - from: "apps/web/src/__tests__/services/token-claim-register.test.ts"
      to: "apps/web/docs/COIN-CLAIM-INVENTORY.md"
      via: "the test reads the register and asserts prohibited phrases are absent from source"
      pattern: "COIN-CLAIM-INVENTORY"
---

<objective>
Bring every public claim about the token into line with COIN-01's written ruling, and make the register the enforced source of truth so a new unapproved claim cannot ship quietly.

**THIS PLAN IS BLOCKED.** Task 1 is a hard gate. Do not execute Tasks 2 or 3 until COIN-01's written Israeli legal sign-off is on file **with a verdict recorded against every row** of `apps/web/docs/COIN-CLAIM-INVENTORY.md`. This plan cannot be executed from judgement: rewriting a securities claim without a ruling is exactly the exposure COIN-01 exists to prevent, and softened wording invented by a developer is no safer than the original.

Purpose: COIN-04 requires that *"every public claim about the token — expected return, pool size, what the money funds — is one Taruu can actually back, with no implied guarantee of profit or of civic outcome."* Today the live `/economics` page tells readers they hold an asset *"בדיוק כמו במניה"* and that *"אם יותר אנשים משקיעים, ה-BAG שווה יותר"* — a securities analogy and an implied return, in production, with no sign-off.

Output: source rewritten to the approved wording, the register updated with what actually shipped, and a test that fails when an unapproved claim appears.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-payment-rails-hardening/03-CONTEXT.md
@CLAUDE.md

@apps/web/docs/COIN-CLAIM-INVENTORY.md

<interfaces>
<!-- Verified 2026-08-03. RE-DERIVE at execution time. Plans 03-04, 03-05, 03-09 and 03-10 land first, and
     this plan sits behind a gate that may not clear for weeks. -->

**COIN-04, verbatim** (`REQUIREMENTS.md:51`):
> Every public claim about the token — expected return, pool size, what the money funds — is one Taruu can
> actually back, with no implied guarantee of profit or of civic outcome.

**The two hardest live claims** (`/economics`, in production today):
```
economics/components/FAQ.tsx:12
  'אתה לא רק תורם, אלא מחזיק נכס שמייצג את התמיכה שלך, בדיוק כמו במניה.
   אם יותר אנשים משקיעים, ה-BAG שווה יותר.'
economics/components/HeroSection.tsx:103
  'לאנשים מבחוץ להשקיע בתנועה הכלכלית של ההצבעה, בדיוק כמו במניה, ולתמוך …'
```
Plan 03-05 **deliberately left both untouched** and asserted their presence in
`__tests__/services/economics-fee-split-copy.test.ts`'s `describe('COIN-04 boundary is intact')`, precisely so
this plan would be the one to move them and the boundary would be visible in the diff. **That assertion must
be flipped by this plan** — it currently asserts `בדיוק כמו במניה` is *present*.

Also in scope, per the register: `economics/page.tsx:15-21` (metadata + openGraph) and `coin/page.tsx:9-10`
(metadata), both carrying the same investment framing.

**Already conservative — confirm the ruling covers them, and prefer their vocabulary as the rewrite target:**
- `app/[locale]/faq/data/faqData.ts:65-67` — *"הקרן מתמלאת מהשקעות חיצוניות ב-BAG של כל הצבעה, לא מכסף של תושבים."*
- `app/[locale]/terms/page.tsx:99` — the same statement in the terms.
- `pricing/components/PricingContent.tsx` — plan 03-09 added the pool's funding source here; check it against
  the ruling and correct it if the approved wording differs.

**Pool-size and outcome claims.** COIN-04 names *pool size* explicitly. After plan 03-11, a pool figure is
producible from `civic_pool_entries`; before it, any figure on a page is a placeholder. Sweep for both:
- a **number** presented as the fund's size that no ledger produces;
- a claim that the money **will** fund a specific civic outcome. `בסיומה התוצאות מוגשות למועצה` is a
  procedural fact and is fine; *"the money makes it happen"* is an outcome guarantee and is not.

Guard precedents to follow, and not to duplicate:
`__tests__/services/money-model-copy.test.ts` (03-04), `economics-fee-split-copy.test.ts` (03-05),
`money-model-sweep.test.ts` (03-09). This plan's guard is about **token** claims, not money-model figures.

Test infrastructure: vitest 1.6.1, `environment: 'node'`, `.tsx` is never collected. The guard must be a
`.ts` file asserting against source, using the `code()` comment-stripper from `dashboard-free-mvp.test.ts`.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" id="03-13-T1" gate="blocking">
  <name>Task 1: GATE — the ruling, row by row</name>
  <decision>Has COIN-01's written sign-off been obtained, and does it carry a verdict and approved wording for every row of `COIN-CLAIM-INVENTORY.md`?</decision>
  <context>
This is a stricter gate than plans 03-11 and 03-12. Those need the sign-off to exist; this one needs its
**content**, per claim. Rewriting *"בדיוק כמו במניה"* into something a developer judges safer, without a
ruling, produces an unreviewed claim that merely looks more careful — which is the failure mode COIN-04
describes.

Before answering, open `apps/web/docs/COIN-CLAIM-INVENTORY.md` (plan 03-10) and confirm:
- the `## Sign-off` box is checked and a written opinion is on file;
- every row has a **Verdict** of `allowed`, `allowed-with-wording`, or `prohibited`;
- every `allowed-with-wording` row has **Replacement wording** filled in by the lawyer, not by us;
- the ruling covers the conservative statements too (`faqData.ts`, `terms/page.tsx`), so the rewrite has an
  approved vocabulary to move toward rather than away from.

If any row is blank, this plan cannot proceed for that row. Partial execution is acceptable and preferable to
guessing — see the second option.
  </context>
  <options>
    <option id="fully-ruled">
      <name>Sign-off on file and every inventory row carries a verdict, with wording supplied where needed</name>
      <pros>Tasks 2 and 3 proceed for every surface</pros>
      <cons>None</cons>
    </option>
    <option id="partially-ruled">
      <name>Sign-off on file but some rows are unruled</name>
      <pros>The ruled claims can be corrected now, which reduces exposure immediately</pros>
      <cons>Execute Tasks 2 and 3 **only** for ruled rows. Leave every unruled claim exactly as it is, record each one in the SUMMARY, and keep the guard test asserting its continued presence so the boundary stays visible. Do not invent wording for an unruled row.</cons>
    </option>
    <option id="not-ruled">
      <name>No sign-off, or no verdicts recorded</name>
      <pros>No unreviewed claim is substituted for another</pros>
      <cons>Do not execute Tasks 2 or 3. COIN-04 stays open and the live claims stay as they are — which is the exposure `03-CONTEXT.md` raised for owner decision, unchanged.</cons>
    </option>
  </options>
  <files>apps/web/docs/COIN-CLAIM-INVENTORY.md</files>
  <action>
Do not write code in this task. Open `apps/web/docs/COIN-CLAIM-INVENTORY.md` (plan 03-10) and audit the
`## Claim inventory` table row by row. For each row record whether the **Verdict** column is filled
(`allowed`, `allowed-with-wording`, `prohibited`) and, for every `allowed-with-wording` row, whether the
**Replacement wording** column carries text the lawyer supplied.

Report the option that holds and list the ruled rows by number. Then stop and wait.

If any row is unruled, the correct answer is `partially-ruled`, not `fully-ruled`. Never fill a Verdict or a
Replacement wording yourself — that is the exact substitution COIN-04 exists to prevent.
  </action>
  <verify>
    <automated>grep -qE "^\s*-\s*\[x\].*[Ss]ign-off" apps/web/docs/COIN-LEGAL-CHECKLIST.md &amp;&amp; grep -qiE "allowed|prohibited" apps/web/docs/COIN-CLAIM-INVENTORY.md</automated>
  </verify>
  <done>Every inventory row's ruling status is known and recorded, and the set of rows this plan may act on is written down before any copy is touched.</done>
  <resume-signal>Reply with `fully-ruled`, `partially-ruled` (and list which rows are ruled), or `not-ruled`.</resume-signal>
</task>

<task type="auto" id="03-13-T2" tdd="true">
  <name>Task 2: Rewrite each ruled claim to its approved wording</name>
  <read_first>
    - apps/web/docs/COIN-CLAIM-INVENTORY.md with the verdicts filled in — this is the specification for this task, and the only source of replacement wording
    - apps/web/src/app/[locale]/economics/components/FAQ.tsx and HeroSection.tsx AS THEY STAND (plans 03-05 and 03-09 changed the money copy around these lines)
    - apps/web/src/app/[locale]/economics/page.tsx and app/[locale]/coin/page.tsx (metadata + openGraph)
    - apps/web/src/app/[locale]/faq/data/faqData.ts lines 63-68 and app/[locale]/terms/page.tsx line 99 (the conservative vocabulary)
    - apps/web/src/__tests__/services/economics-fee-split-copy.test.ts — its `describe('COIN-04 boundary is intact')` asserts `בדיוק כמו במניה` is PRESENT; this task makes that assertion wrong and Task 3 flips it
    - The ruling recorded in Task 1
  </read_first>
  <files>apps/web/src/app/[locale]/economics/components/FAQ.tsx, apps/web/src/app/[locale]/economics/components/HeroSection.tsx, apps/web/src/app/[locale]/economics/page.tsx, apps/web/src/app/[locale]/coin/page.tsx, apps/web/docs/COIN-CLAIM-INVENTORY.md</files>
  <behavior>
    - No surface contains `בדיוק כמו במניה` unless the lawyer ruled it `allowed`
    - No surface states or implies that the token gains value as more people buy, unless ruled `allowed`
    - No surface states a pool size that the ledger cannot produce
    - No surface promises a civic outcome; procedural statements about what happens at vote resolution survive
    - The register records, per row, what actually shipped
  </behavior>
  <action>
Work the register top to bottom. For each row:

- **`prohibited`** → remove the claim. Removing means the sentence is gone, not softened. If removing it
  leaves a paragraph incoherent, rewrite the paragraph using only vocabulary from rows ruled `allowed`.
- **`allowed-with-wording`** → substitute the lawyer's **exact** replacement text. Do not improve it, do not
  adjust it for tone, do not translate it. If the supplied wording does not fit the component's structure,
  change the structure, not the words.
- **`allowed`** → leave it byte-identical.
- **unruled** (only reachable via `partially-ruled`) → leave it byte-identical and record it in the SUMMARY.

Then sweep for the two claim classes COIN-04 names that may not appear as inventory rows because they are
numbers and promises rather than sentences:
- **pool size** — any figure presented as the fund's balance that `civic_pool_entries` cannot produce. Before
  plan 03-11 lands, that is every hardcoded figure. Remove or mark them as placeholders visibly.
- **civic outcome** — any copy stating the money *will* achieve a result. Keep procedural statements
  (*"בסיומה התוצאות מוגשות למועצה"*); remove guarantees.

Finally, update `COIN-CLAIM-INVENTORY.md`: add a **Shipped wording** column recording what is now in source
for each row, and a dated line under `## Sign-off` noting that the register and the code agree as of this
plan. The register stops being a request and becomes the record.

Constraints: text-content edits only — no class names, no CSS modules, no component structure, no new
dependency. `CLAUDE.md` applies: RTL Hebrew, design tokens, no hardcoded values.
  </action>
  <verify>
    <!-- Self-contained: the register guard is Task 3, and economics-fee-split-copy.test.ts currently asserts
         the OLD claim is present, so the suite is expected to be red between Tasks 2 and 3. Gate on
         typecheck plus a positive grep on the register instead. -->
    <automated>pnpm --filter @sync/web typecheck &amp;&amp; grep -q "Shipped wording" apps/web/docs/COIN-CLAIM-INVENTORY.md</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @sync/web typecheck` exits 0
    - For every row ruled `prohibited`, `grep -rc "<the claim>" apps/web/src` returns `0`
    - For every row ruled `allowed-with-wording`, the lawyer's exact replacement string appears in source
    - `grep -q "Shipped wording" apps/web/docs/COIN-CLAIM-INVENTORY.md` exits 0, and every row has that column filled
    - `git diff "apps/web/src/app/[locale]/economics/components/"*.module.css` is empty
    - `git diff --stat "apps/web/src/app/[locale]/faq/data/faqData.ts" "apps/web/src/app/[locale]/terms/page.tsx"` is empty unless the ruling explicitly required a change there
    - No new claim appears that is not a register row — diff every added Hebrew string against the register
    - The full suite is expected RED at this point (`economics-fee-split-copy.test.ts` still asserts the old claim); Task 3 makes it green. Record the failing test names in the SUMMARY.
  </acceptance_criteria>
  <done>Every ruled claim in the product matches what the lawyer approved, word for word, and the register records what shipped.</done>
</task>

<task type="auto" id="03-13-T3">
  <name>Task 3: Bind the shipped copy to the register with a guard</name>
  <read_first>
    - apps/web/src/__tests__/services/economics-fee-split-copy.test.ts — the `describe('COIN-04 boundary is intact')` block this task flips
    - apps/web/src/__tests__/services/money-model-sweep.test.ts (plan 03-09's repo-wide walk — reuse its collection helper shape rather than rewriting it)
    - apps/web/docs/COIN-CLAIM-INVENTORY.md with verdicts and shipped wording
    - apps/web/vitest.config.ts
  </read_first>
  <files>apps/web/src/__tests__/services/token-claim-register.test.ts, apps/web/src/__tests__/services/economics-fee-split-copy.test.ts</files>
  <action>
**Part A — flip plan 03-05's deliberate boundary assertion.** In
`apps/web/src/__tests__/services/economics-fee-split-copy.test.ts`, the `describe('COIN-04 boundary is intact')`
block asserts that `בדיוק כמו במניה` is **present**, as proof that plan 03-05 did not pre-empt a gated
decision. That decision has now been made. Replace the block with one asserting the post-ruling state, and
keep a comment recording the history: the claim was deliberately preserved through plans 03-05 and 03-09 and
was changed here only under the COIN-01 ruling dated `<date>`. Change nothing else in that file — its
fee-split assertions belong to plan 03-05 and are still correct.

**Part B — the register guard**, `apps/web/src/__tests__/services/token-claim-register.test.ts`. This is
COIN-04's proof, and it must be enforcement rather than a snapshot.

Read `apps/web/docs/COIN-CLAIM-INVENTORY.md` with `readFileSync` and parse its `## Claim inventory` table
into rows (a simple split on `|` is sufficient and preferable to a markdown dependency). Then:

`describe('prohibited claims are absent from source')` — for every row whose Verdict is `prohibited`, walk
`src` (reuse `money-model-sweep.test.ts`'s collection helper) and assert the quoted claim appears in no
comment-free source file. Drive it with `it.each` over the parsed rows so a new prohibited row is enforced the
moment it is added to the register, with no test edit.

`describe('approved wording is what ships')` — for every row whose Verdict is `allowed-with-wording`, assert
the register's **Shipped wording** appears in the cited file.

`describe('no unregistered token claim')` — a bounded sweep for the phrases that mark an investment claim
(`כמו במניה`, `שווה יותר`, `תשואה`, `רווח מובטח`, `השקעה בטוחה`). Any hit must correspond to a register row
ruled `allowed`; otherwise it is a violation. Collect `{ file, phrase }` violations into an array and assert
it equals `[]`, so a failure names the file rather than printing `expected false to be true`.

`describe('the register itself is complete')` — every row has a non-empty Verdict, and every
`allowed-with-wording` row has non-empty Replacement wording. This is what stops the guard from silently
passing because the register was emptied.

Use `vitest run` semantics only — no `--watch`, no `.only`. Keep the run under a second.
  </action>
  <verify>
    <automated>pnpm --filter @sync/web test -- src/__tests__/services/token-claim-register.test.ts src/__tests__/services/economics-fee-split-copy.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @sync/web test -- src/__tests__/services/token-claim-register.test.ts src/__tests__/services/economics-fee-split-copy.test.ts` exits 0
    - `grep -c "COIN-CLAIM-INVENTORY" apps/web/src/__tests__/services/token-claim-register.test.ts` is at least `1` — the register is read, not transcribed
    - `grep -c "it.each" apps/web/src/__tests__/services/token-claim-register.test.ts` is at least `2`
    - `grep -c "prohibited" apps/web/src/__tests__/services/token-claim-register.test.ts` is at least `2`
    - `grep -c "COIN-04 boundary is intact" apps/web/src/__tests__/services/economics-fee-split-copy.test.ts` returns `0` — the boundary block was flipped, not left contradicting reality
    - `grep -cE "70%|30%" apps/web/src/__tests__/services/economics-fee-split-copy.test.ts` is unchanged — plan 03-05's assertions survive
    - `grep -cE "\.only\(|--watch" apps/web/src/__tests__/services/token-claim-register.test.ts` returns `0`
    - `pnpm --filter @sync/web test` (full suite) exits 0
    - Break it once to confirm enforcement: temporarily add a prohibited phrase to a `src` file, run the guard, confirm it fails and names the file, then revert. Record the message in the SUMMARY.
  </acceptance_criteria>
  <done>The register is the source of truth for what may be said about the token, and source that disagrees with it fails the suite.</done>
</task>

</tasks>

<verification>
1. Task 1's gate is resolved `fully-ruled` or `partially-ruled` with the ruled rows listed — otherwise nothing below ran
2. `pnpm --filter @sync/web typecheck` — exits 0
3. `pnpm --filter @sync/web test` — full suite exits 0
4. Every `prohibited` register row: `grep -rc "<claim>" apps/web/src` returns 0
5. `apps/web/docs/COIN-CLAIM-INVENTORY.md` has a Verdict and a Shipped wording for every row, and a dated sign-off line
6. `git diff --stat "apps/web/src/app/[locale]/pricing/" "apps/web/src/components/press/Ticker/"` — empty (plans 03-04 and 03-09's surfaces untouched)
</verification>

<success_criteria>
- The gate was honoured: no claim was reworded on a developer's judgement
- Every prohibited claim is gone, and every reworded claim carries the lawyer's exact text
- No pool size is stated that the ledger cannot produce, and no civic outcome is promised
- The register carries a verdict and the shipped wording for every row, and a test enforces both
- A new unapproved investment claim added later fails the suite and names its file
</success_criteria>

<output>
After completion, create `.planning/phases/03-payment-rails-hardening/03-13-SUMMARY.md`
</output>
