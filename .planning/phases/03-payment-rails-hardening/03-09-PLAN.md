---
phase: 03-payment-rails-hardening
plan: 09
type: execute
wave: 2
depends_on: ["03-02", "03-03", "03-04", "03-05"]
files_modified:
  - apps/web/src/app/[locale]/treasury/components/TreasuryDashboard.tsx
  - apps/web/src/app/[locale]/pricing/components/PricingContent.tsx
  - apps/web/src/__tests__/services/money-model-sweep.test.ts
autonomous: true
requirements: [PAY-08]

must_haves:
  truths:
    - "The treasury board never presents a computed 70/30 allocation as accounting — it shows only figures the ledger produces"
    - "The pricing rate card states what funds the civic pool, so a reader is not left to assume their vote pays for it"
    - "One test sweeps the whole of apps/web/src and apps/mobile/app for a retired money figure, so PAY-08 is proven once rather than argued surface by surface"
  artifacts:
    - path: "apps/web/src/app/[locale]/treasury/components/TreasuryDashboard.tsx"
      provides: "allocation board with no fabricated percentage split"
    - path: "apps/web/src/app/[locale]/pricing/components/PricingContent.tsx"
      provides: "rate card that names the pool's funding source"
    - path: "apps/web/src/__tests__/services/money-model-sweep.test.ts"
      provides: "repo-wide guard: PAY-08's closing proof"
  key_links:
    - from: "apps/web/src/__tests__/services/money-model-sweep.test.ts"
      to: "apps/web/src + apps/mobile/app"
      via: "recursive source walk asserting no retired money figure survives"
      pattern: "readdirSync"
---

<objective>
Close PAY-08. Remove the last two surfaces that misstate the money model — the treasury board's fabricated 70/30 allocation and the pricing card's silence about what funds the pool — then prove the whole requirement with one repo-wide sweep instead of five per-surface guards.

Purpose: `TreasuryDashboard.tsx:325-338` is the most serious instance of the split claim, because it does not merely *state* a percentage — it **computes** one. It renders `formatCurrency(treasury.totalILS * 0.7)` and `formatCurrency(treasury.totalILS * 0.3)` inside a `Receipt` footed with *"כל סכום מתועד · חתום בבלוקצ׳יין · ביקורת חשבונאית עצמאית"*. That presents an invented multiplication as audited accounting. Nothing in the codebase implements a 70/30 split; the ledger cannot produce those two numbers.

Output: an allocation board showing only real figures, a rate card that says where the pool's money comes from, and a single sweep test that is PAY-08's proof.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-payment-rails-hardening/03-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Verified 2026-08-03. Plans 03-02/03/04/05 land first; re-read every file before editing. -->

The fabricated accounting — `apps/web/src/app/[locale]/treasury/components/TreasuryDashboard.tsx:305-343`:
```tsx
<h3 className={styles.sectionTitle}>… התפלגות הכנסות</h3>

<AllocationLedger local={treasury.localContributions} external={treasury.externalContributions} />
{/* ^ REAL data, straight off the treasury row. KEEP. */}

<Receipt
  kicker="חלוקה · ALLOCATION"
  rows={[
    { label: '70% לקרן הרשות',        value: formatCurrency(treasury.totalILS * 0.7) },   // INVENTED
    { label: '30% תפעול הפלטפורמה',   value: formatCurrency(treasury.totalILS * 0.3) },   // INVENTED
    { label: 'סך הקרן', value: formatCurrency(treasury.totalILS), strong: true },          // REAL
  ]}
  footer="כל סכום מתועד · חתום בבלוקצ׳יין · ביקורת חשבונאית עצמאית"
/>
```
`AllocationLedger` above it already shows a *real* split — `localContributions` vs `externalContributions`,
both read from the treasury row. That is the honest breakdown and it stays.

The pricing rate card — `apps/web/src/app/[locale]/pricing/components/PricingContent.tsx`:
```typescript
:12  const TRUST_ITEMS = ['אין מנוי', 'אין דמי חבר', 'אין אותיות קטנות'];        // TRUE - keep
:15  const PARTICIPATION_SPEC = [
       'הצבעה מאומתת בנושא מקומי, בלי תשלום',                                   // TRUE
       'זהות ו-GPS · חתום בבלוקצ׳יין',                                          // chain claim - OUT OF SCOPE
       'התמונה המלאה פתוחה לכולם',
     ];
:64  ההצבעה חינם. ₪{CREATE_VOTE_COST} ליצירת הצבעה חדשה. אין מנוי, אין דמי חבר… // TRUE
```
The price is already right. What is missing is PAY-08's third clause — *"the civic pool is funded by the
token"*. A reader of this page has no way to know that, and the natural assumption is that their vote pays
for it. The approved, already-shipped phrasing to reuse:
- `app/[locale]/faq/data/faqData.ts:65-67` — *"הקרן מתמלאת מהשקעות חיצוניות ב-BAG של כל הצבעה, לא מכסף של תושבים."*
- `app/[locale]/terms/page.tsx:99` — the same statement in the terms.

**Out of scope, deliberately:** `PARTICIPATION_SPEC`'s `חתום בבלוקצ׳יין` is a chain-seal claim, not a money
claim. `02.1-VALIDATION.md` recorded the marketing-surface chain sweep as a known gap, and `03-CONTEXT.md`
keeps it deferred. Leave it and say so in the SUMMARY.

`apps/web/src/app/[locale]/treasury/page.tsx:11` and `TreasuryHero.tsx:102` make no percentage claim; leave
both. `apps/web/src/components/sections/FundTransparency/FundTransparency.tsx` is unmounted membership-era
code — record it, do not sweep it.

**What lands before this plan** (all wave 1) — the sweep in Task 3 depends on every one of them:
| Plan | Removed |
|------|---------|
| 03-02 | the ₪3 participation rail, `getPaymentAmounts().voteParticipation`, the published participation price |
| 03-03 | the participation request contract, the api-client method, the mobile checkout branch |
| 03-04 | the Ticker's `₪2`, `MoneyTransparency`, the email's `₪3`, the mobile welcome `₪3` |
| 03-05 | `/economics`'s `70%` / `30%` / `1% על כל עסקה` |

Sibling guards that already exist and must not be duplicated:
`__tests__/services/participation-cost-legacy.test.ts` (plan 02.1-03),
`__tests__/services/money-model-copy.test.ts` (plan 03-04),
`__tests__/services/economics-fee-split-copy.test.ts` (plan 03-05).
This plan's sweep is the union proof, not a fourth copy of them.
</interfaces>
</context>

<tasks>

<task type="auto" id="03-09-T1" tdd="true">
  <name>Task 1: Stop the treasury board from computing an allocation it cannot back</name>
  <read_first>
    - apps/web/src/app/[locale]/treasury/components/TreasuryDashboard.tsx (whole file — the allocation board at :305-343, `AllocationLedger`, and where `treasury.totalILS` / `localContributions` / `externalContributions` come from)
    - apps/web/src/app/api/treasury/[municipality]/route.ts and the `getTreasuryByMunicipality` shape (what the ledger can actually produce)
    - apps/web/src/components/press/Receipt (the component's `rows` / `footer` contract)
    - apps/web/src/app/[locale]/treasury/components/TreasuryDashboard.module.css (the `.allocReceipt` class — decide whether it survives)
    - .planning/phases/03-payment-rails-hardening/03-CONTEXT.md — assumption A6 (no code implements a 70/30 split)
  </read_first>
  <files>apps/web/src/app/[locale]/treasury/components/TreasuryDashboard.tsx</files>
  <behavior>
    - The file's source contains no `* 0.7` and no `* 0.3`
    - The file's source contains no `70%` and no `30%`
    - `AllocationLedger` still receives `localContributions` and `externalContributions` unchanged
    - The board still shows the real fund total
    - The `התפלגות הכנסות` section still renders — the board is corrected, not emptied
  </behavior>
  <action>
Replace the `Receipt`'s invented rows. Two acceptable outcomes — record which you took in the SUMMARY:

- *Preferred:* keep the `Receipt` and reduce it to figures the ledger produces. The treasury row already
  carries `localContributions`, `externalContributions` and `totalILS`; a receipt of
  *local · external · total* is a real breakdown of real money and needs no new API call. Keep the
  `strong: true` on the total.
- *Acceptable:* delete the `Receipt` entirely and let `AllocationLedger` stand alone, then remove the
  `.allocReceipt` rule from the CSS module and the `Receipt` import if nothing else uses it.

Either way:
- Both `formatCurrency(treasury.totalILS * 0.7)` and `formatCurrency(treasury.totalILS * 0.3)` must go. A
  multiplication by a constant that no ledger implements is not a breakdown; it is a number the page made up.
- The footer *"כל סכום מתועד · חתום בבלוקצ׳יין · ביקורת חשבונאית עצמאית"* asserts an independent accounting
  audit and a chain signature over these figures. Keep it **only** if every row above it is a real ledger
  figure; otherwise trim it to what is true. Read `TreasuryHero.tsx:102` for the tone already used on this page.
- Add a short `//` comment where the invented rows were, stating that a percentage split is COIN-02's ledger
  to establish and that it does not exist yet, so nothing here may assert one.

Do not touch `AllocationLedger`'s props, the activity counters, the hero, or the page metadata. Text and
row-data edits only — no new class names, no new API calls.
  </action>
  <verify>
    <!-- Self-contained: the sweep is Task 3 of this plan. -->
    <automated>pnpm --filter @sync/web typecheck &amp;&amp; ! grep -qE "\* 0\.7|\* 0\.3|70%|30%" "apps/web/src/app/[locale]/treasury/components/TreasuryDashboard.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @sync/web typecheck` exits 0
    - `grep -cE "\* 0\.7|\* 0\.3" "apps/web/src/app/[locale]/treasury/components/TreasuryDashboard.tsx"` returns `0`
    - `grep -cE "70%|30%" "apps/web/src/app/[locale]/treasury/components/TreasuryDashboard.tsx"` returns `0`
    - `grep -c "AllocationLedger" "apps/web/src/app/[locale]/treasury/components/TreasuryDashboard.tsx"` is unchanged from before the edit
    - `grep -c "localContributions" "apps/web/src/app/[locale]/treasury/components/TreasuryDashboard.tsx"` is at least `1`
    - `grep -c "התפלגות הכנסות" "apps/web/src/app/[locale]/treasury/components/TreasuryDashboard.tsx"` returns `1` — the section survives
    - `git diff --stat "apps/web/src/app/[locale]/treasury/components/TreasuryHero.tsx" "apps/web/src/app/[locale]/treasury/page.tsx"` is empty
    - `pnpm --filter @sync/web test` (full suite) exits 0
  </acceptance_criteria>
  <done>Every number on the treasury board comes from the ledger, and the page no longer multiplies a balance by a percentage nothing implements.</done>
</task>

<task type="auto" id="03-09-T2" tdd="true">
  <name>Task 2: Make the rate card say what funds the civic pool</name>
  <read_first>
    - apps/web/src/app/[locale]/pricing/components/PricingContent.tsx (whole file — TRUST_ITEMS :12, PARTICIPATION_SPEC :15, CREATE_SPEC :21, the standfirst :63-66, the trust strip :146-163)
    - apps/web/src/app/[locale]/pricing/page.tsx lines 1-20 (the metadata description — already correct, confirm before changing anything)
    - apps/web/src/app/[locale]/faq/data/faqData.ts lines 63-68 (the approved phrasing)
    - apps/web/src/app/[locale]/terms/page.tsx lines 95-102 (the same statement in the terms)
    - apps/web/src/app/[locale]/pricing/components/PricingContent.module.css (add no class)
  </read_first>
  <files>apps/web/src/app/[locale]/pricing/components/PricingContent.tsx</files>
  <behavior>
    - The page states that the civic fund is filled by outside purchases of the vote's BAG, not by residents' money
    - The existing `חינם` and `₪50` figures are unchanged
    - `TRUST_ITEMS` still reads `אין מנוי · אין דמי חבר · אין אותיות קטנות`
    - No percentage, pool size, or expected return is added
    - `PARTICIPATION_SPEC`'s chain-seal line is untouched
  </behavior>
  <action>
Add the pool's funding source to the rate card. PAY-08's three clauses are *participation is free*,
*creation costs ₪50*, and *the civic pool is funded by the token* — this page currently states the first two
and is silent on the third, which leaves a reader to assume their vote pays for the fund.

The lightest place that carries real weight is the free-participation card's `note` block (`:98-104`), which
already explains why participation is free. Extend it, or add a short line beneath the two rate cards, using
the phrasing already approved and shipped in `faqData.ts:65-67`. It must say that the fund is filled by
outside investment in each vote's BAG rather than by residents' money.

Hard constraints:
- No percentage, no pool figure, no return, no guarantee. Naming the funding source is a fact; quantifying it
  is COIN-04's, behind the legal gate.
- Do not touch `PARTICIPATION_SPEC[1]` (`זהות ו-GPS · חתום בבלוקצ׳יין`) — that is the deferred chain-copy
  sweep, not this requirement.
- Do not touch `page.tsx`'s metadata; it is already accurate.
- Reuse existing CSS-module classes only. `CLAUDE.md`: design tokens, no hardcoded values, RTL logical props.
  </action>
  <verify>
    <automated>pnpm --filter @sync/web typecheck &amp;&amp; grep -q "BAG" "apps/web/src/app/[locale]/pricing/components/PricingContent.tsx" &amp;&amp; grep -q "CREATE_VOTE_COST" "apps/web/src/app/[locale]/pricing/components/PricingContent.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @sync/web typecheck` exits 0
    - `grep -c "BAG" "apps/web/src/app/[locale]/pricing/components/PricingContent.tsx"` is at least `1`
    - `grep -cE "70%|30%|₪3|₪6" "apps/web/src/app/[locale]/pricing/components/PricingContent.tsx"` returns `0`
    - `grep -c "אין מנוי" "apps/web/src/app/[locale]/pricing/components/PricingContent.tsx"` is at least `2` (TRUST_ITEMS and the standfirst)
    - `grep -c "חתום בבלוקצ׳יין" "apps/web/src/app/[locale]/pricing/components/PricingContent.tsx"` returns `1` — the deferred chain line is untouched
    - `git diff "apps/web/src/app/[locale]/pricing/components/PricingContent.module.css" "apps/web/src/app/[locale]/pricing/page.tsx"` is empty
    - `pnpm --filter @sync/web test` (full suite) exits 0
  </acceptance_criteria>
  <done>A reader of the pricing page learns that the civic fund is not paid for by their vote.</done>
</task>

<task type="auto" id="03-09-T3">
  <name>Task 3: One repo-wide sweep — PAY-08's closing proof</name>
  <read_first>
    - apps/web/src/__tests__/services/money-model-copy.test.ts (plan 03-04's guard) and .../economics-fee-split-copy.test.ts (plan 03-05's) and .../participation-cost-legacy.test.ts (plan 02.1-03's) — read all three so this sweep complements rather than duplicates them
    - apps/web/src/__tests__/services/dashboard-free-mvp.test.ts (the `code()` comment-stripper)
    - .planning/phases/03-payment-rails-hardening/03-CONTEXT.md — the enumerated surface table under "PAY-08", and the "Already correct — do not touch" list
    - apps/web/vitest.config.ts
  </read_first>
  <files>apps/web/src/__tests__/services/money-model-sweep.test.ts</files>
  <action>
Create `apps/web/src/__tests__/services/money-model-sweep.test.ts` — the requirement-level proof for PAY-08.
The three sibling guards each lock one plan's surfaces; this one asserts that **no surface anywhere** carries
a retired money figure, so a new file added next month is covered without anyone remembering to extend a list.

Walk the source trees recursively with `readdirSync(dir, { withFileTypes: true })`:
- `join(process.cwd(), 'src')` for the web app;
- `join(process.cwd(), '../../apps/mobile/app')` for mobile — guard it with `existsSync` and skip with a
  clear message rather than failing if the path does not resolve, so the suite survives a repo move.

Collect `.ts` and `.tsx` files. **Skip** `node_modules`, `.next`, `.expo`, `__tests__`, `src/lib/supabase/types.ts`
(generated, mirrors the DB enum) and any `.module.css`. Strip comments from each file with the `code()` helper
before matching — a comment explaining that ₪3 was retired must not be read as a live price.

`describe('no retired price survives anywhere')` — assert no collected file's comment-free source contains:
- `₪3` or `₪6`
- `₪2 מכל הצבעה`
- `דמי חבר` **outside** the negations. Handle this precisely: `PricingContent.tsx` legitimately says
  `אין דמי חבר`. Match the term only when it is **not** preceded by `אין `, or simply assert that every
  occurrence of `דמי חבר` in the repo is preceded by `אין `. The second formulation is clearer — write that one.
- `מנוי` under the same not-preceded-by-`אין ` rule

`describe('no unbacked split survives anywhere')` — no collected file contains `70%` or `30%` in a
money context. `%` appears in CSS-in-JS and in `LiveDashboard.tsx:81`'s `(num * 100).toFixed(0)%` percentage
formatter, so match the **Hebrew-adjacent** forms actually used — `70% לקרן`, `30% לפלטפורמה`,
`30% מממן`, `70% זורם` — plus the bare tokens `* 0.7` and `* 0.3` applied to a currency value. Prefer a small
explicit list of retired phrases over a broad `%` regex; a false positive here would block unrelated work.

`describe('the model is stated where it matters')` — positive assertions, so the sweep cannot be satisfied by
deleting everything:
- `PricingContent.tsx` contains `חינם` and `CREATE_VOTE_COST`;
- `faqData.ts` still contains the free-participation answer;
- `packages/shared/src/constants/index.ts` still exports `VOTE_PARTICIPATION_COST = 0` and `CREATE_VOTE_COST = 50`.

On failure the message must name the offending file and the matched phrase — a bare "expected false to be
true" on a 400-file walk is useless. Build the assertion as a collected array of `{ file, phrase }` violations
and assert it equals `[]`, so the diff prints them.

Use `vitest run` semantics only — no `--watch`, no `.only`. Keep the walk under a second; the full suite
currently runs in ~2.3s and must stay fast.
  </action>
  <verify>
    <automated>pnpm --filter @sync/web test -- src/__tests__/services/money-model-sweep.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @sync/web test -- src/__tests__/services/money-model-sweep.test.ts` exits 0
    - `grep -c "readdirSync" apps/web/src/__tests__/services/money-model-sweep.test.ts` is at least `1`
    - `grep -c "existsSync" apps/web/src/__tests__/services/money-model-sweep.test.ts` is at least `1`
    - `grep -c "types.ts" apps/web/src/__tests__/services/money-model-sweep.test.ts` is at least `1` — the generated file is skipped
    - `grep -c "אין " apps/web/src/__tests__/services/money-model-sweep.test.ts` is at least `1` — the negation rule is implemented
    - `grep -cE "\.only\(|--watch" apps/web/src/__tests__/services/money-model-sweep.test.ts` returns `0`
    - `pnpm --filter @sync/web test` (full suite) exits 0 and completes in under 10 seconds
    - Deliberately break it once to confirm the message is useful: temporarily add `const x = '₪3';` to any `src` file, run the test, confirm the failure output names that file, then revert. Record the observed message in the SUMMARY.
  </acceptance_criteria>
  <done>PAY-08 is proven by one test over the whole repository, and a violation names the file that caused it.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @sync/web typecheck` — exits 0
2. `pnpm --filter @sync/web test` — full suite exits 0
3. `pnpm --filter @sync/shared typecheck` · `pnpm --filter @sync/api-client test` · `pnpm --filter @sync/mobile typecheck` — all exit 0
4. `grep -rnE "\* 0\.7|\* 0\.3" "apps/web/src/app/[locale]/treasury/"` — no matches
5. `grep -rn "₪3\|₪6" apps/web/src apps/mobile/app --include=*.ts --include=*.tsx` — matches only inside comments describing the retirement
6. `git diff --stat "apps/web/src/app/[locale]/economics/"` — empty (plan 03-05's surface untouched)
</verification>

<success_criteria>
- The treasury board shows only ledger figures; the invented `× 0.7` / `× 0.3` allocation is gone
- The pricing page states that the civic fund is filled by outside BAG investment, not by residents' money
- A single repo-wide test proves PAY-08 across `apps/web/src` and `apps/mobile/app`, names the file on failure, and keeps the suite under ten seconds
- The deferred chain-seal copy and the generated Supabase types are untouched
</success_criteria>

<output>
After completion, create `.planning/phases/03-payment-rails-hardening/03-09-SUMMARY.md`
</output>
