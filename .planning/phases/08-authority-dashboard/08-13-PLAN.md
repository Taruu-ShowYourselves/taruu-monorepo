---
phase: 08-authority-dashboard
plan: 13
type: execute
wave: 6
depends_on: ["08-05", "08-06", "08-07", "08-09", "08-10", "08-11", "08-12"]
files_modified:
  - apps/web/src/__tests__/rls/authority-tables.rls.test.ts
  - apps/web/src/__tests__/api/authority-scope-guard.test.ts
  - apps/web/docs/AUTHORITY-VERIFICATION.md
  - .planning/phases/08-authority-dashboard/08-VALIDATION.md
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06]

user_setup:
  - service: supabase
    why: "Phase 8's two migrations must be applied to the live database, after Phase 5's two, and there is no `supabase db push` script in this repo"
    env_vars:
      - name: SUPABASE_JWT_SECRET
        source: "Already required by Phase 5 plan 05-09. Confirm it is set locally in apps/web/.dev.vars and as a Worker secret (`cd apps/web && npx wrangler secret list`) — do NOT print its value."
    dashboard_config:
      - task: "Apply supabase/migrations/20260803000001_authority_access.sql then 20260803000002_authority_content.sql, in that order, after Phase 5's 20260802000001 and 20260802000002"
        location: "Supabase Management API (keychain token) or Supabase Studio SQL editor — the path used for the knesset and identity-document migrations"
      - task: "Create one test organization and verify it through /he/admin/authority-claims as the bootstrapped super_admin, so the visual evidence and the live probes have a subject"
        location: "The running app"

must_haves:
  truths:
    - "Both Phase 8 migrations are applied to the live database, in order, after Phase 5's"
    - "No route under src/app/api/authority/ reads a municipality from the request — proven by an automated guard, not by review"
    - "A representative of municipality A reads no authority row belonging to B through the user-scoped client"
    - "An UPDATE or DELETE against official_responses and authority_commitments raises in the database"
    - "A second verified organization for one municipality is rejected by the database"
    - "The public council page renders identically before and after an authority is verified"
  artifacts:
    - path: "apps/web/src/__tests__/api/authority-scope-guard.test.ts"
      provides: "The automated criterion-2 guard: no client-supplied municipality anywhere in the authority API surface"
      contains: "api/authority"
    - path: "apps/web/src/__tests__/rls/authority-tables.rls.test.ts"
      provides: "Cross-municipality RLS proof for the five scoped authority tables, extending the 05-04 harness"
      contains: "expectAnonReadsNothing"
    - path: "apps/web/docs/AUTHORITY-VERIFICATION.md"
      provides: "The recorded outcome of every check this phase cannot run in CI"
      contains: "RLS harness"
  key_links:
    - from: "apps/web/src/__tests__/rls/authority-tables.rls.test.ts"
      to: "apps/web/src/__tests__/rls/harness.ts"
      via: "reuses the Phase 5 harness rather than reinventing setup"
      pattern: "from './harness'"
    - from: "apps/web/docs/AUTHORITY-VERIFICATION.md"
      to: ".planning/phases/08-authority-dashboard/08-VALIDATION.md"
      via: "the manual rows are marked green here"
      pattern: "AUTH-01"
---

<objective>
Land Phase 8 on a real database and record the evidence: apply both migrations, prove
cross-municipality isolation at the database layer, prove the append-only triggers and the
one-verified-authority-per-municipality index actually fire, add the automated scope guard that
criterion 2 depends on, and capture the visual evidence issue #76 asks for.

Purpose: everything before this plan is code and files. Nothing has touched a database, no
organization has ever been verified, and the phase's central safety property — that no route reads a
municipality from a request — has been a discipline rather than a check. This plan closes all of it.

Output: two new test files, one verification record, a signed-off validation contract, and screenshots.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-authority-dashboard/08-CONTEXT.md
@.planning/phases/08-authority-dashboard/08-VALIDATION.md
@.planning/phases/05-rbac-admin-review/05-09-PLAN.md
@.planning/phases/05-rbac-admin-review/05-VALIDATION.md

<interfaces>
<!-- Read from the repo 2026-08-03. Use directly. -->

The Phase 5 RLS harness this plan extends (`apps/web/src/__tests__/rls/harness.ts`, plan 05-04):
```ts
export function readRlsTestEnv(): RlsTestEnv | null;
export function describeRls(name: string, fn: () => void): void;   // skips LOUDLY without creds
export function anonClient(): SupabaseClient;
export function serviceClient(): SupabaseClient;
export function rlsUserClient(userId: string): SupabaseClient;     // anon key + minted token
export async function seedThrowawayUsers(n: number): Promise<string[]>;  // with cleanup
export async function expectAnonReadsNothing(table: string): Promise<void>;
export async function expectCrossUserInvisible(table: string, ...): Promise<void>;
```
Run command: `cd apps/web && set -a && . ./.dev.vars && set +a && npx vitest run src/__tests__/rls`
**A run reporting 0 tests is a skip, not a pass — check the count.**

Phase 8 tables and their policies (plans 08-01, 08-04):
```
authority_organizations              authority_orgs_select_member / _admin
authority_claims                     authority_claims_select_own / _admin
authority_rep_invitations            authority_invites_select_member / _admin
authority_commitments                authority_commitments_select_member / _admin
authority_satisfaction_snapshots     satisfaction_snapshots_select_member / _admin
official_responses                   official_responses_select_public   (deliberately USING (true))
public.is_authority_member(p_space TEXT)   STABLE SECURITY DEFINER, search_path pinned
```

Migration order, absolute:
`20260802000001` → `20260802000002` (Phase 5) → `20260803000001` → `20260803000002` (Phase 8).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The automated scope guard — no client-supplied municipality anywhere</name>
  <read_first>
    - apps/web/src/__tests__/api/municipality-public-surface.test.ts (plan 08-02's sibling guard — match its structure and failure-message quality)
    - .planning/phases/08-authority-dashboard/08-CONTEXT.md (the Phase 7 risk block — this guard is mitigation 1)
  </read_first>
  <files>apps/web/src/__tests__/api/authority-scope-guard.test.ts</files>
  <action>
Create `apps/web/src/__tests__/api/authority-scope-guard.test.ts`.

Header comment: ROADMAP Phase 8 success criterion 2 — cross-municipality isolation is "enforced
server-side through the Phase 5 authorization helper, never inferred client-side". Phase 7 has not
landed, so the application is currently the **only** layer enforcing that boundary
(08-CONTEXT's accepted-risk block). This guard is mitigation 1: it makes "no route reads a
municipality from the request" a test rather than a habit.

Scan every file under `src/app/api/authority/` and `src/server/app/authority/`
(`readdirSync(..., { recursive: true })`, filtered to `.ts`), and assert:

1. **No client-supplied municipality.** No file matches any of:
   - `searchParams.get('municipality` (any suffix)
   - `params.municipality`
   - `body.municipality` / `input.municipality` / `request.*municipality`
   - a destructure of `municipalityId` from a parsed request body
   Implement as a small array of labelled regexes so the failure message can name which pattern
   matched, in which file, on which line.
   Allow exactly one exception, declared explicitly with its reason: the accept-invitation path reads
   its municipality from the **stored invitation row**, not from the request — if the implementation
   names a local variable in a way that trips a pattern, fix the variable name rather than widening
   the guard, and say so in the message.

2. **Scope is always resolved.** Every file under `src/app/api/authority/` that exports a `POST` or a
   `GET` either calls a use-case from `src/server/app/authority/` or is the invitation-accept route.
   And every module in `src/server/app/authority/` other than `require-authority.ts` and `mappers.ts`
   references `resolveAuthorityScope` or `requireAuthority`.

3. **Single authorization path.** No file under `src/server/app/authority/` contains
   `from '@/server/infra/supabase/role.repo'`'s `findLiveGrant` directly — grant lookups go through
   `require-authority.ts`, which goes through Phase 5's `requireRole`. (`role.repo`'s
   `insertGrant` / `setGrantStatus` / `insertAuditEvent` are legitimate imports and must not trip
   this; scope the assertion to `findLiveGrant`.)

4. **No mutation verbs on public authority reads.** No file under `src/app/api/votes/` exports a
   handler that writes to an authority table.

Assert a non-zero scanned-file count in every block, so a path typo fails loudly rather than passing
vacuously.
  </action>
  <verify>
    <automated>cd /Users/saharbarak/personal/taro/apps/web && npx vitest run src/__tests__/api/authority-scope-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The run exits 0 with at least 6 passing tests
    - Every block asserts a non-zero scanned-file count
    - Failures name the file, the line and the matched pattern
    - The `findLiveGrant` assertion does not trip on legitimate `insertGrant` / `setGrantStatus` imports
  </acceptance_criteria>
  <done>Criterion 2's central invariant is a test, not a convention.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the Phase 5 RLS harness to the authority tables</name>
  <read_first>
    - apps/web/src/__tests__/rls/harness.ts (plan 05-04 — reuse every primitive; add nothing to it that
      is Phase 8 specific)
    - apps/web/src/__tests__/rls/role-tables.rls.test.ts (the Phase 5 assertions this file mirrors)
    - apps/web/docs/RLS-TESTING.md (how to point the harness at a database)
  </read_first>
  <files>apps/web/src/__tests__/rls/authority-tables.rls.test.ts</files>
  <action>
Create `apps/web/src/__tests__/rls/authority-tables.rls.test.ts` using `describeRls` so it skips
loudly without credentials.

Setup, through `serviceClient()`: seed two throwaway users, two organizations verified for two
different municipalities (A and B), one active `authority_rep` grant for user A in municipality A, one
claim, one invitation, one commitment and one satisfaction snapshot per municipality, and one
published official response per municipality. Register cleanup for every row.

Assertions:
1. **Cross-municipality invisibility (criterion 2, at the database layer).** Through
   `rlsUserClient(userA)`: reads of `authority_organizations`, `authority_rep_invitations`,
   `authority_commitments` and `authority_satisfaction_snapshots` return municipality A's rows and
   **zero** of B's. Assert both halves — a policy that returns nothing at all would pass a
   B-invisibility check while being broken.
2. **Anon reads nothing** from the five scoped tables (`expectAnonReadsNothing`).
3. **Official responses are public**, deliberately: `anonClient()` reads both municipalities'
   responses. Comment that this is the `USING (true)` policy from `20260803000002` and that its
   written justification lives in that migration — a future reader must not "fix" it.
4. **No recursion (42P17).** A read of `authority_organizations` under user A's token succeeds; assert
   the error is null, since `is_authority_member` queries `role_grants` from inside a policy and would
   raise 42P17 without `SECURITY DEFINER`.
5. **Append-only, in the database.** Through `serviceClient()` (which bypasses RLS, so this tests the
   trigger and not a policy): an UPDATE and a DELETE against `official_responses`,
   `authority_commitments` and `authority_satisfaction_snapshots` each raise. Assert the error is
   non-null for all six.
6. **One verified authority per municipality.** Inserting a second organization with
   `verification_status='verified'` for municipality A raises a unique-violation. Assert the error
   code is `23505`.
7. **Verified requires evidence.** Inserting an organization with `verification_status='verified'` and
   a null `verified_by` raises a check-violation. Assert the error is non-null and mentions
   `authority_org_verified_evidence_ck`.
8. **A user-scoped write is rejected** — there are no INSERT policies, so an insert through
   `rlsUserClient` into `authority_claims` fails.

Assertions 5–7 are the ones that cannot be faked by any amount of application code; they are why this
plan exists.
  </action>
  <verify>
    <automated>cd /Users/saharbarak/personal/taro/apps/web && set -a && . ./.dev.vars && set +a && npx vitest run src/__tests__/rls</automated>
  </verify>
  <acceptance_criteria>
    - The RLS suite runs with a **non-zero** test count (0 tests = skipped, not passed) and exits 0
    - Both halves of the cross-municipality assertion are present (A visible, B invisible)
    - All six append-only UPDATE/DELETE probes assert a non-null error
    - The 23505 and the `authority_org_verified_evidence_ck` probes both assert
    - The file adds nothing Phase-8-specific to `harness.ts`
  </acceptance_criteria>
  <done>Isolation, append-only and the verification constraints are proven against a real Postgres.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Apply the migrations, walk the lifecycle, record the evidence</name>
  <what-built>
    Phase 8 in full: two migrations, the access and content schemas, the authority authorization
    composition over Phase 5's `requireRole`, claim submission and super-admin review, representative
    invite/accept/offboard, the dashboard with its cohort floor, versioned official responses,
    target tracking, exports, and the public official-response surface on the vote page.

    Everything above is code and tests. Nothing has run against a database and no organization has
    ever been verified.
  </what-built>
  <files>apps/web/docs/AUTHORITY-VERIFICATION.md, .planning/phases/08-authority-dashboard/08-VALIDATION.md</files>
  <action>
Apply both Phase 8 migrations to the live database, run every automated suite, execute the four live
SQL probes, walk the full lifecycle in the running app capturing the screenshots issue #76 asks for,
and record every outcome in `apps/web/docs/AUTHORITY-VERIFICATION.md` — then mark the manual rows in
`08-VALIDATION.md` green and set `wave_0_complete: true`. The exact steps, in order, are in
`<how-to-verify>` below; follow them literally rather than summarising them.
  </action>
  <how-to-verify>
    **Step 0 — blocking precondition.** Confirm Phase 5 plan 05-09 is complete: its two migrations are
    applied, `SUPABASE_JWT_SECRET` is set locally and as a Worker secret (`npx wrangler secret list` —
    check the name is listed; **do not print the value**), and at least one `super_admin` grant exists.
    If any of these is missing, **stop and report**. Phase 8 cannot be verified on top of an unverified
    Phase 5, and Phase 5's own summary flags the HS256 assumption as unchecked against the live
    project — if this project uses asymmetric signing keys, the RLS suite cannot run and that finding
    belongs in the record rather than being worked around.

    **Step 1 — apply the migrations,** in order, via the Supabase Management API or the Studio SQL
    editor:
    1. `supabase/migrations/20260803000001_authority_access.sql`
    2. `supabase/migrations/20260803000002_authority_content.sql`
    Then confirm: 6 new tables, 2 views, `public.is_authority_member` present, and the three Phase 5
    CHECK constraints now admitting the authority vocabulary
    (`SELECT conname, pg_get_constraintdef(oid) FROM pg_constraint WHERE conname IN
    ('role_grants_role_check','role_grants_source_check','role_grant_events_subject_type_check');`).

    **Step 2 — run the automated suites** and paste the outputs into the record:
    - `pnpm --filter @sync/web test`
    - `cd apps/web && set -a && . ./.dev.vars && set +a && npx vitest run src/__tests__/rls`
      (**check the test count is non-zero**)
    - `cd apps/web && npx vitest run src/__tests__/api/authority-scope-guard.test.ts src/__tests__/api/authority-copy-guard.test.ts src/__tests__/api/municipality-public-surface.test.ts`

    **Step 3 — live SQL probes** (Studio SQL editor), recording each outcome:
    a. `UPDATE official_responses SET body_he = 'x' WHERE id = <a real id>;` → must raise
    b. `DELETE FROM authority_commitments WHERE id = <a real id>;` → must raise
    c. Insert a second `verification_status='verified'` organization for an already-verified
       municipality → must raise 23505 on `uq_authority_org_verified`
    d. Insert an organization with `verification_status='verified'` and `verified_by = NULL` → must
       raise on `authority_org_verified_evidence_ck`

    **Step 4 — walk the lifecycle in the running app** and capture the screenshots issue #76 asks for,
    with sanitized test records:
    1. `/he/authority/onboarding` — submit a claim for a test municipality with two evidence links
    2. `/he/admin/authority-claims` as the super_admin — the queue with the evidence visible
    3. Approve with a reason — confirm the organization is verified and the claimant now holds
       `authority_admin`
    4. `/he/municipality-admin` — the dashboard. **Deliberately pick a municipality with fewer than 10
       satisfaction responses** and capture the `לא מוצג` withheld state; this is criterion 2's
       visual evidence and the single most important screenshot in the set
    5. `/he/municipality-admin/representatives` — invite a representative, capture the one-time accept
       link screen (**redact the token in the screenshot**), accept it from a second account, then
       offboard them and confirm they remain listed as `סיים/ה תפקיד`
    6. `/he/municipality-admin/inbox` — publish an official response, then publish a revision, and
       capture the version history showing both
    7. `/he/municipality-admin/commitments` — create a target and transition it, capturing the history
       and the disclaimer
    8. The public vote page as a **signed-out** visitor — capture the official response block with its
       verified badge, byline and version history

    **Step 5 — the criterion-6 check.** Screenshot `/he/municipality/<the verified municipality>`
    **before** step 3's approval and **again** after step 6's response is published. The two must be
    identical apart from vote data that changed for unrelated reasons. Any authority artefact visible
    on that page is a phase failure, not a nice surprise — the automated guard says the code is
    untouched; this step says the rendering is.

    **Step 6 — record everything** in `apps/web/docs/AUTHORITY-VERIFICATION.md`: migration application
    (timestamp, method), each suite's output, each SQL probe's outcome, the screenshot inventory, and
    any deviation. Then mark the manual rows in
    `.planning/phases/08-authority-dashboard/08-VALIDATION.md` green and set `wave_0_complete: true`.

    **Never print or paste a secret value** — not `SUPABASE_JWT_SECRET`, not the service-role key, not
    an invitation token. Names and presence only.
  </how-to-verify>
  <verify>
    <automated>cd /Users/saharbarak/personal/taro/apps/web && npx vitest run src/__tests__/api/authority-scope-guard.test.ts src/__tests__/api/authority-copy-guard.test.ts src/__tests__/api/municipality-public-surface.test.ts</automated>
  </verify>
  <done>Both migrations applied in order, every suite green with a non-zero count, all four SQL probes raised as expected, the screenshot set captured, and AUTHORITY-VERIFICATION.md filled in.</done>
  <resume-signal>Type "approved" once the migrations are applied, all suites are green with non-zero counts, the four SQL probes raised as expected, the screenshots are captured, and AUTHORITY-VERIFICATION.md is filled in — or describe what failed.</resume-signal>
</task>

</tasks>

<verification>
- Both Phase 8 migrations applied to the live database, in order, after Phase 5's two
- `pnpm --filter @sync/web test` green
- The RLS suite green with a non-zero test count
- The scope guard, the copy guard and the public-surface guard all green
- All four live SQL probes raised as expected
- `apps/web/docs/AUTHORITY-VERIFICATION.md` complete
- `08-VALIDATION.md` manual rows marked green, `wave_0_complete: true`
</verification>

<success_criteria>
- An unreviewed organization holds no badge, no dashboard access and no authorship — proven in the app
  and by two database constraints
- A representative of one municipality reads nothing belonging to another — proven at the application
  layer by a guard test and at the database layer by the RLS harness
- Official responses and targets cannot be overwritten — proven by six live probes
- The public council page renders identically before and after an authority joins — proven by an
  automated source guard and a before/after screenshot pair
</success_criteria>

<output>
After completion, create `.planning/phases/08-authority-dashboard/08-13-SUMMARY.md` recording the
migration timestamps, every probe outcome, the screenshot inventory, and any Phase 5 precondition that
blocked or degraded verification — in particular whether the HS256 assumption held against this
project.
</output>
