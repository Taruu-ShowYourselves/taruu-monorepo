---
phase: 08-authority-dashboard
plan: 10
type: execute
wave: 4
depends_on: ["08-06", "08-07"]
files_modified:
  - apps/web/src/app/[locale]/municipality-admin/layout.tsx
  - apps/web/src/app/[locale]/municipality-admin/page.tsx
  - apps/web/src/app/[locale]/municipality-admin/page.module.css
  - apps/web/src/app/[locale]/municipality-admin/representatives/page.tsx
  - apps/web/src/app/[locale]/municipality-admin/representatives/page.module.css
  - apps/web/src/components/authority/WithheldAggregate.tsx
autonomous: true
requirements: [AUTH-02, AUTH-03, AUTH-05]

must_haves:
  truths:
    - "A representative opening /he/municipality-admin sees their own municipality's aggregates and no control to change municipality"
    - "A withheld aggregate renders as an explicit Hebrew withheld state naming the cohort floor — never a zero, a dash, or an approximation"
    - "A non-authority user sees an access-denied state because the API returned 403 — the client never decided"
    - "An offboarded representative still appears in the roster with an offboarded state"
    - "Only an authority_admin sees the invite and offboard controls, and the server rejects the rest regardless"
  artifacts:
    - path: "apps/web/src/app/[locale]/municipality-admin/layout.tsx"
      provides: "The authority shell: verified-authority header, section nav, tracking disclaimer"
      min_lines: 60
    - path: "apps/web/src/app/[locale]/municipality-admin/page.tsx"
      provides: "The dashboard home: public totals, floored aggregates, satisfaction history, export"
      min_lines: 170
    - path: "apps/web/src/components/authority/WithheldAggregate.tsx"
      provides: "The single component that renders a withheld aggregate — one definition, no drift"
      min_lines: 30
    - path: "apps/web/src/app/[locale]/municipality-admin/representatives/page.tsx"
      provides: "Roster, invite, suspend/reinstate/offboard"
      min_lines: 170
  key_links:
    - from: "apps/web/src/app/[locale]/municipality-admin/page.tsx"
      to: "/api/authority/dashboard"
      via: "fetch GET on mount, with no municipality parameter"
      pattern: "/api/authority/dashboard"
    - from: "apps/web/src/app/[locale]/municipality-admin/representatives/page.tsx"
      to: "/api/authority/representatives"
      via: "fetch GET on mount, POST invite, POST status change"
      pattern: "/api/authority/representatives"
    - from: "apps/web/src/app/[locale]/municipality-admin/page.tsx"
      to: "apps/web/src/components/authority/WithheldAggregate.tsx"
      via: "every Aggregate value renders through it"
      pattern: "WithheldAggregate"
---

<objective>
Ship the authority's own workspace: the `/[locale]/municipality-admin` shell, the dashboard home with
its floored aggregates and satisfaction history, and the representative roster with invite,
suspend, reinstate and offboard.

Purpose: issue #76 names `/municipality-admin` in its visual-evidence list, and success criterion 2
lives or dies here — this is the surface where a withheld aggregate must visibly read as *withheld*
rather than as a zero, and where the absence of any municipality switcher makes the server-side
scoping legible to the person using it.

Output: one layout, two pages with CSS modules, one shared withheld-aggregate component.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/08-authority-dashboard/08-CONTEXT.md
@.planning/phases/08-authority-dashboard/08-06-SUMMARY.md
@.planning/phases/08-authority-dashboard/08-07-SUMMARY.md
@.planning/phases/08-authority-dashboard/08-02-SUMMARY.md

<interfaces>
<!-- Read from the repo 2026-08-03. Use directly. -->

Routes consumed (plans 08-06, 08-07) — note that **none** takes a municipality parameter:
```
GET  /api/authority/dashboard          → AuthorityDashboardResponse   (403 when not an authority member)
GET  /api/authority/export?dataset=…   → text/csv attachment
GET  /api/authority/representatives    → { representatives: Representative[] }
POST /api/authority/representatives    { email, displayName } → { invitationId, email, expiresAt, acceptUrl }
POST /api/authority/representatives/[id] { action: 'suspend'|'reinstate'|'offboard', reason: min 10 }
```

The aggregate shape every number arrives in (plan 08-02):
```ts
type Aggregate<T> =
  | { withheld: true;  cohortSize: number; minCohortSize: number }
  | { withheld: false; cohortSize: number; value: T };
```

`Representative.state ∈ 'invited' | 'active' | 'suspended' | 'offboarded'` (AUTH-02).

Design system — compose, do not extend: `@/components/press` (Masthead, NewsButton, Segmented,
Stepper), `@/components/uikit` (badge, card, tabs, metric-bar, progress, separator, animate-in),
`@/components/layout` (Header, Footer). Tokens only; CSS Modules; Hebrew; RTL.
`AUTHORITY_TRACKING_DISCLAIMER_HE` comes from `@sync/shared/contracts`.

**Testing constraint:** no jsdom, and the vitest include glob never collects `.tsx`. No render tests.
These tasks gate on typecheck plus a positive grep; the copy assertions land in plan 08-11's
source-scanning guard, which scans this plan's files too.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The authority shell layout + the WithheldAggregate component</name>
  <read_first>
    - apps/web/src/app/[locale]/layout.tsx (how a locale layout is composed, and the RTL/dir handling)
    - apps/web/src/app/[locale]/settings/ pages (the section-nav idiom for a grouped area)
    - apps/web/src/components/uikit/badge.tsx (the badge variants available)
  </read_first>
  <files>apps/web/src/app/[locale]/municipality-admin/layout.tsx, apps/web/src/components/authority/WithheldAggregate.tsx</files>
  <action>
**A. `layout.tsx`.** A thin shell wrapping the three `municipality-admin` pages:
- The organization's `displayNameHe` and municipality as a header, with a `מאומת` badge. Comment that
  this badge appears **here**, inside the authority's own workspace, and on official responses — and
  deliberately **not** on the public `municipality/[slug]` page, which this phase does not touch
  (08-CONTEXT decision 4). A future engineer will want to add it there; the comment is what stops
  them doing it by accident.
- Section nav: `סקירה` (`.`), `הצבעות ותגובות` (`./inbox`), `יעדים` (`./commitments`),
  `נציגים` (`./representatives`). Plan 08-11 creates the first two targets; a nav link to a route
  that does not exist yet is fine within the wave and resolves when 08-11 lands.
- Render `AUTHORITY_TRACKING_DISCLAIMER_HE` once in the shell footer area, so every page under this
  layout carries it. Plan 08-11's copy guard asserts its presence per page; rendering it in the
  layout is the cheapest way to satisfy that for all of them, and the guard should be told so.
- **No municipality switcher, anywhere.** Add a comment stating this is intentional: scope is a
  property of the caller's grants (`resolveAuthorityScope`), not a UI selection, and a switcher would
  imply otherwise even though the server ignores it.

**B. `apps/web/src/components/authority/WithheldAggregate.tsx`.** One component, used by every
number on every authority page, so the withheld rendering cannot drift between screens:

```tsx
interface Props<T> {
  aggregate: Aggregate<T>;
  format: (value: T) => string;
  label: string;
}
```

- Released → `format(value)` plus a quiet `לפי {cohortSize} משיבים`.
- Withheld → the literal Hebrew string `לא מוצג` as the value, and beneath it
  `מוצג רק מ-{minCohortSize} משיבים ומעלה. כרגע: {cohortSize}.`
- **Never** render `0`, `—`, `N/A`, or a rounded stand-in for a withheld aggregate. Comment why: a
  dash reads as missing data and a zero reads as a real measurement; `לא מוצג` says a decision was
  made to protect a small group, which is the truthful statement and the one AUTH-03 requires.
- Style the withheld state as deliberate (a muted token, not an error colour) — it is a privacy
  guarantee, not a failure.
  </action>
  <verify>
    <automated>cd /Users/saharbarak/personal/taro && pnpm --filter @sync/web typecheck && L="apps/web/src/app/[locale]/municipality-admin/layout.tsx" && test -f "$L" && grep -q "AUTHORITY_TRACKING_DISCLAIMER_HE" "$L" && grep -q "מאומת" "$L" && test $(grep -ciE "select.*municipality|switch.*municipality|municipalityPicker" "$L") -eq 0 && W=apps/web/src/components/authority/WithheldAggregate.tsx && test -f $W && grep -q "לא מוצג" $W && test $(grep -cE "'0'|>0<|N/A|—" $W) -eq 0 && echo SHELL_OK</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @sync/web typecheck` clean
    - The layout renders the verified badge and the tracking disclaimer, and contains no municipality switcher
    - `WithheldAggregate` renders `לא מוצג` and never a zero, dash or `N/A` for a withheld value
    - Zero hardcoded hex colours in either file
  </acceptance_criteria>
  <done>The authority workspace has a shell, and withheld aggregates have exactly one rendering.</done>
</task>

<task type="auto">
  <name>Task 2: The dashboard home</name>
  <read_first>
    - apps/web/src/app/[locale]/municipality/[slug]/page.tsx (the metric-card and vote-card visual
      language to echo — this is the authority's private mirror of the public profile, so it should
      look like a sibling, not a different product)
    - .planning/phases/08-authority-dashboard/08-07-SUMMARY.md (the exact dashboard response shape)
  </read_first>
  <files>apps/web/src/app/[locale]/municipality-admin/page.tsx, apps/web/src/app/[locale]/municipality-admin/page.module.css</files>
  <action>
Client component, Hebrew, RTL. `GET /api/authority/dashboard` on mount, **with no query parameters** —
comment that the scope is server-derived and that adding a municipality parameter here would be
ignored by the server anyway (and is grepped for by plan 08-13).

States:
- 401 → `נדרשת התחברות`
- 403 → `אין לכם גישה למרחב הרשות.` plus a link to `/[locale]/authority/onboarding` for someone who
  has not claimed yet. Leak nothing about which municipalities exist in the system.
- 200 → the dashboard

Sections:

1. **`נתונים ציבוריים`** — `publicTotals` (open votes, closed votes, total participants) rendered
   plainly, with a one-line note: `הנתונים האלה גלויים לכל אחד בעמוד הרשות הציבורי.` This is the
   honest framing of 08-CONTEXT decision 5's asymmetry, and it pre-empts "why is this number shown
   but that one hidden".

2. **`נתונים מצטברים`** — `satisfaction`, `engagement.participationRate`,
   `engagement.medianTimeToParticipateHours`, each through `<WithheldAggregate>`. Formatters:
   satisfaction to one decimal out of 5, participation rate as a percentage, time via the
   `formatHours` helper already written in `municipality/[slug]/page.tsx` (copy the function or lift
   it — do **not** import from the public page's module, which would couple this page to a file plan
   08-02 guards).
   Above the section, one line explaining the floor in plain Hebrew:
   `כדי להגן על פרטיות התושבים, נתון שמבוסס על פחות מ-{minCohortSize} משיבים אינו מוצג כלל — הוא לא מעוגל ולא מוערך.`
   That sentence is the product statement of AUTH-03 and should read as a commitment, not a caveat.

3. **`תגובות רשמיות`** — `responseCoverage`: how many votes have a published official response and
   how many are waiting, with a link into `./inbox` (plan 08-11).

4. **`שביעות רצון לאורך זמן`** — `satisfactionHistory` as a simple series. Withheld snapshots render
   as gaps that are visibly labelled `לא מוצג`, not as zeros — a zero here would draw a line to the
   floor of the chart and read as catastrophic satisfaction. If a chart component is not already in
   the design system, render a table instead; do not add a charting dependency.

5. **`ייצוא`** — three download buttons hitting
   `/api/authority/export?dataset=votes|responses|commitments`, with a note that exports carry no
   resident identifying information: `הקבצים מכילים נתונים מצטברים בלבד. אין בהם פרטים מזהים של תושבים.`

No page-level control anywhere selects a municipality.
  </action>
  <verify>
    <automated>cd /Users/saharbarak/personal/taro && pnpm --filter @sync/web typecheck && P="apps/web/src/app/[locale]/municipality-admin/page.tsx" && test -f "$P" && test $(wc -l < "$P") -ge 170 && grep -q "/api/authority/dashboard" "$P" && grep -q "WithheldAggregate" "$P" && grep -q "/api/authority/export" "$P" && grep -q "אין בהם פרטים מזהים" "$P" && test $(grep -c "api/authority/dashboard?" "$P") -eq 0 && test $(grep -c "municipality/\[slug\]" "$P") -eq 0 && test $(grep -cE "#[0-9a-fA-F]{6}" "$P") -eq 0 && echo DASHBOARD_PAGE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @sync/web typecheck` clean
    - The dashboard fetch carries no query string
    - Every floored aggregate renders through `WithheldAggregate`
    - The page imports nothing from `app/[locale]/municipality/[slug]/` (plan 08-02's guarded surface)
    - The cohort-floor explanation and the export privacy note are both present in Hebrew
    - Zero hardcoded hex colours
  </acceptance_criteria>
  <done>An authority sees its own city's numbers, and a suppressed number is visibly suppressed.</done>
</task>

<task type="auto">
  <name>Task 3: The representative roster</name>
  <read_first>
    - .planning/phases/08-authority-dashboard/08-06-SUMMARY.md (the four states, the accept-URL format, the status-change contract)
    - apps/web/src/app/[locale]/settings/social-connections/page.tsx (a list-with-per-row-actions precedent)
  </read_first>
  <files>apps/web/src/app/[locale]/municipality-admin/representatives/page.tsx, apps/web/src/app/[locale]/municipality-admin/representatives/page.module.css</files>
  <action>
Client component, Hebrew, RTL. `GET /api/authority/representatives` on mount.

**Roster.** One row per representative, grouped or badged by `state`:
`invited` → `הוזמן/ה` · `active` → `פעיל/ה` · `suspended` → `מושהה` · `offboarded` → `סיים/ה תפקיד`.
Offboarded rows stay visible, in a muted style, with the date. Add a short line above the list:
`נציגים שסיימו את תפקידם נשארים ברשימה. התוכן שיצרו נשמר עם שמם, גם אחרי סיום התפקיד.` — the
user-facing statement of AUTH-05, and the thing that stops someone asking for a delete button.

**Invite** (rendered only when the caller's `role` is `authority_admin`; the server enforces it
regardless, and the file must say so in a comment so nobody mistakes this for the security control):
- email + display-name fields, POST to `/api/authority/representatives`
- on success, show the returned `acceptUrl` **once**, in a copyable field, with:
  `הקישור מוצג פעם אחת בלבד ולא יישמר. שלחו אותו לנציג/ה בערוץ מאובטח. תוקפו 14 יום.`
- do not persist the URL in component state beyond the current view, do not write it to
  `localStorage` or any storage, and do not log it. Comment each of those three.
- 409 → `כבר קיימת הזמנה פתוchה לכתובת הזו.` (write correct Hebrew: `כבר קיימת הזמנה פתוחה לכתובת הזו.`)

**Per-row actions** (`authority_admin` only): `השהיה`, `החזרה לפעילות`, `סיום תפקיד`, each opening a
required-reason input (min 10 chars, matching `ReviewReasonSchema`) before it can fire. The offboard
confirmation must state what it does and does not do:
`סיום תפקיד מבטל את ההרשאה. התגובות והיעדים שהנציג/ה יצרו נשארים במערכת עם שמם.`
Handle 403 `cross_municipality` and 409 `cannot_modify_own_grant` with clear Hebrew, and never render
an action on the caller's own row.

CSS module: tokens only, mobile-first, the muted offboarded style built from an existing neutral token.
  </action>
  <verify>
    <automated>cd /Users/saharbarak/personal/taro && pnpm --filter @sync/web typecheck && P="apps/web/src/app/[locale]/municipality-admin/representatives/page.tsx" && test -f "$P" && test $(wc -l < "$P") -ge 170 && grep -q "/api/authority/representatives" "$P" && grep -q "סיים/ה תפקיד" "$P" && grep -q "נשמר עם שמם" "$P" && grep -q "פעם אחת בלבד" "$P" && test $(grep -cE "localStorage|sessionStorage" "$P") -eq 0 && test $(grep -c "console.log" "$P") -eq 0 && test $(grep -cE "#[0-9a-fA-F]{6}" "$P") -eq 0 && echo REPS_PAGE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @sync/web typecheck` clean
    - All four AUTH-02 states render, and offboarded representatives remain listed
    - The accept URL is shown once, is never written to any browser storage, and is never logged
    - Every status change requires a ≥10-character reason
    - The offboard confirmation states that created content is retained
    - No action is offered on the caller's own row
    - Zero hardcoded hex colours
  </acceptance_criteria>
  <done>An authority admin can run their team, and the UI tells the truth about what offboarding does.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @sync/web typecheck` clean
- `pnpm --filter @sync/web test` green
- `pnpm --filter @sync/web lint` — no new warnings
- `grep -rn "municipality=" apps/web/src/app/\[locale\]/municipality-admin/` returns nothing
- `grep -rn "localStorage\|sessionStorage" apps/web/src/app/\[locale\]/municipality-admin/` returns nothing
</verification>

<success_criteria>
- A representative sees their own municipality's data with no control that suggests they could see another's
- A withheld aggregate reads as a deliberate privacy decision, never as zero or missing
- Offboarded representatives remain in the roster and the UI states that their work is retained
- The invitation link is shown exactly once and stored nowhere
</success_criteria>

<output>
After completion, create `.planning/phases/08-authority-dashboard/08-10-SUMMARY.md` recording the
layout's nav targets, the `WithheldAggregate` props, and the Hebrew state labels — plan 08-11 builds
its pages inside this shell and its copy guard scans these files.
</output>
