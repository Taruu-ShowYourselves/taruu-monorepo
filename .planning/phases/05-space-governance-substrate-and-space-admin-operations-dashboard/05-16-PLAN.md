---
phase: 05-space-governance-substrate-and-space-admin-operations-dashboard
plan: 16
type: execute
wave: 6
depends_on: ["05-09", "05-10", "05-12", "05-13", "05-14", "05-15"]
files_modified:
  - apps/web/tests/e2e/space-admin.spec.ts
  - apps/web/tests/e2e/fixtures/space-admin-seed.sql
  - apps/web/playwright.config.ts
  - .planning/phases/05-space-governance-substrate-and-space-admin-operations-dashboard/05-EVIDENCE.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [SPACE-10, SPACE-03, SPACE-04]

must_haves:
  truths:
    - "All sixteen required frames exist, at 1440×900 and 390×844, showing populated state rather than empty tables"
    - "The append-only proof has been run against a real database and its transcript is recorded, labelled as manual evidence"
    - "The object-level denial has been exercised against a running app, not only against mocks"
    - "No hardcoded colour or stray pixel length exists anywhere under the space-admin surface"
  artifacts:
    - path: "apps/web/tests/e2e/space-admin.spec.ts"
      provides: "Playwright captures for the sixteen evidence frames"
    - path: ".planning/phases/05-space-governance-substrate-and-space-admin-operations-dashboard/05-EVIDENCE.md"
      provides: "The phase's verification record: screenshot index, SQL transcript, token-hygiene scan output"
  key_links:
    - from: "apps/web/tests/e2e/space-admin.spec.ts"
      to: "/he/space-admin/[spaceId]"
      via: "page.goto against a seeded space"
      pattern: "space-admin"
---

<objective>
Produce the evidence issue #75 asks for: sixteen screenshots at two widths, the manually-run append-only SQL transcript, a live object-level denial check, and a design-token hygiene scan — then have a human confirm the surface actually looks and behaves the way the contract says.

Purpose: three of this phase's guarantees cannot be proven by the unit suite. `vitest` runs with `environment: 'node'` and Supabase fully mocked, so the audit trigger, the rendered appearance, and the end-to-end denial all need evidence gathered outside it. This plan gathers it and writes it down honestly rather than implying CI covers it.
Output: a Playwright spec, a seed fixture, an evidence document, and a human sign-off.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-space-governance-substrate-and-space-admin-operations-dashboard/05-UI-SPEC.md
@.planning/phases/05-space-governance-substrate-and-space-admin-operations-dashboard/05-RESEARCH.md
@apps/web/playwright.config.ts
@apps/web/tests/e2e/council-public-page.spec.ts
@supabase/tests/audit_append_only.sql

<interfaces>
<!-- The sixteen required frames, from 05-UI-SPEC.md. Desktop 1440×900, mobile 390×844.
     Frames 1-12 must show POPULATED state — an empty table is not evidence the surface works. -->
 1-2  Overview      populated figures + capability manifest with at least one `✕ לא מוענק`
 3-4  Proposals     ≥3 rows incl. one `בבדיקה` and one self-submitted row whose actions cell
                    holds the lock text and NO buttons
 5-6  Members       ≥3 rows incl. one `מושעה/ית` showing `ביטול השעיה` and no grant/revoke
 7-8  Statistics    ≥1 suppressed `<5` cell and the aggregate-only footer note
 9-10 Dispatch      preview computed, all four Receipt rows visible, send enabled
11-12 Audit         ≥5 rows, one with an expanded long reason, pagination visible
13    Irreversible dialog — red plate rule, `פעולה בלתי הפיכה · IRREVERSIBLE` kicker, reason
      field, confirm VISIBLY disabled (paper-2 fill, faint text, no hover inversion)
14    Quota exhausted — composer read-only, NO send control rendered, QuotaBlock ink block
      with PAPER kicker text and a red tick glyph
15    No permission — direct navigation to a lacked surface, escalation CTA present
16a   Detail panel, unmoderated — reached at `?proposal={id}`, body visible, `הסתרת תוכן`
      and `סימון לבדיקה` present
16b   Detail panel, hidden AND flagged — both notices above the body, hidden first, with
      `ביטול הסתרה` and `ביטול סימון` in place of their inverses

16a and 16b are two frames because the panel is one-at-a-time and hide/unhide are mutually
exclusive, so no single panel can show both `הסתרת תוכן` and `ביטול הסתרה`.

<!-- Existing Playwright setup -->
`@playwright/test@^1.40.0` is a dependency; `apps/web/playwright.config.ts` exists;
`apps/web/tests/e2e/` already holds council-public-page, identity-document, smoke and
vote-detail specs. `pnpm --filter @sync/web test:e2e` runs them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed fixture and the screenshot spec</name>
  <files>apps/web/tests/e2e/fixtures/space-admin-seed.sql, apps/web/tests/e2e/space-admin.spec.ts, apps/web/playwright.config.ts</files>
  <read_first>
    - apps/web/tests/e2e/council-public-page.spec.ts (the existing e2e shape, including how it handles auth and how it skips when the environment is unavailable)
    - apps/web/playwright.config.ts (projects, baseURL, whether a webServer is configured)
    - .planning/phases/05-space-governance-substrate-and-space-admin-operations-dashboard/05-UI-SPEC.md — the screenshot table and the Responsive Contract
    - supabase/migrations/20260802000001_space_governance.sql (the tables the seed writes into)
  </read_first>
  <action>
`space-admin-seed.sql` — an idempotent fixture creating everything the sixteen frames need against a local Supabase:
- one space seeded from a municipality, plus a second space the test admin has no grant in (for frame 15 and the denial check);
- an admin user holding ten of the eleven capabilities, deliberately missing one so the manifest shows at least one `✕ לא מוענק`;
- a second admin holding only `metrics.read`, for the no-permission frame;
- at least four proposals: one `in_review` submitted by someone else, one `in_review` submitted by the test admin (the self-submitted lock row), one hidden **and** flagged, and one already approved;
- at least three members, one of them suspended with an active `space_member_suspensions` row and its grants suspended;
- a municipality small enough that one metric suppresses to `<5`;
- at least six `space_audit_log` rows, one carrying a reason well over two lines so the clamp and its disclosure are visible;
- for frame 14, a space whose `notification_monthly_quota` is 1 with one campaign already `sent` this month.
Head the file with the exact command to apply it and a note that it is a local-only fixture.

`space-admin.spec.ts` — one Playwright spec with two projects' worth of viewports: desktop `1440×900` and mobile `390×844`. Add the two viewport projects to `playwright.config.ts` if they are not already there, named so the output filenames are self-describing.

Each frame is its own `test(...)` that navigates, waits for the surface's `aria-live` "loaded" announcement or a stable selector rather than a fixed timeout, asserts one thing that proves the frame shows what it must, and then screenshots to `apps/web/tests/e2e/__screenshots__/space-admin/{nn}-{name}-{viewport}.png`.

The assertion per frame matters as much as the image — a screenshot of the wrong state is worse than none:
- frame 1-2: assert `✕ לא מוענק` is visible at least once;
- frame 3-4: assert the self-submitted row's actions cell contains `הצעה שהגשתם — ההכרעה שמורה למנהל אחר.` and contains zero `button` elements;
- frame 5-6: assert a row shows `מושעה/ית` and `ביטול השעיה`, and that the same row has no `ניהול הרשאות`;
- frame 7-8: assert `<5` and `מוסתר — קבוצה קטנה מדי` are both visible, and that no element inside a stat card matches `a, button, [role=button]`;
- frame 9-10: assert all four preview row labels are visible and the send button is enabled;
- frame 11-12: assert at least five rows and that an expanded reason is visible after clicking `הצג נימוק מלא ▾`;
- frame 13: open an approve dialog, type nothing, assert the confirm button is `disabled` **and** that its computed `background-color` differs from an enabled ink button's — this is what proves D17 and D27 actually landed rather than silently losing on specificity;
- frame 14: assert `QuotaBlock` is visible, assert no element with the send label exists, and assert the quota kicker's computed colour is the paper token rather than a red one;
- frame 15: navigate a `metrics.read`-only admin to `/proposals`, assert `NoPermissionPanel` and the `פנייה למנהל־על` CTA, and assert the space name is **not** present for a zero-grant admin;
- frame 16a/16b: navigate with `?proposal={id}` and assert the panel is expanded, the correct pair of content buttons is present, and for 16b that both notices render with the hidden one first.

Skip the whole spec with a clear message when the seeded environment is absent, mirroring how the existing e2e specs degrade, so `pnpm --filter @sync/web test:e2e` does not become red on a machine without a local Supabase.
  </action>
  <verify>
    <automated>pnpm --filter @sync/web exec playwright test tests/e2e/space-admin.spec.ts --list</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @sync/web exec playwright test tests/e2e/space-admin.spec.ts --list` lists at least 16 tests across both viewport projects
    - `playwright.config.ts` defines viewports `1440×900` and `390×844`
    - Every test has at least one assertion beyond the screenshot call — `grep -c "expect(" apps/web/tests/e2e/space-admin.spec.ts` is at least `16`
    - The frame-13 test compares the disabled confirm button's computed `background-color` against an enabled control
    - The frame-14 test asserts the quota kicker's computed colour is the paper token
    - The frame-15 test asserts the space name is absent for a zero-grant admin
    - `space-admin-seed.sql` creates two spaces, at least four proposals including one self-submitted and one hidden-and-flagged, at least three members including one suspended, and at least six audit rows
    - The spec skips cleanly with a message when the environment is unavailable
    - No `waitForTimeout` with a fixed number — waits are on selectors or announcements
  </acceptance_criteria>
  <done>Sixteen frames can be produced on demand, each guarded by an assertion that the frame shows the state it is meant to show.</done>
</task>

<task type="auto">
  <name>Task 2: Run the manual proofs and write the evidence record</name>
  <files>.planning/phases/05-space-governance-substrate-and-space-admin-operations-dashboard/05-EVIDENCE.md</files>
  <read_first>
    - supabase/tests/audit_append_only.sql (05-01 — the probe to run)
    - .planning/phases/05-space-governance-substrate-and-space-admin-operations-dashboard/05-RESEARCH.md — Open Question 5 and the Validation Architecture table (which requirements are unit-tested and which are manual)
    - .planning/REQUIREMENTS.md — the ten SPACE-* statements this document must map evidence to
  </read_first>
  <action>
Run each of the following and paste the real output. Do not summarise; a transcript is the evidence.

1. **Append-only proof.** Apply the migrations to a scratch database and run `psql -f supabase/tests/audit_append_only.sql`. Record the full transcript: the successful insert, the three `42501` refusals for UPDATE, DELETE and TRUNCATE, the whitespace-reason CHECK rejection, and the `23503` refusal to delete a user who has audit rows. If any case does not behave as expected, that is a finding, not something to smooth over — record it and open a follow-up.

2. **Live object-level denial.** Against the running app with the seeded fixture, `curl` each space-admin endpoint with the admin's session cookie and the *other* space's uuid. Record the status and the exact body for each. Every one must be `403` with a body deep-equal to `{"error":"Forbidden","code":"FORBIDDEN"}`. Also record a malformed-uuid call and a nonexistent-uuid call, and confirm all three responses are byte-identical — that is the anti-enumeration property.

3. **Token hygiene scan.** Run and paste:
```bash
grep -rnE '#[0-9a-fA-F]{3,8}\b' apps/web/src/app/\[locale\]/space-admin apps/web/src/components/space-admin
grep -rnE '[^-a-z(]([0-9]+)px' apps/web/src/app/\[locale\]/space-admin apps/web/src/components/space-admin | grep -v '@media'
grep -rn 'header-height\|np-block-red\|components/layout/Header' apps/web/src/app/\[locale\]/space-admin apps/web/src/components/space-admin
```
All three must return nothing. A `px` hit inside an `@media` query is permitted — CSS media queries cannot read custom properties — and that is the only permitted hardcoded length.

4. **Suite and typecheck.** `pnpm typecheck` (root — `turbo typecheck`, covering `@sync/web`, `@sync/mobile` and every package; the phase widens a shared type, and a `--filter @sync/web` gate cannot see a cross-package break), then `pnpm --filter @sync/web test`, `pnpm --filter @sync/web lint`, and `pnpm --filter @sync/api-client exec vitest run` — paste the summary lines. The api-client suite is separate and `--filter @sync/web test` never runs it, but plan 05-10 modifies `packages/api-client/src/__tests__/votes.test.ts`.

Then write `05-EVIDENCE.md` containing:
- a table mapping each of SPACE-01 through SPACE-10 to its evidence, with an explicit column saying whether that evidence is **automated** or **manual**. Be accurate: SPACE-04's immutability is manual, and saying otherwise would misrepresent the phase's coverage;

  Two rows need positive wording rather than a thin cell:
  - **SPACE-02** ("the JWT carries no roles claim, so a stale token cannot carry stale authority") holds **by construction**, and the evidence is the absence of a change: no plan in this phase touches `jose`, `apps/web/src/services/auth/session.ts`, or the session payload shape, and every capability check resolves from `space_capability_grants` per request. Record it as automated, with the standing check `git diff main --stat -- apps/web/src/services/auth/session.ts` returning nothing and `grep -rn "roles\|capabilities" apps/web/src/services/auth/session.ts` returning nothing. "Nothing was added" is a real, checkable claim — write it as one rather than leaving the cell to imply the property was not verified;
  - **SPACE-03** is proven twice, and say so: `space-admin-object-authz.test.ts` covers it against mocks (automated) and the live `curl` denial transcript below covers it against a running app (manual). Neither alone is the whole claim;
- the screenshot index, sixteen rows, each with its file path and the assertion that guards it;
- the three transcripts above;
- an honest limitations section naming what is still unproven — at minimum, that the audit log is tamper-*resistant* rather than WORM (a database superuser can still disable a trigger), and that the `CreationFeePort` records an obligation rather than capturing money until PAY-06 lands.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-space-governance-substrate-and-space-admin-operations-dashboard/05-EVIDENCE.md && grep -q "SPACE-10" .planning/phases/05-space-governance-substrate-and-space-admin-operations-dashboard/05-EVIDENCE.md && grep -q "42501" .planning/phases/05-space-governance-substrate-and-space-admin-operations-dashboard/05-EVIDENCE.md && pnpm typecheck && pnpm --filter @sync/web test && pnpm --filter @sync/web lint && pnpm --filter @sync/api-client exec vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `05-EVIDENCE.md` exists and contains a row for each of SPACE-01 through SPACE-10 with an automated/manual column
    - SPACE-02's row states positively that the property holds by construction and names the two standing greps that check it, rather than leaving the cell thin
    - The SQL transcript shows `42501` three times and `23503` once
    - The denial transcript shows byte-identical bodies for the unauthorized, malformed and nonexistent cases
    - All three token-hygiene greps are recorded as returning nothing
    - Root `pnpm typecheck` exits 0 (proves no cross-package break — CI runs this exact command on every PR to `main`), and `pnpm --filter @sync/web test`, `pnpm --filter @sync/web lint` and `pnpm --filter @sync/api-client exec vitest run` all exit 0, with summary lines pasted
    - The limitations section names the tamper-resistant-not-WORM caveat and the `CreationFeePort` obligation caveat
    - The screenshot index has sixteen rows, each with a path and its guarding assertion
  </acceptance_criteria>
  <done>Every requirement has a named piece of evidence, and the two things the unit suite cannot prove are proven elsewhere and labelled as such.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual and behavioural sign-off</name>
  <what-built>
The full `/he/space-admin/[spaceId]` surface: six routed pages, the governance substrate behind them, and the sixteen evidence frames at desktop and mobile widths. Everything automatable has been automated — the screenshots are captured by Playwright with assertions, the append-only proof has been run and transcribed, the denial check has been curled against a live app, and the token-hygiene scan is clean.
  </what-built>
  <how-to-verify>
Run the app locally with the seeded fixture, then:

1. Open `/he/space-admin/{spaceId}`. Confirm the masthead sits at the top with no gap or overlap, the six-link nav reads right to left in Hebrew, and the capability manifest shows at least one `✕ לא מוענק` row.
2. Narrow the window below 768px. Confirm the nav scrolls horizontally rather than wrapping, and that the tables drop their secondary columns while the row disclosure still reveals those values.
3. Go to `/proposals`, click a proposal title. Confirm the panel expands in place as a table row, that only one panel can be open at a time, and that `Escape` closes it and returns focus to the title.
4. Click `אישור ופרסום` on a proposal you did not submit. Confirm the plate rule is red, the kicker reads `פעולה בלתי הפיכה · IRREVERSIBLE`, focus lands in the reason textarea rather than on the confirm button, and the confirm button looks **visibly** disabled — greyed fill, faint text — and does **not** turn red when you hover it. That last detail is the one most likely to have silently failed.
5. Find the self-submitted proposal. Confirm its actions cell holds the lock text and contains no buttons at all.
6. Go to `/dispatch`, compute an audience, then change one character in the body. Confirm the send disables immediately — before you click away — and the stale banner appears.
7. Go to `/stats`. Try to click a figure. Confirm nothing responds, no cursor changes, and the aggregate-only footer note is present.
8. Go to `/audit`. Expand a long reason and confirm the table does not reflow sideways. Page back with `← רשומות ישנות יותר` and confirm no row repeats.
9. Sign in as the `metrics.read`-only admin and navigate directly to `/proposals`. Confirm the page is coherent — masthead, nav, footer intact — showing `NoPermissionPanel` with a working `פנייה למנהל־על` button.
10. Review the sixteen screenshots in `apps/web/tests/e2e/__screenshots__/space-admin/`. Confirm frames 1–12 show populated state and that nothing reads as an English placeholder.
  </how-to-verify>
  <read_first>
    - .planning/phases/05-space-governance-substrate-and-space-admin-operations-dashboard/05-EVIDENCE.md (Task 2)
    - .planning/phases/05-space-governance-substrate-and-space-admin-operations-dashboard/05-UI-SPEC.md — the screenshot table, for comparison against what was captured
  </read_first>
  <files>.planning/phases/05-space-governance-substrate-and-space-admin-operations-dashboard/05-EVIDENCE.md</files>
  <action>
Do not write code in this task. Prepare the review, hand it over, then stop and wait.

1. Start the app locally against the seeded fixture and confirm it serves `/he/space-admin/{spaceId}`. Paste the URL and the two sign-in credentials the reviewer needs (the ten-capability admin and the `metrics.read`-only admin).
2. Run `pnpm --filter @sync/web exec playwright test tests/e2e/space-admin.spec.ts` and confirm all sixteen frames were written. List the file paths.
3. Present the ten numbered verification steps from `<how-to-verify>` as a checklist the reviewer can work through, calling out step 4 (the disabled confirm's appearance and hover) and step 6 (staleness firing on change, not on blur) as the two most likely silent failures.
4. Wait for the verdict. Append it to `05-EVIDENCE.md` — approval, or each defect found with the surface and the contract clause it violates — before the phase closes.
  </action>
  <verify>
    <automated>ls apps/web/tests/e2e/__screenshots__/space-admin/*.png | wc -l | grep -qE '^\s*(1[6-9]|[2-9][0-9])' && grep -q "SPACE-10" .planning/phases/05-space-governance-substrate-and-space-admin-operations-dashboard/05-EVIDENCE.md</automated>
  </verify>
  <done>All sixteen frames are on disk, the reviewer has walked the ten steps, and their verdict is recorded in the evidence document.</done>
  <acceptance_criteria>
    - All sixteen screenshot files exist under `apps/web/tests/e2e/__screenshots__/space-admin/`
    - Frames 1–12 show populated state, not an empty table
    - The reviewer has confirmed step 4's disabled-confirm appearance and hover behaviour specifically, and step 6's on-change staleness specifically
    - No English placeholder string appears on any surface
    - The verdict, including any defects found, is recorded in `05-EVIDENCE.md` before the phase closes
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe the issues found</resume-signal>
</task>

</tasks>

<verification>
- Root `pnpm typecheck`, plus `@sync/web` test and lint, plus the separate `@sync/api-client` suite — all green
- `pnpm --filter @sync/web exec playwright test tests/e2e/space-admin.spec.ts` — all frames captured
- `05-EVIDENCE.md` maps all ten requirements to evidence with an honest automated/manual split
</verification>

<success_criteria>
1. Sixteen assertion-guarded frames exist at both widths, with frames 1–12 populated.
2. The append-only SQL proof has been run and its transcript recorded.
3. The live object-level denial returns byte-identical opaque 403s for unauthorized, malformed and nonexistent space ids.
4. The token-hygiene scan is clean.
5. A human has confirmed the disabled-confirm appearance and the on-change staleness, and the verdict is recorded.
</success_criteria>

<output>
After completion, create `.planning/phases/05-space-governance-substrate-and-space-admin-operations-dashboard/05-16-SUMMARY.md`, and update `.planning/REQUIREMENTS.md` — which is in this plan's `files_modified` — to mark SPACE-01 through SPACE-10 complete.
</output>
