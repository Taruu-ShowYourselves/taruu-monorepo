---
phase: 05-space-governance-substrate-and-space-admin-operations-dashboard
plan: 11
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - apps/web/package.json
  - pnpm-lock.yaml
  - apps/web/src/components/space-admin/index.ts
  - apps/web/src/components/space-admin/SpaceAdminHeader.tsx
  - apps/web/src/components/space-admin/SpaceAdminHeader.module.css
  - apps/web/src/components/space-admin/SpaceAdminNav.tsx
  - apps/web/src/components/space-admin/SpaceAdminNav.module.css
  - apps/web/src/components/space-admin/PressTable.tsx
  - apps/web/src/components/space-admin/PressTable.module.css
  - apps/web/src/components/space-admin/StatusChip.tsx
  - apps/web/src/components/space-admin/StatusChip.module.css
  - apps/web/src/components/space-admin/ConfirmDialog.tsx
  - apps/web/src/components/space-admin/ConfirmDialog.module.css
  - apps/web/src/components/space-admin/Panels.tsx
  - apps/web/src/components/space-admin/Panels.module.css
  - apps/web/src/components/space-admin/kicker.module.css
  - apps/web/src/app/[locale]/space-admin/[spaceId]/layout.tsx
  - apps/web/src/app/[locale]/space-admin/[spaceId]/layout.module.css
autonomous: true
requirements: [SPACE-10]

must_haves:
  truths:
    - "Every new component paints with --np-* tokens only, with no hex colour and no pixel length outside a media query"
    - "A disabled button is visibly disabled and does not invert on hover, because the override is authored :disabled-qualified"
    - "A data table keeps real table semantics at every width — no cell ever becomes a block"
    - "A confirmation cannot be submitted without a reason of at least ten characters, and Escape or the backdrop cancels"
    - "Red text never appears below 18.66px on an ink background, and no outline control ever takes a red fill"
  artifacts:
    - path: "apps/web/src/components/space-admin/PressTable.tsx"
      provides: "The RTL data-table contract used by proposals, members and audit"
      contains: "scope=\"row\""
    - path: "apps/web/src/components/space-admin/ConfirmDialog.tsx"
      provides: "Radix AlertDialog with a required reason, two kinds, and the disabled-confirm contract"
    - path: "apps/web/src/app/[locale]/space-admin/[spaceId]/layout.tsx"
      provides: "np-page shell with Masthead, header, six-link nav and Colophon"
  key_links:
    - from: "apps/web/src/components/space-admin/ConfirmDialog.module.css"
      to: "NewsButton .ink:hover"
      via: ":disabled-qualified override at specificity (0,2,1)"
      pattern: ":disabled:hover"
    - from: "apps/web/src/app/[locale]/space-admin/[spaceId]/layout.tsx"
      to: "@/components/press"
      via: "Masthead + Colophon, the np-native shell used by /he/knesset"
      pattern: "from '@/components/press'"
---

<objective>
Build the shell and the six new presentational primitives every surface needs, exactly as 05-UI-SPEC.md specifies them, with no data fetching.

Purpose: five surface plans will consume these. Building them once, in Wave 2, in parallel with the authorization core, means the surface plans are composition rather than invention — and it puts the two genuinely subtle pieces (the `:disabled`-qualified override and the responsive table that keeps its semantics) in one reviewable place.
Output: one new dependency, eight components, the route shell, and their CSS modules.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-space-governance-substrate-and-space-admin-operations-dashboard/05-UI-SPEC.md
@apps/web/src/components/press/NewsButton/NewsButton.module.css
@apps/web/src/components/press/PressInput/PressInput.tsx
@apps/web/src/components/press/Segmented/Segmented.tsx
@apps/web/src/app/[locale]/knesset/page.tsx
@apps/web/src/styles/tokens.css
@CLAUDE.md

<interfaces>
<!-- The defect this plan must work around, verbatim from
     apps/web/src/components/press/NewsButton/NewsButton.module.css -->
```css
.btn      { cursor: pointer; }                                     /* (0,1,0) */
.ink:hover    { background: var(--np-red); color: var(--np-paper); }  /* (0,1,1) — 4.03:1 at 16px */
.outline:hover{ background: var(--np-ink); color: var(--np-paper); }  /* (0,1,1) — 16.66:1, compliant */
```
`NewsButton.module.css` has **no** `:disabled` rule at all.

<!-- Tokens that exist (apps/web/src/styles/tokens.css) -->
--np-paper #F4F1E8 · --np-paper-box #FBFAF4 · --np-paper-2 #ECE7D8 · --np-paper-edge rgba(20,17,14,0.12)
--np-ink #14110E · --np-ink-soft #3B362E · --np-ink-faint #6E675A
--np-red #E0301E · --np-red-ink #7A1607 · --np-red-dark #B0220F
--np-rule-hair 1px · --np-rule 2px · --np-rule-heavy 4px
--space-1..--space-16 · --space-11 44px · --np-gutter · --np-container 1600px
--np-dur 200ms · --np-dur-snap 120ms · --np-ease cubic-bezier(0.2,0,0,1)
--color-surface-overlay rgba(0,0,0,0.5) · --z-modal-backdrop 400 · --z-modal 500
--text-xs 11.1px · --text-sm 13.3px · --text-base 16px · --text-lg 19.2px · --text-2xl · --text-3xl

<!-- Reused as-is, do not fork -->
Masthead, Colophon, NewsButton, PressInput (has `multiline`), PressSelect, Receipt, TallyBar
Utilities: .np-container .np-rule* .np-mono .np-halftone .np-block-ink

<!-- Never use -->
`var(--header-height)` — referenced in three stylesheets and defined nowhere, so the whole
`calc()` declaration is invalid and silently dropped. Do not propagate it.
`.np-block-red` — puts 13.3px paper text at 4.03:1.
`.np-kicker` for red text — the shared utility paints text in `--np-red` (4.03:1).
`@/components/layout/Header` and `Footer` — Luminous Civic, deprecated.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Radix dependency, the shell, the header, the nav, and the local kicker</name>
  <files>apps/web/package.json, apps/web/src/app/[locale]/space-admin/[spaceId]/layout.tsx, apps/web/src/app/[locale]/space-admin/[spaceId]/layout.module.css, apps/web/src/components/space-admin/SpaceAdminHeader.tsx, apps/web/src/components/space-admin/SpaceAdminHeader.module.css, apps/web/src/components/space-admin/SpaceAdminNav.tsx, apps/web/src/components/space-admin/SpaceAdminNav.module.css, apps/web/src/components/space-admin/kicker.module.css</files>
  <read_first>
    - apps/web/src/app/[locale]/knesset/page.tsx (the np-native reference shell: `np-page` + `Masthead` + `main` + `Colophon`)
    - apps/web/src/components/press/Segmented/Segmented.tsx (the visual language to borrow; its ARIA is what must NOT be borrowed)
    - apps/web/src/styles/globals.css around line 445 (`.np-kicker` — the utility this phase replaces locally, and why)
    - .planning/phases/05-space-governance-substrate-and-space-admin-operations-dashboard/05-UI-SPEC.md — "Route + Shell Contract", the Shell copy table, D1, D2, D4, D9, D16
  </read_first>
  <action>
1. Add `"@radix-ui/react-alert-dialog": "^1.1.15"` to `apps/web/package.json` dependencies (same vendor and major line as the existing `@radix-ui/react-dropdown-menu@^2.1.22`) and run `pnpm install`. The repo's own precedent is Radix-for-behaviour, press-tokens-for-paint — `Masthead.tsx:48-51` says so in a comment. The hand-rolled alternative in this repo, `GeoGate`, has no focus trap and no Escape handler, which is not acceptable for a destructive confirmation.

2. `kicker.module.css` — the phase-local kicker, because the shared `.np-kicker` utility paints its text in `--np-red` at 4.03:1. Two classes:
```css
/* On paper / paper-box / paper-2: red TEXT must be --np-red-ink (9.56:1). */
.kicker { font-family: var(--np-font-mono); font-size: var(--text-xs); font-weight: 700;
  letter-spacing: 0.12em; text-transform: uppercase; color: var(--np-red-ink);
  display: inline-flex; align-items: center; gap: var(--space-1); }
.kicker .tick { color: var(--np-red); }          /* decorative glyph, aria-hidden, exempt */

/* On ink: there is NO compliant red text. Text is --np-paper (16.66:1);
   only the tick glyph stays red. */
.kickerOnInk { composes: kicker; color: var(--np-paper); }
```

3. `SpaceAdminHeader.tsx` — kicker `■ ניהול מרחב · SPACE ADMIN`, `h1` reading `לוח הניהול — {spaceName}`, and a mono edition-meta line: `{spaceTypeLabel}` `■` `{slug}` `■` `מזהה מרחב · {shortId}` `■` `{date}`. `h1` is `--text-3xl` at Heebo 900, dropping to `--text-2xl` below 768px. The `shortId` and any uuid render inside `<span dir="ltr">` with `unicode-bidi: isolate` and `overflow-wrap: anywhere`, or they scramble in the RTL flow. Dates use `toLocaleDateString('he-IL')`.

   It also renders the suspension banner when `suspended` is true: an `.np-block-ink` block whose kicker is `■ מושעה · SUSPENDED` in `--np-paper` text with a `--np-red` tick, and body `הגישה שלכם למרחב הזה הושעתה על ידי מנהל־על. ההיסטוריה והתיעוד נשמרים במלואם ואינם נמחקים. לבירור — פנו למנהל־על.`

4. `SpaceAdminNav.tsx` — a `<nav aria-label="ניווט לוח הניהול">` containing six `next/link`s with `aria-current="page"` on the active one. Labels, in order: `סקירה` · `הצעות` · `חברים והרשאות` · `נתונים` · `התראות` · `יומן פעולות`, linking to ``, `/proposals`, `/members`, `/stats`, `/dispatch`, `/audit` under `/he/space-admin/{spaceId}`.

   It borrows `Segmented`'s look — boxed row, ink hairlines between cells, active cell inverts to ink fill — but **must not** use the `Segmented` component. That component emits `role="tablist"`/`role="tab"` with no `aria-controls`, no linked `tabpanel` and no roving focus; these are six separate documents, and borrowing tab ARIA without a tabpanel is a false promise to assistive tech.

   Accept a `visibleHrefs` prop: a link the admin lacks the capability for is **not rendered** (Interaction Contract 1 Rule A). Below 768px the nav scrolls horizontally with `overflow-x: auto; scroll-snap-type: inline mandatory` rather than wrapping. Every cell carries `min-block-size: var(--space-11)`.

5. `layout.tsx` — a Server Component rendering the shell:
```tsx
<div className="np-page">
  <Masthead locale={locale} />
  <main>
    {children}
  </main>
  <Colophon locale={locale} />
</div>
```
   `SpaceAdminHeader` and `SpaceAdminNav` are rendered by each **page**, not by the layout, and this needs a comment: a Next.js layout does not re-render on navigation and does not gate nested segments, so it must not be the place authorization or space identity is resolved. The layout renders chrome only.

   `Masthead` is not sticky, so `main` takes no top offset. Page inline padding is `--np-gutter` on a `max-width: var(--np-container)` wrapper. **Never write `var(--header-height)`.**
  </action>
  <verify>
    <automated>pnpm --filter @sync/web typecheck && pnpm --filter @sync/web lint && ! grep -rn "header-height" apps/web/src/components/space-admin apps/web/src/app/\[locale\]/space-admin && ! grep -rnE "#[0-9a-fA-F]{3,8}\b" apps/web/src/components/space-admin/*.css apps/web/src/app/\[locale\]/space-admin/\[spaceId\]/*.css && grep -q "@radix-ui/react-alert-dialog" apps/web/package.json</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @sync/web typecheck` and `pnpm --filter @sync/web lint` both exit 0
    - `@radix-ui/react-alert-dialog` is in `apps/web/package.json`, and `pnpm-lock.yaml` is updated and committed alongside it — both files are in this plan's `files_modified`
    - `grep -rc "header-height" apps/web/src/components/space-admin apps/web/src/app/\[locale\]/space-admin` returns `0` everywhere
    - No hex colour in any new CSS module: `grep -rnE "#[0-9a-fA-F]{3,8}\b" apps/web/src/components/space-admin/*.css` returns nothing
    - No pixel literal outside a media query: `grep -rnE "[^-a-z(]([0-9]+)px" apps/web/src/components/space-admin/*.css | grep -v "@media"` returns nothing
    - `SpaceAdminNav.tsx` does not import `Segmented` — `grep -cE "^\s*import[^;]*Segmented" apps/web/src/components/space-admin/SpaceAdminNav.tsx` returns `0`. (Matching only import statements is deliberate: the file is expected to *mention* `Segmented` in the comment explaining why it borrows the look without the component, so a bare substring count would never be zero and could not be checked mechanically.)
    - `SpaceAdminNav.tsx` contains `aria-label="ניווט לוח הניהול"` and `aria-current`
    - `kicker.module.css` `.kicker` colour is `var(--np-red-ink)` and `.kickerOnInk` colour is `var(--np-paper)`
    - `layout.tsx` imports `Masthead` from `@/components/press` and `Colophon` from `@/components/press/sections`, and imports neither `@/components/layout/Header` nor `Footer`
    - `layout.tsx` carries the comment explaining why it is not the authorization boundary
    - All six Hebrew nav labels appear verbatim
  </acceptance_criteria>
  <done>The dashboard has a locale-correct np-native shell, a header that can show suspension, a six-link nav with honest ARIA, and a kicker that clears AA on both backgrounds.</done>
</task>

<task type="auto">
  <name>Task 2: PressTable, StatusChip, and the three non-happy-path panels</name>
  <files>apps/web/src/components/space-admin/PressTable.tsx, apps/web/src/components/space-admin/PressTable.module.css, apps/web/src/components/space-admin/StatusChip.tsx, apps/web/src/components/space-admin/StatusChip.module.css, apps/web/src/components/space-admin/Panels.tsx, apps/web/src/components/space-admin/Panels.module.css</files>
  <read_first>
    - .planning/phases/05-space-governance-substrate-and-space-admin-operations-dashboard/05-UI-SPEC.md — Interaction Contract 4 in full, D10, D11, D24, the State Matrix, and the `NoPermissionPanel` / `ErrorPanel` copy
    - apps/web/src/app/[locale]/dashboard/page.module.css (the existing `.record` row pattern this mirrors, and the only permitted pixel literals — media-query breakpoints)
    - apps/web/src/components/space-admin/kicker.module.css (Task 1)
  </read_first>
  <action>
`PressTable.tsx` — one implementation, three column sets. The semantics are non-negotiable:
```html
<div class={scroller} role="region" aria-labelledby={captionId} tabindex="0">
  <table>
    <caption id={captionId} class={srOnly}>{description}</caption>
    <thead><tr><th scope="col" data-col="x">…</th></tr></thead>
    <tbody><tr><th scope="row">{primary}</th><td data-col="x">…</td></tr></tbody>
  </table>
</div>
```
- `tabindex="0"` and `role="region"` go on the scroller **only when it actually overflows** (measure with a `ResizeObserver`); otherwise they add a pointless tab stop.
- Responsive reduction is `display: none` on the **matched pair** `th[data-col=x]` and `td[data-col=x]`. Never change a cell's `display` to `block` or `flex` — that destroys table semantics in assistive tech, and the usual "responsive table" trick is an accessibility regression.
- Hidden values stay reachable through a row disclosure (`▾ הצג פרטים`) that expands a `<tr>` with `<td colspan={n}>` holding a mono definition list. Expose that expansion as a prop (`renderExpansion`), because the proposals surface replaces it with its detail panel and there must be exactly one `<tr>` expansion per row at every width.
- Visual contract: `thead th` is `.np-block-ink`, mono `--text-sm`/700, `letter-spacing: 0.08em`, uppercase, `position: sticky; inset-block-start: 0` on desktop. `tbody tr` is `--np-paper-box` separated by `border-block-start: var(--np-rule-hair) solid var(--np-paper-edge)`. Row hover is `--np-paper-2`, never red, never zebra. `:focus-within` gets `outline: var(--np-rule) solid var(--np-red); outline-offset: -2px`. Cell padding `--space-3` block / `--space-4` inline. Row `min-block-size: var(--space-11)`. Whole table bordered `--np-rule solid var(--np-ink)`.
- `text-align: start` on every cell including numeric; `font-variant-numeric: tabular-nums` on dates, counts and ids.
- Long content: 2-line clamp with both `-webkit-line-clamp: 2` and standard `line-clamp: 2` plus `overflow-wrap: anywhere`; uuids render `{id.slice(0,8)}…` in mono, `dir="ltr"`, `unicode-bidi: isolate`, with `title` carrying the full value.
- Cursor pagination controls at the block end, `NewsButton variant="outline" size="sm"` with `min-block-size: var(--space-11)`: `← רשומות ישנות יותר` and `רשומות חדשות יותר →`. Offset paging is not offered — the audit log is append-only and offset pages duplicate and skip rows as new entries land.
- A mobile overflow hint rendered only when the scroller overflows: `החליקו לצדדים לצפייה בכל העמודות` (mono, `--text-sm`, `--np-ink-faint`).
- A skeleton mode: five `--np-paper-2` rows at `--space-11`, container `aria-busy="true"`, a visually-hidden `aria-live="polite"` node announcing `…טוען`, and no shimmer under `prefers-reduced-motion`. Never a spinner and skeletons together.

`StatusChip.tsx` — mono, boxed, hard corners, `--text-sm`. Three appearances and no more:
- `בבדיקה`: `--np-red-ink` text with a `--np-red` hairline border on `--np-paper-box` (10.32:1 text, 3:1 border). Never a red fill — a red-filled chip with paper text is 4.03:1 at 13.3px.
- `מושעה/ית`: inverted ink chip (16.66:1). It means "switched off", not "urgent", so it is not red.
- Everything else (`טיוטה`, `הוחזרה לתיקון`, `נדחתה`, `אושרה ופורסמה`, `פעיל/ה`): ink outline.

`Panels.tsx` — export `EmptyPanel`, `ErrorPanel` and `NoPermissionPanel`, one implementation each, all boxed `--np-paper-box` with a `--np-rule-heavy` ink rule and a kicker:
- `ErrorPanel`: kicker `■ שגיאה · ERROR`, body `לא הצלחנו לטעון את הנתונים. הנתונים עצמם לא נפגעו — נסו שוב; אם זה חוזר, פנו למנהל־על.`, CTA `נסו שוב`.
- `NoPermissionPanel`: kicker `■ אין הרשאה · NO ACCESS`, heading `אין לכם גישה לחלק הזה`, body `החשבון שלכם לא מחזיק את ההרשאה הנדרשת במרחב הזה, או שהגישה הושעתה. אם זו טעות — פנו למנהל־על.`, CTA `פנייה למנהל־על` (`outline`), and a mono faint note `ההרשאות נבדקות בשרת בכל בקשה. הסתרת כפתור אינה מה שמגן על הנתונים.`
- `EmptyPanel`: heading and body as props, since each surface supplies its own.

Every module ends with `@media (prefers-reduced-motion: reduce)` setting every transition and animation to `none`.
  </action>
  <verify>
    <automated>pnpm --filter @sync/web typecheck && pnpm --filter @sync/web lint && grep -q 'scope="row"' apps/web/src/components/space-admin/PressTable.tsx && grep -q "prefers-reduced-motion" apps/web/src/components/space-admin/PressTable.module.css</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @sync/web typecheck` and `pnpm --filter @sync/web lint` both exit 0
    - `PressTable.tsx` renders `<caption>`, `scope="col"` on every head cell and `scope="row"` on the primary body cell
    - `PressTable.module.css` contains no rule setting `display: block` or `display: flex` on a `td` or `th`
    - Responsive hiding is by `data-col` pairs — `grep -q "data-col" apps/web/src/components/space-admin/PressTable.module.css` succeeds
    - Row hover is `var(--np-paper-2)`; `grep -c "np-red" apps/web/src/components/space-admin/PressTable.module.css` shows red only on the focus outline
    - `StatusChip.module.css` has no `background: var(--np-red)` rule — the red chip is text plus hairline border
    - `Panels.tsx` contains all three Hebrew bodies verbatim, including `ההרשאות נבדקות בשרת בכל בקשה. הסתרת כפתור אינה מה שמגן על הנתונים.`
    - Every new CSS module ends with a `@media (prefers-reduced-motion: reduce)` block
    - No hex colour and no non-media-query pixel literal in any of the three modules
  </acceptance_criteria>
  <done>One table implementation with real semantics at every width, one chip with three compliant appearances, and one implementation each of the three non-happy-path panels.</done>
</task>

<task type="auto">
  <name>Task 3: ConfirmDialog and the disabled-state contract</name>
  <files>apps/web/src/components/space-admin/ConfirmDialog.tsx, apps/web/src/components/space-admin/ConfirmDialog.module.css, apps/web/src/components/space-admin/index.ts</files>
  <read_first>
    - .planning/phases/05-space-governance-substrate-and-space-admin-operations-dashboard/05-UI-SPEC.md — Interaction Contract 2 in full (the anatomy diagram, the property table, and the kind table), plus D7, D17, D22, D23, D27
    - apps/web/src/components/press/NewsButton/NewsButton.module.css (the `.btn`, `.ink:hover` and `.outline:hover` specificities this override must beat — and the one it must NOT touch)
    - apps/web/src/components/press/PressInput/PressInput.tsx (it already ships the kicker label, `aria-invalid` and the `role="alert"` error row; do not reimplement them)
    - apps/web/src/components/press/Masthead/Masthead.tsx lines 48-51 (the Radix-for-behaviour, press-tokens-for-paint precedent)
  </read_first>
  <action>
`ConfirmDialog.tsx` — Radix `AlertDialog`, painted locally. Props: `kind: 'audited' | 'irreversible'`, `heading`, `body`, `consequence?`, `confirmLabel`, `reasonLabel` (default `נימוק ההחלטה (חובה)`), `reasonError` (default `נדרש נימוק — לפחות 10 תווים.`), `placeholder`, `onConfirm(reason)`, `open`, `onOpenChange`, `pending`, `error`.

Anatomy, in this order inside a plate that is `--np-paper-box` with `border: var(--np-rule-heavy) solid {ink|red}`, radius 0, padding `--space-8`:
kicker (mono `--text-xs`, `--np-red-ink`) → `h2` (Heebo 800, `--text-base`) → body `p` (Heebo 400, `--text-base`, `--np-ink-soft`) → consequence `p` (mono `--text-sm`, `--np-red-ink`) → `--np-rule` hairline → `PressInput multiline` → mono counter `{n}/500` in `--np-ink-faint` → `--np-rule` hairline → confirm (`NewsButton variant="ink"`) and cancel (`NewsButton variant="outline"`, label `ביטול`).

- `kind="audited"`: ink `--np-rule-heavy` plate rule, kicker `פעולה מתועדת · AUDITED`, consequence optional.
- `kind="irreversible"`: red `--np-rule-heavy` plate rule, kicker `פעולה בלתי הפיכה · IRREVERSIBLE`, consequence **required** — make it a required prop for that kind via a discriminated union, so the compiler enforces it.

Exactly three actions in the whole phase are `irreversible`: approve, reject, send. The test is whether the admin can undo the effect from this dashboard — not whether it is written to the audit log, which is true of every action here. Put that sentence in the component's docblock so nobody re-labels suspension later.

Behaviour: initial focus on the **reason textarea**, never the confirm button. Reason is required, trimmed length 10–500, live counter, no hard truncation on paste (block submit instead). Confirm is `disabled` until the reason is valid, and its label is the action verb — never `אישור` or `OK`. `Escape` and a backdrop click both cancel. On failure the dialog **stays open** with the typed reason preserved and the error rendered inside the plate as `role="alert"` in `--np-red-ink`. On success it closes and the caller flashes the affected row. Backdrop is `--color-surface-overlay` at `--z-modal-backdrop`; the plate sits at `--z-modal`. Entry is a hard inset clip reveal over `--np-dur` with `--np-ease` — no fade, no scale, no bounce — and no transition at all under `prefers-reduced-motion`.

`ConfirmDialog.module.css` — the disabled-state contract, and it must be authored `:disabled`-qualified or it silently loses:
```css
/* (0,2,0) beats .btn (0,1,0) */
.confirmBtn:disabled {
  background: var(--np-paper-2);
  color: var(--np-ink-faint);      /* 4.53:1 on paper-2 — passes AA normal text */
  border-color: var(--np-ink-faint);
  cursor: not-allowed;
}
/* (0,2,1) beats .ink:hover (0,1,1). :hover still fires on a disabled button. */
.confirmBtn:disabled:hover {
  background: var(--np-paper-2);
  color: var(--np-ink-faint);
}
```
Do **not** add `opacity` — it would drag `--np-paper-2` below its 0.03 AA margin. A bare class from `className` is (0,1,0) and cannot beat `.ink:hover` at (0,1,1); an implementation that ships the unqualified form looks correct in review and fails at runtime.

Also add the dense-`ink` hover override, scoped to this phase's own buttons:
```css
/* NewsButton.module.css:44 inverts .ink to a red fill with paper text on
   hover — 4.03:1 at 16px, the same defect that bans .np-block-red. */
.confirmBtn:hover:not(:disabled) { background: var(--np-red-dark); }   /* 6.03:1 */
```
`outline` is **not** overridden. `.outline:hover` inverts to ink at 16.66:1 and is already compliant; applying the red-dark fix there would lower contrast and put a red fill on every destructive row trigger — reject, revoke, suspend, hide, unhide, flag, unflag, reinstate and escalate are all `outline` — which would gut the structural-danger formula. No `outline` control ever takes a red or red-dark fill, at rest or on hover.

Export a `dialogButtons.module.css`-style shared class or re-export `styles.confirmBtn` from `index.ts` so the surface plans apply the same disabled contract to the dispatch send button and the pagination controls, which are the other entries in the permitted-`disabled` list.

Finally, write `apps/web/src/components/space-admin/index.ts` re-exporting every component in **this** plan, and nothing else. Plans 05-12 through 05-15 create five more components but must not reopen this barrel — they run in the same or an adjacent wave and would collide on it — so they import by direct path. Note that in a comment at the top of the file.
  </action>
  <verify>
    <automated>pnpm --filter @sync/web typecheck && pnpm --filter @sync/web lint && grep -q ":disabled:hover" apps/web/src/components/space-admin/ConfirmDialog.module.css && grep -q "np-red-dark" apps/web/src/components/space-admin/ConfirmDialog.module.css && ! grep -q "opacity" apps/web/src/components/space-admin/ConfirmDialog.module.css</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @sync/web typecheck` and `pnpm --filter @sync/web lint` both exit 0
    - `ConfirmDialog.module.css` contains both `.confirmBtn:disabled` and `.confirmBtn:disabled:hover` — the unqualified bare-class form is absent
    - `grep -c "opacity" apps/web/src/components/space-admin/ConfirmDialog.module.css` returns `0`
    - `grep -c "outline" apps/web/src/components/space-admin/ConfirmDialog.module.css` shows no rule giving an `outline`-variant button a red or red-dark background
    - `ConfirmDialog.tsx` imports `AlertDialog` from `@radix-ui/react-alert-dialog`
    - The props type is a discriminated union in which `kind: 'irreversible'` requires `consequence`
    - Both kicker strings appear verbatim: `פעולה מתועדת · AUDITED` and `פעולה בלתי הפיכה · IRREVERSIBLE`
    - Initial focus targets the textarea — `grep -q "onOpenAutoFocus" apps/web/src/components/space-admin/ConfirmDialog.tsx` or an explicit ref focus on the reason field
    - The docblock states the irreversible test ("can the admin undo it from this dashboard") and names the three qualifying actions
    - `index.ts` re-exports all eight components built by this plan: `SpaceAdminHeader`, `SpaceAdminNav`, `PressTable`, `StatusChip`, `ConfirmDialog`, `EmptyPanel`, `ErrorPanel`, `NoPermissionPanel` — and nothing else
  </acceptance_criteria>
  <done>One confirmation component enforces the reason at the UI layer, renders a genuinely disabled confirm button, and keeps every hover state above AA without touching the compliant outline variant.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @sync/web typecheck` and `pnpm --filter @sync/web lint` — both green
- `grep -rnE "#[0-9a-fA-F]{3,8}\b" apps/web/src/components/space-admin/*.css` — nothing
- `grep -rnE "[^-a-z(]([0-9]+)px" apps/web/src/components/space-admin/*.css | grep -v "@media"` — nothing
- `grep -rn "header-height\|np-block-red\|components/layout/Header" apps/web/src/components/space-admin apps/web/src/app/[locale]/space-admin` — nothing
- Manual: render the shell at `/he/space-admin/00000000-0000-0000-0000-000000000000` with a stub page and confirm the masthead, nav and colophon compose without a top offset
</verification>

<success_criteria>
1. `@radix-ui/react-alert-dialog` is installed and is the only new dependency.
2. Eight components exist in `components/space-admin/` — `SpaceAdminHeader`, `SpaceAdminNav`, `PressTable`, `StatusChip`, `ConfirmDialog`, and the three panels from `Panels.tsx` — all painted with `--np-*` tokens.
3. The disabled contract is authored `:disabled`-qualified and carries no opacity.
4. `PressTable` keeps table semantics at every width and reduces columns by paired `data-col` hiding.
5. No hex colour, no stray pixel length, no reference to the undefined `--header-height`.
</success_criteria>

<output>
After completion, create `.planning/phases/05-space-governance-substrate-and-space-admin-operations-dashboard/05-11-SUMMARY.md`, listing each component's props so the five surface plans compose rather than guess.
</output>
