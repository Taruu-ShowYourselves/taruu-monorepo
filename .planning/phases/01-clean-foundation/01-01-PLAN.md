---
phase: 01-clean-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/env.ts
  - apps/web/.dev.vars.example
  - .env.example
  - apps/web/src/lib/supabase/db.ts
  - apps/web/src/lib/supabase/types.ts
  - apps/web/src/app/api/merch/orders/[id]/route.ts
  - packages/shared/src/types/merch.ts
  - supabase/migrations/20260628000001_drop_merch_pod_columns.sql
  - .gitignore
autonomous: true
requirements: [LAND-01]
must_haves:
  truths:
    - "The coherent Auth0 + Printful-removal + RLS-fix working tree lands as ONE clean commit"
    - "pnpm typecheck and pnpm test both pass (no compilation errors, tests green)"
    - "No PRINTFUL_* entries remain in apps/web/.dev.vars.example or .env.example"
    - "Dead getMerchOrderByPodOrderId is removed and orphaned merch_orders pod/tracking columns are dropped via migration"
    - "supabase/.temp/ and .mcp.json are gitignored, not committed; growth/ and .planning/ are NOT in this commit"
    - "Redundant server-only AUTH0_DOMAIN env var is removed from the schema"
  artifacts:
    - path: "supabase/migrations/20260628000001_drop_merch_pod_columns.sql"
      provides: "Drops orphaned pod_order_id, tracking_number, tracking_url, carrier columns from merch_orders"
      contains: "ALTER TABLE public.merch_orders DROP COLUMN"
    - path: "apps/web/src/lib/env.ts"
      provides: "Env schema without the redundant AUTH0_DOMAIN server var"
  key_links:
    - from: "apps/web/src/app/api/merch/orders/[id]/route.ts"
      to: "MerchOrder type"
      via: "no longer reads pod_order_id / tracking_* columns"
      pattern: "podOrderId|trackingNumber|trackingUrl|carrier"
---

<objective>
Land the coherent uncommitted working-tree change (Auth0 OIDC swap + Printful removal + existing RLS `public.user_id()` fixes) as ONE clean, semantic commit — including the two cleanups CONCERNS.md flags for the same commit (dead `PRINTFUL_*` `.dev.vars.example` entries; orphaned `merch_orders` POD/tracking columns + their dead reader code) and the one small env cleanup (redundant server `AUTH0_DOMAIN`).

Purpose: A coherent, deployable, secure base before any payment-rail work begins (Phase 1 goal). Closes LAND-01.
Output: Cleanup edits applied, a new drop-columns migration, supabase/.temp + .mcp.json gitignored, and a single green-CI commit landing the entire bundle.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md

CRITICAL PROCESS RULE (user global): NEVER add Claude/Anthropic as a git co-author. The commit message must contain NO `Co-Authored-By: Claude` / `Anthropic` trailer.

CRITICAL BUILD RULE (project memory): NEVER run `next build` while `next dev` is running — it clobbers `.next`. Verification uses `typecheck` + `test` only, not `build`.

<interfaces>
<!-- Exact current state the executor edits against. No exploration needed. -->

apps/web/src/lib/env.ts — serverEnvSchema currently contains (line 20):
```ts
  // Auth0 (primary login — OIDC Universal Login; federates Google)
  AUTH0_DOMAIN: z.string().min(1, 'AUTH0_DOMAIN is required'),        // ← REDUNDANT: nothing reads process.env.AUTH0_DOMAIN
  AUTH0_CLIENT_ID: z.string().min(1, 'AUTH0_CLIENT_ID is required'),
  AUTH0_CLIENT_SECRET: z.string().min(1, 'AUTH0_CLIENT_SECRET is required'),  // ← KEEP: read by api/auth/callback/route.ts:44
  NEXT_PUBLIC_AUTH0_DOMAIN: z.string()...,   // ← KEEP: read by auth0.ts:49 + AuthProvider.tsx:102
  NEXT_PUBLIC_AUTH0_CLIENT_ID: z.string()...,// ← KEEP: read by auth0.ts:50 + AuthProvider.tsx:104
```
Confirmed reads: `services/auth/auth0.ts` reads `NEXT_PUBLIC_AUTH0_DOMAIN` (line 49) and `NEXT_PUBLIC_AUTH0_CLIENT_ID` (line 50). `api/auth/callback/route.ts:44` reads `AUTH0_CLIENT_SECRET`. NOTHING reads the bare `AUTH0_DOMAIN`. Decision (locked): REMOVE the server `AUTH0_DOMAIN` line from the schema.

apps/web/.dev.vars.example — lines 52–54 are dead after Printful removal:
```
# Printful (POD fulfilment)
PRINTFUL_API_KEY=
PRINTFUL_WEBHOOK_SECRET=
```
Line 19 `AUTH0_DOMAIN=` is the dead server var. Note line 25–28 already correctly mark the legacy Google vars as commented-out.

packages/shared/src/types/merch.ts — MerchOrder (lines 99–106) still has dead POD fields:
```ts
  /** Green Invoice payment / document id once issued. */
  paymentId?: string;
  /** POD partner order id once fulfilment starts. */
  podOrderId?: string;                 // ← REMOVE
  /** Shipment tracking, set when the POD partner ships the order. */
  trackingNumber?: string;             // ← REMOVE
  trackingUrl?: string;                // ← REMOVE
  carrier?: string;                    // ← REMOVE
```

apps/web/src/lib/supabase/db.ts:1984–1999 — `getMerchOrderByPodOrderId()` is dead (no caller outside db.ts; confirmed via grep). REMOVE the whole function + its JSDoc line.

apps/web/src/lib/supabase/types.ts — merch_orders Row/Insert/Update (lines 93–96, 111–114, 121–124) declare `pod_order_id`, `tracking_number`, `tracking_url`, `carrier`. REMOVE all 12 lines.

apps/web/src/app/api/merch/orders/[id]/route.ts:26–29 — toOrder() maps the dead columns:
```ts
    podOrderId: row.pod_order_id ?? undefined,
    trackingNumber: row.tracking_number ?? undefined,
    trackingUrl: row.tracking_url ?? undefined,
    carrier: row.carrier ?? undefined,
```
REMOVE these 4 lines.

Latest migration on disk: `20260622000001_merch_orders_rls.sql`. Today = 2026-06-28 → new migration prefix `20260628000001`. Migrations are idempotent and applied in filename order (STRUCTURE.md).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove redundant AUTH0_DOMAIN env var + dead PRINTFUL_* example entries</name>
  <files>apps/web/src/lib/env.ts, apps/web/.dev.vars.example, .env.example</files>
  <read_first>apps/web/src/lib/env.ts (lines 11–45), apps/web/.dev.vars.example, .env.example</read_first>
  <action>
    1. In `apps/web/src/lib/env.ts`, DELETE the single line in `serverEnvSchema`:
       `  AUTH0_DOMAIN: z.string().min(1, 'AUTH0_DOMAIN is required'),`
       Leave `AUTH0_CLIENT_ID`, `AUTH0_CLIENT_SECRET`, `NEXT_PUBLIC_AUTH0_DOMAIN`, `NEXT_PUBLIC_AUTH0_CLIENT_ID` untouched (they ARE read at runtime). Do NOT touch the `clientEnvSchema`.
    2. In `apps/web/.dev.vars.example`, DELETE the three dead lines:
       `# Printful (POD fulfilment)`, `PRINTFUL_API_KEY=`, `PRINTFUL_WEBHOOK_SECRET=` (the block at lines 52–54). Also DELETE the dead `AUTH0_DOMAIN=` line (line 19), keeping `AUTH0_CLIENT_ID=`, `AUTH0_CLIENT_SECRET=`, `NEXT_PUBLIC_AUTH0_DOMAIN=`, `NEXT_PUBLIC_AUTH0_CLIENT_ID=`.
    3. In `.env.example`, DELETE any `PRINTFUL_API_KEY` / `PRINTFUL_WEBHOOK_SECRET` lines (and their comment header if present) and the bare `AUTH0_DOMAIN=...` line (line 81), keeping the other four AUTH0 vars.
    Do NOT rename `SUPABASE_SERVICE_KEY` → `SUPABASE_SERVICE_ROLE_KEY` here — that is SEC-05 (Phase 3), explicitly out of scope.
  </action>
  <verify>
    <automated>! grep -RnF "PRINTFUL_" apps/web/.dev.vars.example .env.example && ! grep -nF "AUTH0_DOMAIN: z.string().min(1, 'AUTH0_DOMAIN is required')" apps/web/src/lib/env.ts && grep -nF "NEXT_PUBLIC_AUTH0_DOMAIN" apps/web/src/lib/env.ts && grep -nF "AUTH0_CLIENT_SECRET" apps/web/src/lib/env.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -RnF "PRINTFUL_" apps/web/.dev.vars.example .env.example` returns nothing (exit 1)
    - `grep -nF "AUTH0_DOMAIN: z.string().min(1, 'AUTH0_DOMAIN is required')" apps/web/src/lib/env.ts` returns nothing (the server var is gone)
    - `grep -nF "NEXT_PUBLIC_AUTH0_DOMAIN" apps/web/src/lib/env.ts` still matches (public var kept)
    - `grep -nF "AUTH0_CLIENT_SECRET" apps/web/src/lib/env.ts` still matches (server secret kept)
    - `grep -nF "SUPABASE_SERVICE_KEY" apps/web/src/lib/env.ts` still matches (NOT renamed — out of scope)
  </acceptance_criteria>
  <done>The redundant server AUTH0_DOMAIN var and all PRINTFUL_* example entries are gone; every still-read Auth0 var remains.</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead POD/tracking code + drop orphaned merch_orders columns via migration + gitignore CLI temp</name>
  <files>apps/web/src/lib/supabase/db.ts, apps/web/src/lib/supabase/types.ts, apps/web/src/app/api/merch/orders/[id]/route.ts, packages/shared/src/types/merch.ts, supabase/migrations/20260628000001_drop_merch_pod_columns.sql, .gitignore</files>
  <read_first>apps/web/src/lib/supabase/db.ts (lines 1980–2002), apps/web/src/lib/supabase/types.ts (lines 88–126), apps/web/src/app/api/merch/orders/[id]/route.ts, packages/shared/src/types/merch.ts (lines 88–110), .gitignore</read_first>
  <action>
    1. In `packages/shared/src/types/merch.ts`, DELETE the four dead fields from the `MerchOrder` interface (and their doc comments): `podOrderId?`, the `/** Shipment tracking... */` comment, `trackingNumber?`, `trackingUrl?`, `carrier?`. Keep `paymentId?`.
    2. In `apps/web/src/lib/supabase/db.ts`, DELETE the entire `getMerchOrderByPodOrderId()` function including its JSDoc line `/** Look up a merch order by its POD partner order id (for fulfilment webhooks). */` (lines ~1984–1999). It has no caller (grep-confirmed dead code).
    3. In `apps/web/src/lib/supabase/types.ts`, DELETE the `pod_order_id`, `tracking_number`, `tracking_url`, `carrier` lines from all three merch_orders shapes (Row lines 93–96, Insert 111–114, Update 121–124 — 12 lines total).
    4. In `apps/web/src/app/api/merch/orders/[id]/route.ts`, DELETE the four mapping lines in `toOrder()`: `podOrderId:`, `trackingNumber:`, `trackingUrl:`, `carrier:` (lines 26–29).
    5. CREATE `supabase/migrations/20260628000001_drop_merch_pod_columns.sql` that drops the orphaned columns (POD is definitively abandoned per CONCERNS §1/§8). Use IF EXISTS for idempotency, matching the migration style:
       ```sql
       -- Drop orphaned Printful/POD columns from merch_orders.
       -- POD fulfilment was removed (Printful service + fulfilment webhook deleted in
       -- the same change); pod_order_id, tracking_number, tracking_url and carrier are
       -- now permanently NULL. CONCERNS.md §1/§8. Revert: re-add as TEXT NULL columns.
       ALTER TABLE public.merch_orders DROP COLUMN IF EXISTS pod_order_id;
       ALTER TABLE public.merch_orders DROP COLUMN IF EXISTS tracking_number;
       ALTER TABLE public.merch_orders DROP COLUMN IF EXISTS tracking_url;
       ALTER TABLE public.merch_orders DROP COLUMN IF EXISTS carrier;
       ```
    6. In `.gitignore`, append two entries under a clear comment so the Supabase CLI scratch dir and local MCP config are never committed:
       ```
       # Supabase CLI local state
       supabase/.temp/

       # Local MCP server config (machine-specific)
       .mcp.json
       ```
  </action>
  <verify>
    <automated>grep -F "ALTER TABLE public.merch_orders DROP COLUMN IF EXISTS pod_order_id" supabase/migrations/20260628000001_drop_merch_pod_columns.sql && ! grep -RnE "podOrderId|trackingNumber|trackingUrl|getMerchOrderByPodOrderId" apps/web/src packages/shared/src && ! grep -RnE "pod_order_id|tracking_number|tracking_url|carrier" apps/web/src/lib/supabase/types.ts && grep -qx "supabase/.temp/" .gitignore && grep -qx ".mcp.json" .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/20260628000001_drop_merch_pod_columns.sql` exists and contains four `DROP COLUMN IF EXISTS` statements (pod_order_id, tracking_number, tracking_url, carrier)
    - `grep -RnE "podOrderId|trackingNumber|trackingUrl|getMerchOrderByPodOrderId" apps/web/src packages/shared/src` returns nothing (exit 1)
    - `grep -RnE "pod_order_id|tracking_number|tracking_url|carrier" apps/web/src/lib/supabase/types.ts` returns nothing
    - `.gitignore` contains a line exactly `supabase/.temp/` and a line exactly `.mcp.json`
    - `pnpm --filter @sync/web typecheck` exits 0 (the deletions are type-consistent)
  </acceptance_criteria>
  <done>All dead POD/tracking code is gone, the orphaned columns are dropped by an idempotent migration, and CLI/MCP scratch files are gitignored.</done>
</task>

<task type="auto">
  <name>Task 3: Verify green CI and land the single LAND-01 commit</name>
  <files>(git operation — stages the explicit bundle below)</files>
  <read_first>git status --porcelain (confirm the working-tree set matches expectations before staging)</read_first>
  <action>
    1. Run the automated checks and confirm both pass BEFORE committing:
       - `pnpm --filter @sync/web typecheck`
       - `pnpm --filter @sync/web test`
       (Do NOT run `pnpm build` / `next build` — project rule: it clobbers `.next` if dev is running.)
    2. Quick safety scan: open `.claude/settings.local.json` and confirm it contains NO secrets/API keys (it is local permission config). If it contains anything secret, do NOT stage it and note this in the SUMMARY; otherwise stage it per the bundle.
    3. Stage EXACTLY this set (explicit paths — do NOT use `git add -A`, which would wrongly pull in growth/ and .planning/):
       - Modified: `.claude/settings.local.json`, `.env.example`, `.gitignore`, `apps/web/.dev.vars.example`, `apps/web/src/__tests__/api/auth-callback.test.ts`, `apps/web/src/__tests__/integration/auth.test.ts`, `apps/web/src/app/[locale]/layout.tsx`, `apps/web/src/app/api/auth/callback/route.ts`, `apps/web/src/app/api/merch/webhook/route.ts`, `apps/web/src/components/AnalyticsEvents.tsx`, `apps/web/src/components/press/sections/Lead.tsx`, `apps/web/src/lib/env.ts`, `apps/web/src/lib/merch/catalog.ts`, `apps/web/src/lib/supabase/db.ts`, `apps/web/src/lib/supabase/types.ts`, `apps/web/src/app/api/merch/orders/[id]/route.ts`, `apps/web/src/providers/AuthProvider.tsx`, `packages/shared/src/types/merch.ts`, `supabase/config.toml`, `supabase/migrations/20240101000001_rls_policies.sql`, `supabase/migrations/20250115000001_push_tokens_and_wallet.sql`
       - Deletions (use `git rm`): `apps/web/src/__tests__/api/merch-fulfillment-webhook.test.ts`, `apps/web/src/__tests__/services/printful.test.ts`, `apps/web/src/app/api/merch/fulfillment-webhook/route.ts`, `apps/web/src/services/fulfillment/printful.ts`
       - New files to add: `apps/web/src/services/auth/auth0.ts`, `supabase/migrations/20260622000001_merch_orders_rls.sql`, `supabase/migrations/20260628000001_drop_merch_pod_columns.sql`, `apps/web/docs/INTEGRATIONS.md`, `apps/web/docs/MORNING-CHECKLIST.md`, `apps/web/scripts/sync-secrets.sh`
    4. EXPLICITLY DO NOT stage: anything under `growth/`, anything under `.planning/`, `.mcp.json`, `supabase/.temp/` (the latter two are now gitignored). These are separate concerns / planning artifacts.
    5. Verify the staged set with `git status --short` — every `growth/` and `.planning/` path must still show as unstaged (` M`/`??`), and `.mcp.json` + `supabase/.temp/` must NOT appear (gitignored).
    6. Commit with a semantic message and NO Claude/Anthropic co-author trailer:
       `git commit -m "feat(foundation): land Auth0 OIDC swap, remove Printful POD, fix RLS user_id helper" -m "Auth0 Universal Login replaces direct Google OAuth; Printful fulfilment service/webhook/tests removed and orphaned merch_orders POD/tracking columns dropped; existing RLS policies corrected to public.user_id(); merch_orders RLS enabled. Closes LAND-01."`
       Confirm with `git log -1 --format='%an <%ae>%n%b'` that the author is the user (SaharBarak) and the body contains NO `Co-Authored-By` line mentioning Claude or Anthropic.
  </action>
  <verify>
    <automated>pnpm --filter @sync/web typecheck && pnpm --filter @sync/web test && git log -1 --format='%s' | grep -q "land Auth0 OIDC swap" && ! git log -1 --format='%b' | grep -iE "co-authored-by.*(claude|anthropic)"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @sync/web typecheck` exits 0
    - `pnpm --filter @sync/web test` exits 0 (all vitest suites pass, including updated auth-callback + integration/auth tests)
    - `git log -1 --format='%s'` contains `land Auth0 OIDC swap`
    - `git log -1 --format='%b'` does NOT match `co-authored-by.*(claude|anthropic)` (case-insensitive)
    - `git show --stat HEAD` lists `apps/web/src/services/auth/auth0.ts` and `supabase/migrations/20260628000001_drop_merch_pod_columns.sql`, and lists NO path under `growth/` or `.planning/`
    - `git status --short` shows `.mcp.json` and `supabase/.temp/` absent (gitignored)
  </acceptance_criteria>
  <done>One clean semantic commit lands the entire LAND-01 bundle with green typecheck + tests; no planning artifacts and no Claude co-author leaked in.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @sync/web typecheck` exits 0 and `pnpm --filter @sync/web test` exits 0.
- `git show --stat HEAD` shows the Auth0/Printful/RLS bundle + the new drop-columns migration as a single commit; no `growth/` or `.planning/` paths.
- `grep -RnF "PRINTFUL_" apps/web/.dev.vars.example .env.example` returns nothing.
- `grep -RnE "podOrderId|trackingNumber|getMerchOrderByPodOrderId" apps/web/src packages/shared/src` returns nothing.
- The commit body contains no Claude/Anthropic co-author trailer.
</verification>

<success_criteria>
LAND-01 is closed: the coherent working-tree change plus the two flagged cleanups and the env cleanup are committed as one clean, green-CI commit; no dead Printful artifacts, orphaned columns, or redundant env vars remain; CLI/MCP scratch files are gitignored.
</success_criteria>

<output>
After completion, create `.planning/phases/01-clean-foundation/01-01-SUMMARY.md` documenting the commit SHA, the exact staged file set, the cleanup decisions (AUTH0_DOMAIN removed from schema; POD columns dropped vs reserved), and any deviation (e.g., settings.local.json withheld for secrets).
</output>
