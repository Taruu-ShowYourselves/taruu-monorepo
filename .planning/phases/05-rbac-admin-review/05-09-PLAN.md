---
phase: 05-rbac-admin-review
plan: 09
type: execute
wave: 6
depends_on: ["05-02", "05-04", "05-05", "05-06", "05-07", "05-08"]
files_modified:
  - .planning/phases/05-rbac-admin-review/05-VALIDATION.md
  - apps/web/docs/RBAC-VERIFICATION.md
autonomous: false
requirements: [RBAC-01, RBAC-03, RBAC-04, RLS-04, RLS-05]

user_setup:
  - service: supabase
    why: "The RLS transport signs its own Supabase access tokens; the project JWT secret is only readable from the dashboard, and the Worker needs it at runtime"
    env_vars:
      - name: SUPABASE_JWT_SECRET
        source: "Supabase Dashboard → Project Settings → API → JWT Settings → JWT Secret. Then `cd apps/web && npx wrangler secret put SUPABASE_JWT_SECRET`, and add it to local `.dev.vars`."
    dashboard_config:
      - task: "Apply supabase/migrations/20260802000001_rls_transport.sql and 20260802000002_role_grants_and_applications.sql, in that order, via the Supabase Management API (the path used for the knesset and identity-document migrations). There is no `supabase db push` script in this repo."
        location: "Supabase Management API / Supabase Studio SQL editor"

must_haves:
  truths:
    - "Both migrations are applied to the live database, in order, and the three tables exist"
    - "public.user_id() reads only the JWT claim, and set_claim no longer exists in the database"
    - "The six SELECT policies exist and the RLS-04 harness passes against the live database"
    - "An UPDATE or DELETE against role_grant_events raises, proving append-only"
    - "SUPABASE_JWT_SECRET is set as a Worker secret, so the transport works in production and not only locally"
    - "A first super_admin grant exists so the console is reachable by a human"
    - "The applicant screen, the review console, and an approve/reject transition are captured as visual evidence"
  artifacts:
    - path: "apps/web/docs/RBAC-VERIFICATION.md"
      provides: "The recorded outcome of every check this phase cannot run in CI"
      contains: "RLS harness"
  key_links:
    - from: "apps/web/docs/RBAC-VERIFICATION.md"
      to: ".planning/phases/05-rbac-admin-review/05-VALIDATION.md"
      via: "the RLS-04 and RLS-05 rows are marked green here"
      pattern: "RLS-04"
---

<objective>
Land Phase 5 on a real database and record the evidence: apply both migrations, run the RLS-04
harness against the live project, prove the append-only trigger, bootstrap the first `super_admin`,
and walk the submit → approve → suspend lifecycle with screenshots.

Purpose: everything before this plan is code and files. Nothing has touched a database, no
`super_admin` grant exists anywhere, and `SUPABASE_JWT_SECRET` is not on the Worker — so the console
403s for every human including you, and the RLS policies are unproven. This plan closes all three.

Note what changed from the first planning pass: the anon-key RLS check is **no longer a manual SQL
session**. Plan 05-04 automated it. This plan runs that harness and pastes its output, which is a
stronger and repeatable result.

Output: two applied migrations, a Worker secret, a bootstrapped super_admin, a filled-in
verification record, and 05-VALIDATION.md signed off.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-rbac-admin-review/05-VALIDATION.md
@.planning/phases/05-rbac-admin-review/05-CONTEXT.md
@.planning/phases/05-rbac-admin-review/05-01-SUMMARY.md
@.planning/phases/05-rbac-admin-review/05-02-SUMMARY.md
@.planning/phases/05-rbac-admin-review/05-04-SUMMARY.md
@.planning/phases/05-rbac-admin-review/05-06-SUMMARY.md
@.planning/phases/05-rbac-admin-review/05-07-SUMMARY.md
@.planning/phases/05-rbac-admin-review/05-08-SUMMARY.md

<interfaces>
Migrations to apply, **in this order**:
1. `supabase/migrations/20260802000001_rls_transport.sql` (plan 05-01) — JWT-only `public.user_id()`,
   `DROP FUNCTION set_claim`
2. `supabase/migrations/20260802000002_role_grants_and_applications.sql` (plan 05-02) — three tables,
   two `SECURITY DEFINER` scope helpers, six SELECT policies, append-only trigger

Order matters: the policies in the second migration call `public.user_id()` as redefined by the
first.

Automated proof to run (plan 05-04), documented in `apps/web/docs/RLS-TESTING.md`:
```bash
cd apps/web && set -a && . ./.dev.vars && set +a && npx vitest run src/__tests__/rls
```
It needs four env vars — `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`,
`SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_JWT_SECRET` — and **skips silently with 0 tests** if any is
missing. A run reporting 0 tests is not a pass.

Project fact (from project memory, `taruu-knesset`): Supabase DDL in this project is applied via the
**Supabase Management API using a keychain-stored token**; there is no `supabase db push` script in
`package.json` and no running local Postgres (`docker info` fails on this machine, verified
2026-08-02). Apply these migrations the same way previous ones were applied.

Screens delivered by plans 05-07 and 05-08:
- `/he/settings/community-manager` — applicant submission form + status panel
- `/he/admin/manager-applications` — review console (unlisted route, server-gated)

Issue #79's visual-evidence requirement: "screenshots of the application form, admin review, and
manager activation/suspension screens with sanitized test records."
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Apply both migrations, set the Worker secret, run the RLS harness, bootstrap a super_admin</name>
  <read_first>
    - supabase/migrations/20260802000001_rls_transport.sql (the transport DDL being applied first)
    - supabase/migrations/20260802000002_role_grants_and_applications.sql (the tables, helpers and policies applied second)
    - apps/web/docs/RLS-TESTING.md (created by plan 05-04 — the exact command and what it does to the database)
    - .planning/phases/05-rbac-admin-review/05-VALIDATION.md (the per-requirement map this task fills in)
    - .planning/phases/05-rbac-admin-review/05-02-SUMMARY.md (the final column list and policy names, in case the executor adjusted anything)
  </read_first>
  <files>apps/web/docs/RBAC-VERIFICATION.md</files>
  <what-built>
Plans 05-01 through 05-08 produced: a working RLS transport (short-lived Supabase-signed tokens on
an anon-key client) with the dead `withUserContext`/`set_claim` plumbing deleted; a three-table
migration with real SELECT policies, two `SECURITY DEFINER` scope helpers and an append-only trigger
on `role_grant_events`; the `requireRole` authorization helper; the repo's first automated RLS
harness; the applicant API and screen; and the admin review API and console.

Nothing has touched a live database, `SUPABASE_JWT_SECRET` is not on the Worker, and no
`super_admin` grant exists anywhere — so the console currently 403s for every human, including you,
and the RLS harness currently skips.
  </what-built>
  <how-to-verify>
Run these six steps and paste the actual output into `apps/web/docs/RBAC-VERIFICATION.md`.

**1. Set `SUPABASE_JWT_SECRET`.**
Copy it from Supabase Dashboard → Project Settings → API → JWT Settings → JWT Secret. If that
section shows only asymmetric signing keys and no legacy HS256 secret, **STOP** — RLS-01 assumes
HS256 and the approach needs revisiting rather than working around.

```bash
# local
printf 'SUPABASE_JWT_SECRET=<secret>\n' >> apps/web/.dev.vars
# production Worker
cd apps/web && npx wrangler secret put SUPABASE_JWT_SECRET
```
Record only that it was set — never the value.

**2. Apply both migrations, in order.**
`20260802000001_rls_transport.sql` first, then `20260802000002_role_grants_and_applications.sql`,
through the Supabase Management API. Then confirm:
```sql
SELECT table_name FROM information_schema.tables
WHERE table_schema = 'public'
  AND table_name IN ('role_grants','community_manager_applications','role_grant_events')
ORDER BY table_name;

-- transport: set_claim is gone, user_id() no longer mentions app.current_user_id
SELECT proname FROM pg_proc
WHERE proname IN ('set_claim','user_id','is_platform_admin','can_admin_space');

SELECT prosrc LIKE '%app.current_user_id%' AS still_has_dead_fallback
FROM pg_proc WHERE proname = 'user_id';
```
Expected: exactly three tables; `set_claim` **absent**; `user_id`, `is_platform_admin` and
`can_admin_space` present; `still_has_dead_fallback = false`.

**3. Confirm RLS is on and the six policies exist.**
```sql
SELECT relname, relrowsecurity FROM pg_class
WHERE relname IN ('role_grants','community_manager_applications','role_grant_events');

SELECT tablename, policyname, cmd, roles FROM pg_policies
WHERE tablename IN ('role_grants','community_manager_applications','role_grant_events')
ORDER BY tablename, policyname;
```
Expected: `relrowsecurity = true` for all three; **six** policies, every one `cmd = SELECT` and
`roles = {authenticated}`. Any policy with a `cmd` other than SELECT, or any row with `{public}` in
roles, is a failure — stop and report.

**4. Run the RLS-04 harness against the live database.** This replaces the manual anon-key SQL check
the first planning pass specified.
```bash
cd apps/web && set -a && . ./.dev.vars && set +a && npx vitest run src/__tests__/rls
```
Expected: **8 passed, 0 skipped**. Paste the full vitest summary including the test count. If it
reports `0 tests` the env vars did not load — fix that before continuing; a 0-test run is not a
pass. If a test fails with Postgres code `42P17`, a policy is recursing and `can_admin_space` is not
being used — report it, do not work around it.

**5. Prove the audit log is append-only.** With the service-role connection:
```sql
INSERT INTO role_grant_events (subject_type, subject_id, event, reason)
VALUES ('role_grant', gen_random_uuid(), 'granted', 'append-only probe')
RETURNING id;

UPDATE role_grant_events SET reason = 'tampered' WHERE reason = 'append-only probe';
-- Expected: ERROR: role_grant_events is append-only (attempted UPDATE)

DELETE FROM role_grant_events WHERE reason = 'append-only probe';
-- Expected: ERROR: role_grant_events is append-only (attempted DELETE)
```
The probe row stays — that is the point of an append-only log. Note its id in the record.

**6. Bootstrap the first super_admin.** With the service-role connection, replacing the email:
```sql
INSERT INTO role_grants (user_id, role, space_id, status, source, granted_by)
SELECT id, 'super_admin', NULL, 'active', 'manual', id
FROM users WHERE email = 'sahar.h.barak@gmail.com'
RETURNING id, user_id, role, status;
```
Expected: one row. Then sign in on the site and open `/he/admin/manager-applications` — it must
render the queue rather than the access-denied panel.

Record all six results in a new `apps/web/docs/RBAC-VERIFICATION.md` with sections
`## 1 Worker secret set`, `## 2 Migrations applied`, `## 3 RLS enabled, six SELECT policies`,
`## 4 RLS harness`, `## 5 Append-only enforced`, `## 6 First super_admin`, each containing the
command run and its actual output (redact every key and secret).
  </how-to-verify>
  <acceptance_criteria>
    - `apps/web/docs/RBAC-VERIFICATION.md` exists and contains the headings `## 1 Worker secret set`, `## 2 Migrations applied`, `## 3 RLS enabled, six SELECT policies`, `## 4 RLS harness`, `## 5 Append-only enforced`, `## 6 First super_admin`
    - Section 2 records three tables present, `set_claim` absent, and `still_has_dead_fallback` false
    - Section 3 records `relrowsecurity` true for all three tables and exactly six `pg_policies` rows, every one `SELECT` / `{authenticated}`
    - Section 4 contains the pasted vitest summary with a non-zero passing count and zero skipped — the file contains the literal string `RLS harness` and a `N passed` count
    - Section 5 records the literal error text `role_grant_events is append-only` for both UPDATE and DELETE
    - Section 6 records exactly one inserted grant with `role = super_admin` and `status = active`
    - The file contains no API keys, tokens, or service-role secrets (`grep -cE "eyJ|service_role|SUPABASE_SERVICE|SUPABASE_JWT_SECRET=" apps/web/docs/RBAC-VERIFICATION.md` returns 0)
  </acceptance_criteria>
  <action>
Human-executed. Set `SUPABASE_JWT_SECRET` locally and on the Worker; apply
`supabase/migrations/20260802000001_rls_transport.sql` then
`supabase/migrations/20260802000002_role_grants_and_applications.sql` via the Supabase Management
API; run the six verification steps in `<how-to-verify>` above; and record every command with its
actual (key-redacted) output in a new `apps/web/docs/RBAC-VERIFICATION.md` using the six headings
named in `<acceptance_criteria>`.

Step 4 (the RLS harness) is the automated replacement for the manual anon-key check — do not
substitute a hand-run `SELECT` for it. Step 6's bootstrap INSERT is required: without it no human
can open the review console, because there is no `super_admin` grant anywhere in the system.
  </action>
  <verify>
    <automated>test -f apps/web/docs/RBAC-VERIFICATION.md && for h in "## 1 Worker secret set" "## 2 Migrations applied" "## 3 RLS enabled, six SELECT policies" "## 4 RLS harness" "## 5 Append-only enforced" "## 6 First super_admin"; do grep -qF "$h" apps/web/docs/RBAC-VERIFICATION.md || { echo "MISSING: $h"; exit 1; }; done; grep -qF "role_grant_events is append-only" apps/web/docs/RBAC-VERIFICATION.md || { echo "MISSING append-only proof"; exit 1; }; grep -qE "[1-9][0-9]* passed" apps/web/docs/RBAC-VERIFICATION.md || { echo "MISSING passing harness count"; exit 1; }; ! grep -qE "eyJ|service_role|SUPABASE_SERVICE|SUPABASE_JWT_SECRET=" apps/web/docs/RBAC-VERIFICATION.md || { echo "SECRET LEAKED"; exit 1; }; echo OK</automated>
  </verify>
  <done>All six sections are recorded with real output: both migrations applied in order, `set_claim` gone, six SELECT-only policies present, the RLS harness green with a non-zero test count, both append-only errors reproduced, one super_admin grant created, and no secret in the file.</done>
  <resume-signal>Type "approved" once all six sections are recorded, or paste the failing output.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual evidence and end-to-end review walkthrough</name>
  <read_first>
    - .planning/phases/05-rbac-admin-review/05-07-SUMMARY.md (the applicant route and its states)
    - .planning/phases/05-rbac-admin-review/05-08-SUMMARY.md (the console route and its states)
    - .planning/phases/05-rbac-admin-review/05-CONTEXT.md (the Specifics section: issue #79's visual-evidence requirement)
    - .planning/phases/05-rbac-admin-review/05-VALIDATION.md (the sign-off checklist to tick)
  </read_first>
  <files>apps/web/docs/RBAC-VERIFICATION.md, .planning/phases/05-rbac-admin-review/05-VALIDATION.md</files>
  <what-built>
The full role/approval lifecycle: submit → review → approve/reject → suspend, with an audit row per
transition and one authorization helper enforcing every privileged route — on top of an RLS
transport that now actually works.
  </what-built>
  <how-to-verify>
Run the dev server (`pnpm dev --filter @sync/web`; do **not** run `next build` concurrently — it
clobbers `.next`). With two accounts: your super_admin from Task 1, and a second ordinary account.

1. **Applicant form.** As the ordinary account, open `/he/settings/community-manager`. Confirm the
   page is Hebrew and RTL, that "שליחת בקשה" stays disabled until a space is picked and the
   motivation reaches 40 characters, and submit. **Screenshot: the filled form.**
2. **Status panel.** The page should now show the status panel with `ממתין לאישור` and the line
   `אישור הבקשה הוא תנאי מוקדם בלבד…`. **Screenshot.**
3. **Duplicate submit.** Reload and try to submit again (or POST twice). Expect the status panel,
   and if the API is hit directly, a 409.
4. **Access denial is server-driven.** Still as the ordinary account, open
   `/he/admin/manager-applications`. Expect `אין לכם הרשאת בדיקה.` **Screenshot.** Confirm in the
   network tab that the API returned **403** — the page did not decide locally.
5. **Review console.** As the super_admin, open `/he/admin/manager-applications`. Expect the
   application in the queue with the applicant's name, space, motivation and any evidence link,
   and `היקף: כל הרשויות`. Confirm both action buttons are disabled until a ≥10-character reason
   is typed. **Screenshot: the queue with the reason field filled.**
6. **Approve.** Approve with a reason. Expect the card to disappear and the message
   `הבקשה אושרה. ההרשאה נוצרה — היא עדיין כפופה לכל התנאים.` **Screenshot.**
7. **Audit trail.** With the service-role connection:
   ```sql
   SELECT subject_type, event, actor_user_id, subject_user_id, space_id, reason, created_at
   FROM role_grant_events ORDER BY created_at DESC LIMIT 10;
   ```
   Expect, newest first: `role_grant/granted`, `community_manager_application/approved`,
   `community_manager_application/submitted` — each with a non-null `actor_user_id`, the reason
   you typed on the two decision rows, and a timestamp. **Paste this output.**
8. **Suspend.** Take the `role_grants.id` created by the approval and:
   ```bash
   curl -X POST "http://localhost:3000/api/admin/role-grants/<GRANT_ID>" \
     -H "Content-Type: application/json" -b "sync-session=<SUPER_ADMIN_COOKIE>" \
     -d '{"action":"suspend","reason":"בדיקת השעיה ידנית"}'
   ```
   Expect 200 with `grant.status === 'suspended'`, and a new `role_grant/suspended` audit row.
   Repeat the same call — expect **409**. **Paste both responses.**
9. **Cross-space denial.** Grant yourself (or a test account) `space_admin` for `'חיפה'` only,
   then attempt to decide an application whose space is `'תל אביב-יפו'`. Expect **403**.

Append the screenshots and pasted output to `apps/web/docs/RBAC-VERIFICATION.md` under a
`## 7 End-to-end walkthrough` section. Sanitize: no real resident names, no real emails, no
cookies or keys in any screenshot.

Finally, in `.planning/phases/05-rbac-admin-review/05-VALIDATION.md`:
- tick every box in the **Validation Sign-Off** section
- change every Status cell in the Per-Task Verification Map from `⬜ pending` to `✅ green`
- set the frontmatter to `nyquist_compliant: true`, `wave_0_complete: true`, `status: complete`
- change `**Approval:** pending` to `**Approval:** approved <date>`
  </how-to-verify>
  <acceptance_criteria>
    - `apps/web/docs/RBAC-VERIFICATION.md` contains a `## 7 End-to-end walkthrough` section
    - Section 7 contains the pasted `role_grant_events` query output showing at least the three events `submitted`, `approved`, `granted`
    - Section 7 records the suspend call returning 200 and the repeated suspend returning 409
    - Section 7 records the cross-space decision attempt returning 403
    - Section 7 references at least five screenshot files (applicant form, status panel, access-denied, review queue, post-approval)
    - `.planning/phases/05-rbac-admin-review/05-VALIDATION.md` frontmatter contains `nyquist_compliant: true` and `wave_0_complete: true`
    - `.planning/phases/05-rbac-admin-review/05-VALIDATION.md` no longer contains the string `**Approval:** pending`
    - `.planning/phases/05-rbac-admin-review/05-VALIDATION.md` contains no remaining `⬜ pending` in the Per-Task Verification Map
    - `pnpm --filter @sync/web test` exits 0 at the time of sign-off
  </acceptance_criteria>
  <action>
Human-executed. Walk the nine steps in `<how-to-verify>` above against a running dev server using
two accounts (the super_admin bootstrapped in Task 1 and one ordinary account), capture the five
named screenshots plus the pasted SQL and curl output into a `## 7 End-to-end walkthrough` section
of `apps/web/docs/RBAC-VERIFICATION.md`, sanitizing all names, emails, cookies and keys.

Then edit `.planning/phases/05-rbac-admin-review/05-VALIDATION.md` with these exact changes:
set frontmatter `status: complete`, `nyquist_compliant: true`, `wave_0_complete: true`; change every
Status cell in the Per-Task Verification Map from `⬜ pending` to `✅ green`; tick every `- [ ]` in
the Validation Sign-Off section to `- [x]`; and replace `**Approval:** pending` with
`**Approval:** approved 2026-08-02`.
  </action>
  <verify>
    <automated>pnpm --filter @sync/web test && grep -qF "## 7 End-to-end walkthrough" apps/web/docs/RBAC-VERIFICATION.md && V=.planning/phases/05-rbac-admin-review/05-VALIDATION.md; grep -qF "nyquist_compliant: true" "$V" && grep -qF "wave_0_complete: true" "$V" && ! grep -qF "**Approval:** pending" "$V" && ! grep -qF "⬜ pending" "$V" && echo OK</automated>
  </verify>
  <done>The walkthrough section records the submit→approve→suspend lifecycle with its audit trail, the 409 replay, the 403 cross-space denial and five screenshots; 05-VALIDATION.md is signed off with no pending rows.</done>
  <resume-signal>Type "approved" once every step passed and 05-VALIDATION.md is signed off, or describe the failing step.</resume-signal>
</task>

</tasks>

<verification>
1. `pnpm --filter @sync/web test` exits 0 (the full credential-free suite: minter, user client, policy, require-role, applicant routes, admin routes, profile mapper; the RLS suite skips here by design).
2. `cd apps/web && set -a && . ./.dev.vars && set +a && npx vitest run src/__tests__/rls` reports a non-zero passing count against the live database.
3. `pnpm --filter @sync/web typecheck` and `pnpm --filter @sync/web lint` exit 0.
4. `apps/web/docs/RBAC-VERIFICATION.md` has all seven sections filled with real output.
5. `.planning/phases/05-rbac-admin-review/05-VALIDATION.md` is signed off.
</verification>

<success_criteria>
- Both migrations are live, in order; `set_claim` is gone and `public.user_id()` reads only the JWT.
- Six SELECT-only policies exist on the three tables, and the automated harness proves cross-user reads and anon reads are denied — no hand-run SQL is the evidence for this any more.
- The append-only trigger is proven to reject both UPDATE and DELETE with real error output.
- `SUPABASE_JWT_SECRET` is on the Worker, so the transport is not a local-only artefact.
- One `super_admin` exists and can open the console; an ordinary account cannot, and the denial came from the server.
- The submit → approve → suspend lifecycle is walked end to end with the audit trail pasted as evidence.
- Every ROADMAP Phase 5 success criterion has either an automated test (1, 2, 4, 6, 7, 8) or recorded manual evidence (3, 5).
</success_criteria>

<output>
After completion, create `.planning/phases/05-rbac-admin-review/05-09-SUMMARY.md`.
Record: the applied migration timestamps, the RLS harness test count and runtime against the live
database, the append-only error text, the bootstrapped super_admin's user id (email redacted), and
any deviation found during the walkthrough. Note explicitly what remains for Phase 7: 25 tables and
112 `db.ts` exports still read through the service-role client and are therefore still unprotected
by the policies this phase proved.
</output>
