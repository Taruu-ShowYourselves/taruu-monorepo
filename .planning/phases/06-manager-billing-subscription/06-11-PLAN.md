---
phase: 06-manager-billing-subscription
plan: 11
type: execute
wave: 7
depends_on: ["06-02", "06-05", "06-06", "06-07", "06-08", "06-09", "06-10"]
files_modified:
  - apps/web/docs/MANAGER-BILLING-VERIFICATION.md
  - .planning/phases/06-manager-billing-subscription/06-VALIDATION.md
autonomous: false
requirements: [MGR-01, MGR-02, MGR-03, MGR-04, MGR-05]

user_setup:
  - service: supabase
    why: "The four billing tables exist only as a migration file; nothing in this phase has touched a database"
    dashboard_config:
      - task: "Apply supabase/migrations/20260803000001_manager_billing.sql via the Supabase Management API — the path used for the knesset, identity-document and Phase 5 migrations. There is no `supabase db push` script in this repo and no local Postgres."
        location: "Supabase Management API / Supabase Studio SQL editor"
  - service: green_invoice
    why: "The subscription rail needs its own webhook secret, and the renewal job needs a cron secret, both in the Worker secret store"
    env_vars:
      - name: GREENINVOICE_BILLING_WEBHOOK_SECRET
        source: "Generate a fresh random value. Register it on the notifyUrl of /api/manager-billing/webhook in the Green Invoice dashboard, then `cd apps/web && npx wrangler secret put GREENINVOICE_BILLING_WEBHOOK_SECRET`. It is DISTINCT from GREENINVOICE_WEBHOOK_SECRET — do not reuse the vote rail's."
      - name: CRON_SECRET
        source: "Already required by the existing cron routes. Confirm it is set on the Worker; the renewal job 401s without it."
    dashboard_config:
      - task: "Register the manager-billing notifyUrl and confirm the hosted checkout page renders in Hebrew with the ₪50 amount."
        location: "Green Invoice (morning) dashboard"

must_haves:
  truths:
    - "The four billing tables exist in the live database with SELECT-only RLS and a working append-only trigger"
    - "A full sandbox lifecycle has been walked by a human: checkout, activation, access granted, renewal, failure, grace, expiry, access lost"
    - "An approved manager with no billing was observed to be denied, and the denial came from the server"
    - "A ₪50 charge from someone never approved was observed to be refused before any money moved"
    - "A duplicated webhook delivery was observed to produce one charge row and one transition"
    - "The renewal cron actually fires on Cloudflare, or the reason it does not is written down"
    - "The reconciliation report was run and its openMismatches count recorded — including whether the provider side was verifiable"
  artifacts:
    - path: "apps/web/docs/MANAGER-BILLING-VERIFICATION.md"
      provides: "The recorded outcome of every check this phase cannot run in CI"
      contains: "openMismatches"
  key_links:
    - from: "apps/web/docs/MANAGER-BILLING-VERIFICATION.md"
      to: ".planning/phases/06-manager-billing-subscription/06-VALIDATION.md"
      via: "every MGR row is marked green here"
      pattern: "MGR-05"
---

<objective>
Land Phase 6 on a real database and a real provider, and record the evidence: apply the migration,
set the two secrets, walk the whole subscription lifecycle in the Green Invoice sandbox, prove the
two independent prerequisites hold, confirm the cron actually fires, run the reconciliation, and
capture the screenshots issue #79 asks for.

Purpose: everything before this plan is code and files. No table exists, no secret is set, no charge
has been made through the new rail, and — because `apps/web/wrangler.jsonc:58` records an
account-level cron gate — nobody has confirmed that the renewal job runs at all in production. Each
of those is the difference between a subscription system and a subscription system's source code.

Output: an applied migration, two Worker secrets, a filled verification record, and
`06-VALIDATION.md` signed off.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-manager-billing-subscription/06-CONTEXT.md
@.planning/phases/06-manager-billing-subscription/06-VALIDATION.md
@.planning/phases/06-manager-billing-subscription/06-01-SUMMARY.md
@.planning/phases/06-manager-billing-subscription/06-02-SUMMARY.md
@.planning/phases/06-manager-billing-subscription/06-06-SUMMARY.md
@.planning/phases/06-manager-billing-subscription/06-07-SUMMARY.md
@.planning/phases/06-manager-billing-subscription/06-08-SUMMARY.md
@.planning/phases/06-manager-billing-subscription/06-09-SUMMARY.md
@.planning/phases/06-manager-billing-subscription/06-10-SUMMARY.md

<interfaces>
Migration to apply: `supabase/migrations/20260803000001_manager_billing.sql` (plan 06-02) — four
tables, one `SECURITY DEFINER` read helper, six `SELECT` policies, an append-only trigger reusing
`public.reject_audit_mutation()`. It depends on Phase 5's `20260802000002` migration already being
applied, because `manager_subscriptions.role_grant_id` references `role_grants(id)`.

Routes to exercise:
```
GET  /api/manager-billing?spaceId=<code>
POST /api/manager-billing/checkout
POST /api/manager-billing/webhook            (provider callback, secret-guarded)
POST /api/manager-billing/cancel
POST /api/admin/manager-subscriptions/[id]   (suspend | reinstate)
POST /api/cron/manager-renewals              (CRON_SECRET bearer)
POST /api/cron/manager-billing-reconcile     (CRON_SECRET bearer)
```

Screen: `/he/settings/community-manager` — Phase 5's applicant form and status panel, plus plan
06-09's billing panel beneath it.

Project fact (project memory, `taruu-knesset`): Supabase DDL in this project is applied via the
**Supabase Management API with a keychain-stored token**. There is no `supabase db push` script and
no running local Postgres.

Project fact (project memory, `taruu-dev-verify`): never run `next build` while `next dev` is
running — it clobbers `.next`.

The cron gate: `apps/web/wrangler.jsonc:58` records that the schedules API rejected this account's
cron list. Only `0 */6 * * *` is live, and plans 06-07 and 06-10 hung both billing jobs off it via
`worker.ts`'s `CRON_ROUTES`. Whether that dispatch actually reaches them in production has never
been observed — step 7 below is the first time.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Apply the migration, set the secrets, and walk the sandbox lifecycle</name>
  <read_first>
    - supabase/migrations/20260803000001_manager_billing.sql (the DDL being applied)
    - .planning/phases/06-manager-billing-subscription/06-02-SUMMARY.md (the as-built column lists, index names and policy names — verify against what actually lands)
    - .planning/phases/06-manager-billing-subscription/06-06-SUMMARY.md (the webhook notifyUrl and secret transport to register with Green Invoice)
    - .planning/phases/06-manager-billing-subscription/06-07-SUMMARY.md (the CRON_ROUTES entry and the batch limit)
    - apps/web/docs/RBAC-VERIFICATION.md (Phase 5's equivalent record — mirror its structure and its redaction discipline)
  </read_first>
  <files>apps/web/docs/MANAGER-BILLING-VERIFICATION.md</files>
  <what-built>
Plans 06-01 through 06-10 produced: a four-table migration with `SELECT`-only RLS and an append-only
audit trigger; a pure state machine, retry ladder and grace policy; a deterministic idempotency key
and a claim-before-charge repository; a Green Invoice adapter for card setup, off-session charging
and webhook verification; the billing requirement wired into Phase 5's `requireRole`; an
approval-gated checkout and an idempotent activation webhook; a due-check renewal job on the one
live Cloudflare trigger; end-of-period cancellation and admin suspension; a Hebrew/RTL billing
panel; and a two-directional reconciliation check.

None of it has touched a database. `GREENINVOICE_BILLING_WEBHOOK_SECRET` does not exist. No charge
has been made through this rail. And nobody has confirmed that the `worker.ts` dispatch reaches the
renewal route in production, because the account-level cron gate at `wrangler.jsonc:58` means the
whole scheduling arrangement is a workaround that has never been observed working.
  </what-built>
  <how-to-verify>
Run these seven steps and paste the actual, secret-redacted output into
`apps/web/docs/MANAGER-BILLING-VERIFICATION.md`.

**1. Set the secrets.**
```bash
# local
printf 'GREENINVOICE_BILLING_WEBHOOK_SECRET=<fresh random>\n' >> apps/web/.dev.vars
# Worker
cd apps/web && npx wrangler secret put GREENINVOICE_BILLING_WEBHOOK_SECRET
npx wrangler secret list          # confirm CRON_SECRET is already present
```
Register the same value on the `notifyUrl` for `/api/manager-billing/webhook` in the Green Invoice
dashboard. **Do not reuse `GREENINVOICE_WEBHOOK_SECRET`** — one secret per surface is the whole
point. Record only that each was set, never a value.

**2. Apply the migration.** `supabase/migrations/20260803000001_manager_billing.sql` via the
Management API. Phase 5's `20260802000002` must already be applied or the `role_grants` foreign key
will fail. Then confirm:
```sql
SELECT table_name FROM information_schema.tables
WHERE table_schema = 'public' AND table_name IN
  ('billing_payment_methods','manager_subscriptions','manager_subscription_charges','manager_subscription_events')
ORDER BY table_name;

SELECT relname, relrowsecurity FROM pg_class WHERE relname LIKE 'manager_%' OR relname = 'billing_payment_methods';

SELECT tablename, policyname, cmd, roles FROM pg_policies
WHERE tablename IN ('billing_payment_methods','manager_subscriptions','manager_subscription_charges','manager_subscription_events')
ORDER BY tablename, policyname;

SELECT indexname FROM pg_indexes WHERE tablename = 'manager_subscription_charges';
```
Expected: four tables; `relrowsecurity = true` for all four; **six** policies, every one
`cmd = SELECT` and `roles = {authenticated}`; and both
`uq_manager_subscription_charges_settled_period` and the `idempotency_key` unique present. Any
policy with a `cmd` other than SELECT, or `{public}` in roles, is a failure — stop and report.

**3. Prove the audit log is append-only.** With the service-role connection:
```sql
INSERT INTO manager_subscription_events (subscription_id, to_status, event, reason)
VALUES (gen_random_uuid(), 'pending', 'checkout_started', 'append-only probe') RETURNING id;

UPDATE manager_subscription_events SET reason = 'tampered' WHERE reason = 'append-only probe';
-- Expected: ERROR: role_grant_events is append-only (attempted UPDATE)
--   ^ the message names role_grant_events because the trigger REUSES Phase 5's
--     reject_audit_mutation(). That is intentional. Record the literal text you see.

DELETE FROM manager_subscription_events WHERE reason = 'append-only probe';
-- Expected: the same error for DELETE
```

**4. Prove the two prerequisites are independent — the heart of issue #79.**
Using an ordinary signed-in account that has **never** applied:
```bash
curl -i -X POST http://localhost:3000/api/manager-billing/checkout \
  -H 'Content-Type: application/json' -b "sync-session=<ORDINARY_COOKIE>" \
  -d '{"spaceId":"תל אביב-יפו"}'
```
Expect **403**. Paste it. This is acceptance criterion 1 observed rather than asserted: a ₪50
payment from someone never approved cannot even start.

Then approve that account through Phase 5's review console, and **before** paying, confirm the
billing panel at `/he/settings/community-manager` shows no access and
`GET /api/manager-billing?spaceId=…` returns `accessActive: false`. That is acceptance criterion 2.

**5. Walk the sandbox lifecycle.** With `GREENINVOICE_ENV` on sandbox and sandbox credentials:
- `POST /api/manager-billing/checkout` ⇒ 201 with a `checkoutUrl`. Open it, pay ₪50 with a sandbox
  card.
- Confirm the webhook landed: `manager_subscriptions.status = 'active'`, one
  `manager_subscription_charges` row with `status = 'succeeded'` and a non-null
  `provider_document_id`, one `billing_payment_methods` row whose `provider_token_id` is set, and
  `manager_subscription_events` carrying `checkout_started` then `activated`.
- **Replay the webhook**: re-POST the identical body to `/api/manager-billing/webhook` with the
  secret. Expect 200 `{ idempotent: true }` and **still exactly one** charge row. Paste both the
  response and a `SELECT count(*) FROM manager_subscription_charges WHERE subscription_id = …`.
- Confirm `GET /api/manager-billing` now returns `accessActive: true`.

**6. Force the failure ladder.** Fastest honest route: `UPDATE manager_subscriptions SET
next_charge_at = now() - interval '1 minute'` and detach or invalidate the payment method, then
drive `POST /api/cron/manager-renewals` with the `CRON_SECRET` bearer repeatedly, moving
`next_charge_at` back each time. Record after each run: `status`, `failed_attempts`,
`next_charge_at`, `grace_until`, and whether `GET /api/manager-billing` still reports
`accessActive: true`.

Expected walk: `active → past_due` (access **retained**) → `past_due` ×3 → `grace` (access
**retained**, `grace_until` 14 days out) → after moving `grace_until` into the past → `expired`
(access **lost**). Confirm at `expired` that the `role_grants` row is **still active** — billing
expiry does not revoke approval, and `requireRole` denies on `billing_inactive` instead. Paste the
grant row.

**7. Confirm the cron actually fires in production.** This is the step the account-level gate makes
necessary. Deploy, then either wait for a `0 */6 * * *` tick or use
`npx wrangler tail taruu-web --format pretty` and look for the scheduled invocation reaching
`/api/cron/manager-renewals` **and** `/api/cron/manager-billing-reconcile` alongside
`/api/cron/knesset-agenda`.

If the dispatch does **not** reach the new routes, record exactly that and stop — do not paper over
it. The fallback is plan 06-01's option-b (resolve the Cloudflare account gate) or option-c (an
external scheduler), and choosing one is a decision, not a patch.

Record all seven in `apps/web/docs/MANAGER-BILLING-VERIFICATION.md` under the headings
`## 1 Secrets set`, `## 2 Migration applied`, `## 3 Append-only enforced`,
`## 4 Approval and billing are independent`, `## 5 Sandbox lifecycle`, `## 6 Failure ladder`,
`## 7 Cron dispatch observed` — each with the command run and its actual output. Redact every key,
token, cookie, card number and document id.
  </how-to-verify>
  <acceptance_criteria>
    - `apps/web/docs/MANAGER-BILLING-VERIFICATION.md` exists with all seven headings
    - Section 2 records four tables, `relrowsecurity` true for all four, exactly six `SELECT` / `{authenticated}` policies, and both unique indexes on the charges table
    - Section 3 records the literal append-only error text for both UPDATE and DELETE
    - Section 4 records the 403 from an unapproved checkout **and** `accessActive: false` for an approved-but-unpaid account
    - Section 5 records one charge row surviving a replayed webhook, with the `count(*)` output pasted
    - Section 6 records the full ladder with `status`, `failed_attempts` and `accessActive` at each step, and the `role_grants` row still active at `expired`
    - Section 7 records whether the scheduled dispatch reached both new routes, with the tail output or an explicit statement that it did not
    - No secret, cookie, token or card number is in the file (`grep -cE "eyJ|_SECRET=|sync-session=|[0-9]{13,16}" ` returns 0)
  </acceptance_criteria>
  <action>
Human-executed. Run the seven steps in `<how-to-verify>` and record each with its real,
secret-redacted output in a new `apps/web/docs/MANAGER-BILLING-VERIFICATION.md` using the seven
headings named in `<acceptance_criteria>`.

Step 4 and step 6 are the two that cannot be substituted with a code reading: they are the only
direct observations that approval and billing are genuinely independent, and that losing billing
does not destroy the approval record.

If step 7 shows the cron dispatch not reaching the new routes, stop and report — that is a real
finding about the Cloudflare account gate, and the remedy is a decision from plan 06-01's options,
not a workaround invented here.
  </action>
  <verify>
    <automated>V=apps/web/docs/MANAGER-BILLING-VERIFICATION.md; test -f "$V" || { echo "missing $V"; exit 1; }; for h in "## 1 Secrets set" "## 2 Migration applied" "## 3 Append-only enforced" "## 4 Approval and billing are independent" "## 5 Sandbox lifecycle" "## 6 Failure ladder" "## 7 Cron dispatch observed"; do grep -qF "$h" "$V" || { echo "MISSING: $h"; exit 1; }; done; grep -qF "append-only" "$V" || { echo "MISSING append-only proof"; exit 1; }; grep -qE "\b403\b" "$V" || { echo "MISSING the unapproved-checkout 403"; exit 1; }; grep -qF "accessActive" "$V" || { echo "MISSING access observations"; exit 1; }; ! grep -qE "eyJ|_SECRET=|sync-session=|[0-9]{13,16}" "$V" || { echo "SECRET LEAKED"; exit 1; }; echo OK</automated>
  </verify>
  <done>All seven sections carry real output: the migration is live with SELECT-only RLS and a working append-only trigger, an unapproved checkout was refused, an approved-unpaid account had no access, a replayed webhook produced one charge, the failure ladder walked to expiry without revoking the grant, and the cron dispatch was either observed or its absence recorded.</done>
  <resume-signal>Type "approved" once all seven sections are recorded, or paste the failing step.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual evidence, reconciliation run, and validation sign-off</name>
  <read_first>
    - .planning/phases/06-manager-billing-subscription/06-09-SUMMARY.md (the panel's states and their exact Hebrew copy — you are screenshotting these)
    - .planning/phases/06-manager-billing-subscription/06-10-SUMMARY.md (whether the provider document listing worked, or whether the degraded `verifiable: false` mode is live — it determines what the zero means)
    - .planning/phases/06-manager-billing-subscription/06-08-SUMMARY.md (the two-lever suspension model — both levers get tested separately here)
    - .planning/phases/06-manager-billing-subscription/06-VALIDATION.md (the sign-off checklist to complete)
    - apps/web/docs/MANAGER-BILLING-VERIFICATION.md (written in Task 1 — this appends to it)
  </read_first>
  <files>apps/web/docs/MANAGER-BILLING-VERIFICATION.md, .planning/phases/06-manager-billing-subscription/06-VALIDATION.md</files>
  <what-built>
The complete manager-billing lifecycle, now live on a real database with a real sandbox provider:
approval-gated checkout, idempotent activation, a due-check renewal engine with a documented retry
and grace ladder, end-of-period cancellation, admin suspension on two independent levers, a
Hebrew/RTL billing panel, and a two-directional reconciliation check.
  </what-built>
  <how-to-verify>
Run the dev server (`pnpm dev --filter @sync/web`; do **not** run `next build` concurrently — it
clobbers `.next`). Two accounts: the `super_admin` bootstrapped in Phase 5, and one approved
community manager.

1. **Panel — no subscription.** As the approved manager, open `/he/settings/community-manager`.
   Confirm Hebrew and RTL, that Phase 5's application status panel is still there, that the new
   two-prerequisite sentence appears, and that the panel offers to subscribe.
   **Screenshot: the unsubscribed panel.**
2. **Checkout.** Click through to the Green Invoice hosted page. Confirm it renders in Hebrew and
   shows ₪50. **Screenshot: the hosted checkout page** (sanitize; use a sandbox card only).
3. **Panel — active.** After paying, reload. Confirm the access badge reads granted, the next-charge
   date is shown, and the charge history lists one row with a receipt link.
   **Screenshot: the active panel.**
4. **Panel — grace.** Move the subscription to `grace` in the database and reload. Confirm the copy
   names the date access ends and still says access continues. **Screenshot.**
5. **Cancel.** Restore `active`, click cancel, and confirm the confirmation text names the date
   access runs until **and** says there is no partial refund **before** you confirm. Confirm, then
   verify `cancel_at_period_end = true` with the status still `active`.
   **Screenshot: the confirmation dialog.**
6. **Two levers, tested separately.**
   ```bash
   # (a) billing lever
   curl -X POST "http://localhost:3000/api/admin/manager-subscriptions/<SUB_ID>" \
     -H 'Content-Type: application/json' -b "sync-session=<SUPER_ADMIN_COOKIE>" \
     -d '{"action":"suspend","reason":"בדיקת השעיית חיוב"}'
   # expect 200, status suspended, next_charge_at null; then GET /api/manager-billing → accessActive:false
   # repeat the same call → expect 409

   # (b) role lever (Phase 5, independent)
   curl -X POST "http://localhost:3000/api/admin/role-grants/<GRANT_ID>" \
     -H 'Content-Type: application/json' -b "sync-session=<SUPER_ADMIN_COOKIE>" \
     -d '{"action":"suspend","reason":"בדיקת השעיית תפקיד"}'
   # expect 200; requireRole now denies with grant_suspended rather than billing_inactive
   ```
   Paste all four responses. **Screenshot: the suspended panel** (it must offer no payment CTA —
   paying does not lift a suspension).
7. **Audit trail.** With the service-role connection:
   ```sql
   SELECT event, from_status, to_status, actor_user_id, reason, created_at
   FROM manager_subscription_events ORDER BY created_at DESC LIMIT 15;
   ```
   Expect, newest first, the events you triggered — `suspended`, `cancellation_requested`,
   `activated`, `checkout_started` — each with a timestamp, an `actor_user_id` that is null only for
   system transitions, and a reason on every admin action. **Paste the output.**
8. **Notifications.** Confirm an email was attempted for each access-affecting transition
   (activation, grace, cancellation request, suspension) and **not** for a routine successful
   renewal. Resend dashboard or the application log is fine. Paste the list of subjects.
9. **Reconciliation.**
   ```bash
   curl -X POST http://localhost:3000/api/cron/manager-billing-reconcile \
     -H "authorization: Bearer $CRON_SECRET"
   ```
   Paste the report. Record `openMismatches` **and** whether `notes` says the provider direction was
   unverified — a zero from a one-directional comparison is a different claim from a zero from a
   two-directional one, and the record must not blur them.

Append everything to `apps/web/docs/MANAGER-BILLING-VERIFICATION.md` under
`## 8 End-to-end walkthrough` and `## 9 Reconciliation`. Sanitize: no real resident names, no real
emails, no cookies, no keys, no card numbers in any screenshot.

Finally, in `.planning/phases/06-manager-billing-subscription/06-VALIDATION.md`:
- tick every box in **Validation Sign-Off**
- change every Status cell in the Per-Task Verification Map from `⬜ pending` to `✅ green`
- set the frontmatter to `nyquist_compliant: true`, `wave_0_complete: true`, `status: complete`
- replace `**Approval:** pending` with `**Approval:** approved <date>`
  </how-to-verify>
  <acceptance_criteria>
    - `apps/web/docs/MANAGER-BILLING-VERIFICATION.md` contains `## 8 End-to-end walkthrough` and `## 9 Reconciliation`
    - Section 8 references at least five screenshot files: unsubscribed panel, hosted checkout, active panel, cancel confirmation, suspended panel
    - Section 8 records the suspend call returning 200 and the repeated suspend returning 409
    - Section 8 records both levers exercised separately, with the two distinct deny reasons named (`billing_inactive` and `grant_suspended`)
    - Section 8 contains the pasted `manager_subscription_events` output showing at least `checkout_started`, `activated`, `cancellation_requested` and `suspended`, each with a timestamp
    - Section 8 records that no email was sent for a routine successful renewal
    - Section 9 records `openMismatches` and states explicitly whether the provider direction was verifiable
    - `06-VALIDATION.md` frontmatter contains `nyquist_compliant: true` and `wave_0_complete: true`
    - `06-VALIDATION.md` contains neither `**Approval:** pending` nor any `⬜ pending`
    - `pnpm --filter @sync/web test` and `pnpm --filter @sync/web typecheck` exit 0 at the time of sign-off
    - No secret, cookie or card number anywhere in the file
  </acceptance_criteria>
  <action>
Human-executed. Walk the nine steps in `<how-to-verify>` against a running dev server using the
Phase 5 super admin and one approved community manager; capture the five named screenshots plus the
pasted SQL, curl and reconciliation output into `## 8 End-to-end walkthrough` and
`## 9 Reconciliation` of `apps/web/docs/MANAGER-BILLING-VERIFICATION.md`, sanitizing all names,
emails, cookies, keys and card numbers.

Then edit `.planning/phases/06-manager-billing-subscription/06-VALIDATION.md`: frontmatter
`status: complete`, `nyquist_compliant: true`, `wave_0_complete: true`; every Status cell
`⬜ pending` → `✅ green`; every `- [ ]` in Validation Sign-Off → `- [x]`; and
`**Approval:** pending` → `**Approval:** approved 2026-08-03`.

Step 6 is the one that must not be collapsed into a single call. The two levers denying for two
different reasons is what "suspend access independently of billing" means, and observing only one of
them proves half the requirement.
  </action>
  <verify>
    <automated>V=apps/web/docs/MANAGER-BILLING-VERIFICATION.md; grep -qF "## 8 End-to-end walkthrough" "$V" &amp;&amp; grep -qF "## 9 Reconciliation" "$V" &amp;&amp; grep -qF "billing_inactive" "$V" &amp;&amp; grep -qF "grant_suspended" "$V" &amp;&amp; grep -qF "openMismatches" "$V" &amp;&amp; ! grep -qE "eyJ|_SECRET=|sync-session=|[0-9]{13,16}" "$V" &amp;&amp; P=.planning/phases/06-manager-billing-subscription/06-VALIDATION.md &amp;&amp; grep -qF "nyquist_compliant: true" "$P" &amp;&amp; grep -qF "wave_0_complete: true" "$P" &amp;&amp; ! grep -qF "**Approval:** pending" "$P" &amp;&amp; ! grep -qF "⬜ pending" "$P" &amp;&amp; pnpm --filter @sync/web typecheck &amp;&amp; pnpm --filter @sync/web test</automated>
  </verify>
  <done>The walkthrough records the full lifecycle with its audit trail, both suspension levers denying for their own distinct reasons, the notification set including the renewal that correctly sent nothing, and a reconciliation report whose zero is qualified by whether the provider side was verifiable; 06-VALIDATION.md is signed off with no pending rows.</done>
  <resume-signal>Type "approved" once every step passed and 06-VALIDATION.md is signed off, or describe the failing step.</resume-signal>
</task>

</tasks>

<verification>
1. `pnpm --filter @sync/web test` exits 0 — the full credential-free suite: the state machine, the schedule, the notices, the idempotency key, the provider adapter, the authorization gate, the four route suites, the reconciliation diff and the panel model.
2. `pnpm --filter @sync/web typecheck` and `pnpm --filter @sync/web lint` exit 0.
3. `apps/web/docs/MANAGER-BILLING-VERIFICATION.md` has all nine sections filled with real output.
4. `apps/web/docs/SPIKE-RESULT.md` still contains zero `(pending live run)` rows — the gate plan 06-01 closed has not reopened.
5. `.planning/phases/06-manager-billing-subscription/06-VALIDATION.md` is signed off.
</verification>

<success_criteria>
- The four billing tables are live with `SELECT`-only RLS, and the append-only trigger is proven to
  reject both UPDATE and DELETE with real error output.
- Both prerequisites were observed independently: an unapproved checkout was refused with 403, and
  an approved-but-unpaid account had `accessActive: false`.
- A replayed activation webhook produced exactly one charge row, observed against the live database.
- The failure ladder walked `active → past_due → grace → expired`, access was lost only at `expired`,
  and the `role_grants` row survived it — billing expiry does not destroy approval.
- Both suspension levers were exercised separately and denied for their own distinct reasons.
- The reconciliation report was run and its `openMismatches` recorded, qualified by whether the
  provider direction was verifiable.
- The Cloudflare cron dispatch was either observed reaching both new routes, or its failure to do so
  is written down as a finding with the fallback options named.
</success_criteria>

<output>
After completion, create `.planning/phases/06-manager-billing-subscription/06-11-SUMMARY.md`.
Record: the applied migration timestamp; the observed sandbox charge and document ids (redacted to
their shape); the exact ladder timings observed; whether the cron dispatch fired in production and,
if not, which fallback was chosen; the reconciliation `openMismatches` and whether the provider
direction was verifiable; and, explicitly, what remains open — no route in the codebase yet calls
`requireRole(userId, 'community_manager', space)`, so the billing gate is proven at the enforcement
point and by test but is not yet consumed by any manager-facing feature. That is by design and
belongs to a later phase; say so rather than letting a future reader mistake it for an omission.
</output>
