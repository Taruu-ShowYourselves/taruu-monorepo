---
phase: 02-spike-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/docs/GI-LEGAL-CHECKLIST.md
  - apps/web/docs/GI-PRIME-CHECKLIST.md
autonomous: true
requirements: [SPIKE-02, SPIKE-03]
must_haves:
  truths:
    - "A legal/accountant checklist doc exists covering correct GI document type per flow, VAT treatment, refund/credit-note (זיכוי) mechanics, and Israeli consumer-protection obligations — the document the owner hands to the accountant/lawyer."
    - "A GI Prime / credentials / clearing-terms checklist doc exists covering Prime plan (₪0.15/receipt) written confirmation, real GREENINVOICE_* + Supabase prod creds staged in the Cloudflare Workers secret store, and written clearing terms (clearing %, hard minimums, brand/tourist-card surcharges, settlement payout threshold)."
    - "[HUMAN-VERIFIED / DEFERRED] Accountant/legal sign-off is obtained (or a written timeline is on file); GI Prime is provisioned and confirmed in writing; real creds are staged and clearing terms are on file."
  artifacts:
    - path: "apps/web/docs/GI-LEGAL-CHECKLIST.md"
      provides: "SPIKE-02 accountant/legal merchant-of-record checklist"
      contains: "חשבונית"
    - path: "apps/web/docs/GI-PRIME-CHECKLIST.md"
      provides: "SPIKE-03 Prime / credentials / clearing-terms checklist"
      contains: "Prime"
  key_links: []
---

<objective>
Produce the two external-track checklist documents for Phase 2's human gates:
SPIKE-02 (accountant/legal merchant-of-record sign-off) and SPIKE-03 (GI Prime
provisioning + real credentials + written clearing terms). These are the documents
the owner hands to the accountant/lawyer and the Green Invoice rep.

Purpose: The two slowest go-live dependencies are not code — they are human sign-offs.
Capturing exactly what must be confirmed lets the owner start both tracks in parallel
NOW and gate Phase 4 correctly. Both docs are fully writable without any credentials.
Output: apps/web/docs/GI-LEGAL-CHECKLIST.md and apps/web/docs/GI-PRIME-CHECKLIST.md.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-spike-gate/02-CONTEXT.md

@apps/web/docs/INTEGRATIONS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: SPIKE-02 — legal/accountant merchant-of-record checklist</name>
  <files>apps/web/docs/GI-LEGAL-CHECKLIST.md</files>
  <read_first>
    - apps/web/docs/INTEGRATIONS.md (docs/ markdown convention — tables, legend, runbook tone)
    - .planning/ROADMAP.md (Phase 2 success criterion #3 — the four legal items this doc must cover)
    - .planning/REQUIREMENTS.md (SPIKE-02 wording; PAY-07 receipt fields; BAG-04 / HARD-02 refund context for the זיכוי section)
  </read_first>
  <action>
    Create apps/web/docs/GI-LEGAL-CHECKLIST.md — a checklist the owner hands to the accountant/lawyer to obtain
    merchant-of-record sign-off. Open with a one-paragraph context line (membership model: first vote of a calendar
    month ₪6, rest free; ₪50 vote creation; ₪2.10 of each ₪6 routes to a monthly civic pool). Then four `## ` sections,
    each with concrete checkbox questions (`- [ ]`) the accountant must answer in writing:

    1. `## Document type per flow` — which GI document is correct for each charge: the ₪6 membership charge to a private
       payer, the ₪50 vote-creation charge, and the civic-pool portion. Explicitly ask: חשבונית קבלה (invoice-receipt)
       vs חשבונית מס (tax invoice) vs קבלה (receipt) per flow, and whether the ₪2.10 civic-pool split changes the
       document or is purely internal ledger.
    2. `## VAT treatment` — is VAT (מע״מ) included in the ₪6 / ₪50, and at what rate; how is it shown on the document;
       any exemption for the civic-pool portion; reverse-charge or special handling for a civic/non-profit-adjacent flow.
    3. `## Refund / credit-note (זיכוי) mechanics` — how a refund issues a credit note (חשבונית זיכוי) against the
       original document; partial vs full; the GI API/dashboard path; impact on the already-accrued ₪2.10 pool row;
       chargeback handling. (Ties to v2 BAG-04 / HARD-02 — note as forward-looking.)
    4. `## Consumer-protection obligations (Israeli law)` — cooling-off / cancellation rights on a recurring-membership
       digital charge under חוק הגנת הצרכן; required pre-charge disclosure of the off-session card-on-file consent;
       receipt-delivery obligation; how "first vote then free" must be presented so it is not a deceptive recurring charge.

    End with a `## Sign-off` section: a line for accountant name + date and a `- [ ] Written sign-off obtained (or timeline on file)`
    checkbox. Add a top note: "Status: PENDING — external human track; gates Phase 4 go-live, not Phase 3 build."
  </action>
  <verify>
    <automated>cd apps/web && grep -q "## Document type per flow" docs/GI-LEGAL-CHECKLIST.md && grep -q "## VAT treatment" docs/GI-LEGAL-CHECKLIST.md && grep -q "זיכוי" docs/GI-LEGAL-CHECKLIST.md && grep -q "## Consumer-protection" docs/GI-LEGAL-CHECKLIST.md && grep -q "Sign-off" docs/GI-LEGAL-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "## Document type per flow" apps/web/docs/GI-LEGAL-CHECKLIST.md` exits 0
    - `grep -q "## VAT treatment" apps/web/docs/GI-LEGAL-CHECKLIST.md` exits 0
    - `grep -q "חשבונית קבלה" apps/web/docs/GI-LEGAL-CHECKLIST.md` exits 0 AND `grep -q "חשבונית מס" apps/web/docs/GI-LEGAL-CHECKLIST.md` exits 0
    - `grep -q "זיכוי" apps/web/docs/GI-LEGAL-CHECKLIST.md` exits 0
    - `grep -q "## Consumer-protection" apps/web/docs/GI-LEGAL-CHECKLIST.md` exits 0
    - `grep -qF "Sign-off" apps/web/docs/GI-LEGAL-CHECKLIST.md` exits 0
    - `grep -qF "PENDING" apps/web/docs/GI-LEGAL-CHECKLIST.md` exits 0 (flagged as external human track)
    - At least 4 `- [ ]` checkbox lines: `grep -c "\- \[ \]" apps/web/docs/GI-LEGAL-CHECKLIST.md` >= 4
  </acceptance_criteria>
  <done>GI-LEGAL-CHECKLIST.md exists with the four required sections (document type, VAT, זיכוי refund/credit-note, consumer-protection), concrete checkbox questions, a sign-off section, and a PENDING external-track status note.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: SPIKE-03 — GI Prime / credentials / clearing-terms checklist</name>
  <files>apps/web/docs/GI-PRIME-CHECKLIST.md</files>
  <read_first>
    - apps/web/docs/INTEGRATIONS.md (the GREENINVOICE_* / Supabase rows + sync-secrets.sh deploy sequence — reuse names exactly)
    - apps/web/scripts/sync-secrets.sh (how secrets reach the Cloudflare Worker secret store)
    - .planning/ROADMAP.md (Phase 2 success criterion #4 — Prime, real creds staged, written clearing terms)
    - .planning/STATE.md (CONCERNS: tourist/foreign-card surcharge ~3.5% erodes the ₪6 — must appear in clearing terms)
  </read_first>
  <action>
    Create apps/web/docs/GI-PRIME-CHECKLIST.md — the checklist the owner works through with the Green Invoice rep and
    when staging production secrets. Top note: "Status: PENDING — external human track; gates Phase 4 go-live, not Phase 3 build."
    Open with one context line: the ₪0.15/receipt Prime rate is the rate the unit economics depend on (₪6 charge,
    ₪2.10 → civic pool, ₪3.90 → platform; a fat per-receipt fee or surcharge breaks the model). Then three `## ` sections,
    each with `- [ ]` checkboxes:

    1. `## GI Prime plan` — obtain WRITTEN confirmation of the Prime plan at ₪0.15/receipt; effective date; any volume
       minimum or monthly fee; that both the ₪6 membership receipt and the ₪50 creation receipt bill at this rate.
    2. `## Real credentials staged` — list the exact secrets to stage in the Cloudflare Workers secret store via
       `./scripts/sync-secrets.sh`: `GREENINVOICE_API_KEY_ID`, `GREENINVOICE_API_SECRET`, `GREENINVOICE_PLUGIN_ID`,
       `GREENINVOICE_WEBHOOK_SECRET`, plus prod Supabase `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`,
       `SUPABASE_SERVICE_ROLE_KEY`, and `GREENINVOICE_ENV=production`. Checkbox: secrets pushed + `wrangler` confirms them set.
    3. `## Written clearing terms` — obtain on file, in writing: the actual clearing % (עמלת סליקה), any hard minimum
       per transaction, brand/tourist/foreign-card surcharges (call out the ~3.5% foreign-card surcharge that erodes the
       ₪6 — decision: block or flag at charge time), and the settlement payout threshold + payout cadence.

    End with `## Sign-off`: GI rep name + date and `- [ ] Prime confirmed in writing + creds staged + clearing terms on file`.
  </action>
  <verify>
    <automated>cd apps/web && grep -q "## GI Prime plan" docs/GI-PRIME-CHECKLIST.md && grep -q "0.15" docs/GI-PRIME-CHECKLIST.md && grep -q "GREENINVOICE_API_KEY_ID" docs/GI-PRIME-CHECKLIST.md && grep -q "## Written clearing terms" docs/GI-PRIME-CHECKLIST.md && grep -q "surcharge" docs/GI-PRIME-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "## GI Prime plan" apps/web/docs/GI-PRIME-CHECKLIST.md` exits 0
    - `grep -q "0.15" apps/web/docs/GI-PRIME-CHECKLIST.md` exits 0 (the rate the economics depend on)
    - `grep -q "GREENINVOICE_API_KEY_ID" apps/web/docs/GI-PRIME-CHECKLIST.md` AND `grep -q "SUPABASE_SERVICE_ROLE_KEY" apps/web/docs/GI-PRIME-CHECKLIST.md` each exit 0
    - `grep -q "## Real credentials staged" apps/web/docs/GI-PRIME-CHECKLIST.md` exits 0
    - `grep -q "## Written clearing terms" apps/web/docs/GI-PRIME-CHECKLIST.md` exits 0
    - `grep -qE "surcharge|tourist|foreign-card" apps/web/docs/GI-PRIME-CHECKLIST.md` exits 0 (the ~3.5% erosion concern captured)
    - `grep -qF "PENDING" apps/web/docs/GI-PRIME-CHECKLIST.md` exits 0
    - At least 4 `- [ ]` checkbox lines: `grep -c "\- \[ \]" apps/web/docs/GI-PRIME-CHECKLIST.md` >= 4
  </acceptance_criteria>
  <done>GI-PRIME-CHECKLIST.md exists with the three required sections (Prime plan @ ₪0.15, real GREENINVOICE_*/Supabase creds staged via sync-secrets.sh, written clearing terms incl. the foreign-card surcharge concern), checkboxes, a sign-off section, and a PENDING status note.</done>
</task>

</tasks>

<verification>
- apps/web/docs/GI-LEGAL-CHECKLIST.md covers document type, VAT, זיכוי refund/credit-note, and consumer-protection, with a sign-off section and PENDING status.
- apps/web/docs/GI-PRIME-CHECKLIST.md covers Prime @ ₪0.15/receipt, real GREENINVOICE_*/Supabase creds staged via sync-secrets.sh, and written clearing terms including the foreign-card surcharge concern.
- Both docs are pure deliverables requiring no credentials to author; the actual sign-offs / provisioning are flagged human-verified / deferred.
</verification>

<success_criteria>
- Each external gate (SPIKE-02 legal, SPIKE-03 Prime/creds/clearing) has a concrete, hand-to-the-expert checklist with verifiable section headings.
- Both docs follow the apps/web/docs/ runbook convention and are flagged PENDING as external human tracks that gate Phase 4.
- No task required live GI credentials.
</success_criteria>

<output>
After completion, create `.planning/phases/02-spike-gate/02-02-SUMMARY.md`.
</output>
