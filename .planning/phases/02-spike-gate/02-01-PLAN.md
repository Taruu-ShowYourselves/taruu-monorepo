---
phase: 02-spike-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/services/greenInvoice/index.ts
  - apps/web/scripts/gi-spike.ts
  - apps/web/package.json
  - apps/web/docs/SPIKE-RESULT.md
autonomous: true
requirements: [SPIKE-01]
must_haves:
  truths:
    - "The greenInvoice service exposes a chargeToken() that POSTs to /payments/tokens/{id}/charge (the net-new off-session MIT surface)."
    - "A runnable standalone TypeScript spike harness exists under apps/web/scripts/ that exercises the full card-on-file sequence and hard-exits with a clear message when isGreenInvoiceConfigured() is false — safe to commit and run later without creds."
    - "A SPIKE-RESULT.md doc exists with the exact observation fields criterion #1/#2 require AND a code-derived trace section that pre-answers criterion #2 from the existing merch flow."
    - "[HUMAN-VERIFIED / DEFERRED] The harness has been run against the live GI sandbox with real creds and SPIKE-RESULT.md is filled with observed 3DS/SCA, soft-decline, charge id, document id, and settlement-timing values."
  artifacts:
    - path: "apps/web/src/services/greenInvoice/index.ts"
      provides: "chargeToken() for POST /payments/tokens/{id}/charge"
      contains: "/payments/tokens/"
    - path: "apps/web/scripts/gi-spike.ts"
      provides: "Runnable GI sandbox spike harness, guarded on isGreenInvoiceConfigured()"
      contains: "isGreenInvoiceConfigured"
    - path: "apps/web/package.json"
      provides: "spike:gi script + tsx devDependency to run the harness"
      contains: "spike:gi"
    - path: "apps/web/docs/SPIKE-RESULT.md"
      provides: "Observation template + code-derived trace pre-answering criterion #2"
      contains: "Code-derived trace"
  key_links:
    - from: "apps/web/scripts/gi-spike.ts"
      to: "apps/web/src/services/greenInvoice/index.ts"
      via: "imports getToken + chargeToken + isGreenInvoiceConfigured"
      pattern: "from.*services/greenInvoice"
    - from: "apps/web/src/services/greenInvoice/index.ts"
      to: "POST /payments/tokens/{id}/charge"
      via: "fetch in chargeToken"
      pattern: "payments/tokens/.*charge"
---

<objective>
Build the codeable half of SPIKE-01: the runnable Green Invoice card-on-file
sandbox spike harness, the net-new `chargeToken()` service call it exercises, and
the SPIKE-RESULT doc (observation template + a code-derived trace that pre-answers
success criterion #2 from the already-shipped merch flow).

Purpose: De-risk the one GI capability the merch flow never exercised — the saved-card
off-session MIT charge (`POST /payments/tokens/{id}/charge`) — so Phase 3 productionizes
a verified surface, not a guess. Everything here is committable and runnable WITHOUT real
sandbox credentials; the live run is the human gate.
Output: An extended greenInvoice service, a guarded spike script, a wired npm runner, and
SPIKE-RESULT.md.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-spike-gate/02-CONTEXT.md

@apps/web/src/services/greenInvoice/index.ts
@apps/web/src/app/api/merch/webhook/route.ts

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — use directly, no exploration. -->

From apps/web/src/services/greenInvoice/index.ts (existing exports):
```typescript
export function isGreenInvoiceConfigured(): boolean;        // true when apiKeyId && apiSecret present
export async function getToken(): Promise<string>;          // POST /account/token, JWT cached in-memory
export async function createPaymentForm(order: MerchOrder, urls: PaymentFormUrls): Promise<string>;
// module-private `config` holds: apiKeyId, apiSecret, baseUrl (sandbox.d.greeninvoice.co.il/api/v1), pluginId
```

Auth header pattern already used by createPaymentForm:
```typescript
headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${token}` }
```

Defensive document-id read pattern from apps/web/src/app/api/merch/webhook/route.ts:
```typescript
const paymentId = (payload.id as string) || (payload.documentId as string) || (payload.paymentId as string) || null;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add chargeToken() — the off-session MIT charge call</name>
  <files>apps/web/src/services/greenInvoice/index.ts</files>
  <read_first>
    - apps/web/src/services/greenInvoice/index.ts (the file being modified — model the new fn on createPaymentForm: getToken then fetch then defensive parse then logger)
    - apps/web/src/app/api/merch/webhook/route.ts (for the defensive document-id read pattern: payload.id || documentId || paymentId)
  </read_first>
  <action>
    Append an exported async function `chargeToken` to apps/web/src/services/greenInvoice/index.ts that issues
    the saved-card off-session MIT charge — the surface the merch flow never exercises.

    Signature:
    ```typescript
    export interface TokenChargeInput {
      tokenId: string;              // GI saved-card token id (path param)
      sum: number;                  // amount in ILS (e.g. 6 or 50)
      description: string;          // document description (Hebrew)
      client: { name: string; emails?: string[]; taxId?: string };
      custom?: string;              // correlation id, mirrors merch `custom` field
    }
    export interface TokenChargeResult {
      chargeId: string | null;      // transaction/charge id
      documentId: string | null;    // issued tax-document id
      raw: Record<string, unknown>; // full response for SPIKE-RESULT capture
    }
    export async function chargeToken(input: TokenChargeInput): Promise<TokenChargeResult>;
    ```

    Implementation requirements:
    - Throw `new Error('Green Invoice is not configured')` if `isGreenInvoiceConfigured()` is false (mirror getToken).
    - `const token = await getToken();`
    - `fetch(\`${config.baseUrl}/payments/tokens/${encodeURIComponent(input.tokenId)}/charge\`, { method: 'POST', headers: { 'Content-Type': 'application/json', Authorization: \`Bearer ${token}\` }, body: JSON.stringify(body) })`
      where `body` carries `{ sum, currency: 'ILS', lang: 'he', description, client, custom: input.custom, type: 320 }` (type 320 = issue a receipt/invoice on charge, same as createPaymentForm).
    - On `!res.ok`: read `res.text()` and throw `Green Invoice token charge failed (${res.status}): ${detail}` (mirror createPaymentForm's error path).
    - Parse JSON; derive `chargeId` defensively from `data.transactionId || data.chargeId || data.id || null` and `documentId` from `data.documentId || data.id || data.paymentId || null` (defensive, per the webhook pattern). Return `{ chargeId, documentId, raw: data }`.
    - `logger.info('Green Invoice token charge issued', { custom: input.custom });` on success — do NOT log the token id or full payload.
    - Do not remove or alter getToken / createPaymentForm / isGreenInvoiceConfigured.
  </action>
  <verify>
    <automated>cd apps/web && grep -q "export async function chargeToken" src/services/greenInvoice/index.ts && grep -q "payments/tokens/" src/services/greenInvoice/index.ts && grep -q "TokenChargeResult" src/services/greenInvoice/index.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export async function chargeToken" apps/web/src/services/greenInvoice/index.ts` exits 0
    - `grep -q "/payments/tokens/" apps/web/src/services/greenInvoice/index.ts` exits 0
    - `grep -qE "encodeURIComponent\(input.tokenId\)" apps/web/src/services/greenInvoice/index.ts` exits 0
    - `grep -qE "data.documentId \|\| data.id \|\| data.paymentId" apps/web/src/services/greenInvoice/index.ts` exits 0
    - `cd apps/web && pnpm typecheck` exits 0
    - `grep -q "export async function getToken" apps/web/src/services/greenInvoice/index.ts` still exits 0 (existing exports intact)
  </acceptance_criteria>
  <done>chargeToken() is exported, calls POST /payments/tokens/{id}/charge with a Bearer token, parses charge id + document id defensively, guards on isGreenInvoiceConfigured(), and the package typechecks.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Spike harness script + tsx runner wiring</name>
  <files>apps/web/scripts/gi-spike.ts, apps/web/package.json</files>
  <read_first>
    - apps/web/src/services/greenInvoice/index.ts (after Task 1 — imports getToken, chargeToken, isGreenInvoiceConfigured)
    - apps/web/scripts/sync-secrets.sh (existing scripts/ convention)
    - apps/web/package.json (to add the runner script + tsx devDependency)
    - apps/web/.dev.vars.example (env var names: GREENINVOICE_API_KEY_ID, GREENINVOICE_API_SECRET, GREENINVOICE_PLUGIN_ID)
  </read_first>
  <action>
    Create apps/web/scripts/gi-spike.ts — a standalone, deliberately-invoked TypeScript harness (NOT a vitest test;
    it must never run in CI). Import from `../src/services/greenInvoice/index` (relative import). Use plain
    `console.log` / `console.error` for ALL output — do NOT import `@/lib/logger` (keep the script free of Next
    path-alias dependencies so tsx resolves it cleanly). Never print secrets or the raw token string.

    Behaviour, in order:
    1. GUARD FIRST — before any network call: `if (!isGreenInvoiceConfigured()) { console.error('[gi-spike] SKIPPED: Green Invoice is not configured — set GREENINVOICE_API_KEY_ID / GREENINVOICE_API_SECRET in apps/web/.dev.vars then re-run. No live call attempted.'); process.exit(1); }`
       The message MUST contain the substring `not configured` and the script MUST exit before issuing any fetch.
    2. Print a banner naming the sandbox base and the sequence about to run.
    3. STEP 1 — token: `const token = await getToken();` then `console.log('[gi-spike] token acquired (len=' + token.length + ')')` (never print the token value).
    4. STEP 2 — card setup: call `createPaymentForm(stubOrder, urls)` with a minimal stub order to obtain the hosted card-entry URL; print the URL and `console.log('[gi-spike] MANUAL STEP: open the form URL, complete card setup, capture the webhook payload, then set GI_SPIKE_TOKEN_ID and re-run with --charge')`.
    5. STEP 3 — token charge, gated on BOTH `process.argv.includes('--charge')` AND `process.env.GI_SPIKE_TOKEN_ID`: call `chargeToken({ tokenId: process.env.GI_SPIKE_TOKEN_ID, sum: 6, description: 'תַּרְאוּ — חיוב חברות בדיקה', client: { name: 'Spike Test' }, custom: 'gi-spike-' + Date.now() })` and print `chargeId`, `documentId`, and `JSON.stringify(raw)`. When the flag/env is absent, print that STEP 3 is skipped and how to enable it.
    6. Wrap each network step in try/catch; on error `console.error('[gi-spike] STEP FAILED: ' + String(err))` then `process.exit(1)`.

    In apps/web/package.json:
    - Add `"tsx": "^4.0.0"` to devDependencies.
    - Add script `"spike:gi": "tsx --env-file=.dev.vars scripts/gi-spike.ts"` (loads `.dev.vars` dotenv into process.env so the service `config` picks up GREENINVOICE_* when present).
    - Run `pnpm install` at repo root so tsx resolves, then verify the guard path: `pnpm --filter @sync/web spike:gi` (creds empty) prints the `not configured` message and exits with code 1 WITHOUT any network call.
  </action>
  <verify>
    <automated>cd apps/web && grep -q "isGreenInvoiceConfigured" scripts/gi-spike.ts && grep -q "not configured" scripts/gi-spike.ts && grep -q "spike:gi" package.json && grep -q '"tsx"' package.json && grep -q "services/greenInvoice" scripts/gi-spike.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "isGreenInvoiceConfigured" apps/web/scripts/gi-spike.ts` exits 0
    - `grep -q "not configured" apps/web/scripts/gi-spike.ts` exits 0
    - `grep -qE "process.exit\(1\)" apps/web/scripts/gi-spike.ts` exits 0
    - `grep -q "chargeToken" apps/web/scripts/gi-spike.ts` exits 0 AND `grep -q "createPaymentForm" apps/web/scripts/gi-spike.ts` exits 0
    - `grep -qF "from '../src/services/greenInvoice/index'" apps/web/scripts/gi-spike.ts` exits 0
    - NOT present: `grep -q "@/lib/logger" apps/web/scripts/gi-spike.ts` exits 1 (no Next alias import)
    - `grep -q "\"spike:gi\"" apps/web/package.json` exits 0 AND `grep -q "--env-file=.dev.vars" apps/web/package.json` exits 0
    - `grep -q "\"tsx\"" apps/web/package.json` exits 0
    - Running `cd apps/web && pnpm --filter @sync/web spike:gi` with empty creds prints a line containing `not configured` and exits non-zero, with no network request issued
  </acceptance_criteria>
  <done>The guarded spike harness exists, imports the service via relative path, exercises token → card-form → token-charge in sequence, and `pnpm spike:gi` hard-exits with the `not configured` message when creds are absent.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: SPIKE-RESULT.md — observation template + code-derived trace</name>
  <files>apps/web/docs/SPIKE-RESULT.md</files>
  <read_first>
    - apps/web/docs/INTEGRATIONS.md (docs/ markdown convention — tables, REQ/PAY/OPT legend style)
    - apps/web/src/app/api/merch/webhook/route.ts (source for the code-derived trace: custom field, ?token=/x-greeninvoice-token secret, payload.id||documentId||paymentId)
    - apps/web/src/services/greenInvoice/index.ts (source for token transport + type 320 + sandbox base)
    - .planning/ROADMAP.md (Phase 2 success criteria #1 and #2 — the fields this doc must capture)
  </read_first>
  <action>
    Create apps/web/docs/SPIKE-RESULT.md with two distinct parts.

    PART A — "Live sandbox observations (fill after running `pnpm spike:gi --charge`)": an empty-but-structured
    template with one labelled field per item ROADMAP criterion #1/#2 require. Use these exact field headings
    (leave each value as `_(pending live run)_`):
    - `Token-charge id` (the charge/transaction id returned by POST /payments/tokens/{id}/charge)
    - `Document id` (tax document id returned in the SAME response — confirms criterion #1)
    - `3DS / SCA behavior` (did the off-session MIT charge trigger a challenge? frictionless?)
    - `Soft-decline behavior` (what a soft-decline returns: status code, error shape, retriable?)
    - `Webhook payload shape` (keys delivered to notifyUrl on a token charge)
    - `Secret transport observed` (header vs query param actually used by GI for this flow)
    - `Settlement timing` (authorization → settlement delay observed)

    PART B — "## Code-derived trace (pre-answers criterion #2 from the shipped merch flow)": a section that, WITHOUT
    a live run, documents what the existing merch integration already proves, citing file + line context:
    - Token transport: GI token read from `X-Authorization-Bearer` response header OR JSON body (`getToken`, greenInvoice/index.ts) — used as `Authorization: Bearer <token>`.
    - Payment-form type: `type: 320` issues a receipt/invoice on success (`createPaymentForm`).
    - Webhook order correlation: order id carried in the `custom` field (`merch/webhook/route.ts`, read as `payload.custom`).
    - Webhook secret transport: `?token=<secret>` query param OR `x-greeninvoice-token` header, `timingSafeEqual` with length-guard, fail-CLOSED in production.
    - Document id read: defensive `payload.id || payload.documentId || payload.paymentId`.
    - The gap: `POST /payments/tokens/{id}/charge` (off-session MIT) is NOT exercised by the merch flow — this is exactly what the harness + live run must confirm (returns usable charge id + document id in one response).
    Close PART B with an explicit note: criterion #2's "different webhook shape / header-vs-query secret / settlement timing" deltas are pre-answered from code here; only the token-charge-specific webhook shape and timing remain for the live run (PART A).
  </action>
  <verify>
    <automated>cd apps/web && grep -q "Code-derived trace" docs/SPIKE-RESULT.md && grep -q "Token-charge id" docs/SPIKE-RESULT.md && grep -q "3DS" docs/SPIKE-RESULT.md && grep -q "payments/tokens/" docs/SPIKE-RESULT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Code-derived trace" apps/web/docs/SPIKE-RESULT.md` exits 0
    - All seven PART A field headings present: `grep -q "Token-charge id"`, `grep -q "Document id"`, `grep -q "3DS"`, `grep -q "Soft-decline"`, `grep -q "Webhook payload shape"`, `grep -q "Secret transport"`, `grep -q "Settlement timing"` each exit 0 against apps/web/docs/SPIKE-RESULT.md
    - `grep -q "custom" apps/web/docs/SPIKE-RESULT.md` exits 0 (code-derived webhook correlation documented)
    - `grep -q "x-greeninvoice-token" apps/web/docs/SPIKE-RESULT.md` exits 0 (secret transport documented)
    - `grep -q "payments/tokens/" apps/web/docs/SPIKE-RESULT.md` exits 0 (the gap / MIT surface named)
    - `grep -q "pending live run" apps/web/docs/SPIKE-RESULT.md` exits 0 (PART A values left as placeholders)
  </acceptance_criteria>
  <done>SPIKE-RESULT.md has a PART A live-observation template with all seven required fields left pending, and a PART B code-derived trace that pre-answers criterion #2 from the merch flow (token transport, type 320, custom-field correlation, secret transport, defensive document-id read, and the named token-charge gap).</done>
</task>

</tasks>

<verification>
- `apps/web/src/services/greenInvoice/index.ts` exports `chargeToken` calling `POST /payments/tokens/{id}/charge`; `pnpm typecheck` is green.
- `apps/web/scripts/gi-spike.ts` exists, imports the service via relative path, guards on `isGreenInvoiceConfigured()`, and `pnpm --filter @sync/web spike:gi` hard-exits with the `not configured` message (no creds, no network call).
- `apps/web/package.json` has the `spike:gi` script (`--env-file=.dev.vars`) and `tsx` in devDependencies.
- `apps/web/docs/SPIKE-RESULT.md` has both the PART A live-observation template (seven fields) and the PART B code-derived trace.
- No task required live GI credentials to complete.
</verification>

<success_criteria>
- chargeToken() is the only net-new GI surface added and it targets the off-session MIT endpoint.
- The harness is runnable, deliberately-invoked (never CI), and provably guarded without creds.
- SPIKE-RESULT.md pre-answers success criterion #2 from code and stages the exact fields the live run (criterion #1) must fill.
- The live sandbox run + filled observations remain a flagged human-verified / deferred must_have.
</success_criteria>

<output>
After completion, create `.planning/phases/02-spike-gate/02-01-SUMMARY.md`.
</output>
