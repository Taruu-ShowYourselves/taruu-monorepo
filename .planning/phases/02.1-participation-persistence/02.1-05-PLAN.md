---
phase: 02.1-participation-persistence
plan: 05
type: execute
wave: 2
depends_on: ["02.1-01"]
files_modified:
  - apps/web/src/app/[locale]/votes/[id]/flow/submitParticipation.ts
  - apps/web/src/app/[locale]/votes/[id]/flow/ParticipationFlow.tsx
  - apps/web/src/app/[locale]/votes/[id]/flow/ParticipationFlow.module.css
  - apps/web/src/app/[locale]/votes/[id]/page.tsx
  - apps/web/src/app/[locale]/votes/components/VotesList.tsx
  - apps/web/src/__tests__/services/submit-participation.test.ts
  - apps/web/src/__tests__/services/participation-receipt-honesty.test.ts
autonomous: true
requirements: [VOTE-02, VOTE-04]

must_haves:
  truths:
    - "Confirming a vote sends a request to /api/votes/{id}/participate and the receipt appears only after the server confirms the write"
    - "A rejected or failed write shows a Hebrew/RTL error, keeps the user on the confirm step, and shows no receipt"
    - "mockHash() does not exist anywhere in the codebase"
    - "The receipt shows the server's ballot id, the server's timestamp, the chosen option, cost חינם and a status word that is not נחתם — and no SealCard, no BLOCK number, no chain claim"
    - "The casting funnel (choice step, confirm step, receipt, results panel, vote list ballot note) claims no blockchain seal"
  artifacts:
    - path: "apps/web/src/app/[locale]/votes/[id]/flow/submitParticipation.ts"
      provides: "pure, fetch-injectable participation submit returning a discriminated result"
      exports: ["submitParticipation", "ParticipationSubmission", "RecordedBallot"]
    - path: "apps/web/src/app/[locale]/votes/[id]/flow/ParticipationFlow.tsx"
      provides: "server-confirmed flow with pending/error states and an honest receipt"
    - path: "apps/web/src/__tests__/services/submit-participation.test.ts"
      provides: "behavioural proof of the submit module across every server outcome"
    - path: "apps/web/src/__tests__/services/participation-receipt-honesty.test.ts"
      provides: "source-assertion guard: no mockHash, no SealCard, no chain copy in the casting funnel"
  key_links:
    - from: "apps/web/src/app/[locale]/votes/[id]/flow/ParticipationFlow.tsx"
      to: "/api/votes/{voteId}/participate"
      via: "submitParticipation on confirm"
      pattern: "submitParticipation"
    - from: "apps/web/src/app/[locale]/votes/[id]/flow/submitParticipation.ts"
      to: "POST /api/votes/[id]/participate"
      via: "fetch with body { optionId }"
      pattern: "api/votes/.*participate"
    - from: "apps/web/src/app/[locale]/votes/[id]/page.tsx"
      to: "ParticipationFlow onComplete"
      via: "receipt dismissal sets hasVoted and the chosen option"
      pattern: "onComplete"
---

<objective>
Make the participation UI reach the server and tell the truth about what happened.

Purpose: `ParticipationFlow.tsx` contains zero `fetch()` calls. `sealVote()` (`:149-157`) fabricates a hash with `mockHash()` (`:54-67`) plus a random block number, sets React state, and stops. The receipt then claims `נחתם` and `✓ חתום בבלוקצ׳יין · בלתי ניתן לשינוי` for a ballot that was never recorded. This is the resident-facing half of the P0.

There is also a second, quieter bug to fix while you are here: `sealVote` calls `onComplete()` immediately, which sets `hasVoted` on the page, which makes `showFlow` false (`page.tsx:227`), which unmounts the flow — so the receipt stage is effectively unreachable and the user lands on the results panel that says `הצבעתכם נקלטה ונחתמה בבלוקצ׳יין`. The receipt must survive until the user dismisses it.

Output: a pure submit module, a flow that awaits a server-confirmed write before showing a receipt built from server facts, an honest results panel, and two tests.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-participation-persistence/02.1-CONTEXT.md
@.planning/phases/02.1-participation-persistence/02.1-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- The server contract from plan 01. Build against this; do not inspect the route. -->

`POST /api/votes/{voteId}/participate`
Request body: `{ "optionId": "<uuid>" }` — nothing else.
Success (200):
```json
{ "success": true,
  "alreadyRecorded": false,
  "participation": { "id": "<uuid>", "voteId": "<uuid>", "userId": "<uuid>",
                     "optionId": "<uuid>", "createdAt": "<ISO-8601>" } }
```
Failure statuses the client must handle: 401 (no session), 403 with `{ code: 'RESIDENCY_NOT_VERIFIED' | 'IDENTITY_NOT_VERIFIED' }`, 429 (rate limited, 3/min), 400 (bad option / vote not active / vote ended), 404 (vote not found), 500.

Existing press components (`apps/web/src/components/press/index.ts`) — `SealCard` stays in the library, it is only removed from THIS receipt:
```typescript
export function Receipt(props: {
  kicker?: React.ReactNode; title?: React.ReactNode;
  rows: { label: React.ReactNode; value: React.ReactNode; strong?: boolean }[];
  footer?: React.ReactNode; className?: string;
}): JSX.Element
export function Stepper(props: { steps: { label: string }[]; current: number }): JSX.Element
export function NewsButton(props: { variant?: 'red' | 'outline'; size?: 'lg'; disabled?: boolean; onClick?: () => void; trailing?: React.ReactNode; className?: string; children: React.ReactNode }): JSX.Element
```

Design tokens available in `ParticipationFlow.module.css` — reuse these, add no literals:
`--np-ink`, `--np-ink-soft`, `--np-ink-faint`, `--np-red`, `--np-paper-2`, `--np-paper-edge`, `--np-rule`, `--np-rule-hair`, `--np-font-mono`, `--np-font-body`, `--space-2..--space-5`, `--text-xs`, `--text-sm`.
The existing `.gateNote` rule (`:78-90`) is the shape to copy for the new error note.

Test infrastructure reality: `apps/web/vitest.config.ts` runs `environment: 'node'` with `include: ['src/**/*.test.ts']` — no jsdom, no `@testing-library/react`, and `.tsx` test files are not even collected. Do NOT add a DOM testing stack for a P0. `apps/web/src/__tests__/services/dashboard-free-mvp.test.ts` is the established in-repo pattern for asserting against component SOURCE, and its header explains exactly why. Follow it.

Vote page wiring (`apps/web/src/app/[locale]/votes/[id]/page.tsx`):
- `:87-88` `selectedOption` / `hasVoted` state
- `:113-119` on load, `raw.userVote` (the option id, served by `GET /api/votes/[id]` at `route.ts:78-85`) already sets `hasVoted` + `selectedOption` — so once the server persists, a reload correctly shows "already voted" with no further work
- `:227` `const showFlow = isActive && !hasVoted;`
- `:461-469` renders `<ParticipationFlow ... onComplete={() => setHasVoted(true)} />`
- `:524` `הצבעתכם נקלטה ונחתמה בבלוקצ׳יין`, `:528-532` footer `מאומת · זהות + GPS` ■ `חתום בבלוקצ׳יין`, `:427-430` context bullet `... נספר פעם אחת ונחתם בבלוקצ׳יין — בלתי ניתן לשינוי או לזיוף.`
</interfaces>
</context>

<tasks>

<task type="auto" id="02.1-05-T1" tdd="true">
  <name>Task 1: Extract a pure, testable participation submit module</name>
  <read_first>
    - apps/web/src/app/[locale]/votes/[id]/flow/ParticipationFlow.tsx (the component this module is extracted from — read all 355 lines)
    - apps/web/vitest.config.ts (node environment, `.ts` only — the reason this logic is being extracted out of the `.tsx` component)
    - apps/web/src/__tests__/api/bags.test.ts lines 1-40 (repo conventions for vitest mocking)
    - packages/shared/src/contracts/vote.ts (plan 01 — the authoritative response shape)
    - CLAUDE.md (strict TypeScript, no `any`, functional/composable style)
  </read_first>
  <files>apps/web/src/app/[locale]/votes/[id]/flow/submitParticipation.ts, apps/web/src/__tests__/services/submit-participation.test.ts</files>
  <behavior>
    - 200 with `alreadyRecorded: false` → `{ status: 'recorded', alreadyRecorded: false, ballot: { id, optionId, createdAt } }`
    - 200 with `alreadyRecorded: true` → `{ status: 'recorded', alreadyRecorded: true, ballot: <existing> }`
    - 401 → `{ status: 'rejected', code: 'UNAUTHENTICATED', message: 'צריך להתחבר כדי להצביע.' }`
    - 403 with `code: 'RESIDENCY_NOT_VERIFIED'` → `{ status: 'rejected', code: 'RESIDENCY_NOT_VERIFIED', message: 'צריך לאמת תושבוּת לפני ההצבעה.' }`
    - 403 with `code: 'IDENTITY_NOT_VERIFIED'` → `{ status: 'rejected', code: 'IDENTITY_NOT_VERIFIED', message: 'צריך לאמת זהות לפני ההצבעה.' }`
    - 429 → message `יותר מדי בקשות. נסו שוב בעוד דקה.`
    - 400 → message `לא ניתן לרשום את הקול הזה. רעננו את הדף ונסו שוב.`
    - 404 → message `ההצבעה לא נמצאה.`
    - 500 or any other status → message `הקול לא נרשם. נסו שוב בעוד רגע.`
    - `fetch` throwing (offline) → message `הקול לא נרשם. בדקו את החיבור ונסו שוב.`
    - A 200 whose JSON is missing `participation.id` → rejected, not recorded (never trust a malformed success)
    - No branch ever surfaces a raw server `error` string to the caller
  </behavior>
  <action>
Create `apps/web/src/app/[locale]/votes/[id]/flow/submitParticipation.ts`:

```typescript
/**
 * Casting a ballot, as a pure async function.
 *
 * Extracted from ParticipationFlow so the network contract has executable
 * proof: this repo's vitest runs `environment: 'node'` with a `.ts`-only
 * include glob, so logic living inside the `.tsx` component cannot be tested
 * at all. `fetch` is injected for the same reason.
 *
 * Server messages are never surfaced verbatim — the caller gets a Hebrew,
 * user-facing string chosen from the status and the machine code.
 */

export interface RecordedBallot {
  readonly id: string;
  readonly optionId: string;
  /** ISO-8601, straight from the server row. Never a client clock. */
  readonly createdAt: string;
}

export type ParticipationRejectionCode =
  | 'UNAUTHENTICATED'
  | 'RESIDENCY_NOT_VERIFIED'
  | 'IDENTITY_NOT_VERIFIED'
  | 'RATE_LIMITED'
  | 'INVALID'
  | 'NOT_FOUND'
  | 'SERVER_ERROR'
  | 'NETWORK_ERROR';

export type ParticipationSubmission =
  | { readonly status: 'recorded'; readonly ballot: RecordedBallot; readonly alreadyRecorded: boolean }
  | { readonly status: 'rejected'; readonly code: ParticipationRejectionCode; readonly message: string };

export async function submitParticipation(
  input: { readonly voteId: string; readonly optionId: string },
  fetchImpl: typeof fetch = fetch
): Promise<ParticipationSubmission>
```

Implementation notes:
- POST to `/api/votes/${input.voteId}/participate` with `method: 'POST'`, `headers: { 'Content-Type': 'application/json' }`, `credentials: 'same-origin'`, `body: JSON.stringify({ optionId: input.optionId })`.
- Wrap the whole call in try/catch; the catch returns the `NETWORK_ERROR` rejection.
- Parse the JSON defensively (`await response.json().catch(() => null)`).
- On `response.ok`, require `payload?.participation?.id`, `payload.participation.optionId` and `payload.participation.createdAt` to be non-empty strings before returning `recorded`; otherwise return the `SERVER_ERROR` rejection.
- Map non-ok statuses using a small lookup keyed by status, with the 403 branch further keyed on `payload?.code`. Default an unknown 403 to `RESIDENCY_NOT_VERIFIED`.
- Export a named `const PARTICIPATION_MESSAGES: Record<ParticipationRejectionCode, string>` so the test asserts against the same strings the UI shows.
- No `any`. Type the parsed payload as `unknown` and narrow it with a small type guard.

Then create `apps/web/src/__tests__/services/submit-participation.test.ts` covering every bullet in the `<behavior>` block above, passing a `vi.fn()` as `fetchImpl` (never touching global fetch). Also assert that the request body is exactly `JSON.stringify({ optionId })` — no payment or GPS field — and that the URL is `/api/votes/${voteId}/participate`.
  </action>
  <verify>
    <automated>pnpm --filter @sync/web test -- src/__tests__/services/submit-participation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/app/[locale]/votes/[id]/flow/submitParticipation.ts` exists and exports `submitParticipation`, `ParticipationSubmission`, `RecordedBallot`, `PARTICIPATION_MESSAGES`
    - `grep -c "paymentTxId\|gpsCoordinates" "apps/web/src/app/[locale]/votes/[id]/flow/submitParticipation.ts"` returns `0`
    - `grep -c ": any" "apps/web/src/app/[locale]/votes/[id]/flow/submitParticipation.ts"` returns `0`
    - `pnpm --filter @sync/web test -- src/__tests__/services/submit-participation.test.ts` exits 0 with at least 12 passing tests
    - `grep -c "RESIDENCY_NOT_VERIFIED" apps/web/src/__tests__/services/submit-participation.test.ts` returns at least `1`
  </acceptance_criteria>
  <done>Casting a ballot is a pure function with a discriminated result, every server outcome maps to a Hebrew message, and all of it is covered by a passing node-environment test.</done>
</task>

<task type="auto" id="02.1-05-T2">
  <name>Task 2: Rewrite ParticipationFlow — server-confirmed write, honest receipt, no seal</name>
  <read_first>
    - apps/web/src/app/[locale]/votes/[id]/flow/ParticipationFlow.tsx (the file being rewritten — read all 355 lines)
    - apps/web/src/app/[locale]/votes/[id]/flow/ParticipationFlow.module.css (the file being modified — note `.gateNote` at :78-90 and `.seal` at :234-236)
    - apps/web/src/app/[locale]/votes/[id]/flow/submitParticipation.ts (as written in Task 1 — the module being wired in)
    - apps/web/src/components/press/Receipt/Receipt.tsx (the `rows`/`footer` props being fed)
    - apps/web/src/components/press/NewsButton.tsx (the `disabled` / `trailing` props for the pending state)
    - apps/web/src/app/[locale]/votes/[id]/page.tsx lines 220-232 and 455-470 (`showFlow` and the `onComplete` wiring — proof of the unmount bug this task also fixes)
    - .planning/phases/02.1-participation-persistence/02.1-CONTEXT.md — the LOCKED "Receipt and seal" decision
  </read_first>
  <files>apps/web/src/app/[locale]/votes/[id]/flow/ParticipationFlow.tsx, apps/web/src/app/[locale]/votes/[id]/flow/ParticipationFlow.module.css</files>
  <action>
**Deletions (no replacement, no commented-out remnants):**
- The whole `mockHash()` helper (`:53-67`) and its `/* Helpers */` banner if nothing else lives under it.
- The `SealCard` import (it is imported from `@/components/press` on `:7` — remove only that named import, keep `Stepper` and `Receipt`).
- The `<SealCard ... />` element at `:338-346`.
- The trust line `✓ חתום בבלוקצ׳יין · בלתי ניתן לשינוי.` at `:348`.
- The `seal` state (`:93`) and everything that built it.
- In the CSS module, the `.seal` rule at `:234-236`.

**State.** Replace the `seal` state with:
```typescript
  const [ballot, setBallot] = useState<RecordedBallot | null>(null);
  const [alreadyRecorded, setAlreadyRecorded] = useState(false);
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState<string | null>(null);
```

**Submit.** Replace `sealVote` with an async `recordVote`:
```typescript
  const recordVote = useCallback(async () => {
    if (!selectedOption || submitting) return;
    setSubmitting(true);
    setSubmitError(null);
    const result = await submitParticipation({ voteId, optionId: selectedOption });
    setSubmitting(false);
    if (result.status === 'rejected') {
      setSubmitError(result.message);
      return;                      // stay on 'confirm'; no receipt, no onComplete
    }
    setBallot(result.ballot);
    setAlreadyRecorded(result.alreadyRecorded);
    setStage('receipt');
  }, [selectedOption, submitting, voteId]);
```
`handleConfirm` keeps its auth and residency redirects unchanged and awaits `recordVote()` in place of `sealVote()`. Do NOT call `onComplete()` here — see the receipt below.

**Confirm step (`stage === 'confirm'`).**
- Replace the lead paragraph at `:256-260` with exactly:
  `ההשתתפות חינם, בלי תשלום ובלי חסמים. נדרש אימות זהות ותושבוּת חד-פעמי, כדי שכל קול ישויך לתושב אמיתי אחד. הקול נרשם פעם אחת ואי אפשר לשנות אותו.`
- Replace the trust line at `:272` with exactly: `מאומת זהות ותושבוּת · נרשם פעם אחת.`
- Below the gate notice, render the error when present:
```tsx
            {submitError && (
              <p className={styles.errorNote} role="alert">
                <span aria-hidden>■ </span>
                {submitError}
              </p>
            )}
```
- The confirm `NewsButton` gets `disabled={submitting}` and its label becomes `submitting ? 'רושמים את הקול…' : 'אשרו והצביעו'` for the signed-in, verified branch. The two gate labels (`התחברו והשלימו`, `אמתו תושבוּת והשלימו`) are unchanged.
- The `↳ חזרה לבחירה` back button gets `disabled={submitting}`.

**Choice step.** Replace the trust line at `:230` with exactly:
  `הקול שלכם נרשם פעם אחת ומשויך לתושב מאומת. אי אפשר לשנות אותו בדיעבד.`

**Steps and receipt.** Change `STEPS` (`:35-39`) third label from `'חתימה'` to `'רישום'`.
Replace the whole `stage === 'receipt'` section (`:313-350`) with a receipt guarded on `ballot` (not `seal`):
- kicker: `שלב 03 · רישום`
- heading: `הקול שלכם <span className={styles.red}>נרשם.</span>`
- lead: when `alreadyRecorded` is false → `הרישום הושלם. בחרתם: <strong>{selectedText}</strong>.`; when true → `כבר הצבעתם בהצבעה הזו. זה הרישום הקיים שלכם: <strong>{selectedText}</strong>.`
- `<Receipt>` with `kicker="קבלה · RECEIPT"`, `title="רישום השתתפות"`, and rows exactly:
  `{ label: 'עמדה', value: selectedText || '—' }`,
  `{ label: 'עלות', value: 'חינם' }`,
  `{ label: 'סטטוס', value: 'נרשם', strong: true }`,
  `{ label: 'מספר רישום', value: ballot.id }`
  and `footer={`הצבעה ${voteId} · ${recordedAt}`}` where
  `const recordedAt = new Date(ballot.createdAt).toLocaleString('he-IL', { dateStyle: 'short', timeStyle: 'short' });`
  — derived from the SERVER timestamp, never `new Date()`.
- trust line: `הרישום נשמר בשרת ומשויך לתושב מאומת אחד.` (no chain claim, no `נחתם`, no `BLOCK`)
- a closing action that hands control back to the page:
```tsx
            <div className={styles.actions}>
              <NewsButton
                variant="red"
                size="lg"
                className={styles.cta}
                onClick={() => onComplete(ballot.optionId)}
                trailing={<span aria-hidden>←</span>}
              >
                צפו בתוצאות
              </NewsButton>
            </div>
```

**Prop change.** `onComplete` becomes `(optionId: string) => void` in `ParticipationFlowProps` (`:30`). This is deliberate: calling it at seal time is what unmounted the receipt (`page.tsx:227` flips `showFlow` off), and passing the option id lets the results panel highlight the resident's own choice. Task 3 updates the call site.

**Docstring.** Replace the stale `:69-77` block — it still says the flow "Seals in-page; server-side recording hooks in once the participate API drops its payment-shaped contract." Write what is now true: choice → confirmation → server-recorded receipt; the ballot is persisted by `POST /api/votes/[id]/participate` before the receipt is shown; nothing here is chain-anchored.

**CSS.** Add one rule to `ParticipationFlow.module.css`, immediately after `.gateNote`, using tokens only:
```css
.errorNote {
  font-family: var(--np-font-mono);
  font-size: var(--text-sm);
  line-height: 1.45;
  color: var(--np-red);
  background: var(--np-paper-2);
  border: var(--np-rule-hair) solid var(--np-red);
  padding: var(--space-3) var(--space-4);
  margin-block-start: var(--space-4);
}
```
No hardcoded colours, sizes or spacing anywhere in this task.
  </action>
  <verify>
    <!-- The copy-honesty guard test is written in Task 3 of this plan; pointing this at it
         now makes vitest exit 1 on "No test files found", which reads as a real failure.
         This pair is self-contained and actually proves the wiring: typecheck compiles the
         rewritten component against the Task 1 module, and the Task 1 behavioural test
         (which already exists) must still pass. The grep conditions below are the
         copy-honesty proof for this task. -->
    <automated>pnpm --filter @sync/web typecheck &amp;&amp; pnpm --filter @sync/web test -- src/__tests__/services/submit-participation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @sync/web typecheck` exits 0
    - `grep -c "mockHash" "apps/web/src/app/[locale]/votes/[id]/flow/ParticipationFlow.tsx"` returns `0`
    - `grep -rc "mockHash" apps/web/src` totals `0` across the tree
    - `grep -c "SealCard" "apps/web/src/app/[locale]/votes/[id]/flow/ParticipationFlow.tsx"` returns `0`
    - `grep -c "בבלוקצ׳יין\|בבלוקצ'יין" "apps/web/src/app/[locale]/votes/[id]/flow/ParticipationFlow.tsx"` returns `0`
    - `grep -c "נחתם\|חתימה" "apps/web/src/app/[locale]/votes/[id]/flow/ParticipationFlow.tsx"` returns `0`
    - `grep -c "submitParticipation" "apps/web/src/app/[locale]/votes/[id]/flow/ParticipationFlow.tsx"` returns at least `2`
    - `grep -c "ballot.createdAt" "apps/web/src/app/[locale]/votes/[id]/flow/ParticipationFlow.tsx"` returns at least `1`
    - `grep -c "errorNote" "apps/web/src/app/[locale]/votes/[id]/flow/ParticipationFlow.module.css"` returns at least `1`
    - `grep -c "\.seal " "apps/web/src/app/[locale]/votes/[id]/flow/ParticipationFlow.module.css"` returns `0`
    - `grep -cE "#[0-9a-fA-F]{3,6}|[0-9]+px" "apps/web/src/app/[locale]/votes/[id]/flow/ParticipationFlow.module.css"` returns `0` (design tokens only)
    - `pnpm --filter @sync/web lint` reports no new errors for this file
  </acceptance_criteria>
  <done>Confirming a vote awaits a server-confirmed write; a rejection shows a Hebrew error and no receipt; the receipt shows the server's ballot id and timestamp with a status of `נרשם`, and neither `mockHash` nor any seal claim exists in the flow.</done>
</task>

<task type="auto" id="02.1-05-T3">
  <name>Task 3: Make the vote page and vote list honest, and wire the new onComplete</name>
  <read_first>
    - apps/web/src/app/[locale]/votes/[id]/page.tsx (the file being modified — lines 85-150 for state and load, 220-232 for showFlow, 418-435 for the context bullets, 455-470 for the flow wiring, 505-535 for the results panel and its footer)
    - apps/web/src/app/[locale]/votes/components/VotesList.tsx lines 260-275 (the per-ballot trust note in the casting funnel)
    - apps/web/src/app/[locale]/votes/[id]/flow/ParticipationFlow.tsx (as rewritten in Task 2 — the new `onComplete: (optionId: string) => void` signature)
    - apps/web/src/__tests__/services/dashboard-free-mvp.test.ts (the source-assertion test pattern to copy for Task 3's guard test)
  </read_first>
  <files>apps/web/src/app/[locale]/votes/[id]/page.tsx, apps/web/src/app/[locale]/votes/components/VotesList.tsx, apps/web/src/__tests__/services/participation-receipt-honesty.test.ts</files>
  <action>
**`apps/web/src/app/[locale]/votes/[id]/page.tsx`:**

1. Update the flow wiring at `:468` from `onComplete={() => setHasVoted(true)}` to:
```tsx
                  onComplete={(optionId: string) => {
                    setSelectedOption(optionId);
                    setHasVoted(true);
                  }}
```
   This both matches the new prop signature and makes the results panel's `isMine` check (`:488`) highlight the resident's own choice.

2. Replace the context bullet at `:427-430`:
```tsx
                    כל קול מאומת בזהות ובמיקום, נספר פעם אחת ונחתם בבלוקצ׳יין —
                    בלתי ניתן לשינוי או לזיוף.
```
   with exactly:
```tsx
                    כל קול מאומת בזהות ובמיקום ונספר פעם אחת בלבד — קול אחד
                    לתושב, בלי כפילויות.
```

3. Replace the post-vote confirmation at `:524` (`הצבעתכם נקלטה ונחתמה בבלוקצ׳יין`) with exactly:
```tsx
                      הצבעתכם נרשמה ונספרה
```

4. Replace the results footer at `:528-532`. The two spans currently read `מאומת · זהות + GPS` and `חתום בבלוקצ׳יין`; keep the first, and change the second to exactly `קול אחד לתושב מאומת`.

Change nothing else on this page — the certificate block, share row, timeline and knesset copy are out of scope.

**`apps/web/src/app/[locale]/votes/components/VotesList.tsx`:**

Replace the trust note at `:268-270` (`הקול שלכם ייחתם בבלוקצ׳יין. אי אפשר לשנות אותו בדיעבד.`) with exactly:
```tsx
                    הקול שלכם נרשם פעם אחת ומשויך לתושב מאומת. אי אפשר לשנות
                    אותו בדיעבד.
```
This is the same sentence the flow's choice step now shows, so the funnel reads consistently.

**`apps/web/src/__tests__/services/participation-receipt-honesty.test.ts`:**

Create it modelled on `dashboard-free-mvp.test.ts` — same header explaining why source assertions are the right tool here (no jsdom, no @testing-library; every requirement is "this string must not exist"), same `code()` helper stripping `//` and block comments before asserting.

Read and assert against four files: `app/[locale]/votes/[id]/flow/ParticipationFlow.tsx`, `app/[locale]/votes/[id]/flow/submitParticipation.ts`, `app/[locale]/votes/[id]/page.tsx`, `app/[locale]/votes/components/VotesList.tsx`.

`describe('mockHash and the fabricated seal are gone')`
- ParticipationFlow code contains no `mockHash`, no `crypto.getRandomValues`, no `SealCard`, no `18_400_000`, no `BLOCK`

`describe('the casting funnel claims no blockchain seal')`
- none of the four files contain `בבלוקצ׳יין` or `בבלוקצ'יין` (test both apostrophe variants)
- ParticipationFlow contains neither `נחתם` nor `חתימה`
- the vote page contains neither `ונחתמה` nor `חתום בבלוקצ׳יין`

`describe('the flow reaches the server before it shows a receipt')`
- ParticipationFlow code contains `submitParticipation`
- ParticipationFlow code contains `setStage('receipt')` and, in the same file, `result.status === 'rejected'`
- submitParticipation code contains the literal `/participate`
- ParticipationFlow code contains `ballot.createdAt` and does NOT contain `new Date().toLocaleString`

`describe('the receipt shows only server-backed facts')`
- ParticipationFlow contains `'נרשם'`, `'חינם'` and `'מספר רישום'`

Add a header note recording the scope boundary: chain-seal copy still exists on marketing surfaces (`TrustBar`, `Hero/ConsensusVisual`, `VotesHero`, `ArchiveHero`, `PricingContent`, `about/Mission`, sign-in / sign-up, coin, treasury, verification pages). Those are outside the casting funnel and outside this P0 — recorded, not silently ignored.
  </action>
  <verify>
    <automated>pnpm --filter @sync/web test -- src/__tests__/services/participation-receipt-honesty.test.ts &amp;&amp; pnpm --filter @sync/web test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "בבלוקצ׳יין\|בבלוקצ'יין" "apps/web/src/app/[locale]/votes/[id]/page.tsx"` returns `0`
    - `grep -c "בבלוקצ׳יין\|בבלוקצ'יין" "apps/web/src/app/[locale]/votes/components/VotesList.tsx"` returns `0`
    - `grep -c "onComplete={(optionId" "apps/web/src/app/[locale]/votes/[id]/page.tsx"` returns `1`
    - `grep -c "הצבעתכם נרשמה ונספרה" "apps/web/src/app/[locale]/votes/[id]/page.tsx"` returns `1`
    - `apps/web/src/__tests__/services/participation-receipt-honesty.test.ts` exists and `pnpm --filter @sync/web test -- src/__tests__/services/participation-receipt-honesty.test.ts` exits 0 with at least 12 passing assertions
    - `grep -c "TrustBar" apps/web/src/__tests__/services/participation-receipt-honesty.test.ts` returns at least `1` (the out-of-scope boundary is recorded)
    - `pnpm --filter @sync/web test` (full suite) exits 0
  </acceptance_criteria>
  <done>The vote page hands the chosen option back from the flow, the casting funnel makes no blockchain-seal claim anywhere, and a guard test fails if `mockHash`, `SealCard` or seal copy returns.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @sync/web test -- src/__tests__/services/submit-participation.test.ts` — exits 0
2. `pnpm --filter @sync/web test -- src/__tests__/services/participation-receipt-honesty.test.ts` — exits 0
3. `pnpm --filter @sync/web typecheck` — exits 0
4. `pnpm --filter @sync/web test` — full suite exits 0
5. `grep -rn "mockHash" apps/web/src` — no matches
6. `pnpm --filter @sync/web lint` — no new errors in the four modified source files
7. Manual (recorded in VALIDATION.md, not automatable here): complete a vote as a verified resident and confirm the receipt shows a real `user_votes.id`, a server timestamp, status `נרשם`, no SealCard and no BLOCK row.
</verification>

<success_criteria>
- `ParticipationFlow.tsx` calls `submitParticipation` and advances to the receipt only on `status === 'recorded'`
- A rejected write shows a Hebrew/RTL error, keeps the user on the confirm step, and never calls `onComplete`
- `mockHash()` is gone from the codebase; the receipt carries no SealCard, no BLOCK, no chain claim
- The receipt's id and timestamp come from the server row
- The whole casting funnel — choice step, confirm step, receipt, results panel, vote-list ballot note — makes no blockchain-seal claim
- Two automated tests cover the network contract and the copy honesty; the full web suite is green
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-participation-persistence/02.1-05-SUMMARY.md`
</output>
