---
phase: 02.1-participation-persistence
verified: 2026-08-02T14:52:09Z
status: passed
score: 5/5 must-haves verified
human_verification:
  - test: "Cast a free vote as a real, verified resident on the deployed environment (or against real Supabase, not mocks), and confirm the receipt shows a real user_votes.id, a server created_at, status נרשם, no SealCard, no BLOCK row."
    expected: "A user_votes row exists, the option's tally and the vote's participant_count both moved by exactly 1, and the UI never claims a blockchain seal."
    why_human: "All code paths, unit tests, and the Supabase schema are verified statically in this pass; no live Supabase instance or authenticated browser session was exercised end-to-end against production/staging."
---

# Phase 02.1: Participation Persistence Verification Report

**Phase Goal:** A resident's free vote is actually recorded. The participation flow reaches the server, the ballot lands in `user_votes`, the tally and participant count move, and the receipt states only what is true — no fabricated blockchain seal.
**Verified:** 2026-08-02T14:52:09Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP success criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A signed-in, residency-verified resident casting a free vote produces a `user_votes` row, an `incrementVoteOption` bump, and an updated `participant_count`. | ✓ VERIFIED | `route.ts:101-118` calls `recordUserVoteOnce({user_id, vote_id, option_id})` (no `payment_id`), then `if (created) { await incrementVoteOption(optionId); await supabaseAdmin.from('votes').update({participant_count: ...}) }`. `recordUserVoteOnce` (`db.ts:1064-1081`) does a real insert, catching Postgres `23505` (unique violation) and reading the existing row back rather than throwing. Read live source; ran the plan-02 unit suite (`participation-primitives.test.ts`, 12 tests) and plan-04 route suite (17 tests in the `participate` describe) — both green against real code, not stubs. |
| 2 | `/api/votes/[id]/participate` accepts a free-participation body with no `paymentTxId` and never returns 402 for it; remains gated on session and residency; a double submission records exactly one vote rather than a second row or a 500. | ✓ VERIFIED | Route imports only `ParticipateRequestSchema` (`{ optionId }` — confirmed in `packages/shared/src/contracts/vote.ts`); grepped route source for `402`, `503`, `paymentTxId`, `gpsCoordinates`, `qubik` — all zero. Session gate at `route.ts:34-38`; `checkVoterEligibility` gate at `:91-97` (403 `RESIDENCY_NOT_VERIFIED`/`IDENTITY_NOT_VERIFIED`). Duplicate path verified two ways: (a) `recordUserVoteOnce`'s own unit test forces a `23505` conflict and asserts `{created: false, vote: existingRow}` off the real DB-shaped mock chain; (b) the route test `'returns the existing ballot on a duplicate submit without moving the tally'` asserts `alreadyRecorded: true`, `incrementVoteOption` NOT called, and the tally not moved. **Tally-guard specifically re-read**: the `incrementVoteOption` + `participant_count` update block is textually inside `if (created)` — confirmed by direct file read, not just grep. |
| 3 | `ParticipationFlow.tsx` calls the endpoint and advances to the receipt only after a server-confirmed write; a failed or rejected write shows a Hebrew/RTL error and no seal. | ✓ VERIFIED | Read the full 377-line component. `recordVote()` (`:143-156`) does `const result = await submitParticipation(...)`; on `result.status === 'rejected'` it sets `submitError` and `return`s — stage stays `'confirm'`, `ballot` is never set, so `stage === 'receipt' && ballot` (`:325`) cannot render. The rejection branch renders `<p className={styles.errorNote} role="alert">{submitError}</p>` (`:288-293`) with Hebrew copy sourced from `PARTICIPATION_MESSAGES` in `submitParticipation.ts` (8 Hebrew strings, one per failure code). `.errorNote` CSS uses only design tokens (`var(--np-red)`, `var(--text-sm)`, etc. — no hardcoded hex/px). |
| 4 | `mockHash()` is gone and no user-facing copy claims a blockchain seal unless an actual chain write backs it. | ✓ VERIFIED | `grep -rn "mockHash" apps/web/src/` returns matches only inside `participation-receipt-honesty.test.ts` (assertions that it's absent from source) — zero occurrences in any shipped component. `SealCard` is imported nowhere under `votes/[id]/` or `votes/components/`; its only remaining use is `votes/create/page.tsx` (vote-creation flow, explicitly out of scope). Status word in the receipt is `נרשם` (recorded), not `נחתם` (sealed/signed); step 3 label is `רישום`, not `חתימה`. Ran `participation-receipt-honesty.test.ts` directly — 26/26 pass, independently confirming absence of `mockHash`, `crypto.getRandomValues`, `SealCard`, fabricated block numbers, and both apostrophe variants of `חתום בבלוקצ'יין` across `ParticipationFlow.tsx`, `submitParticipation.ts`, `page.tsx`, and `VotesList.tsx`. |
| 5 | The ₪3 legacy is reconciled: the participate route no longer mints 3 tokens or emails `amount: 3`, and `packages/shared/src/constants/index.ts` no longer leaves mobile charging ₪3. | ✓ VERIFIED | `grep -c "tokensEarned\|mintTokens\|sendPaymentReceiptEmail"` on the route returns 0. `packages/shared/src/constants/index.ts` exports `VOTE_PARTICIPATION_COST = 0` and `CREATE_VOTE_COST = 50`; `grep -rn "VOTE_COST\b"` (bare, not `CREATE_VOTE_COST`) across the whole repo returns zero non-comment, non-test-assertion hits. Mobile `vote/[id].tsx` cost copy rewritten to `"ההשתתפות חינם • כל קול נרשם ומשויך לתושב מאומת אחד"` / `"אין תשלום ואין חסמים..."` — no ₪3, no blockchain claim. |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `apps/web/src/app/api/votes/[id]/participate/route.ts` | Free-participation POST handler | ✓ VERIFIED | Full rewrite read directly; contains `recordUserVoteOnce`, `checkVoterEligibility`, `ParticipateRequestSchema`; zero `paymentTxId`/`gpsCoordinates`/`402`/`503`/`qubik`/`tokensEarned`/`: any`. |
| `apps/web/src/lib/supabase/db.ts` (`recordUserVoteOnce`) | Idempotent insert on `UNIQUE(user_id, vote_id)` | ✓ VERIFIED | Keys off SQLSTATE `23505`, reads existing row back on conflict; does not silently swallow other error codes. |
| `apps/web/src/services/verification/eligibility.ts` | Server-side gate mirroring `isEligibleToVote` | ✓ VERIFIED | `checkVoterEligibility` = `identity_score >= 40` AND (`verification_status === 'verified'` OR `completed_check_ins >= 1`) — same two-clause shape as the client's `isEligibleToVote`. Pure decision function unit-tested (12 tests, real truth table). |
| `apps/web/src/app/[locale]/votes/[id]/flow/submitParticipation.ts` | Pure, fetch-injectable network client mapping every server outcome | ✓ VERIFIED | Discriminated `ParticipationSubmission`; never trusts a malformed 200 (missing `id`/`optionId`/`createdAt` → `SERVER_ERROR`); 15 tests (`submit-participation.test.ts`), all green. |
| `apps/web/src/app/[locale]/votes/[id]/flow/ParticipationFlow.tsx` | Client flow, server-confirmed receipt | ✓ VERIFIED | `mockHash`/`sealVote`/`SealCard` fully removed; `onComplete` fires only from the receipt's own button. |
| `apps/web/src/__tests__/api/vote-participation.test.ts` | Route tests for free contract + duplicate path | ✓ VERIFIED | 31/31 tests pass (17 `participate`, 8 `verify-location`, 5 `participated` — the latter two describes untouched, both still meaningfully asserting behavior, not gutted). |
| `packages/shared/src/constants/index.ts` | `VOTE_COST` retired | ✓ VERIFIED | `VOTE_PARTICIPATION_COST = 0`; `CREATE_VOTE_COST = 50` (untouched, still the live paid path). |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `participate/route.ts` | `recordUserVoteOnce` (`@/lib/supabase/db`) | idempotent insert | ✓ WIRED | Imported and called with `{user_id, vote_id, option_id}`, no `payment_id`. |
| `participate/route.ts` | `checkVoterEligibility` (`@/services/verification/eligibility`) | server-side residency+identity gate | ✓ WIRED | Called after `getUserByGoogleId`; 403 branch returns `eligibility.message`/`eligibility.code`. |
| `participate/route.ts` | `votes.participant_count` | `supabaseAdmin` update, gated on `created` | ✓ WIRED | Confirmed the update call sits inside `if (created) { ... }` by direct source read — not just a grep count. |
| `ParticipationFlow.tsx` | `submitParticipation` | `await` in `recordVote()` | ✓ WIRED | Result is branched on `status`; `'rejected'` returns before any state that would show a receipt. |
| `ParticipationFlow.tsx` | `page.tsx`'s `onComplete` | receipt CTA `onClick` | ✓ WIRED | `onComplete(ballot.optionId)` fires only on the receipt's `"צפו בתוצאות"` button — not from `recordVote()` or any effect — so the receipt is genuinely reachable and stays mounted until the resident dismisses it. |
| `submitParticipation.ts` | `POST /api/votes/[id]/participate` | `fetch` with `{ optionId }` body | ✓ WIRED | `credentials: 'same-origin'`, body is exactly `{ optionId }`; response mapped through explicit status-code switch, no raw server string surfaced to the user. |

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|---|---|---|---|---|
| VOTE-01 | 01, 02, 04 | Route accepts free body, no 402, gated on session+residency | ✓ SATISFIED | Verified directly in route source and route test suite (31/31 green). Not over-claimed — plan 01's SUMMARY explicitly self-corrected an initial premature `requirements mark-complete` for this ID and reverted it to Pending until plan 04 actually shipped the route-level behavior; REQUIREMENTS.md's final "Complete" annotation correctly attributes it to plans 01+02+04, matching what each plan actually contributed. |
| VOTE-02 | 04 (server idempotency), 05 (client flow) | Flow reaches receipt only on confirmed write; rejected write shows Hebrew error; double submit = one vote | ✓ SATISFIED | Server idempotency confirmed in `db.ts`/route; client gating confirmed by full read of `ParticipationFlow.tsx` (`recordVote`'s early return on rejection) plus `participation-receipt-honesty.test.ts` case `'only advances to the receipt stage via setStage(\'receipt\')'` and `'branches on a rejected result before the receipt can render'`. |
| VOTE-03 | 02, 04 | `user_votes` row + `incrementVoteOption` + `participant_count` for a free vote | ✓ SATISFIED | Confirmed in route source, gated correctly on `created`. |
| VOTE-04 | 05 | `mockHash()` removed; no unbacked blockchain-seal copy | ✓ SATISFIED | Confirmed zero `mockHash` in shipped code, zero `SealCard` in the casting funnel, `נחתם`/`חתום בבלוקצ׳יין` absent from `ParticipationFlow.tsx`, `submitParticipation.ts`, `votes/[id]/page.tsx`, `VotesList.tsx`. |
| VOTE-05 | 01, 03, 04 | ₪3 legacy reconciled across route + shared constants + mobile | ✓ SATISFIED | `VOTE_COST` retired repo-wide; route mints no tokens, emails no receipt; mobile copy no longer quotes ₪3. **Caveat (not a VOTE-05 failure, but a related half-state — see Anti-Patterns below):** mobile's `handleVote` button still routes to a payment checkout screen despite the copy now saying the vote is free. |

No orphaned requirements — REQUIREMENTS.md maps exactly VOTE-01..05 to Phase 02.1, and all five appear in at least one plan's frontmatter `requirements` field.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `apps/mobile/app/vote/[id].tsx` | 154-174, 339-345 | Copy/behavior contradiction: cost line now reads "ההשתתפות חינם... אין תשלום ואין חסמים" (free, no payment, no barriers) but `handleVote` still `router.push({ pathname: '/payment/checkout', params: { type: 'vote', ... } })` | ⚠️ Warning | Explicitly out of this phase's scope per `02.1-CONTEXT.md` ("Does NOT deliver: any payment code... any change to who is allowed to vote") and explicitly flagged as deliberately deferred in plan 03's SUMMARY. **Not newly broken by this phase in the sense of "previously worked, now doesn't"** — pressing the mobile vote button was already going to fail before this phase, independently of it: `handleVote` passes `type: 'vote'`, but `payment/checkout.tsx` and `POST /api/payments/create` only recognize `'vote_participation'`/`'vote_creation'` (`create/route.ts:39`), a pre-existing, unrelated type-string mismatch that predates this phase (confirmed via `git log` — the `type: 'vote'` line is untouched by any 02.1 commit). What this phase *did* change is the copy directly above that broken button, which now actively promises "no payment" immediately before a still-nonfunctional payment button — a UX inconsistency worth a follow-up mobile plan, not a P0 regression this phase introduced. |
| `apps/web/src/app/[locale]/votes/[id]/page.tsx` | 140-149 | Stale `?payment=success` URL-param handler (`setHasVoted(true)` on that query string) left over from the old paid-participation flow | ℹ️ Info | Nothing in the current casting funnel constructs a `?payment=success` URL anymore (`ParticipationFlow` never redirects there), so this branch is now dead code rather than an active bug. Harmless; worth a cleanup pass but not a phase-goal blocker. |

No blocker-level anti-patterns found in any file this phase shipped or is responsible for (route, `ParticipationFlow.tsx`, `submitParticipation.ts`, `db.ts`, `eligibility.ts`).

### Scope Boundary Check

Chain-seal marketing copy deliberately remains outside the casting funnel, exactly as documented:
- `apps/web/src/app/[locale]/votes/archive/components/ArchiveHero.tsx:25` — `"כל רשומה חתומה בבלוקצ׳יין"`
- `apps/web/src/app/[locale]/votes/components/VotesHero.tsx:39` — `"חתום בבלוקצ׳יין"`
- `apps/web/src/app/[locale]/votes/create/page.tsx:309,453` — `"תיחתם בבלוקצ׳יין"`, `"חתום בבלוקצ׳יין"`

Confirmed by direct read: none of these three files sit inside the casting funnel (`ParticipationFlow.tsx`, `votes/[id]/page.tsx`, `VotesList.tsx`), which are independently confirmed clean of any `חתום`/`בבלוקצ׳יין`/`נחתם` string. `participation-receipt-honesty.test.ts` itself records this boundary in an explicit test (`'records that TrustBar and other marketing surfaces still carry chain-seal copy, unasserted here'`), so the boundary is documented in the test suite, not just in prose.

One additional surface checked beyond the task's list: `apps/web/src/components/certificate/CertificateCard.tsx`, rendered on `votes/[id]/page.tsx` for ended votes, uses `נחתם בשרשרת` — but this is a genuinely separate, real NFT-certificate feature (`voteId`, `mintTxHash`, `mintAddress` populated from `/api/user/nfts`, not client-fabricated), shown only after a vote resolves, and outside the plan's declared scope. Not flagged as a violation.

### Test Suite Integrity

Independently re-ran (not trusting the SUMMARY claims):
- `pnpm --filter @sync/web test -- src/__tests__/api/vote-participation.test.ts` → 31/31 pass (17 participate, 8 verify-location, 5 participated — the latter two describes confirmed byte-present with real assertions, not stubs or `it.skip`).
- `pnpm --filter @sync/web test` (full suite) → **62 files / 755 tests, all green** — matches the orchestrator's claim exactly.
- `pnpm --filter @sync/web test -- src/__tests__/services/participation-receipt-honesty.test.ts src/__tests__/services/submit-participation.test.ts` → 41/41 pass.
- `pnpm --filter @sync/web typecheck` → clean.
- `pnpm --filter @sync/mobile typecheck` → clean.
- `pnpm --filter @sync/shared typecheck` → clean.
- `pnpm --filter @sync/web lint` → 0 errors, 2 pre-existing unrelated warnings (`postcss.config.mjs`, `worker.ts`) — matches the orchestrator's claim.
- `grep -rn "mockHash" apps/web/src/` → present only inside the guard test asserting its absence.
- `grep -rln "SealCard" apps/web/src/app/[locale]/votes/` → only `votes/create/page.tsx` (out of scope, unchanged).

### Human Verification Required

1. **Live end-to-end vote cast**
   **Test:** As a real, verified resident (or a seeded equivalent) on a deployed or locally-running environment backed by an actual Supabase instance, cast a free vote through the full UI: choose an option, confirm, and observe the receipt.
   **Expected:** A `user_votes` row exists with the resident's `user_id`/`vote_id`/`option_id`, the option's tally count and the vote's `participant_count` both increased by exactly 1, the receipt shows a real `user_votes.id` under "מספר רישום" and a plausible recent timestamp, status reads `נרשם`, and there is no `SealCard`/`BLOCK` anywhere on the page.
   **Why human:** This verification pass confirmed the code paths, the Postgres unique-constraint handling logic, and all unit/integration tests running against mocked Supabase clients — but did not execute against a live database or an authenticated browser session, which is the only way to catch schema drift, RLS policy issues, or environment-specific auth wiring that unit tests with mocked `supabaseAdmin` cannot surface.

## Gaps Summary

No gaps found against the phase's five ROADMAP success criteria — all five are backed by code that was read directly (not merely grepped) and by tests that were re-run independently rather than trusted from the SUMMARYs. The tally-guard specifically named in the verification brief (`incrementVoteOption`/`participant_count` running only when `created` is true) was confirmed by direct source inspection, not just a passing grep. The previously-unreachable-receipt bug (`onComplete()` firing synchronously and unmounting the flow before the receipt could render) is genuinely fixed — `onComplete` now fires only from the receipt's own CTA button.

One item is worth carrying forward as a follow-up, not a phase failure: mobile's vote screen now tells residents voting is free immediately above a button that still opens a payment checkout. That button was already non-functional before this phase (an unrelated `type: 'vote'` vs `'vote_participation'` string mismatch predating 02.1), and rewiring mobile to call `votesApi.participate()` was explicitly out of this P0's scope per `02.1-CONTEXT.md`. It is documented here, in plan 03's own SUMMARY, and belongs on the Phase 3 re-scope backlog.

---
*Verified: 2026-08-02T14:52:09Z*
*Verifier: Claude (gsd-verifier)*
