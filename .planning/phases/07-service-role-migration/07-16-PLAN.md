---
phase: 07-service-role-migration
plan: 16
type: execute
wave: 6
depends_on: ["07-15"]
files_modified:
  - apps/web/src/__tests__/rls/coverage.test.ts
  - apps/web/docs/SERVICE-ROLE-MIGRATION.md
  - .planning/phases/07-service-role-migration/07-VALIDATION.md
autonomous: false
requirements: [MIG-01, MIG-02, MIG-03, MIG-04]

must_haves:
  truths:
    - "All 25 pre-existing RLS-enabled tables have a test proving cross-user reads are denied, and a test asserts that ledger rather than a human reading it"
    - "The full RLS suite runs green against a live database with a non-zero test count"
    - "The app still works for a real signed-in resident and a real anonymous visitor"
    - "The phase's outcome is recorded with numbers a later reader can re-derive"
  artifacts:
    - path: "apps/web/src/__tests__/rls/coverage.test.ts"
      provides: "The MIG-04 completeness check — every table in RLS_TABLES is asserted somewhere in the rls suite"
      contains: "RLS_TABLES"
    - path: "apps/web/docs/SERVICE-ROLE-MIGRATION.md"
      provides: "The phase record: before/after counts, what moved, what stayed and why, and what was deferred"
      contains: "MIG-04"
      min_lines: 90
  key_links:
    - from: "apps/web/src/__tests__/rls/coverage.test.ts"
      to: "apps/web/src/__tests__/rls/harness.ts"
      via: "reads RLS_TABLES and asserts each name appears in some *.rls.test.ts file"
      pattern: "RLS_TABLES"
---

<objective>
Close the phase. Prove the MIG-04 coverage ledger mechanically, run the whole RLS suite green
against a live database, confirm the app still works for both a signed-in resident and an anonymous
visitor, and record the outcome with numbers.

Purpose: MIG-04 says migration is proven, not asserted. Seven slices each proved their own tables;
nothing yet proves the *set* is complete. A table can be dropped from the ledger silently, and a
suite that skips reports a green exit code with zero tests. This plan closes both gaps and then
checks the one thing no test can: that a person can still use the site.

Output: one coverage test, one phase record, one human verification.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-service-role-migration/07-CONTEXT.md
@.planning/phases/07-service-role-migration/07-VALIDATION.md
@.planning/phases/07-service-role-migration/07-15-SUMMARY.md

<interfaces>
<!-- Read every 07-NN-SUMMARY.md before writing the record. The numbers come from them, not from here. -->

**The coverage ledger** (`07-VALIDATION.md`), which this plan turns into an assertion:

| Test file | Tables |
|---|---|
| `role-tables.rls.test.ts` (Phase 5) | role_grants, community_manager_applications, role_grant_events |
| `identity.rls.test.ts` (07-08) | users, social_proofs, identity_documents, identity_document_events |
| `verification.rls.test.ts` (07-09) | verification_runs, verification_schedule, verification_attempts, phone_verifications |
| `ballots.rls.test.ts` (07-10) | user_votes, vote_options |
| `catalogue.rls.test.ts` (07-11) | votes, vote_sources, knesset_items, knesset_rankings, municipalities |
| `payments.rls.test.ts` (07-12) | payments, entitlements |
| `treasury.rls.test.ts` (07-13) | treasury, treasury_transactions, issue_coins, issue_coin_holdings |
| `system-tables.rls.test.ts` (07-14) | vote_nfts, push_tokens, webhook_events, merch_orders |

25 pre-existing tables plus Phase 5's 3 = the 28 entries in `RLS_TABLES` (plan 07-03).

**The baseline numbers**, from `07-CONTEXT.md`, re-counted 2026-08-03:

- 25 RLS-enabled tables, 36 live distinct policies (39 `CREATE POLICY` statements), 14 `USING (true)`
- 30 files referencing `supabaseAdmin` — 19 source, 11 test
- `db.ts`: 2441 lines, 114 exports, 112 `supabaseAdmin` references
- 7 API routes using it directly

**Drift from the roadmap** that the record must state plainly, so a later reader is not confused by
the roadmap's numbers: it says 39 policies (statement count, not live count), 15 `USING (true)` (the
15th is a comment), 27 files (now 30), and 2404 lines / 112 exports (actually 2441 / 114). MIG-02 is
written against "112 exports"; the real figure is 114.

**The two findings** this phase closed: FINDING-1 (`webhook_events` policy defaulting to `TO PUBLIC`
— anon read *and* write of the replay guard) and FINDING-2 (`vote_nfts` `SELECT USING (true)` over a
table carrying `user_id` and `wallet_address`). Both now have permanent guards in
`system-tables.rls.test.ts`.

**Suite behaviour.** The RLS suite skips loudly with a non-zero exit of 0 when any of
`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` or
`SUPABASE_JWT_SECRET` is missing. **A run reporting 0 tests is a skip, not a pass.** This plan's
whole point is checking the count, not the exit code.

**Secret discipline.** Reference secrets by name only. No value appears in any command output,
document, commit or summary.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assert the coverage ledger mechanically</name>
  <read_first>
    - apps/web/src/__tests__/rls/harness.ts (RLS_TABLES — the 28-entry source of truth)
    - .planning/phases/07-service-role-migration/07-VALIDATION.md (the table-to-file ledger)
    - apps/web/src/__tests__/lib/privileged-access.test.ts (created by plan 07-15 — the node:fs tree-walking pattern to reuse)
  </read_first>
  <files>apps/web/src/__tests__/rls/coverage.test.ts</files>
  <action>
Create `apps/web/src/__tests__/rls/coverage.test.ts`. Note the filename: it **is** a `.test.ts` and
is collected by vitest, but unlike its siblings it needs no database — it reads files, so it runs
everywhere including credential-free CI. That is deliberate: the completeness check should never be
one of the things that skips.

```ts
/**
 * MIG-04 completeness. Each *.rls.test.ts file proves its own tables; nothing
 * proved the set was complete. A table can be added to RLS_TABLES, or dropped
 * from the ledger, with every other test still green.
 *
 * This test needs no database — it reads the suite directory from disk. It must
 * stay that way, so the completeness check is never one of the things that
 * skips.
 */
import { readdirSync, readFileSync } from 'node:fs';
import { RLS_TABLES } from './harness';
```

Three assertions:

1. **Every table in `RLS_TABLES` is named in at least one `*.rls.test.ts` file.** Read every file in
   `apps/web/src/__tests__/rls/` matching `*.rls.test.ts`, concatenate, and check each table name
   appears. The failure message must list the uncovered tables by name and point at
   `07-VALIDATION.md`'s ledger.
2. **`RLS_TABLES` has exactly 28 entries, and every one is a real RLS-enabled table.** Read
   `supabase/migrations/` for `ENABLE ROW LEVEL SECURITY` statements, extract the table names, and
   assert set equality with `RLS_TABLES`. This catches both directions: a table given RLS in a new
   migration but never added to the harness, and a harness entry for a table that no longer has RLS.
   Resolve the migrations directory relative to the repo root so the test works from `apps/web` and
   from the root.
3. **Every `*.rls.test.ts` uses `describeRls`, not bare `describe`.** A bare `describe` in an RLS file
   fails the credential-free full-suite run, and that failure is confusing enough that catching it
   here is worth three lines.

Match table names on a word boundary so `votes` does not match inside `user_votes` — that false
positive would let the most important table in the phase pass as covered when it is not.

Do not import any `*.rls.test.ts` file; read them as text. Importing a vitest suite from another
suite registers its tests twice.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; npx vitest run src/__tests__/rls/coverage.test.ts &amp;&amp; cd ../.. &amp;&amp; pnpm --filter @sync/web typecheck &amp;&amp; pnpm --filter @sync/web test</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/__tests__/rls/coverage.test.ts` exists and imports only `node:fs`, vitest and `./harness`
    - It passes **without** database credentials — it is not wrapped in `describeRls`
    - It asserts all three properties above
    - Table-name matching uses a word boundary (`votes` does not match inside `user_votes`)
    - Failure messages name the uncovered or extra tables
    - It passes: all 28 `RLS_TABLES` entries are covered, and `RLS_TABLES` equals the set of RLS-enabled tables in the migrations
    - `pnpm --filter @sync/web test` exits 0
  </acceptance_criteria>
  <done>The coverage ledger is a passing assertion rather than a table someone has to read, so a table cannot be added or dropped without the build noticing.</done>
</task>

<task type="auto">
  <name>Task 2: Record the phase with numbers</name>
  <read_first>
    - every .planning/phases/07-service-role-migration/07-*-SUMMARY.md (the numbers come from these; do not re-derive what a slice already measured)
    - apps/web/docs/RLS-POLICY-AUDIT.md and apps/web/docs/DB-ACCESS-CLASSIFICATION.md and apps/web/docs/PRIVILEGED-ACCESS.md (the three artefacts this record indexes)
    - .planning/ROADMAP.md Phase 7 (the four success criteria this record must answer one by one)
    - .planning/phases/07-service-role-migration/07-CONTEXT.md (the baseline counts and the recorded drift)
  </read_first>
  <files>apps/web/docs/SERVICE-ROLE-MIGRATION.md, .planning/phases/07-service-role-migration/07-VALIDATION.md</files>
  <action>
**a. Write `apps/web/docs/SERVICE-ROLE-MIGRATION.md`** — the record a person reads in a year when
they want to know why a particular query uses the client it uses.

*Section 1 — What this phase was, and was not.* Two paragraphs. The goal was never zero
`supabaseAdmin` uses; webhooks, cron routes, minting and notification fan-out have no session and
legitimately require privileged access. The change is that privileged access is now a visible,
justified exception. Say it plainly enough that nobody later "finishes the job" by breaking a
webhook.

*Section 2 — Before and after, in numbers.* A table with columns Before / After / Where measured:

| Metric | Before | After | Source |
|---|---|---|---|
| RLS-enabled tables | 25 | 25 | `grep -rh "ENABLE ROW LEVEL SECURITY" supabase/migrations/` |
| Live distinct policies | 36 | ? | `RLS-POLICY-AUDIT.md` |
| `USING (true)` policies | 14 | ? | same |
| Source files importing `supabaseAdmin` | 19 | ? | `PRIVILEGED-ACCESS.md` allow-list |
| `supabaseAdmin` refs inside `db/` | 112 | ? | `grep -ch supabaseAdmin apps/web/src/lib/supabase/db/*.ts` |
| `db.ts` | 2441 lines, 1 file | 15 modules + barrel | `07-06-SUMMARY.md` |
| Exports on `createUserScopedClient` | 0 | ? | slice summaries |
| Exports on `createAnonClient` | 0 | ? | slice summaries |
| Tables with an RLS test | 3 (Phase 5) | 28 | `coverage.test.ts` |

Fill every `?` from the summaries. State the drift from the roadmap's figures — 39 vs 36 policies,
15 vs 14 `USING (true)`, 27 vs 30 files, 2404/112 vs 2441/114 — so the next reader trusts the newer
number and knows why they differ.

*Section 3 — The two findings.* FINDING-1 and FINDING-2: what was wrong, what an anon-key holder
could do, which migration closed it, and which test guards it permanently. These are the phase's
concrete security outcomes and deserve to be findable without reading a planning directory.

*Section 4 — The three clients.* `supabaseAdmin` / `createUserScopedClient` / `createAnonClient`,
one line each on posture and when to use it, and a pointer to `DB-ACCESS-CLASSIFICATION.md` for the
per-export decisions.

*Section 5 — Answering the roadmap's four success criteria*, one at a time, each with the command
or document that proves it.

*Section 6 — Deferred.* Write policies at scale (decision D2), per-domain privileged clients
(`webhookDb`, `mintDb`), pinning `@supabase/supabase-js` to `^2.90.0` (the whole transport depends on
`accessToken` existing, and `package.json` still declares `^2.39.0` while the lockfile resolves
2.90.1), and any export a slice classified `dead`. Each with one sentence on why it was left.

**b. Update `07-VALIDATION.md`.** Fill the Status column of the per-requirement map with the actual
command results. Tick the sign-off checklist. Set `nyquist_compliant: true` in the frontmatter only
if every task in the phase had an `<automated>` verify command that was actually run — if not, say
which did not and leave it false. Do not tick a box you did not verify; a validation document that
lies is worse than one that admits a gap.
  </action>
  <verify>
    <automated>test -f apps/web/docs/SERVICE-ROLE-MIGRATION.md &amp;&amp; grep -q "FINDING-1" apps/web/docs/SERVICE-ROLE-MIGRATION.md &amp;&amp; grep -q "createAnonClient" apps/web/docs/SERVICE-ROLE-MIGRATION.md &amp;&amp; pnpm --filter @sync/web test</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/docs/SERVICE-ROLE-MIGRATION.md` exists and is at least 90 lines
    - Section 2's table has no `?` left — every after-number is filled from a slice summary
    - The roadmap drift is stated explicitly (36 vs 39, 14 vs 15, 30 vs 27, 2441/114 vs 2404/112)
    - FINDING-1 and FINDING-2 each name their migration and their guarding test
    - All three clients are described with their RLS posture
    - Each of the roadmap's four Phase 7 success criteria is answered with a command or a document
    - Section 6 lists the deferred items including the `@supabase/supabase-js` version pin
    - `07-VALIDATION.md`'s status column is filled and its checklist reflects what was actually run
    - `pnpm --filter @sync/web test` exits 0
  </acceptance_criteria>
  <done>A reader in a year can answer "why does this query use that client" from one document, and the numbers in it can be re-derived from the commands beside them.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: The full suite live, and the app still works</name>
  <files>apps/web/docs/SERVICE-ROLE-MIGRATION.md</files>
  <what-built>
    Phase 7 is complete pending this check. What was automated:

    - `db.ts` (2441 lines, 114 exports) is 15 domain modules behind `db/index.ts`; all 62 importers
      were unchanged.
    - Every export was classified user-initiated or system in
      `apps/web/docs/DB-ACCESS-CLASSIFICATION.md`; user-owned reads moved to
      `createUserScopedClient`, public reads to the new `createAnonClient`, and the rest stayed
      privileged with a written reason.
    - All 25 pre-existing RLS-enabled tables were audited in `apps/web/docs/RLS-POLICY-AUDIT.md`;
      each of the 14 `USING (true)` policies got a keep-or-replace verdict.
    - Two live findings were closed: `webhook_events` was anon read/write because its only policy
      omitted its `TO` clause (FINDING-1), and `vote_nfts` published `user_id` and `wallet_address`
      to the anon key (FINDING-2). Both now have permanent guards in `system-tables.rls.test.ts`.
    - Eight `*.rls.test.ts` files cover 28 tables; `coverage.test.ts` asserts the set is complete;
      `privileged-access.test.ts` fails CI on an unjustified `supabaseAdmin` import.
  </what-built>
  <how-to-verify>
    Two checks. The first is the phase's MIG-04 gate; the second is the one no test can make.

    **1. The full RLS suite, live, with a non-zero count.**

    ```bash
    cd apps/web && set -a && . ./.dev.vars && set +a && npx vitest run src/__tests__/rls
    ```

    Read the **count**, not the exit code. A green run reporting 0 tests is a skip — it means a
    credential is missing, not that the phase passed. Expect roughly 55–70 passing tests across nine
    files (eight `*.rls.test.ts` plus `coverage.test.ts`). Confirm every file reports tests; a file
    reporting zero while others pass means its `beforeAll` seeded nothing.

    Then the credential-free run, which must also be green:

    ```bash
    pnpm --filter @sync/web test
    ```

    **2. The app still works, for both kinds of visitor.** Run `pnpm dev --filter @sync/web`.

    *As an anonymous visitor* (use a private window — no session), in Hebrew/RTL:
    - `/he` — the home feed lists votes
    - `/he/knesset` — the Knesset feed renders with items and rankings
    - `/he/municipality/<slug>` — the council page renders with its metrics, not an empty shell
    - `/he/coin` — token/trending surfaces render
    - the public NFT gallery surface — it must show what it showed before, **minus** any holder
      identity

    *As a signed-in resident:*
    - sign in
    - `/he/dashboard` — your NFTs and stats appear, not an empty state
    - your participation history — your past votes appear
    - `/he/settings/profile` and `/he/settings/notifications` — your own data loads, and a change
      saves
    - cast a free vote on an open vote — the receipt appears with a real recorded ballot id

    **A blank list or an empty state where content used to be is the failure mode this phase can
    produce**, because a read moved to a client whose policies do not match returns `200 []` rather
    than an error. If you see one, note the exact page and the `07-NN` slice that owns it — the
    slice summaries list which functions each page calls — and report rather than guessing.
  </how-to-verify>
  <action>
Human-executed. Run the two checks in `<how-to-verify>` above and record the results in a
"Verification" section of `apps/web/docs/SERVICE-ROLE-MIGRATION.md`: the per-file RLS test counts,
the credential-free suite count, and the list of pages walked with their outcome.

Record the **count**, not the exit code. A green run reporting 0 tests is a skip. A file reporting
zero tests while its siblings pass means its `beforeAll` seeded nothing, and that file's assertions
are vacuous.

If a page comes back empty, name it and the `07-NN` slice that owns it — each slice summary lists
which functions its pages call — and report rather than reverting a client by guess.

Never paste a key into the document or the terminal transcript.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; set -a &amp;&amp; . ./.dev.vars &amp;&amp; set +a &amp;&amp; npx vitest run src/__tests__/rls &amp;&amp; cd ../.. &amp;&amp; pnpm --filter @sync/web test</automated>
  </verify>
  <done>The RLS suite is green against a live database with a non-zero count and every file reporting tests; the credential-free suite is green; an anonymous visitor and a signed-in resident both completed their walkthrough with no empty surface; all of it is recorded in SERVICE-ROLE-MIGRATION.md.</done>
  <resume-signal>Type "approved" with the observed RLS test count, or name the exact page that came back empty and which slice owns it.</resume-signal>
</task>

</tasks>

<verification>
1. `cd apps/web && set -a && . ./.dev.vars && set +a && npx vitest run src/__tests__/rls` — green with a **non-zero** count, every file reporting tests.
2. `pnpm --filter @sync/web test` — green in a credential-free environment.
3. `cd apps/web && npx vitest run src/__tests__/rls/coverage.test.ts src/__tests__/lib/privileged-access.test.ts` — both pass.
4. `pnpm --filter @sync/web typecheck` and `lint` exit 0 (lint baseline: 2 pre-existing warnings).
5. `apps/web/docs/SERVICE-ROLE-MIGRATION.md` answers each of the roadmap's four Phase 7 success criteria with a command or a document.
6. The human checkpoint returned "approved" with a recorded test count.
7. No secret value appears in any output, document or commit.
</verification>

<success_criteria>
- All 25 pre-existing RLS-enabled tables have a cross-user denial test, and a passing test asserts the set is complete rather than a human checking a table.
- The RLS suite is green against a real database with a count, not an exit code.
- Both findings have permanent guards, so the one-time manual proof at plan 07-07 becomes a build failure if it regresses.
- A real anonymous visitor and a real signed-in resident can both still use the site — the failure mode this phase can produce is silent emptiness, and it was checked by a person.
- The phase's outcome is recorded with numbers a later reader can re-derive from the commands beside them.
</success_criteria>

<output>
After completion, create `.planning/phases/07-service-role-migration/07-16-SUMMARY.md`.
Record: the observed RLS suite count per file; the credential-free suite count; every number in
`SERVICE-ROLE-MIGRATION.md`'s before/after table; the checkpoint result including any page that came
back empty and how it was resolved; whether `nyquist_compliant` was set true and why; and the
deferred list handed forward — particularly the `@supabase/supabase-js` version pin, which the whole
transport depends on.
</output>
