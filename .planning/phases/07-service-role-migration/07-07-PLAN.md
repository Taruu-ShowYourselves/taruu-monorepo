---
phase: 07-service-role-migration
plan: 07
type: execute
wave: 3
depends_on: ["07-03", "07-05"]
files_modified:
  - apps/web/docs/RLS-POLICY-AUDIT.md
autonomous: false
requirements: [MIG-01, MIG-04]

must_haves:
  truths:
    - "The Phase 5 gate is confirmed clear before anything is applied — secret set, transport migrations live, HS256 valid"
    - "Both Phase 7 migrations are applied to the live database, in order"
    - "The anon key can no longer read or write webhook_events, proven against the live project"
    - "The anon key can no longer read vote_nfts.user_id or wallet_address, proven against the live project"
    - "The Phase 5 RLS suite is green with a non-zero test count after the apply"
  artifacts:
    - path: "apps/web/docs/RLS-POLICY-AUDIT.md"
      provides: "An Applied section recording what was applied, when, against which project ref, and the observed suite count"
      contains: "Applied"
  key_links:
    - from: "supabase/migrations/20260803000002_rls_policy_audit.sql"
      to: "the live Supabase project"
      via: "Supabase Management API DDL apply"
      pattern: "20260803000002"
---

<objective>
Apply the phase's two migrations to the live database and prove, against that database, that the two
findings are closed — then unblock wave 4.

Purpose: everything after this plan migrates reads onto RLS. A read switched to the user-scoped
client with no matching policy returns **200 and an empty array**, not an error. Seven parallel
slices built on unapplied policies would each produce a green-looking test suite over a silently
broken app. This plan is the wall between "policies decided" and "routes migrated."

Output: two applied migrations, four live proofs, and an Applied record in the audit document.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-service-role-migration/07-CONTEXT.md
@.planning/phases/07-service-role-migration/07-VALIDATION.md
@.planning/phases/07-service-role-migration/07-01-SUMMARY.md
@.planning/phases/07-service-role-migration/07-05-SUMMARY.md

<interfaces>
<!-- Environment reality, verified across Phase 5. -->

There is **no** `supabase db push` script and no local Postgres — `docker info` fails on this
machine. DDL is applied through the **Supabase Management API** with a token from the macOS
keychain. Phase 5 plan 05-09 Task 1 is the established procedure; follow it rather than inventing
one, and read `.planning/phases/05-rbac-admin-review/05-09-SUMMARY.md` if it exists for what
actually worked.

**Migration apply order — all four, oldest first.** The first two are Phase 5's and must already be
applied; the last two are this phase's:

```
20260802000001_rls_transport.sql                (Phase 5 — redefines public.user_id(), drops set_claim)
20260802000002_role_grants_and_applications.sql (Phase 5 — depends on the above)
20260803000001_webhook_events_rls_hotfix.sql    (Phase 7 plan 07-01 — FINDING-1)
20260803000002_rls_policy_audit.sql             (Phase 7 plan 07-05 — FINDING-2 + required additions)
```

**The Phase 5 gate, from `05-01-SUMMARY.md` → Blockers.** All must hold before applying anything:

1. `SUPABASE_JWT_SECRET` is set locally (in `apps/web/.dev.vars`) and as a Cloudflare Worker secret.
   It exists only in the Supabase dashboard: Project Settings → API → JWT Settings → JWT Secret. It
   is **not** the anon key, **not** the service-role key, and **not** `JWT_SECRET`.
2. The two Phase 5 migrations are applied.
3. The project still uses **HS256** legacy JWT signing. Projects migrated to asymmetric signing keys
   have the legacy secret disabled; if this project is one of them, `mintSupabaseAccessToken` cannot
   produce a token PostgREST accepts, and **the entire phase has no transport**. Stop-and-report
   condition, not a workaround condition.
4. `cd apps/web && set -a && . ./.dev.vars && set +a && npx vitest run src/__tests__/rls` reports a
   **non-zero** passing count. Zero tests is a skip, not a pass.

**The two findings, and what proves each closed:**

- FINDING-1 — `webhook_events` had `FOR ALL USING (true) WITH CHECK (true)` with no `TO` clause,
  hence `TO PUBLIC`. Proof: an anon-key select returns zero rows, and an anon-key insert is rejected.
- FINDING-2 — `vote_nfts` had `FOR SELECT USING (true)` over a table carrying `user_id` and
  `wallet_address`. Proof: an anon-key select of those columns returns zero rows, while whatever
  public path plan 07-05 chose (projection view or privileged aggregate) still works.

**Secret discipline.** Reference every secret by name. Never echo, `cat`, log, or paste a value.
When a command would print one, redirect or redact. `SUPABASE_JWT_SECRET` in particular must never
appear in a terminal transcript, a summary, or a commit.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preflight — confirm the Phase 5 gate is actually clear</name>
  <read_first>
    - .planning/phases/05-rbac-admin-review/05-01-SUMMARY.md (the three blockers, verbatim)
    - .planning/phases/05-rbac-admin-review/05-09-SUMMARY.md if it exists (whether the gate was cleared, and how the DDL was applied)
    - apps/web/docs/RLS-TESTING.md (the exact suite command and the 0-tests-is-not-a-pass warning)
    - apps/web/src/lib/supabase/user-token.ts (getSupabaseJwtSecret throws when the var is unset — the fastest way to detect blocker 1)
  </read_first>
  <files>apps/web/docs/RLS-POLICY-AUDIT.md</files>
  <action>
Check the gate before touching the database. Do not "apply and see."

**1. Is `SUPABASE_JWT_SECRET` set?** Check presence only, never the value:

```bash
cd apps/web && set -a && . ./.dev.vars && set +a && \
  { [ -n "$SUPABASE_JWT_SECRET" ] && echo "SUPABASE_JWT_SECRET: set"; } || echo "SUPABASE_JWT_SECRET: MISSING"
```

Also confirm the Worker secret exists by name — `wrangler secret list` prints names, not values. If
either is missing: **STOP.** Report which one, and that the value can only come from the Supabase
dashboard. Do not proceed and do not improvise a value.

**2. Are the two Phase 5 migrations applied?** Query the live project for the objects they create:
`public.user_id()` should be JWT-only with no `app.current_user_id` fallback, `set_claim` should not
exist, and the three Phase 5 tables should exist with their six policies. If they are not applied:
**STOP.** Phase 5 plan 05-09 owns that apply, not this plan.

**3. Does HS256 still work?** The decisive test, and the cheapest — run the Phase 5 RLS suite:

```bash
cd apps/web && set -a && . ./.dev.vars && set +a && npx vitest run src/__tests__/rls
```

- Non-zero passing count → the gate is clear; record the count.
- **Zero tests** → a credential is missing; go back to step 1.
- Failures on the `rpc('user_id')` probe returning null under a minted token → the token is being
  rejected, which is almost certainly the asymmetric-signing-key case. **STOP and report**: RLS-01
  as built does not apply to this project and the phase needs a JWKS/RS256 approach before it can
  continue. Do not attempt a workaround here.

**4. Record the preflight.** Add an "Applied" section to `apps/web/docs/RLS-POLICY-AUDIT.md` with
the date, the project ref (the ref is not a secret; the keys are), which of the four gate conditions
held, and the observed suite count. Leave the apply rows blank for Task 2.

Apply nothing in this task.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; set -a &amp;&amp; . ./.dev.vars &amp;&amp; set +a &amp;&amp; test -n "$SUPABASE_JWT_SECRET" &amp;&amp; npx vitest run src/__tests__/rls</automated>
  </verify>
  <acceptance_criteria>
    - `SUPABASE_JWT_SECRET` is confirmed present locally and as a Worker secret, **by name only** — no value appears in any output
    - Both Phase 5 migrations are confirmed applied to the live project
    - `npx vitest run src/__tests__/rls` reports a **non-zero** passing count, and the count is written down
    - `apps/web/docs/RLS-POLICY-AUDIT.md` has an "Applied" section recording the preflight result and the project ref
    - If any gate condition failed, execution STOPPED and reported rather than proceeding
  </acceptance_criteria>
  <done>The transport is proven working against the live database before a single policy is changed.</done>
</task>

<task type="auto">
  <name>Task 2: Apply both Phase 7 migrations and read the catalog back</name>
  <read_first>
    - supabase/migrations/20260803000001_webhook_events_rls_hotfix.sql (plan 07-01 — apply first)
    - supabase/migrations/20260803000002_rls_policy_audit.sql (plan 07-05 — apply second; its `-- rollback:` comments are your undo path)
    - .planning/phases/05-rbac-admin-review/05-09-SUMMARY.md if it exists (the Management API apply procedure that actually worked)
    - .planning/phases/07-service-role-migration/07-05-SUMMARY.md (which blocks exist and the FINDING-2 approach taken, so you know what to verify afterwards)
  </read_first>
  <files>apps/web/docs/RLS-POLICY-AUDIT.md</files>
  <action>
Apply the two migrations through the Supabase Management API — `20260803000001` first, then
`20260803000002`. Both are idempotent (`DROP POLICY IF EXISTS` before every `CREATE POLICY`), so a
partial failure can be retried once its cause is fixed.

Apply them as **separate** requests and verify between them. If the first fails, do not attempt the
second: the second assumes `webhook_events` is already scoped.

After each apply, read the live catalog back and compare against the file. Do not trust the API's
success response alone:

```sql
SELECT schemaname, tablename, policyname, roles, cmd, qual, with_check
FROM pg_policies
WHERE schemaname = 'public'
ORDER BY tablename, policyname;
```

Check specifically:

- `webhook_events`'s single policy now lists `{service_role}` in `roles`, not `{public}`.
- `vote_nfts` no longer has a policy whose `qual` is `true` with `roles` `{public}`.
- Every policy `20260803000002` creates is present with the `roles` and `qual` the file specifies.
- The total live policy count equals the pre-apply count plus this migration's net additions. A
  count that does not reconcile means a `DROP` matched something you did not intend — investigate
  before continuing.

**If anything is wrong**, use the `-- rollback:` comment on the affected block to restore prior
state, then stop and report. Do not iterate DDL live against production.

Record in the "Applied" section of `apps/web/docs/RLS-POLICY-AUDIT.md`: each migration's filename,
the apply timestamp, the pre- and post-apply policy counts, and the `pg_policies` rows for
`webhook_events` and `vote_nfts` (their `roles`, `cmd` and `qual` — no row data, no keys).
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; set -a &amp;&amp; . ./.dev.vars &amp;&amp; set +a &amp;&amp; npx vitest run src/__tests__/rls</automated>
  </verify>
  <acceptance_criteria>
    - Both migrations applied, in order, each verified by reading `pg_policies` back
    - `webhook_events`'s policy `roles` column reads `{service_role}`
    - No `vote_nfts` policy has `qual = true` with `roles = {public}`
    - Pre- and post-apply policy counts reconcile with the migration's net additions
    - `apps/web/docs/RLS-POLICY-AUDIT.md`'s Applied section names both migration files with timestamps and the observed counts
    - The Phase 5 RLS suite still reports a non-zero passing count after the apply — the new policies did not break the existing proofs
    - No secret value appears in any output or in the document
  </acceptance_criteria>
  <done>The phase's policy changes are live, verified against `pg_policies` rather than against an API status code.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the two findings are closed against the live project</name>
  <files>apps/web/docs/RLS-POLICY-AUDIT.md</files>
  <what-built>
    Both Phase 7 migrations are applied to the live Supabase project and verified against
    `pg_policies`:

    - `20260803000001_webhook_events_rls_hotfix.sql` scopes `webhook_events`'s only policy
      `TO service_role`. Before this, the policy had no `TO` clause and therefore applied
      `TO PUBLIC` — anyone holding the published `NEXT_PUBLIC_SUPABASE_ANON_KEY` could read
      `payload_hash` and `idempotency_key`, DELETE rows to re-enable webhook replay, or INSERT a row
      for a future `event_id` to make a real webhook look already-processed.
    - `20260803000002_rls_policy_audit.sql` replaces `vote_nfts`'s `SELECT USING (true)` with an
      own-row policy, plus every other verdict and required addition from
      `apps/web/docs/RLS-POLICY-AUDIT.md`. Before this, the anon key could join a Taruu user id to a
      specific vote and a Solana wallet — the product's secret ballot, published.

    The Phase 5 RLS suite is green with a non-zero count after the apply.
  </what-built>
  <how-to-verify>
    Three checks. The first two are the findings; the third is that nothing user-facing broke.

    **1. `webhook_events` is closed.** With `NEXT_PUBLIC_SUPABASE_URL` and
    `NEXT_PUBLIC_SUPABASE_ANON_KEY` from `apps/web/.dev.vars` (the anon key is public by design, so
    this is safe to run):

    ```bash
    curl -s "$NEXT_PUBLIC_SUPABASE_URL/rest/v1/webhook_events?select=id&limit=1" \
      -H "apikey: $NEXT_PUBLIC_SUPABASE_ANON_KEY"
    ```
    Expected: `[]` or a permission error. **Any row is a failure.**

    ```bash
    curl -s -X POST "$NEXT_PUBLIC_SUPABASE_URL/rest/v1/webhook_events" \
      -H "apikey: $NEXT_PUBLIC_SUPABASE_ANON_KEY" \
      -H "Content-Type: application/json" \
      -d '{"event_id":"rls-harness-probe","status":"pending"}'
    ```
    Expected: a `42501` / row-level-security error. **A 201 is a failure.** If it somehow succeeds,
    delete the row through the service role immediately and report.

    **2. `vote_nfts` no longer publishes holders.**

    ```bash
    curl -s "$NEXT_PUBLIC_SUPABASE_URL/rest/v1/vote_nfts?select=user_id,wallet_address&limit=5" \
      -H "apikey: $NEXT_PUBLIC_SUPABASE_ANON_KEY"
    ```
    Expected: `[]` or a permission error. **Any row containing a `user_id` or `wallet_address` is a
    failure.**

    **3. Nothing user-facing broke.** Run the app (`pnpm dev --filter @sync/web`) and load, in
    Hebrew/RTL:
    - `/he` — the home feed still lists votes
    - `/he/knesset` — the Knesset feed still renders
    - `/he/municipality/<any slug>` — the council page still renders
    - the public NFT gallery surface, if plan 07-05 chose a projection view — it must still show
      what it showed before, minus any holder identity

    A blank list where there was content means a policy `DROP` went further than intended. Use the
    `-- rollback:` comment for that block.
  </how-to-verify>
  <action>
Human-executed. Run the three checks in `<how-to-verify>` above against the live project and
record each result — the command, the response, and the verdict — in the "Applied" section of
`apps/web/docs/RLS-POLICY-AUDIT.md`.

If check 1 or 2 returns a row, the migration did not take effect as written: stop, use the
`-- rollback:` comment for the affected block, and report. If check 3 shows a page that lost content,
a policy `DROP` went further than intended — name the page and the table and report rather than
adjusting policies live.

Never paste a key into the document or the terminal transcript. The project ref is fine; the anon
key, the service-role key and `SUPABASE_JWT_SECRET` are not.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; set -a &amp;&amp; . ./.dev.vars &amp;&amp; set +a &amp;&amp; npx vitest run src/__tests__/rls</automated>
  </verify>
  <done>All three checks are recorded in RLS-POLICY-AUDIT.md with their actual responses: anon reads and writes of webhook_events are denied, anon cannot read vote_nfts holder identity, and the four public pages still render their content.</done>
  <resume-signal>Type "approved" when all three checks pass, or describe exactly which check failed and what it returned (never paste a key).</resume-signal>
</task>

</tasks>

<verification>
1. `cd apps/web && set -a && . ./.dev.vars && set +a && npx vitest run src/__tests__/rls` — non-zero passing count.
2. `pnpm --filter @sync/web test` — green, count unchanged from plan 07-06.
3. `apps/web/docs/RLS-POLICY-AUDIT.md` contains an "Applied" section naming `20260803000001` and `20260803000002` with timestamps, the project ref, and pre/post policy counts.
4. `pg_policies` shows `webhook_events` with `roles = {service_role}` and no `vote_nfts` policy with `qual = true, roles = {public}`.
5. The human checkpoint returned "approved".
6. No secret value appears in any command output, document, or commit.
</verification>

<success_criteria>
- The Phase 5 gate was verified rather than assumed, and a failed HS256 assumption would have stopped the phase here instead of surfacing as seven silently-empty test suites.
- Both findings are closed against the live project, proven with the real anon key, not with a mock.
- The public pages that depend on public reads still render — denial was targeted, not indiscriminate.
- Wave 4 can now migrate reads onto policies that actually exist.
</success_criteria>

<output>
After completion, create `.planning/phases/07-service-role-migration/07-07-SUMMARY.md`.
Record: the preflight result for all four gate conditions; the apply timestamps and pre/post policy
counts; the exact `pg_policies` rows for `webhook_events` and `vote_nfts` after the apply; the RLS
suite count before and after; the three checkpoint results; and any rollback that was used and why.
Never record a secret value.
</output>
