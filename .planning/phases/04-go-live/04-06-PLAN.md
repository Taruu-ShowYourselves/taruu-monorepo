---
phase: 04-go-live
plan: 06
type: execute
wave: 4
depends_on: ["04-03", "04-05"]
files_modified:
  - apps/web/docs/GO-LIVE-EVIDENCE.md
  - .planning/phases/04-go-live/04-VALIDATION.md
  - .planning/phases/04-go-live/04-GO-NOGO.md
autonomous: false
requirements: [GO-01, GO-02]

user_setup:
  - service: green-invoice
    why: "Criterion 2 requires a REAL ₪50 charge on a real card, and the issued חשבונית can only be inspected in the Green Invoice dashboard. No test card and no sandbox run satisfies it."
    dashboard_config:
      - task: "After the charge, open the issued document and confirm the Israeli private-payer fields, the ₪50 amount, the VAT presentation, and the document type the accountant signed off in GI-LEGAL-CHECKLIST.md. Record the document id."
        location: "Green Invoice dashboard -> Documents"
      - task: "Download the settlement/document export covering the charge window, for the reconciliation run."
        location: "Green Invoice dashboard -> Reports / settlement export"
  - service: supabase
    why: "Criteria 2 and 3 are both read-backs from the live production database — the payments row and the user_votes row must be read from Supabase, not inferred from a UI receipt."
    dashboard_config:
      - task: "Run the recorded SELECT statements against the production project via the SQL editor or the Management API (the path used for previous migrations — there is no `supabase db push` script in this repo)."
        location: "Supabase Studio SQL editor / Management API"

must_haves:
  truths:
    - "A real ₪50 vote-creation charge settled on a real card, a חשבונית was issued with correct Israeli private-payer fields, and the document id is stored on the payments row — all three read back from live systems."
    - "A real resident cast a free vote on production and the user_votes row, the option tally bump, and the participant_count bump were read back from live Supabase — the one Phase 02.1 check no automated test can make."
    - "The receipt that resident saw shows the real user_votes.id and claims no blockchain seal — the P0 that Phase 02.1 fixed is confirmed closed on live traffic."
    - "The GI settlement export and the internal payments table reconcile with zero open mismatches, proven by the plan-04-03 tool's verbatim verdict line."
    - "Every result is recorded with its actual output, redacted of secrets and resident identity, and 04-VALIDATION.md is signed off against the real outcome."
  artifacts:
    - path: "apps/web/docs/GO-LIVE-EVIDENCE.md"
      provides: "Sections 8-11: the live money check, the free-vote read-back, and the reconciliation verdict"
      contains: "RECONCILED"
    - path: ".planning/phases/04-go-live/04-VALIDATION.md"
      provides: "The signed-off validation contract for the phase"
      contains: "Approval"
  key_links:
    - from: "apps/web/docs/GO-LIVE-EVIDENCE.md"
      to: "apps/web/scripts/reconcile-gi.ts"
      via: "the reconciliation section pastes the CLI's verbatim verdict line"
      pattern: "RECONCILED: 0 open mismatches"
    - from: "apps/web/docs/GO-LIVE-EVIDENCE.md"
      to: "user_votes"
      via: "the free-vote section pastes the SELECT result for the recorded ballot"
      pattern: "user_votes"
---

<objective>
Prove the three things only production can prove: that ₪50 of real money moves correctly end to end,
that a resident's free vote is actually recorded, and that the two ledgers agree.

Purpose: these are Phase 4 success criteria 2, 3, and 4. Criterion 3 is the specific check
`02.1-05-SUMMARY.md` flagged as non-automatable — Phase 02.1 closed a P0 where residents were shown
`נחתם` / `✓ חתום בבלוקצ׳יין` receipts for votes that were never recorded, and the fix is green in CI
but has never been confirmed against live traffic. Criterion 4 is where the reconciliation engine
built in plan 04-03 gets used for real.

What this plan deliberately does **not** touch: the `treasury_transactions` ledger. That leg moved to
the token track with COIN-01, and there is no participation charge to reconcile because participation
is free.

Output: `GO-LIVE-EVIDENCE.md` sections 8 through 11 with real read-backs, and a signed-off
`04-VALIDATION.md`.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-go-live/04-CONTEXT.md
@.planning/phases/04-go-live/04-VALIDATION.md
@.planning/phases/04-go-live/04-GO-NOGO.md
@.planning/phases/04-go-live/04-03-SUMMARY.md
@.planning/phases/04-go-live/04-05-SUMMARY.md

@apps/web/docs/RECONCILIATION.md
@.planning/phases/02.1-participation-persistence/02.1-05-SUMMARY.md

<interfaces>
**Preconditions.** `apps/web/docs/GO-LIVE-EVIDENCE.md` has sections 1–7 from plan 04-05, and
`04-GO-NOGO.md`'s `## Decision` reads `go` or `go-restricted`.

If the decision was **`go-restricted`** (`GREENINVOICE_ENV=sandbox`), criterion 2's real-money leg
**cannot** be performed — a sandbox charge is not a real ₪50 charge. In that case: perform criterion 3
(free vote) and criterion 4 (reconciliation over whatever real settled rows exist, which may be zero),
and record criterion 2 as **DEFERRED — blocked on gates G1/G2** with the reason. A zero-row
reconciliation that correctly reports zero mismatches is a real result and should be recorded as such.

**The money path being exercised** — the only paid flow:
- `POST /api/payments/create` with `type: 'vote_creation'` → creates a `payments` row at
  `status='pending'` with a `UNIQUE idempotency_key`, then a GI hosted payment page (`type: 320`)
- resident pays on the GI-hosted form
- GI notifies `POST /api/payments/webhook` → `markPaymentCompleted(payment.id, event.paymentId)`
  (`route.ts:108`) flips `status` to `completed` and writes the GI document id into `provider_id`, in
  one statement guarded by `.eq('status','pending')` so a replay is idempotent
- `amount` is stored in **agorot**: a ₪50 charge is `5000`

**The free-vote path being confirmed** — shipped in Phase 02.1:
- `POST /api/votes/[id]/participate` with `{ optionId }` only; no `paymentTxId`, never a 402
- `recordUserVoteOnce` inserts into `user_votes` tolerating a `23505` unique violation
- `incrementVoteOption` bumps the option tally **only on a genuinely new row**
- `votes.participant_count` bumps with it
- the receipt shows the server's `user_votes.id` under `מספר רישום`, cost `חינם`, status `נרשם`,
  and a `recordedAt` derived from the server's `created_at` — **no `SealCard`, no `BLOCK` row, no
  `חתום בבלוקצ׳יין`**

**Read-back SQL** (production, service-role connection). Substitute real ids; redact emails in output:

```sql
-- Criterion 2: the ₪50 charge
SELECT id, type, amount, currency, status, provider, provider_id, idempotency_key, vote_id, created_at
FROM payments
WHERE type = 'vote_creation' AND status = 'completed'
ORDER BY created_at DESC LIMIT 5;
-- expect: amount = 5000 (agorot), provider = 'green_invoice', provider_id = the GI document id, NOT NULL

-- Criterion 2b: exactly one row per settled charge (no replay duplicate)
SELECT idempotency_key, COUNT(*) FROM payments
WHERE type = 'vote_creation' GROUP BY idempotency_key HAVING COUNT(*) > 1;
-- expect: zero rows

-- Criterion 3: the free ballot
SELECT id, user_id, vote_id, option_id, payment_id, created_at
FROM user_votes WHERE vote_id = '<VOTE_ID>' AND user_id = '<USER_ID>';
-- expect: exactly one row; payment_id IS NULL (participation is free)

-- Criterion 3b: the tally and the participant count
SELECT id, option_id, votes FROM vote_options WHERE vote_id = '<VOTE_ID>' ORDER BY id;
SELECT id, participant_count FROM votes WHERE id = '<VOTE_ID>';
-- capture BOTH before and after casting; expect each to have moved by exactly 1
```

**Reconciliation** (plan 04-03):
```bash
cd apps/web
pnpm reconcile:gi -- --gi ~/Downloads/gi-settlement.csv --since <ISO date>
```
Exit `0` prints the verbatim line `RECONCILED: 0 open mismatches (N matched)`. Exit `1` prints
`OPEN MISMATCHES: <n>` with per-bucket tables. `apps/web/docs/RECONCILIATION.md` explains what each
bucket means and what to do.

**Out of scope, and it must stay that way:** `treasury_transactions`. That reconciliation leg belongs
to the token track behind COIN-01. Do not query it, do not reconcile it, do not report on it.

**Redaction.** Record: payment ids, `provider_id`, `idempotency_key`, amounts, statuses, `user_votes`
ids, vote ids, option ids, counts, and timestamps. Never record: an email, a real name, a phone
number, any card detail, a session cookie, or any key.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: The real ₪50 charge — settled, documented, and stored</name>
  <read_first>
    - apps/web/docs/GO-LIVE-EVIDENCE.md (sections 1-7 from plan 04-05; this task appends section 8)
    - apps/web/docs/GI-LEGAL-CHECKLIST.md (as signed in plan 04-04 — the document type and VAT presentation the accountant approved; the issued חשבונית is checked against it)
    - apps/web/src/app/api/payments/webhook/route.ts (lines 95-145 — what the webhook writes and when)
    - apps/web/src/app/api/payments/create/route.ts (the creation request shape and the idempotency key)
  </read_first>
  <files>apps/web/docs/GO-LIVE-EVIDENCE.md</files>
  <what-built>
    The site is deployed and serving (plan 04-05, sections 1–7). The ₪50 vote-creation rail was built
    in Phase 3 and has never taken a real payment under the current gate state. The reconciliation
    engine exists and is tested but has never seen a real settlement export.
  </what-built>
  <how-to-verify>
Append `## 8 Real ₪50 vote-creation charge` to `apps/web/docs/GO-LIVE-EVIDENCE.md`.

**If `04-GO-NOGO.md`'s decision was `go-restricted`** (GI on sandbox), skip to the bottom of this
section and record: `Criterion 2 DEFERRED — GREENINVOICE_ENV=sandbox under the go-restricted
decision; a sandbox charge is not a real ₪50 charge. Blocked on gates G1/G2.` Then move to Task 2.
Do **not** flip GI to production to satisfy this task.

**1. Create a vote and pay ₪50, for real.** Signed in on production, with a real card. Record the
vote id and the timestamp. Do not use a test card, and do not simulate the webhook.

**2. Read the payment row back from live Supabase.** Run the criterion-2 SELECT from `<interfaces>`.
Paste the row with the email column absent. Confirm and state explicitly:
- `amount = 5000` — agorot, i.e. ₪50. A value of `50` would mean the agorot conversion is wrong and
  the charge was ₪0.50.
- `status = 'completed'`
- `provider = 'green_invoice'`
- `provider_id` is **NOT NULL** and equals the GI document id — this is criterion 2's storage claim
- `idempotency_key` is present and, per SEC-04, is **not** `Date.now()`-derived. Paste it; a trailing
  13-digit epoch means SEC-04 did not land and that is a finding.

**3. Inspect the issued document in the GI dashboard.** Open the חשבונית. Confirm against what the
accountant signed off in `GI-LEGAL-CHECKLIST.md`:
- the document **type** matches their written answer (חשבונית קבלה vs חשבונית מס vs קבלה)
- the amount is ₪50
- VAT is presented as they specified
- the payer fields are correct for an Israeli **private** individual (פרטי)
- the document id matches `payments.provider_id` exactly
**Screenshot: the issued document with the payer's name, address, and any ID number redacted.**

**4. Prove the replay guard held.** Run the criterion-2b query — duplicate `idempotency_key` rows must
be zero. If GI delivered the notify more than once, `markPaymentCompleted`'s
`.eq('status','pending')` guard means only one delivery ran fulfilment; confirm exactly one
`payments` row and exactly one created vote. Also check `webhook_events` for that document id and
paste the row count and status.

**5. Confirm the vote exists and is attributable.** The created vote should be visible on production
with the correct title and municipality. Confirm no second, duplicate vote was created.
**Screenshot: the created vote's public page.**

If anything fails — no document issued, `provider_id` null, amount not 5000, a duplicate row — stop
and record the failure verbatim rather than retrying until it looks right. A failed criterion 2 that
is written down accurately is worth more than a green one that was massaged.
  </how-to-verify>
  <acceptance_criteria>
    - `apps/web/docs/GO-LIVE-EVIDENCE.md` contains `## 8 Real ₪50 vote-creation charge`
    - Section 8 either contains the pasted `payments` row showing `amount` `5000`, `status` `completed`, and a non-null `provider_id`; **or** contains the literal string `Criterion 2 DEFERRED` with the go-restricted reason
    - If performed: the GI document id in the dashboard is stated to equal `payments.provider_id`, and the document type is stated to match the accountant's written answer
    - If performed: the duplicate-`idempotency_key` query result is pasted and is zero rows
    - If performed: the `idempotency_key` is pasted and it is stated whether it is deterministic or `Date.now()`-derived
    - If performed: two screenshots are referenced (issued document with payer fields redacted, created vote page)
    - **No secret or identity:** `grep -cE "eyJ[A-Za-z0-9_-]{10,}|service_role|sync-session=|@gmail\.com|[0-9]{13,16}" apps/web/docs/GO-LIVE-EVIDENCE.md` returns 0
  </acceptance_criteria>
  <action>
Human-executed. If the go/no-go decision was `go-restricted`, record criterion 2 as DEFERRED with the
sandbox reason and move on — do not flip GI to production to satisfy this task.

Otherwise: create a vote on production and pay the ₪50 with a real card; read the `payments` row back
from live Supabase and confirm `amount = 5000` agorot, `status = completed`, and a non-null
`provider_id`; open the issued חשבונית in the GI dashboard and check its type, amount, VAT
presentation, and Israeli private-payer fields against the accountant's signed answers, confirming the
document id matches `provider_id`; prove no duplicate `idempotency_key` row and exactly one created
vote; and record whether the idempotency key is deterministic or still `Date.now()`-derived.

Record everything as section 8 with two redacted screenshots. Record failures verbatim rather than
retrying until the result looks acceptable.
  </action>
  <verify>
    <automated>F=apps/web/docs/GO-LIVE-EVIDENCE.md; grep -qF "## 8 Real ₪50 vote-creation charge" $F || { echo "MISSING section 8"; exit 1; }; if grep -qF "Criterion 2 DEFERRED" $F; then echo "DEFERRED path recorded"; else grep -qF "provider_id" $F || { echo "MISSING provider_id read-back"; exit 1; }; grep -qE "\b5000\b" $F || { echo "MISSING agorot amount"; exit 1; }; grep -qF "idempotency_key" $F || { echo "MISSING idempotency evidence"; exit 1; }; fi; if grep -qE "eyJ[A-Za-z0-9_-]{10,}|service_role|sync-session=|@gmail\.com" $F; then echo "SECRET OR IDENTITY LEAKED"; exit 1; fi; echo OK</automated>
  </verify>
  <done>Either a real ₪50 charge is evidenced end to end — settled, documented in GI against the accountant's signed answers, stored on the `payments` row with a non-null `provider_id` and no duplicate — or criterion 2 is explicitly recorded as DEFERRED under the go-restricted decision with its reason.</done>
  <resume-signal>Type "approved" once section 8 is recorded (including the DEFERRED path), or paste the failing check.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: The free vote — the Phase 02.1 check no test can make</name>
  <read_first>
    - .planning/phases/02.1-participation-persistence/02.1-05-SUMMARY.md ("Next Phase Readiness" — this exact check, named as non-automatable)
    - apps/web/src/app/[locale]/votes/[id]/flow/ParticipationFlow.tsx (the receipt: `מספר רישום`, `חינם`, `נרשם`, and the absence of SealCard)
    - apps/web/src/app/api/votes/[id]/participate/route.ts (the free contract and the idempotent insert)
    - apps/web/docs/GO-LIVE-EVIDENCE.md (sections 1-8; this task appends section 9)
  </read_first>
  <files>apps/web/docs/GO-LIVE-EVIDENCE.md</files>
  <what-built>
    Phase 02.1 closed a P0: `ParticipationFlow.tsx` was sealing votes with a client-side `mockHash()`
    and a fabricated block number, showing residents `נחתם` and `✓ חתום בבלוקצ׳יין · בלתי ניתן לשינוי`
    for ballots that were never written to the database. The server now persists every free ballot
    idempotently through `recordUserVoteOnce`, and the client only shows a receipt after that write is
    confirmed.

    All of that is green in CI against mocks. None of it has been confirmed against live traffic. This
    is that confirmation, and `02.1-05-SUMMARY.md` names it explicitly as the one check no automated
    test can make.
  </what-built>
  <how-to-verify>
Append `## 9 Free vote recorded on production` to `apps/web/docs/GO-LIVE-EVIDENCE.md`.

You need a **real, residency-verified** account on production — the server gates on
`identity_score >= 40` AND (`verification_status = 'verified'` OR `completed_check_ins >= 1`).

**1. Capture the BEFORE state.** Pick an active vote. Before casting, run and paste:
```sql
SELECT id, option_id, votes FROM vote_options WHERE vote_id = '<VOTE_ID>' ORDER BY id;
SELECT id, participant_count FROM votes WHERE id = '<VOTE_ID>';
SELECT COUNT(*) FROM user_votes WHERE vote_id = '<VOTE_ID>';
```
Without a before, "it incremented" is an assertion rather than a measurement.

**2. Cast the vote.** Complete the participation flow on production as that verified resident.
Note which option you chose.

**3. Read the ballot back.**
```sql
SELECT id, user_id, vote_id, option_id, payment_id, created_at
FROM user_votes WHERE vote_id = '<VOTE_ID>' AND user_id = '<USER_ID>';
```
Expect **exactly one** row, with `payment_id` **NULL** — participation is free and stores no payment.
Paste the row; redact nothing but keep the `user_id` as a UUID (a UUID is not an identity).

**4. Read the AFTER state.** Re-run the three BEFORE queries. Confirm and state:
- the chosen option's `votes` incremented by **exactly 1**
- `votes.participant_count` incremented by **exactly 1**
- `COUNT(*)` on `user_votes` incremented by **exactly 1**
- **no other option's tally moved**

**5. Confirm the receipt is honest.** On the receipt screen, confirm:
- `מספר רישום` shows the **same** `user_votes.id` you just read back from the database
- cost reads `חינם`
- status reads `נרשם` — **not** `נחתם`
- there is **no** `SealCard`, **no** `BLOCK` row, and **no** `✓ חתום בבלוקצ׳יין · בלתי ניתן לשינוי`
**Screenshot: the receipt.**

**6. Double-submit is idempotent.** Reload and attempt to vote again on the same vote (or fire the
request twice). Expect the existing receipt with the already-voted copy — not a second row and not a
500. Re-run the count and tally queries and confirm **nothing moved a second time**. Paste both
results. **Screenshot: the already-voted state.**

**7. The results panel is honest too.** After voting, confirm the results panel highlights your own
option and that its footer makes no blockchain-seal claim. **Screenshot.**

Redact the resident's email and name from every screenshot. UUIDs, option ids, counts, and timestamps
are fine to record.
  </how-to-verify>
  <acceptance_criteria>
    - `apps/web/docs/GO-LIVE-EVIDENCE.md` contains `## 9 Free vote recorded on production`
    - Section 9 contains **both** the before and after results of the tally, participant-count, and row-count queries
    - It contains the pasted `user_votes` row with `payment_id` NULL and exactly one row for that `(user_id, vote_id)`
    - It states explicitly that the chosen option's tally, `participant_count`, and `user_votes` count each moved by exactly 1, and that no other option moved
    - It states that the receipt's `מספר רישום` equals the `user_votes.id` read from the database
    - It states that `נרשם` appeared and that `נחתם`, `SealCard`, `BLOCK`, and `חתום בבלוקצ׳יין` did not
    - It records the double-submit result showing no second row and no further tally movement
    - At least three screenshots are referenced (receipt, already-voted state, results panel)
    - **No identity:** `grep -cE "@gmail\.com|@[a-z0-9-]+\.co\.il|eyJ[A-Za-z0-9_-]{10,}|sync-session=" apps/web/docs/GO-LIVE-EVIDENCE.md` returns 0
  </acceptance_criteria>
  <action>
Human-executed. As a real residency-verified resident on production: capture the before state of the
option tallies, `participant_count`, and the `user_votes` count; cast a free vote; read the
`user_votes` row back and confirm exactly one row with a NULL `payment_id`; re-run all three queries
and confirm each moved by exactly one and no other option moved; confirm the receipt's `מספר רישום`
matches the database id, that it reads `חינם` and `נרשם`, and that no `SealCard`, `BLOCK` row, or
`חתום בבלוקצ׳יין` claim appears anywhere; then double-submit and confirm nothing moves a second time.

Record all of it as section 9 with three sanitized screenshots. This closes the Phase 02.1 P0 on live
traffic.
  </action>
  <verify>
    <automated>F=apps/web/docs/GO-LIVE-EVIDENCE.md; grep -qF "## 9 Free vote recorded on production" $F || { echo "MISSING section 9"; exit 1; }; for s in "user_votes" "participant_count" "vote_options" "נרשם" "מספר רישום"; do grep -qF "$s" $F || { echo "MISSING: $s"; exit 1; }; done; grep -qE "payment_id" $F || { echo "MISSING payment_id NULL evidence"; exit 1; }; grep -qE "\.png|\.jpg|screenshot" $F || { echo "MISSING screenshots"; exit 1; }; if grep -qE "@gmail\.com|eyJ[A-Za-z0-9_-]{10,}|sync-session=" $F; then echo "IDENTITY OR SECRET LEAKED"; exit 1; fi; echo OK</automated>
  </verify>
  <done>A real resident's free vote on production is evidenced by a before/after read-back showing exactly one new `user_votes` row with a NULL `payment_id`, a tally and `participant_count` each moved by exactly 1 with no other option touched, a receipt whose `מספר רישום` matches the database id and which claims no blockchain seal, and an idempotent double-submit that moved nothing.</done>
  <resume-signal>Type "approved" once section 9 is recorded, or describe which read-back disagreed.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Reconcile to zero, and sign off the phase</name>
  <read_first>
    - apps/web/docs/RECONCILIATION.md (created in plan 04-03 — where to get the export, how to run it, what each bucket means)
    - apps/web/docs/GO-LIVE-EVIDENCE.md (sections 1-9; this task appends sections 10 and 11)
    - .planning/phases/04-go-live/04-VALIDATION.md (the per-plan verification map to sign off)
    - .planning/phases/04-go-live/04-GO-NOGO.md (the gate ledger to close out)
  </read_first>
  <files>apps/web/docs/GO-LIVE-EVIDENCE.md, .planning/phases/04-go-live/04-VALIDATION.md, .planning/phases/04-go-live/04-GO-NOGO.md</files>
  <what-built>
    A real charge has landed (or been recorded as deferred), a real free vote has been recorded and
    read back, and the reconciliation engine built in plan 04-03 is tested against fixtures but has
    never seen a real settlement export. This is where the two ledgers are made to agree, and where
    the phase either signs off or states what it did not achieve.
  </what-built>
  <how-to-verify>
Append `## 10 Reconciliation` and `## 11 Phase outcome` to `apps/web/docs/GO-LIVE-EVIDENCE.md`.

**1. Download the GI settlement export** covering the charge window, per
`apps/web/docs/RECONCILIATION.md`. CSV or JSON both work.

**2. Run the reconciliation.**
```bash
cd apps/web
pnpm reconcile:gi -- --gi <path-to-export> --since <ISO date>; echo "exit=$?"
```
Paste the full report and the exit code. The target is the verbatim line:
`RECONCILED: 0 open mismatches (N matched)` with exit `0`.

**3. If it is not zero, work the buckets** using `RECONCILIATION.md`'s table — do not paper over it:
- **`giOnly`** — GI settled money Taruu has no completed record of. The serious one. Check
  `deliveredButUnfulfilled`: `true` means the webhook arrived and fulfilment broke (check
  `webhook_events` and the Worker logs); `false` means GI never reached the notify endpoint (check
  the registered webhook URL and secret in the GI dashboard).
- **`internalOnly` / `pending-no-document`** — an opened payment that never settled; usually an
  abandoned checkout. Becomes serious if the vote was created anyway — check.
- **`internalOnly` / `completed-not-in-settlement`** — check the export's date window first; a window
  mismatch is the common cause.
- **`amountMismatches`** — same document, different money, or a non-completed internal status.
- **`duplicates`** — resolve manually before trusting any other bucket.
Record each open mismatch with its bucket, its cause, and whether it was resolved or carried. A
carried mismatch is acceptable **only** if it is written down with an owner.

Note: if criterion 2 was DEFERRED (go-restricted), the export may legitimately contain zero rows. A
zero-row run reporting zero mismatches is a real result — record it as such, and record that
criterion 4 is proven only vacuously until a real charge exists.

**4. Section 11 — the phase outcome.** State, plainly, one line per criterion:
- **Criterion 1** (deploy + `validateEnv()` + serving) — MET / PARTIALLY MET / NOT MET, citing
  sections 1–7. If `validateEnv()` still has no call site, this is PARTIALLY MET, owner Phase 3 SEC-05.
- **Criterion 2** (real ₪50 charge + חשבונית + stored document id) — MET / DEFERRED, citing section 8.
- **Criterion 3** (free vote read back from live Supabase) — MET / NOT MET, citing section 9.
- **Criterion 4** (zero open mismatches) — MET / MET-VACUOUSLY / NOT MET, citing section 10.
Then list what is carried into production unresolved: the three untriggered cron routes including
vote resolution, any degraded service accepted at gate G6, any carried reconciliation mismatch, and —
always — that no token surface is live because COIN-01 is unmet.

**5. Close the gate ledger.** In `.planning/phases/04-go-live/04-GO-NOGO.md`, append a
`## Post-launch state` section recording the final state of each gate after the live run, and pointing
at `apps/web/docs/GO-LIVE-EVIDENCE.md` as the evidence.

**6. Sign off the validation contract.** In `.planning/phases/04-go-live/04-VALIDATION.md`:
- change every `⬜ pending` in the Per-Task Verification Map to `✅ green` (or `❌ red` where a check
  genuinely failed — do not paint a red check green)
- tick every box in the Validation Sign-Off section
- set frontmatter `status: complete` and `nyquist_compliant: true`
- replace `**Approval:** pending` with `**Approval:** approved <ISO date>`
  </how-to-verify>
  <acceptance_criteria>
    - `apps/web/docs/GO-LIVE-EVIDENCE.md` contains `## 10 Reconciliation` and `## 11 Phase outcome`
    - Section 10 contains the pasted reconciliation report and its exit code, and either the verbatim `RECONCILED: 0 open mismatches` line or a per-bucket account of every open mismatch with a cause and an owner
    - Section 11 states a verdict for each of criteria 1, 2, 3, and 4, each citing the evidence section that supports it
    - Section 11 lists what is carried into production unresolved, and explicitly states that no token surface is live because COIN-01 is unmet
    - `.planning/phases/04-go-live/04-GO-NOGO.md` contains a `## Post-launch state` section referencing `GO-LIVE-EVIDENCE.md`
    - `.planning/phases/04-go-live/04-VALIDATION.md` frontmatter contains `status: complete` and `nyquist_compliant: true`, no longer contains `**Approval:** pending`, and has no remaining `⬜ pending` rows
    - `pnpm --filter @sync/web test` exits 0 at the time of sign-off
    - **No secret or identity anywhere:** `grep -cE "eyJ[A-Za-z0-9_-]{10,}|service_role|sync-session=|@gmail\.com" apps/web/docs/GO-LIVE-EVIDENCE.md .planning/phases/04-go-live/04-GO-NOGO.md` returns 0 for both files
  </acceptance_criteria>
  <action>
Human-executed. Download the GI settlement export for the charge window, run
`pnpm reconcile:gi -- --gi <export> --since <date>`, and paste the full report plus the exit code. If
it is not zero, work each bucket against `RECONCILIATION.md` and record every open mismatch with its
cause and an owner rather than papering over it.

Then write section 11 with a plain per-criterion verdict citing its evidence section, and an explicit
list of what is carried into production unresolved — the untriggered cron routes including vote
resolution, any degraded service accepted at G6, any carried mismatch, and the fact that no token
surface is live because COIN-01 is unmet.

Finally append `## Post-launch state` to `04-GO-NOGO.md`, and sign off `04-VALIDATION.md`: every map
row green (or honestly red), every sign-off box ticked, frontmatter `status: complete` and
`nyquist_compliant: true`, and `**Approval:** approved <ISO date>`.
  </action>
  <verify>
    <automated>cd /Users/saharbarak/personal/taro && pnpm --filter @sync/web test && F=apps/web/docs/GO-LIVE-EVIDENCE.md && G=.planning/phases/04-go-live/04-GO-NOGO.md && V=.planning/phases/04-go-live/04-VALIDATION.md && grep -qF "## 10 Reconciliation" $F && grep -qF "## 11 Phase outcome" $F && grep -qF "COIN-01" $F && grep -qE "RECONCILED|OPEN MISMATCHES" $F && grep -qF "## Post-launch state" $G && grep -qF "GO-LIVE-EVIDENCE" $G && grep -qF "status: complete" $V && grep -qF "nyquist_compliant: true" $V && ! grep -qF "**Approval:** pending" $V && ! grep -qF "⬜ pending" $V && ! grep -qE "eyJ[A-Za-z0-9_-]{10,}|service_role|sync-session=|@gmail\.com" $F $G && echo OK</automated>
  </verify>
  <done>The reconciliation ran against a real GI settlement export with its report and exit code recorded; every open mismatch (if any) has a bucket, a cause, and an owner; section 11 states a cited verdict for all four criteria and lists everything carried into production unresolved including the COIN-01 token exclusion; the gate ledger has a post-launch state; and `04-VALIDATION.md` is signed off with no pending rows.</done>
  <resume-signal>Type "approved" once sections 10 and 11 are recorded and 04-VALIDATION.md is signed off, or paste the open mismatches.</resume-signal>
</task>

</tasks>

<verification>
1. `pnpm --filter @sync/web test` exits 0 (the full credential-free suite, including the plan-04-03 reconciliation fixtures).
2. `pnpm --filter @sync/web typecheck` and `pnpm --filter @sync/web lint` exit 0.
3. `apps/web/docs/GO-LIVE-EVIDENCE.md` has sections 1 through 11 filled with real output.
4. Section 10 records the reconciliation verdict from a real GI settlement export.
5. `.planning/phases/04-go-live/04-VALIDATION.md` is signed off with no `⬜ pending` rows.
6. `.planning/phases/04-go-live/04-GO-NOGO.md` has a `## Post-launch state` section.
7. No secret, key, cookie, or resident identity appears in any evidence artifact or screenshot.
</verification>

<success_criteria>
- Criterion 2 is answered by a real ₪50 charge read back from three independent places — the GI dashboard document, the `payments` row, and the reconciliation — or is honestly recorded as deferred.
- Criterion 3 is answered by a before/after database measurement, not by a UI receipt, closing the Phase 02.1 P0 on live traffic.
- Criterion 4 is answered by a tool whose matching logic is proven in CI, not by a human comparing two screens.
- Every ROADMAP Phase 4 success criterion has either recorded live evidence or a written statement of why it is not met and who owns it.
- The `treasury_transactions` leg was not touched, and no token surface went live — COIN-01 remains the gate it is.
</success_criteria>

<output>
After completion, create `.planning/phases/04-go-live/04-06-SUMMARY.md`.
Record: the payment id / document id / agorot amount of the real charge (or the deferral reason), the
`user_votes.id` and the exact before/after tally deltas for the free vote, the reconciliation counts
per bucket and the verdict line, the per-criterion outcome, and everything carried into production
unresolved. Note explicitly for Phase 5: whether `validateEnv()` still has no call site, and whether
the three cron routes remain untriggered.
</output>
