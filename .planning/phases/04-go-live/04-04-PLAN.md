---
phase: 04-go-live
plan: 04
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - .planning/phases/04-go-live/04-GO-NOGO.md
  - apps/web/docs/GI-LEGAL-CHECKLIST.md
  - apps/web/docs/GI-PRIME-CHECKLIST.md
  - apps/web/wrangler.jsonc
autonomous: false
requirements: [GO-01]

user_setup:
  - service: green-invoice
    why: "The ₪50 vote-creation charge is the only paid flow at go-live and it runs on Green Invoice as Israeli Merchant of Record. Production credentials exist only in the GI dashboard, and the Prime rate plus clearing terms are commercial facts only the account rep can confirm."
    env_vars:
      - name: GREENINVOICE_API_KEY_ID
        source: "Green Invoice dashboard -> Settings -> API. Set with `cd apps/web && npx wrangler secret put GREENINVOICE_API_KEY_ID`. Never paste the value anywhere else."
      - name: GREENINVOICE_API_SECRET
        source: "Green Invoice dashboard -> Settings -> API. Set with `npx wrangler secret put GREENINVOICE_API_SECRET`."
      - name: GREENINVOICE_PLUGIN_ID
        source: "Green Invoice dashboard -> account settings. Set with `npx wrangler secret put GREENINVOICE_PLUGIN_ID`."
      - name: GREENINVOICE_WEBHOOK_SECRET
        source: "Generate fresh for production (do NOT reuse the sandbox value), set with `npx wrangler secret put GREENINVOICE_WEBHOOK_SECRET`, then register the matching notify URL in the GI dashboard."
    dashboard_config:
      - task: "Confirm the Prime plan rate in writing, and obtain written clearing terms: clearing %, hard minimum per transaction, card-brand surcharges, foreign-card surcharge, settlement payout threshold and cadence."
        location: "Green Invoice account rep (email or contract)"
      - task: "Register the production webhook notify URL for the hosted payment form and confirm the secret transport matches what /api/payments/webhook accepts."
        location: "Green Invoice dashboard -> webhooks / notify URL"
  - service: accountant-legal
    why: "SPIKE-02 merchant-of-record sign-off is a written opinion from an Israeli accountant/lawyer covering GI document type, VAT, credit-note mechanics, and consumer-protection obligations on the ₪50 charge. Nothing in this repo can produce it."
    dashboard_config:
      - task: "Send the re-scoped apps/web/docs/GI-LEGAL-CHECKLIST.md and obtain written answers to every question plus a dated signature."
        location: "External accountant / lawyer"
  - service: cloudflare
    why: "Gate G6 requires the production Worker secrets to be set, and gate G5 requires a decision on whether CI may deploy. Both need Cloudflare and GitHub access no agent holds."
    env_vars:
      - name: UPSTASH_REDIS_REST_URL
        source: "console.upstash.com -> database -> REST API. Set with `npx wrangler secret put UPSTASH_REDIS_REST_URL`, or record a deliberate decision to launch on in-memory rate limiting."
      - name: UPSTASH_REDIS_REST_TOKEN
        source: "console.upstash.com -> database -> REST API. Set with `npx wrangler secret put UPSTASH_REDIS_REST_TOKEN`."
      - name: SMS_API_KEY
        source: "The chosen SMS gateway. Set with `npx wrangler secret put SMS_API_KEY` (with SMS_API_URL and SMS_FROM), or record a deliberate decision to launch with phone OTP soft-passing."
    dashboard_config:
      - task: "Decide gate G5: either add CLOUDFLARE_API_TOKEN as a repository or `production`-environment secret so CI can deploy, or record in 04-GO-NOGO.md that deploys stay manual."
        location: "GitHub -> Settings -> Secrets and variables -> Actions"

must_haves:
  truths:
    - "Every gate in 04-GO-NOGO.md carries a real state, a named decider, and a date — no row is left at its creation-time BLOCKED without a decision having been made about it."
    - "The legal and Prime checklists are either signed with every box ticked, or the ledger says explicitly that go-live is proceeding without them and who accepted that risk."
    - "GREENINVOICE_ENV is reconciled with the gate state: either it is sandbox, or an ACCEPTED-WITH-RISK row names the person who accepted production traffic ahead of sign-off."
    - "The production Worker's secret inventory is recorded by NAME and set/unset state only — no value appears in any artifact this plan touches."
    - "The verdict line at the top of 04-GO-NOGO.md reflects the actual state, and is GO only if no blocking gate remains red."
  artifacts:
    - path: ".planning/phases/04-go-live/04-GO-NOGO.md"
      provides: "The decided gate ledger — the authority for whether plan 04-05 may deploy"
      contains: "Decided by"
  key_links:
    - from: ".planning/phases/04-go-live/04-GO-NOGO.md"
      to: "apps/web/wrangler.jsonc"
      via: "the G3 row states the GREENINVOICE_ENV value that is actually in the file"
      pattern: "GREENINVOICE_ENV"
---

<objective>
Close the external gates, or say out loud that we are launching without them.

Purpose: `REQUIREMENTS.md` marks SPIKE-02 and SPIKE-03 complete. They are not. `GI-LEGAL-CHECKLIST.md`
has 0 boxes ticked and no signature; `GI-PRIME-CHECKLIST.md` has 0 boxes ticked and no rep name; and
`wrangler.jsonc:74` has been running `GREENINVOICE_ENV: production` in front of both. Everything
before this plan was preparation — this is the plan where a human does the slow, unautomatable work
and the answer gets written down.

The output is not a green light. The output is an **honest ledger**. If the gates clear, the ledger
says GO and plan 04-05 may deploy. If they do not, the ledger says NO-GO or names exactly who
accepted which risk. Both are acceptable outcomes; a ledger that quietly stays ambiguous is not.

Output: `04-GO-NOGO.md` with every row decided, both checklists in their true state, and
`GREENINVOICE_ENV` reconciled with reality.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-go-live/04-CONTEXT.md
@.planning/phases/04-go-live/04-GO-NOGO.md
@.planning/phases/04-go-live/04-01-SUMMARY.md
@.planning/phases/04-go-live/04-02-SUMMARY.md

@apps/web/docs/GI-LEGAL-CHECKLIST.md
@apps/web/docs/GI-PRIME-CHECKLIST.md

<interfaces>
**What plan 04-01 produced:** two checklists re-scoped to the real money model (free participation,
one-time ₪50 hosted-form creation charge, COIN-01 named as a separate token engagement), and
`04-GO-NOGO.md` with all seven gates at `🔴 BLOCKED`.

**What plan 04-02 produced:** a fail-fast credential guard in `deploy.yml`, a names-only
`apps/web/scripts/preflight-prod.sh`, and a corrected `INTEGRATIONS.md` documenting both deploy paths.

**The only sanctioned secret-inspection commands.** Both emit NAMES only:
```bash
cd apps/web
./scripts/preflight-prod.sh          # BLOCKER / PAID / DEGRADES tiers, set|MISSING per name
node_modules/.bin/wrangler secret list
```
**Forbidden in every step of this plan:** `wrangler secret get`, `cat .dev.vars`, echoing any value,
pasting a value into any file, or capturing a value in a screenshot. If a value must be moved, it
goes hand-to-dashboard via `wrangler secret put <NAME>` interactively, which reads from stdin and
prints nothing.

**Gate states the ledger permits:**
- `🟢 CLEARED` — the evidence exists and is dated and named
- `🔴 BLOCKED` — not done
- `🟡 ACCEPTED-WITH-RISK` — **permitted only for G3 and G5**, requires a named decider and a one-line
  statement of the residual risk. Never for G0, G1, G2, G4, or G6.

**The G3 decision.** `apps/web/wrangler.jsonc:74` reads `"GREENINVOICE_ENV": "production"`.
`apps/web/src/services/greenInvoice/index.ts:36-39` is the single reader — `production` points the
base URL at the live GI API. Exactly two acceptable outcomes:
1. edit line 74 to `"sandbox"` until G1 and G2 clear, or
2. leave it and record a `🟡 ACCEPTED-WITH-RISK` row naming the person who accepted running real
   card traffic ahead of legal and commercial sign-off.

**The G0 reality check.** `.planning/phases/` contains no `03-*` directory. Phase 3 is not planned,
let alone executed, and Phase 4's criterion 1 (`validateEnv()` passes at startup) is unachievable
until SEC-05 lands. If G0 is still red, this plan's honest output is a NO-GO ledger — and that is a
successful execution of this plan, not a failure of it.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Close the external gates — legal sign-off, Prime terms, credentials, deploy path</name>
  <read_first>
    - apps/web/docs/GI-LEGAL-CHECKLIST.md (the re-scoped questions to send to the accountant/lawyer)
    - apps/web/docs/GI-PRIME-CHECKLIST.md (the re-scoped questions for the GI rep, and the secret-name list)
    - apps/web/scripts/preflight-prod.sh (the names-only readiness check, created in plan 04-02)
    - .planning/phases/04-go-live/04-GO-NOGO.md (the ledger rows this task decides)
    - .planning/phases/04-go-live/04-CONTEXT.md (the `<gates>` section — the full statement of each gate)
  </read_first>
  <files>apps/web/docs/GI-LEGAL-CHECKLIST.md, apps/web/docs/GI-PRIME-CHECKLIST.md, .planning/phases/04-go-live/04-GO-NOGO.md</files>
  <what-built>
    Plans 04-01 through 04-03 produced everything that could be produced without a human: two
    external checklists re-scoped away from the retired ₪6 membership to the real free-participation
    + ₪50-creation model; a gate ledger with all seven gates recorded as BLOCKED against real
    evidence; a deploy workflow that now fails in seconds with an actionable message instead of after
    a full build; a names-only production-secret preflight; and a tested reconciliation engine with a
    CLI.

    None of it clears a single external gate. The lawyer has not answered, the GI rep has not
    confirmed a rate, `CLOUDFLARE_API_TOKEN` is still absent from the repository secrets, and
    `wrangler.jsonc:74` still points production traffic at the live Green Invoice API in front of an
    unsigned checklist.
  </what-built>
  <how-to-verify>
Work these five gates. Record the outcome of each in `.planning/phases/04-go-live/04-GO-NOGO.md`.
A gate you cannot close gets an honest `🔴 BLOCKED` with a date and a note — never a hopeful green.

**1. G1 — legal / accountant sign-off (SPIKE-02).**
Send `apps/web/docs/GI-LEGAL-CHECKLIST.md` to the accountant or lawyer. Get written answers to every
question, then in that file: tick each `- [ ]` you have a written answer for, fill
`**Accountant / lawyer name:**` and `**Date:**`, and — this is the one that has been wrong since
Phase 2 — confirm explicitly in writing that the engagement does **not** cover the Bags.fm token.
Token sign-off is COIN-01, a separate and stricter opinion.
If sign-off is not yet in hand but a written timeline is, tick the timeline box and record the
promised date in the ledger. G1 stays `🔴 BLOCKED` until the opinion itself arrives. **G1 may not be
ACCEPTED-WITH-RISK.**

**2. G2 — GI Prime, credentials, clearing terms (SPIKE-03).**
Work `apps/web/docs/GI-PRIME-CHECKLIST.md` with the GI account rep. Fill every blank: the Prime rate
and its effective date, any monthly fee or volume minimum, the clearing percentage, the hard minimum
per transaction, brand surcharges, the foreign-card surcharge, and the settlement threshold and
cadence. Record the block-or-flag foreign-card decision on the `Decision on file:` line — at ~3.5% of
₪50 that is ~₪1.75 per foreign charge, so the decision is real money, not a formality.

Then stage the production Green Invoice secrets. Interactively, one at a time:
```bash
cd apps/web
npx wrangler secret put GREENINVOICE_API_KEY_ID
npx wrangler secret put GREENINVOICE_API_SECRET
npx wrangler secret put GREENINVOICE_PLUGIN_ID
npx wrangler secret put GREENINVOICE_WEBHOOK_SECRET   # fresh for production — do NOT reuse sandbox
```
Register the matching notify URL in the GI dashboard. **Record only that each name was set.** No
value goes into any file, terminal transcript, commit, or screenshot.

**3. G6 — production Worker secrets.**
```bash
cd apps/web && ./scripts/preflight-prod.sh
```
Paste its output — it is names-only by construction — into the ledger under G6. Resolve every
`BLOCKER` and `PAID` MISSING. For the two `DEGRADES` sets that have been empty since 2026-07-28,
make an explicit decision and write it down rather than drifting into launch:
- **UPSTASH** unset → rate limiting falls back to in-memory, which does not survive across Worker
  isolates. Set it, or record that launch traffic is accepted without durable rate limiting.
- **SMS** unset → phone OTP soft-passes. Set it, or record that phone verification is knowingly
  degraded at launch.
G6 clears only when `./scripts/preflight-prod.sh` exits `0`. Paste the exit code.

**4. G5 — deploy path.**
Pick one and record it:
- **CI deploys:** add `CLOUDFLARE_API_TOKEN` under GitHub → Settings → Secrets and variables →
  Actions, either as a repository secret or on the `production` environment (`CLOUDFLARE_ACCOUNT_ID`
  is already set). Then trigger `workflow_dispatch` on `deploy.yml` and paste the run URL and
  conclusion. → `🟢 CLEARED`
- **Manual only:** record `🟡 ACCEPTED-WITH-RISK` with your name, the date, and the residual risk in
  one line (no deploy audit trail, no rollback automation, single-operator dependency).
Either way, confirm the plan-04-02 guard works: a dispatch without the token must now fail at
`Verify deploy credentials` in seconds rather than after `pnpm cf:build`.

**5. G3 — reconcile `GREENINVOICE_ENV` with the gate state.**
```bash
grep -n GREENINVOICE_ENV apps/web/wrangler.jsonc
```
If G1 or G2 is still red, either edit line 74 to `"sandbox"` (→ `🟢 CLEARED`, production no longer
runs ahead of its gate) or record `🟡 ACCEPTED-WITH-RISK` naming who accepted live card traffic
without legal and commercial sign-off. Paste the resulting line either way.

**Finally — set the verdict.** Update the top line of `04-GO-NOGO.md`:
- `**Verdict:** GO` only if G0, G1, G2, G4, and G6 are all `🟢 CLEARED` and G3 and G5 are each either
  `🟢 CLEARED` or `🟡 ACCEPTED-WITH-RISK` with a named decider.
- otherwise `**Verdict:** NO-GO` with the blocking gate ids listed on the same line.

Note on G0 and G4: both belong to Phase 3. If Phase 3 has not executed, they stay `🔴 BLOCKED` and
the verdict is NO-GO. **That is a correct outcome for this plan.** Do not mark them green to unblock
the next plan; plan 04-05's own gate re-reads this file.
  </how-to-verify>
  <acceptance_criteria>
    - `.planning/phases/04-go-live/04-GO-NOGO.md` has zero cells reading `—` in the `Decided by` or `Date` columns for gates G1, G2, G3, G5, G6 — each has a real name and a real ISO date
    - Every gate row's `State` is one of `🟢 CLEARED`, `🔴 BLOCKED`, `🟡 ACCEPTED-WITH-RISK`
    - `🟡 ACCEPTED-WITH-RISK` appears **only** on G3 and/or G5 rows, and each such row contains a one-line residual-risk statement
    - The G6 row contains the pasted `preflight-prod.sh` output and its exit code
    - The G5 row contains either a `deploy.yml` run URL with its conclusion, or an explicit manual-only acceptance
    - The G3 row contains the current literal `GREENINVOICE_ENV` line from `wrangler.jsonc`
    - The verdict line reads `**Verdict:** GO` or `**Verdict:** NO-GO`, and if GO then no row is `🔴 BLOCKED`
    - If G1 cleared: `grep -c '^\- \[x\]' apps/web/docs/GI-LEGAL-CHECKLIST.md` is greater than 0 and `___________________________` no longer appears on the name or date line
    - If G2 cleared: `grep -c '^\- \[x\]' apps/web/docs/GI-PRIME-CHECKLIST.md` is greater than 0
    - **No secret leaked into any artifact:** `grep -cE "eyJ[A-Za-z0-9_-]{10,}|service_role|sk_live|SUPABASE_SERVICE_ROLE_KEY=|GREENINVOICE_API_SECRET=" .planning/phases/04-go-live/04-GO-NOGO.md apps/web/docs/GI-PRIME-CHECKLIST.md apps/web/docs/GI-LEGAL-CHECKLIST.md` returns 0 for all three files
  </acceptance_criteria>
  <action>
Human-executed. Work the five gates in `<how-to-verify>`: obtain the legal opinion against the
re-scoped `GI-LEGAL-CHECKLIST.md` (G1); work the Prime rate, clearing terms, foreign-card decision
and production GI credentials with the account rep against `GI-PRIME-CHECKLIST.md` (G2); run
`./scripts/preflight-prod.sh` and resolve or explicitly accept every missing Worker secret (G6);
decide and record the deploy path (G5); and reconcile `GREENINVOICE_ENV` in `wrangler.jsonc` with
the gate state (G3).

Record every outcome in `.planning/phases/04-go-live/04-GO-NOGO.md` with a state, a name, a date,
and pasted evidence, then set the verdict line. Secret **names** and set/unset state only — never a
value, in any file, transcript, commit, or screenshot.

If G0 (Phase 3) or G4 (`validateEnv()`) is still red, leave them red and write NO-GO. An honest
NO-GO is this plan succeeding.
  </action>
  <verify>
    <automated>F=.planning/phases/04-go-live/04-GO-NOGO.md && test -f $F && grep -qE '^\*\*Verdict:\*\* (GO|NO-GO)' $F; for g in G1 G2 G3 G5 G6; do grep -E "\*\*$g" $F | grep -qE "CLEARED|BLOCKED|ACCEPTED-WITH-RISK" || { echo "GATE $g HAS NO STATE"; exit 1; }; done; grep -qF "preflight-prod.sh" $F || { echo "MISSING G6 preflight evidence"; exit 1; }; grep -qF "GREENINVOICE_ENV" $F || { echo "MISSING G3 evidence"; exit 1; }; if grep -qE "ACCEPTED-WITH-RISK" $F; then grep -E "ACCEPTED-WITH-RISK" $F | grep -qE "\*\*G3|\*\*G5" || { echo "ACCEPTED-WITH-RISK used on a gate that forbids it"; exit 1; }; fi; if grep -qE '^\*\*Verdict:\*\* GO' $F; then ! grep -q "BLOCKED" $F || { echo "GO verdict with a BLOCKED gate"; exit 1; }; fi; ! grep -qE "eyJ[A-Za-z0-9_-]{10,}|service_role|sk_live|GREENINVOICE_API_SECRET=" $F apps/web/docs/GI-PRIME-CHECKLIST.md apps/web/docs/GI-LEGAL-CHECKLIST.md || { echo "SECRET LEAKED"; exit 1; }; echo OK</automated>
  </verify>
  <done>Every gate in the ledger carries a state, a named decider, a date, and pasted evidence; ACCEPTED-WITH-RISK appears only on G3 or G5 with a stated residual risk; the verdict line matches the row states; and no secret value exists in any of the three files.</done>
  <resume-signal>Type "approved" once every gate row is decided and the verdict line is set — including if the verdict is NO-GO. Otherwise describe which gate is stuck and why.</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: The go / no-go decision itself</name>
  <read_first>
    - .planning/phases/04-go-live/04-GO-NOGO.md (as completed by Task 1 — the decided ledger)
    - .planning/ROADMAP.md (Phase 4 success criteria — what "live" is being claimed)
    - .planning/STATE.md (Blockers/Concerns — anything that surfaced since planning)
  </read_first>
  <files>.planning/phases/04-go-live/04-GO-NOGO.md, apps/web/wrangler.jsonc</files>
  <decision>
    Given the decided ledger, does Taruu deploy to production now?
  </decision>
  <context>
    Task 1 recorded the truth. This task acts on it. The decision is explicit and recorded because the
    failure mode this phase exists to prevent is drifting into a launch — which is exactly what
    happened with `GREENINVOICE_ENV: production` running for weeks in front of an unsigned checklist,
    and with `REQUIREMENTS.md` marking SPIKE-02 and SPIKE-03 complete against artifacts nobody had
    filled in.

    Note what is **not** on the table: no token surface goes live regardless of this decision — COIN-01
    is unmet and nothing in this repo can meet it. The `treasury_transactions` reconciliation leg goes
    with it. The scope of "live" here is: residents vote free, and creating a vote costs ₪50 with a
    correct Israeli receipt.
  </context>
  <options>
    <option id="go">
      <name>GO — proceed to plan 04-05 and deploy</name>
      <pros>The product ships. The launch window holds. The free-vote path — the P0 that Phase 02.1 fixed — gets its production confirmation, which no automated test can produce.</pros>
      <cons>Only valid if G0, G1, G2, G4 and G6 are all green. Choosing this with a red blocking gate is the exact drift this phase exists to stop.</cons>
    </option>
    <option id="go-restricted">
      <name>GO RESTRICTED — deploy with GREENINVOICE_ENV=sandbox</name>
      <pros>The site goes live and residents vote for free — the whole free-participation product — while the paid ₪50 creation flow stays on sandbox until G1 and G2 clear. Free participation depends on no external gate whatsoever.</pros>
      <cons>Vote creation is unavailable or non-charging in production, which needs honest Hebrew/RTL copy on the creation surface rather than a silent failure. Requires flipping `wrangler.jsonc:74` and confirming no creation path takes real money.</cons>
    </option>
    <option id="no-go">
      <name>NO-GO — hold, and work the red gates</name>
      <pros>Nothing ships in front of an unsigned legal opinion or an unfixed `validateEnv()`. Phase 3 finishes first, which criterion 1 requires anyway.</pros>
      <cons>The launch window slips. The P0 production confirmation waits.</cons>
    </option>
  </options>
  <action>
    Human-executed. Read the decided ledger and choose. Append a `## Decision` section to
    `.planning/phases/04-go-live/04-GO-NOGO.md` recording: the chosen option id (`go`,
    `go-restricted`, or `no-go`), the decider's name, the ISO date, a one-paragraph rationale, and —
    for `go` or `go-restricted` — the explicit list of residual risks being carried into production.

    If `go-restricted` is chosen, also set `apps/web/wrangler.jsonc:74` to `"sandbox"` in the same
    change, and note in the Decision section which user-facing surface needs honest copy about vote
    creation being unavailable.

    If `no-go` is chosen, list the specific red gates being worked and, where known, who owns each.
    Plans 04-05 and 04-06 do not execute until this section reads `go` or `go-restricted`.
  </action>
  <verify>
    <automated>F=.planning/phases/04-go-live/04-GO-NOGO.md && grep -qF "## Decision" $F && grep -qE "chosen[: ].*(go|go-restricted|no-go)|\*\*Option:\*\* (go|go-restricted|no-go)" $F && grep -qE "[0-9]{4}-[0-9]{2}-[0-9]{2}" $F && if grep -qE "go-restricted" $F; then grep -qF '"GREENINVOICE_ENV": "sandbox"' apps/web/wrangler.jsonc || { echo "go-restricted chosen but GREENINVOICE_ENV is not sandbox"; exit 1; }; fi; ! grep -qE "eyJ[A-Za-z0-9_-]{10,}|service_role|sk_live" $F || { echo "SECRET LEAKED"; exit 1; }; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `04-GO-NOGO.md` contains a `## Decision` section naming exactly one of `go`, `go-restricted`, `no-go`
    - The section records a decider name and an ISO date
    - For `go` or `go-restricted`: the residual risks being carried into production are listed explicitly
    - For `go-restricted`: `apps/web/wrangler.jsonc` contains `"GREENINVOICE_ENV": "sandbox"`
    - For `no-go`: the specific red gates being worked are listed
    - No secret value appears in the file
  </acceptance_criteria>
  <done>The go/no-go call is written down with a name, a date, a rationale, and either an explicit list of accepted residual risks or an explicit list of gates being worked — and if restricted, the sandbox flag is actually set.</done>
  <resume-signal>Select: go, go-restricted, or no-go — and confirm the `## Decision` section is written.</resume-signal>
</task>

</tasks>

<verification>
- `.planning/phases/04-go-live/04-GO-NOGO.md` has every gate decided with a state, a name, a date, and pasted evidence.
- `ACCEPTED-WITH-RISK` appears only on G3 and/or G5, each with a stated residual risk.
- A `## Decision` section records the go / go-restricted / no-go call with a decider and a date.
- Neither checklist, nor the ledger, contains a secret value, a JWT, or a service-role string.
- If `go-restricted`: `apps/web/wrangler.jsonc` reads `"GREENINVOICE_ENV": "sandbox"`.
- `pnpm --filter @sync/web typecheck` and `pnpm --filter @sync/web test` remain green (this plan touches at most one JSONC value).
</verification>

<success_criteria>
- The two external gates that have been silently open since Phase 2 are either closed with a signature or explicitly, namedly accepted — never left ambiguous.
- `GREENINVOICE_ENV` no longer contradicts the gate state without somebody's name against that contradiction.
- Nobody can drift into launch: the deploy plan reads this file and refuses without a `go` or `go-restricted` decision.
- No secret value was read, printed, pasted, committed, or screenshotted at any point.
</success_criteria>

<output>
After completion, create `.planning/phases/04-go-live/04-04-SUMMARY.md`.
Record: the final state of all seven gates, which were cleared and which accepted with risk and by
whom, the decision taken, the `preflight-prod.sh` exit code, the checklist tick counts before and
after, and — explicitly — that no secret value was written to any file.
</output>
