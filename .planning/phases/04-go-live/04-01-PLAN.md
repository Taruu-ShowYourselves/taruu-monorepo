---
phase: 04-go-live
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/docs/GI-LEGAL-CHECKLIST.md
  - apps/web/docs/GI-PRIME-CHECKLIST.md
  - .planning/phases/04-go-live/04-GO-NOGO.md
autonomous: true
requirements: [GO-01]

must_haves:
  truths:
    - "The legal checklist a lawyer receives asks about the product Taruu actually sells — free participation and a ₪50 creation fee — and contains no question premised on the retired ₪6 membership, the ₪2.10 pool split, or an off-session card-on-file MIT charge."
    - "The Prime checklist a GI rep receives prices the flows that exist: one ₪50 creation receipt per created vote, and zero participation receipts."
    - "A single gate ledger records every go-live gate G0–G6 with a state, a date, a decider, and an evidence pointer — so the phase's readiness is one file to read rather than seven files to infer."
    - "Both checklists still state, in the file, that they are unsigned external human tracks that block go-live."
  artifacts:
    - path: "apps/web/docs/GI-LEGAL-CHECKLIST.md"
      provides: "SPIKE-02 accountant/legal checklist re-scoped to the free-participation + ₪50-creation model"
      contains: "חשבונית"
    - path: "apps/web/docs/GI-PRIME-CHECKLIST.md"
      provides: "SPIKE-03 Prime / credentials / clearing-terms checklist re-scoped to the ₪50 creation receipt"
      contains: "Prime"
    - path: ".planning/phases/04-go-live/04-GO-NOGO.md"
      provides: "The G0–G6 gate ledger — the single readiness record for the phase"
      contains: "G5"
  key_links:
    - from: ".planning/phases/04-go-live/04-GO-NOGO.md"
      to: "apps/web/docs/GI-LEGAL-CHECKLIST.md"
      via: "G1's evidence pointer names the checklist file and its box count"
      pattern: "GI-LEGAL-CHECKLIST"
    - from: ".planning/phases/04-go-live/04-GO-NOGO.md"
      to: "apps/web/docs/GI-PRIME-CHECKLIST.md"
      via: "G2's evidence pointer names the checklist file and its box count"
      pattern: "GI-PRIME-CHECKLIST"
---

<objective>
Make the two documents that leave this building describe the product that exists, and create the
one file that says whether Taruu may go live.

Purpose: `GI-LEGAL-CHECKLIST.md` and `GI-PRIME-CHECKLIST.md` are the artifacts handed to an
accountant and to a Green Invoice rep. Both are currently written against the ₪6/month card-on-file
membership that `cfa5d25` retired — they ask about a ₪2.10 civic-pool split, an off-session MIT
recurring charge, and cooling-off rights on a subscription Taruu does not sell. Sending them
out unchanged buys sign-off on the wrong product and burns a real-world round trip measured in
weeks. This plan re-scopes both, then creates `04-GO-NOGO.md` so the phase's readiness is a
single file to read instead of seven to infer.

Output: two corrected external-track checklists and a gate ledger with G0–G6 all recorded as
BLOCKED with real evidence pointers.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-go-live/04-CONTEXT.md

@apps/web/docs/GI-LEGAL-CHECKLIST.md
@apps/web/docs/GI-PRIME-CHECKLIST.md
@.planning/phases/02-spike-gate/02-02-PLAN.md

<interfaces>
The current money model, verified in code — every checklist question must be consistent with this
and nothing else:

- **Participation: FREE.** `apps/web/src/app/api/votes/[id]/participate/route.ts` takes `{ optionId }`
  and charges nothing. `VOTE_COST` was retired from `@sync/shared` in Phase 02.1. There is no
  participation receipt, so there is no participation document type, no participation VAT question,
  and no participation refund.
- **Vote creation: ₪50, GI hosted form.** `apps/web/src/services/payments/greenInvoice.ts:213`
  (`createPaymentForm`, document `type: 320`), amount from `getPaymentAmounts().voteCreation`
  (`:345-351`). 100% platform — the civic pool is **not** credited on creation.
- **Payer:** Israeli private individual (פרטי, not a business). Unchanged.
- **Civic pool:** funded by a tradeable Bags.fm token, **not** by any per-charge split. That entire
  surface is behind COIN-01 (written Israeli legal sign-off on securities status, treasury custody,
  and permissible claims) and is **out of scope for both checklists** — COIN-01 is a separate,
  stricter engagement and must be named as such rather than folded into SPIKE-02.
- **No card-on-file.** `chargeToken()` exists in `services/greenInvoice/index.ts:220` but no product
  flow calls it. There is no saved card, no off-session MIT, no recurring charge, and therefore no
  recurring-subscription consumer-protection question.

Gate facts to record in the ledger, all verified during planning:

| Gate | Fact |
|---|---|
| G0 | No `.planning/phases/03-*` directory exists — Phase 3 is not planned |
| G1 | `GI-LEGAL-CHECKLIST.md` — 0 of 19 `- [ ]` ticked, name/date blank |
| G2 | `GI-PRIME-CHECKLIST.md` — 0 of 24 `- [ ]` ticked, name/date blank |
| G3 | `apps/web/wrangler.jsonc:74` → `"GREENINVOICE_ENV": "production"` |
| G4 | `apps/web/src/lib/env.ts:146` `validateEnv()` — zero callers; requires 6 vars with 0 runtime readers |
| G5 | `CLOUDFLARE_API_TOKEN` absent from `gh secret list`; last 10 `deploy.yml` runs failed |
| G6 | UPSTASH / GI-production / SMS Worker secrets still empty |

Box counts are verified with: `grep -c '^\- \[ \]' <file>` (unticked) and `grep -c '^\- \[x\]' <file>`
(ticked). Note the leading `^` — nested checkboxes under a parent bullet are indented and are counted
separately; keep top-level questions unindented so the count stays meaningful.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-scope the legal/accountant checklist to free participation + ₪50 creation</name>
  <read_first>
    - apps/web/docs/GI-LEGAL-CHECKLIST.md (the file being rewritten — keep its four-section shape and runbook tone)
    - .planning/phases/04-go-live/04-CONTEXT.md (the `<gates>` G1 entry: exactly why this document is wrong today)
    - .planning/REQUIREMENTS.md (SPIKE-02 wording; PAY-06/07 for the surviving flow; COIN-01 for what must NOT be folded in)
    - apps/web/src/services/payments/greenInvoice.ts (lines 200-230 and 340-355 — the hosted form and the real amounts)
  </read_first>
  <files>apps/web/docs/GI-LEGAL-CHECKLIST.md</files>
  <action>
    Rewrite `apps/web/docs/GI-LEGAL-CHECKLIST.md` in place. Keep the existing document shape — a
    `> **Status: PENDING**` banner, a `## Context` paragraph, four `## ` question sections, and a
    `## Sign-off` block — and keep the runbook tone of `apps/web/docs/INTEGRATIONS.md`. Change what
    it asks about.

    **Banner.** Keep `> **Status: PENDING** - external human track`. Change the scope line to say it
    blocks Phase 4 go-live (it no longer merely "gates Phase 4, not Phase 3 build" — Phase 4 is now
    the phase being executed). Add one line: `> Re-scoped 2026-08-03: the ₪6 card-on-file membership
    this document originally described is retired. Participation is free.`

    **`## Context`.** Replace the membership paragraph entirely. State: participation in a vote is
    **free**; creating a vote costs **₪50**, charged once through the Green Invoice **hosted payment
    form** (`type: 320`) with no saved card and no recurring charge; the payer is an Israeli private
    individual (פרטי); the civic pool is funded by a tradeable token and is **explicitly out of scope
    for this engagement** — it is COIN-01, a separate and stricter sign-off on Israeli securities
    law, treasury custody, and permissible claims, and must not be conflated with merchant-of-record
    status.

    **`## Document type per flow`.** Now a single flow, not three. Ask, as `- [ ]` questions:
    which GI document is correct for a one-time ₪50 charge to a private payer — **חשבונית קבלה**
    (combined invoice-receipt) vs **חשבונית מס** (tax invoice, needing a separate קבלה) vs a plain
    **קבלה**; whether `type: 320`'s auto-issued document is the right instrument for a one-time
    hosted-form charge to a private payer; and — explicitly — confirmation **in writing that a free
    action requires no GI document at all**, since Taruu issues nothing for a free vote and needs
    that stated rather than assumed.

    **`## VAT treatment`.** Ask: is מע״מ included in the ₪50 or added on top, and at what current
    rate; how it must appear on the document for a private payer with no deduction right; and
    whether a one-time civic-platform service fee attracts any exemption, reduced rate, or
    reverse-charge treatment. Delete every ₪2.10/civic-pool VAT question.

    **`## Refund / credit-note (זיכוי) mechanics`.** Keep this section — it is the one part that
    survives intact, because a ₪50 charge can still need reversing. Ask: the correct instrument
    (**חשבונית זיכוי** against the original document id); whether partial credit is permitted or the
    note must match the full amount; what happens to a vote that was created by a charge later
    refunded (is the created vote withdrawn, and is that a legal question or a product one — ask,
    do not assume); chargeback handling and whether GI issues the note automatically; and any
    statutory deadline. Drop the ₪2.10 pool-reversal question.

    **`## Consumer-protection obligations (Israeli law)`.** Rewrite for a one-time purchase, not a
    subscription. Ask: cooling-off / cancellation rights under חוק הגנת הצרכן for a **one-time
    digital service fee** (and whether the "service" is deemed delivered the moment the vote is
    created); the required pre-charge disclosure at the hosted form; the receipt-delivery obligation
    (email/SMS immediately vs available on demand); and how "participation is free, creating a vote
    costs ₪50" must be presented so it is unambiguous and non-deceptive. **Delete** every
    recurring-charge, card-on-file-consent, self-service-card-removal, and cooling-off-on-a-
    subscription question — none of those flows exist.

    **`## Sign-off`.** Keep name/date blanks and the single `- [ ] Written sign-off obtained (or
    written timeline on file)` box. Add one explicit line beneath it:
    `- [ ] Confirmed in writing that this engagement does NOT cover the Bags.fm token (COIN-01) — that is a separate sign-off.`

    Do not tick any box. Do not fill any blank. The document leaves here unsigned by design.
  </action>
  <verify>
    <automated>cd apps/web && F=docs/GI-LEGAL-CHECKLIST.md && grep -qF "## Document type per flow" $F && grep -qF "## VAT treatment" $F && grep -qF "זיכוי" $F && grep -q "## Consumer-protection" $F && grep -qF "## Sign-off" $F && grep -qF "PENDING" $F && grep -qF "COIN-01" $F && grep -q "50" $F && ! grep -qE "₪6\b|₪2\.10|membership|card-on-file|off-session|\bMIT\b" $F && [ "$(grep -c '^\- \[ \]' $F)" -ge 12 ] && [ "$(grep -c '^\- \[x\]' $F)" -eq 0 ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qF "## Document type per flow" apps/web/docs/GI-LEGAL-CHECKLIST.md` exits 0
    - `grep -qF "## VAT treatment" apps/web/docs/GI-LEGAL-CHECKLIST.md` exits 0
    - `grep -qF "זיכוי" apps/web/docs/GI-LEGAL-CHECKLIST.md` exits 0
    - `grep -q "## Consumer-protection" apps/web/docs/GI-LEGAL-CHECKLIST.md` exits 0
    - `grep -qF "## Sign-off" apps/web/docs/GI-LEGAL-CHECKLIST.md` exits 0
    - `grep -qF "PENDING" apps/web/docs/GI-LEGAL-CHECKLIST.md` exits 0 — still flagged as an unsigned external track
    - `grep -qF "COIN-01" apps/web/docs/GI-LEGAL-CHECKLIST.md` exits 0 — the token engagement is named as separate
    - **Retired model is gone:** `grep -qE "₪6\b|₪2\.10|membership|card-on-file|off-session|\bMIT\b" apps/web/docs/GI-LEGAL-CHECKLIST.md` exits **non-zero**
    - At least 12 unticked top-level questions: `grep -c '^\- \[ \]' apps/web/docs/GI-LEGAL-CHECKLIST.md` >= 12
    - Nothing pre-ticked: `grep -c '^\- \[x\]' apps/web/docs/GI-LEGAL-CHECKLIST.md` == 0
  </acceptance_criteria>
  <done>The legal checklist asks only about free participation and the one-time ₪50 hosted-form creation charge; every ₪6-membership, ₪2.10-pool, and card-on-file/MIT question is gone; COIN-01 is named as a separate engagement; and the document is unsigned with at least 12 open questions.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-scope the GI Prime / credentials / clearing-terms checklist</name>
  <read_first>
    - apps/web/docs/GI-PRIME-CHECKLIST.md (the file being rewritten — keep its three-section shape)
    - apps/web/scripts/sync-secrets.sh (how secrets reach the Worker; the `SKIP` list; `--dry-run` prints names only)
    - apps/web/.dev.vars.example (the exact secret NAMES — copy them verbatim, never a value)
    - .planning/phases/04-go-live/04-CONTEXT.md (the `<gates>` G2 and G6 entries)
  </read_first>
  <files>apps/web/docs/GI-PRIME-CHECKLIST.md</files>
  <action>
    Rewrite `apps/web/docs/GI-PRIME-CHECKLIST.md` in place, keeping its shape: `> **Status: PENDING**`
    banner, `## Context`, `## GI Prime plan`, `## Real credentials staged`, `## Written clearing
    terms`, `## Sign-off`.

    **Banner + `## Context`.** Same re-scope note as Task 1. Replace the economics paragraph: there
    is exactly **one** paid flow — a ₪50 vote-creation charge — so Taruu issues roughly **one receipt
    per created vote and zero receipts for participation**. Say plainly what that changes: at ₪50 a
    ₪0.15 per-receipt fee is 0.3% and immaterial, whereas the **clearing percentage** is now the
    dominant cost and the ~3.5% foreign-card surcharge is ~₪1.75 on a ₪50 charge. The commercial
    question has moved from per-receipt fees to clearing terms, and the checklist must weight it
    that way.

    **`## GI Prime plan`.** Keep the written-confirmation, effective-date, and volume-minimum/monthly-
    fee questions. Rewrite the "both receipt types" question to a **single** flow: confirm in writing
    that the ₪50 vote-creation receipt bills at the Prime rate. Add one question the old version
    lacked: given the far lower receipt volume of a single paid flow, confirm **whether Prime is
    still the right plan at all**, or whether a lower tier is cheaper — record the comparison.

    **`## Real credentials staged`.** List the secrets to stage, **by name only**. Take the names
    verbatim from `apps/web/.dev.vars.example`. Group them:
    - Green Invoice (blocks the paid flow): `GREENINVOICE_API_KEY_ID`, `GREENINVOICE_API_SECRET`,
      `GREENINVOICE_PLUGIN_ID`, `GREENINVOICE_WEBHOOK_SECRET`
    - Supabase (blocks everything): `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`,
      `SUPABASE_SERVICE_ROLE_KEY`
    - Session / identity: `JWT_SECRET`, `SUPABASE_JWT_SECRET`, `IDENTITY_HASH_SECRET`
    - Auth: `NEXT_PUBLIC_GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`
    - Ops: `CRON_SECRET`, `RESEND_API_KEY`, `NEXT_PUBLIC_APP_URL`
    - Still empty per the deploy record, each with its stated degradation:
      `UPSTASH_REDIS_REST_URL` / `UPSTASH_REDIS_REST_TOKEN` (rate limiting falls back to in-memory,
      which does not survive across Worker isolates), `SMS_API_URL` / `SMS_API_KEY` / `SMS_FROM`
      (phone OTP soft-passes — decide explicitly whether that is acceptable at launch)
    Note the runtime flag separately: `GREENINVOICE_ENV` lives in `wrangler.jsonc` `vars`, **not** in
    the secret store — `sync-secrets.sh` skips it by design (`SKIP="GREENINVOICE_ENV QUBIK_NETWORK"`).

    Add a bold safety line, verbatim in spirit: **never print, echo, paste, or copy a secret value
    into this file, into a terminal transcript, into a commit, or into a screenshot. The only
    sanctioned checks are `./scripts/sync-secrets.sh --dry-run` and
    `node_modules/.bin/wrangler secret list`, both of which emit names only.**

    **`## Written clearing terms`.** Keep every question — clearing % (עמלת סליקה), hard minimum per
    transaction, card-brand surcharges, tourist/foreign-card surcharge, settlement payout threshold,
    settlement cadence — but re-anchor every worked example to ₪50 instead of ₪6. Restate the
    foreign-card decision on the new numbers (~3.5% of ₪50 ≈ ₪1.75) and keep the explicit
    block-or-flag choice with a `Decision on file: ___` line.

    **`## Sign-off`.** Keep the rep name/date blanks and the three summary boxes, with the credential
    box's name list updated to the grouped list above.

    Tick nothing. Fill nothing.
  </action>
  <verify>
    <automated>cd apps/web && F=docs/GI-PRIME-CHECKLIST.md && grep -qF "## GI Prime plan" $F && grep -qF "## Real credentials staged" $F && grep -qF "## Written clearing terms" $F && grep -qF "## Sign-off" $F && grep -qF "PENDING" $F && grep -qF "0.15" $F && grep -qF "GREENINVOICE_API_KEY_ID" $F && grep -qF "SUPABASE_SERVICE_ROLE_KEY" $F && grep -qF "UPSTASH_REDIS_REST_TOKEN" $F && grep -qF "SMS_API_KEY" $F && grep -qE "surcharge|foreign-card" $F && grep -qF "wrangler secret list" $F && grep -q "50" $F && ! grep -qE "₪6\b|₪2\.10|membership" $F && [ "$(grep -c '^\- \[ \]' $F)" -ge 14 ] && [ "$(grep -c '^\- \[x\]' $F)" -eq 0 ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All four headings present: `## GI Prime plan`, `## Real credentials staged`, `## Written clearing terms`, `## Sign-off`
    - `grep -qF "PENDING" apps/web/docs/GI-PRIME-CHECKLIST.md` exits 0
    - `grep -qF "0.15" apps/web/docs/GI-PRIME-CHECKLIST.md` exits 0 — the Prime rate is still on file
    - Secret names present by name: `GREENINVOICE_API_KEY_ID`, `SUPABASE_SERVICE_ROLE_KEY`, `UPSTASH_REDIS_REST_TOKEN`, `SMS_API_KEY` each `grep -qF` exit 0
    - `grep -qF "wrangler secret list" apps/web/docs/GI-PRIME-CHECKLIST.md` exits 0 — the names-only check is named
    - `grep -qE "surcharge|foreign-card" apps/web/docs/GI-PRIME-CHECKLIST.md` exits 0
    - **Retired model is gone:** `grep -qE "₪6\b|₪2\.10|membership" apps/web/docs/GI-PRIME-CHECKLIST.md` exits **non-zero**
    - At least 14 unticked top-level boxes: `grep -c '^\- \[ \]' apps/web/docs/GI-PRIME-CHECKLIST.md` >= 14
    - Nothing pre-ticked: `grep -c '^\- \[x\]' apps/web/docs/GI-PRIME-CHECKLIST.md` == 0
    - **No secret value anywhere:** `grep -cE "eyJ[A-Za-z0-9_-]{10,}|service_role|sk_live|=[A-Za-z0-9+/]{24,}" apps/web/docs/GI-PRIME-CHECKLIST.md` returns 0
  </acceptance_criteria>
  <done>The Prime checklist prices one ₪50 creation receipt and no participation receipt, weights clearing terms over per-receipt fees, enumerates every required Worker secret by name (including the still-empty UPSTASH and SMS sets with their degradation stated), forbids ever writing a value, and is unsigned with at least 14 open boxes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create the G0–G6 gate ledger</name>
  <read_first>
    - .planning/phases/04-go-live/04-CONTEXT.md (the `<gates>` section — G0 through G6 verbatim; this ledger is its operational form)
    - .planning/STATE.md (Blockers/Concerns — the source of G3, G4, G5)
    - apps/web/wrangler.jsonc (line 74 — the live `GREENINVOICE_ENV`)
    - .github/workflows/deploy.yml (lines 59-64 — the deploy step with the unset token)
  </read_first>
  <files>.planning/phases/04-go-live/04-GO-NOGO.md</files>
  <action>
    Create `.planning/phases/04-go-live/04-GO-NOGO.md` — the single file that answers "may Taruu go
    live?".

    Open with a one-line verdict field: `**Verdict:** NO-GO` (it is NO-GO on creation and stays that
    way until every row clears). Then a `## Gate ledger` table with these columns:
    `Gate | What must be true | State | Evidence | Decided by | Date`.

    Seven rows, one per gate, each with `State` = `🔴 BLOCKED` at creation and `Evidence` pointing at
    a real file:

    - **G0 — Phase 3 complete.** SEC-02..05 + PAY-06/07/08 executed. Evidence: `.planning/phases/03-*`
      does not exist; Phase 3 is unplanned.
    - **G1 — SPIKE-02 legal sign-off obtained.** Evidence: `apps/web/docs/GI-LEGAL-CHECKLIST.md`,
      0 ticked / N open, unsigned.
    - **G2 — SPIKE-03 Prime provisioned, credentials staged, clearing terms on file.** Evidence:
      `apps/web/docs/GI-PRIME-CHECKLIST.md`, 0 ticked / N open, unsigned.
    - **G3 — `GREENINVOICE_ENV` reconciled with the gate state.** Evidence: `apps/web/wrangler.jsonc:74`
      is `production` while G1 and G2 are blocked. Resolution is either flip to `sandbox` or a dated
      written owner acceptance — record which.
    - **G4 — `validateEnv()` passes in production.** Evidence: `apps/web/src/lib/env.ts:146`, zero
      callers, requires 6 variables with 0 runtime readers. Owner: **Phase 3 SEC-05** — this row is
      verified here, not fixed here.
    - **G5 — Deploy path decided.** Either CI deploys green, or manual-only is recorded deliberately.
      Evidence: `CLOUDFLARE_API_TOKEN` absent from `gh secret list`; last 10 `deploy.yml` runs failed
      on `In a non-interactive environment, it's necessary to set a CLOUDFLARE_API_TOKEN`.
    - **G6 — Production Worker secrets set.** Evidence: UPSTASH / GI-production / SMS still empty per
      the deploy record. Checked by **name only** via `wrangler secret list`.

    Below the table add:

    `## How a gate clears` — a gate moves to `🟢 CLEARED` only when its Evidence cell points at a
    file containing the actual proof, with a date and a human name. `🟡 ACCEPTED-WITH-RISK` is
    permitted **only** for G3 and G5, requires a named decider and a one-line statement of the
    residual risk, and never for G0, G1, G2, G4, or G6.

    `## Out of scope for this ledger` — one paragraph naming what deliberately is **not** a gate here:
    COIN-01 and every token surface; the `treasury_transactions` reconciliation leg (moved to the
    token track); SPIKE-01's `SPIKE-RESULT.md` Part A, which gates Phase 6's off-session token charge
    and not this phase's hosted-form flow.

    `## Secret handling` — one paragraph: this file, and every artifact this phase produces, records
    secret **names** and **set/unset** state only. No value is ever written, echoed, pasted, or
    screenshotted. `wrangler secret list` and `sync-secrets.sh --dry-run` are the sanctioned checks.

    Do not mark any gate cleared. Every `State` cell is `🔴 BLOCKED` and every `Decided by` / `Date`
    cell is `—` when this task finishes.
  </action>
  <verify>
    <automated>F=.planning/phases/04-go-live/04-GO-NOGO.md && test -f $F && grep -qF "**Verdict:** NO-GO" $F && grep -qF "## Gate ledger" $F && for g in G0 G1 G2 G3 G4 G5 G6; do grep -qE "\*\*$g" $F || { echo "MISSING $g"; exit 1; }; done && grep -qF "## How a gate clears" $F && grep -qF "## Out of scope for this ledger" $F && grep -qF "## Secret handling" $F && grep -qF "COIN-01" $F && grep -qF "GI-LEGAL-CHECKLIST" $F && grep -qF "GI-PRIME-CHECKLIST" $F && grep -qF "CLOUDFLARE_API_TOKEN" $F && grep -qF "wrangler secret list" $F && [ "$(grep -c 'BLOCKED' $F)" -ge 7 ] && ! grep -qE "eyJ[A-Za-z0-9_-]{10,}|service_role|sk_live" $F && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/04-go-live/04-GO-NOGO.md` exists and contains `**Verdict:** NO-GO`
    - Contains `## Gate ledger`, `## How a gate clears`, `## Out of scope for this ledger`, `## Secret handling`
    - All seven gate ids `G0`–`G6` appear as bolded row labels
    - At least 7 occurrences of `BLOCKED` — every gate starts blocked
    - Evidence pointers are real: `GI-LEGAL-CHECKLIST`, `GI-PRIME-CHECKLIST`, `CLOUDFLARE_API_TOKEN` each `grep -qF` exit 0
    - `grep -qF "COIN-01" .planning/phases/04-go-live/04-GO-NOGO.md` exits 0 — the token track is named out of scope
    - `grep -qF "wrangler secret list" .planning/phases/04-go-live/04-GO-NOGO.md` exits 0
    - No secret value: `grep -cE "eyJ[A-Za-z0-9_-]{10,}|service_role|sk_live" .planning/phases/04-go-live/04-GO-NOGO.md` returns 0
  </acceptance_criteria>
  <done>`04-GO-NOGO.md` exists with a NO-GO verdict, all seven gates recorded as BLOCKED with real evidence pointers, the clearing rules stated (ACCEPTED-WITH-RISK permitted only for G3 and G5), COIN-01 and the treasury leg named out of scope, and a names-only secret-handling rule.</done>
</task>

</tasks>

<verification>
- `apps/web/docs/GI-LEGAL-CHECKLIST.md` asks only about free participation and the one-time ₪50 hosted-form charge; no ₪6, ₪2.10, membership, card-on-file, off-session, or MIT question survives.
- `apps/web/docs/GI-PRIME-CHECKLIST.md` prices one ₪50 receipt per created vote and zero participation receipts, weights clearing terms over per-receipt fees, and enumerates every Worker secret by name only.
- Neither checklist has a single pre-ticked box — both leave here unsigned by design.
- `.planning/phases/04-go-live/04-GO-NOGO.md` records G0–G6 as BLOCKED with real evidence pointers and a NO-GO verdict.
- No file touched by this plan contains a secret value, a JWT, or a service-role string.
</verification>

<success_criteria>
- The two documents that go to external humans describe the product Taruu actually sells.
- COIN-01 is named as a separate, stricter engagement in both the legal checklist and the ledger — nobody can mistake merchant-of-record sign-off for token sign-off.
- Phase readiness is one file to read.
- Nothing in this plan required a credential, a deploy, or a network call.
</success_criteria>

<output>
After completion, create `.planning/phases/04-go-live/04-01-SUMMARY.md`.
Record: the before/after box counts for both checklists, every question deleted as belonging to the
retired membership model, and the exact gate-ledger row states.
</output>
