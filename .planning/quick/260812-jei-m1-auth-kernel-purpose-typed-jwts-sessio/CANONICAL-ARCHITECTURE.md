# Canonical Architecture — moved

The Issue #71 canonical architecture document now lives at the repository's
durable docs location:

    specs/mfa-architecture.md

This planning copy is retained only as a historical pointer so the M1 PLAN's
references still resolve. **Do not edit this file** — a second copy of the
architecture would drift from the source of truth. Make all changes in
`specs/mfa-architecture.md` (design intent), and see
`specs/mfa-engineering-model.md` for the implementation contract.
