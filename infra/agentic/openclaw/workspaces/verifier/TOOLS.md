# Local tools

Use the repository's pnpm validation commands and the managed OpenClaw browser.
Copy focused screenshots into the issue evidence directory. Never open cloud
consoles, credential pages, private user records, or identity documents.
