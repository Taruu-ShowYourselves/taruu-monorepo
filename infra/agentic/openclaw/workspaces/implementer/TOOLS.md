# Local tools

Work only in the orchestrator-provided `cwd`. Use `rg` for discovery, the
repository's pnpm commands for validation, and `ctx7` for current external
documentation. GitHub writes and deployment tools are outside your role.
