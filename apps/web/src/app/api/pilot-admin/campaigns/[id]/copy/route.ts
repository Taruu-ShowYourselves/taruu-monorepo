import type { NextRequest } from 'next/server'; import { SaveCopyRequestSchema } from '@sync/shared/contracts'; import { savePilotCampaignCopy } from '@/server/app/pilot/campaigns'; import { parse, respond } from '@/server/http/respond'; import { getSessionFromRequest } from '@/services/auth/session';
export async function PUT(request: NextRequest,{params}:{params:Promise<{id:string}>}) { const session=await getSessionFromRequest(request); const {id}=await params; return respond(parse(SaveCopyRequestSchema,await request.json().catch(()=>null)).asyncAndThen((body)=>savePilotCampaignCopy(session,id,body))); }
