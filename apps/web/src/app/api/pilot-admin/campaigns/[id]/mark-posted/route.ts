import type { NextRequest } from 'next/server'; import { MarkPostedRequestSchema } from '@sync/shared/contracts'; import { markPilotCampaignPosted } from '@/server/app/pilot/campaigns'; import { parse, respond } from '@/server/http/respond'; import { getSessionFromRequest } from '@/services/auth/session';
export async function POST(request: NextRequest,{params}:{params:Promise<{id:string}>}) { const session=await getSessionFromRequest(request); const {id}=await params; return respond(parse(MarkPostedRequestSchema,await request.json().catch(()=>null)).asyncAndThen((body)=>markPilotCampaignPosted(session,id,body))); }
